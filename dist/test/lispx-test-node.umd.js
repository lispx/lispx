/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory(require("../lispx-vm.umd.js"));
	else if(typeof define === 'function' && define.amd)
		define(["../lispx-vm.umd.js"], factory);
	else if(typeof exports === 'object')
		exports["lispx-test-node"] = factory(require("../lispx-vm.umd.js"));
	else
		root["lispx-test-node"] = factory(root["../lispx-vm.umd.js"]);
})(this, (__WEBPACK_EXTERNAL_MODULE_lispx_vm__) => {
return /******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ "./node_modules/assertion-error/index.js":
/*!***********************************************!*\
  !*** ./node_modules/assertion-error/index.js ***!
  \***********************************************/
/***/ ((module) => {

eval("/*!\n * assertion-error\n * Copyright(c) 2013 Jake Luer <jake@qualiancy.com>\n * MIT Licensed\n */\n\n/*!\n * Return a function that will copy properties from\n * one object to another excluding any originally\n * listed. Returned function will create a new `{}`.\n *\n * @param {String} excluded properties ...\n * @return {Function}\n */\n\nfunction exclude () {\n  var excludes = [].slice.call(arguments);\n\n  function excludeProps (res, obj) {\n    Object.keys(obj).forEach(function (key) {\n      if (!~excludes.indexOf(key)) res[key] = obj[key];\n    });\n  }\n\n  return function extendExclude () {\n    var args = [].slice.call(arguments)\n      , i = 0\n      , res = {};\n\n    for (; i < args.length; i++) {\n      excludeProps(res, args[i]);\n    }\n\n    return res;\n  };\n};\n\n/*!\n * Primary Exports\n */\n\nmodule.exports = AssertionError;\n\n/**\n * ### AssertionError\n *\n * An extension of the JavaScript `Error` constructor for\n * assertion and validation scenarios.\n *\n * @param {String} message\n * @param {Object} properties to include (optional)\n * @param {callee} start stack function (optional)\n */\n\nfunction AssertionError (message, _props, ssf) {\n  var extend = exclude('name', 'message', 'stack', 'constructor', 'toJSON')\n    , props = extend(_props || {});\n\n  // default values\n  this.message = message || 'Unspecified AssertionError';\n  this.showDiff = false;\n\n  // copy from properties\n  for (var key in props) {\n    this[key] = props[key];\n  }\n\n  // capture stack trace\n  ssf = ssf || AssertionError;\n  if (Error.captureStackTrace) {\n    Error.captureStackTrace(this, ssf);\n  } else {\n    try {\n      throw new Error();\n    } catch(e) {\n      this.stack = e.stack;\n    }\n  }\n}\n\n/*!\n * Inherit from Error.prototype\n */\n\nAssertionError.prototype = Object.create(Error.prototype);\n\n/*!\n * Statically set name\n */\n\nAssertionError.prototype.name = 'AssertionError';\n\n/*!\n * Ensure correct constructor\n */\n\nAssertionError.prototype.constructor = AssertionError;\n\n/**\n * Allow errors to be converted to JSON for static transfer.\n *\n * @param {Boolean} include stack (default: `true`)\n * @return {Object} object that can be `JSON.stringify`\n */\n\nAssertionError.prototype.toJSON = function (stack) {\n  var extend = exclude('constructor', 'toJSON', 'stack')\n    , props = extend({ name: this.name }, this);\n\n  // include stack if exists and not turned off\n  if (false !== stack && this.stack) {\n    props.stack = this.stack;\n  }\n\n  return props;\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/assertion-error/index.js?");

/***/ }),

/***/ "./node_modules/chai/index.js":
/*!************************************!*\
  !*** ./node_modules/chai/index.js ***!
  \************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("module.exports = __webpack_require__(/*! ./lib/chai */ \"./node_modules/chai/lib/chai.js\");\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/index.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai.js":
/*!***************************************!*\
  !*** ./node_modules/chai/lib/chai.js ***!
  \***************************************/
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

eval("/*!\n * chai\n * Copyright(c) 2011-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\nvar used = [];\n\n/*!\n * Chai version\n */\n\nexports.version = '4.3.3';\n\n/*!\n * Assertion Error\n */\n\nexports.AssertionError = __webpack_require__(/*! assertion-error */ \"./node_modules/assertion-error/index.js\");\n\n/*!\n * Utils for plugins (not exported)\n */\n\nvar util = __webpack_require__(/*! ./chai/utils */ \"./node_modules/chai/lib/chai/utils/index.js\");\n\n/**\n * # .use(function)\n *\n * Provides a way to extend the internals of Chai.\n *\n * @param {Function}\n * @returns {this} for chaining\n * @api public\n */\n\nexports.use = function (fn) {\n  if (!~used.indexOf(fn)) {\n    fn(exports, util);\n    used.push(fn);\n  }\n\n  return exports;\n};\n\n/*!\n * Utility Functions\n */\n\nexports.util = util;\n\n/*!\n * Configuration\n */\n\nvar config = __webpack_require__(/*! ./chai/config */ \"./node_modules/chai/lib/chai/config.js\");\nexports.config = config;\n\n/*!\n * Primary `Assertion` prototype\n */\n\nvar assertion = __webpack_require__(/*! ./chai/assertion */ \"./node_modules/chai/lib/chai/assertion.js\");\nexports.use(assertion);\n\n/*!\n * Core Assertions\n */\n\nvar core = __webpack_require__(/*! ./chai/core/assertions */ \"./node_modules/chai/lib/chai/core/assertions.js\");\nexports.use(core);\n\n/*!\n * Expect interface\n */\n\nvar expect = __webpack_require__(/*! ./chai/interface/expect */ \"./node_modules/chai/lib/chai/interface/expect.js\");\nexports.use(expect);\n\n/*!\n * Should interface\n */\n\nvar should = __webpack_require__(/*! ./chai/interface/should */ \"./node_modules/chai/lib/chai/interface/should.js\");\nexports.use(should);\n\n/*!\n * Assert interface\n */\n\nvar assert = __webpack_require__(/*! ./chai/interface/assert */ \"./node_modules/chai/lib/chai/interface/assert.js\");\nexports.use(assert);\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/assertion.js":
/*!*************************************************!*\
  !*** ./node_modules/chai/lib/chai/assertion.js ***!
  \*************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * chai\n * http://chaijs.com\n * Copyright(c) 2011-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\nvar config = __webpack_require__(/*! ./config */ \"./node_modules/chai/lib/chai/config.js\");\n\nmodule.exports = function (_chai, util) {\n  /*!\n   * Module dependencies.\n   */\n\n  var AssertionError = _chai.AssertionError\n    , flag = util.flag;\n\n  /*!\n   * Module export.\n   */\n\n  _chai.Assertion = Assertion;\n\n  /*!\n   * Assertion Constructor\n   *\n   * Creates object for chaining.\n   *\n   * `Assertion` objects contain metadata in the form of flags. Three flags can\n   * be assigned during instantiation by passing arguments to this constructor:\n   *\n   * - `object`: This flag contains the target of the assertion. For example, in\n   *   the assertion `expect(numKittens).to.equal(7);`, the `object` flag will\n   *   contain `numKittens` so that the `equal` assertion can reference it when\n   *   needed.\n   *\n   * - `message`: This flag contains an optional custom error message to be\n   *   prepended to the error message that's generated by the assertion when it\n   *   fails.\n   *\n   * - `ssfi`: This flag stands for \"start stack function indicator\". It\n   *   contains a function reference that serves as the starting point for\n   *   removing frames from the stack trace of the error that's created by the\n   *   assertion when it fails. The goal is to provide a cleaner stack trace to\n   *   end users by removing Chai's internal functions. Note that it only works\n   *   in environments that support `Error.captureStackTrace`, and only when\n   *   `Chai.config.includeStack` hasn't been set to `false`.\n   *\n   * - `lockSsfi`: This flag controls whether or not the given `ssfi` flag\n   *   should retain its current value, even as assertions are chained off of\n   *   this object. This is usually set to `true` when creating a new assertion\n   *   from within another assertion. It's also temporarily set to `true` before\n   *   an overwritten assertion gets called by the overwriting assertion.\n   *\n   * @param {Mixed} obj target of the assertion\n   * @param {String} msg (optional) custom error message\n   * @param {Function} ssfi (optional) starting point for removing stack frames\n   * @param {Boolean} lockSsfi (optional) whether or not the ssfi flag is locked\n   * @api private\n   */\n\n  function Assertion (obj, msg, ssfi, lockSsfi) {\n    flag(this, 'ssfi', ssfi || Assertion);\n    flag(this, 'lockSsfi', lockSsfi);\n    flag(this, 'object', obj);\n    flag(this, 'message', msg);\n\n    return util.proxify(this);\n  }\n\n  Object.defineProperty(Assertion, 'includeStack', {\n    get: function() {\n      console.warn('Assertion.includeStack is deprecated, use chai.config.includeStack instead.');\n      return config.includeStack;\n    },\n    set: function(value) {\n      console.warn('Assertion.includeStack is deprecated, use chai.config.includeStack instead.');\n      config.includeStack = value;\n    }\n  });\n\n  Object.defineProperty(Assertion, 'showDiff', {\n    get: function() {\n      console.warn('Assertion.showDiff is deprecated, use chai.config.showDiff instead.');\n      return config.showDiff;\n    },\n    set: function(value) {\n      console.warn('Assertion.showDiff is deprecated, use chai.config.showDiff instead.');\n      config.showDiff = value;\n    }\n  });\n\n  Assertion.addProperty = function (name, fn) {\n    util.addProperty(this.prototype, name, fn);\n  };\n\n  Assertion.addMethod = function (name, fn) {\n    util.addMethod(this.prototype, name, fn);\n  };\n\n  Assertion.addChainableMethod = function (name, fn, chainingBehavior) {\n    util.addChainableMethod(this.prototype, name, fn, chainingBehavior);\n  };\n\n  Assertion.overwriteProperty = function (name, fn) {\n    util.overwriteProperty(this.prototype, name, fn);\n  };\n\n  Assertion.overwriteMethod = function (name, fn) {\n    util.overwriteMethod(this.prototype, name, fn);\n  };\n\n  Assertion.overwriteChainableMethod = function (name, fn, chainingBehavior) {\n    util.overwriteChainableMethod(this.prototype, name, fn, chainingBehavior);\n  };\n\n  /**\n   * ### .assert(expression, message, negateMessage, expected, actual, showDiff)\n   *\n   * Executes an expression and check expectations. Throws AssertionError for reporting if test doesn't pass.\n   *\n   * @name assert\n   * @param {Philosophical} expression to be tested\n   * @param {String|Function} message or function that returns message to display if expression fails\n   * @param {String|Function} negatedMessage or function that returns negatedMessage to display if negated expression fails\n   * @param {Mixed} expected value (remember to check for negation)\n   * @param {Mixed} actual (optional) will default to `this.obj`\n   * @param {Boolean} showDiff (optional) when set to `true`, assert will display a diff in addition to the message if expression fails\n   * @api private\n   */\n\n  Assertion.prototype.assert = function (expr, msg, negateMsg, expected, _actual, showDiff) {\n    var ok = util.test(this, arguments);\n    if (false !== showDiff) showDiff = true;\n    if (undefined === expected && undefined === _actual) showDiff = false;\n    if (true !== config.showDiff) showDiff = false;\n\n    if (!ok) {\n      msg = util.getMessage(this, arguments);\n      var actual = util.getActual(this, arguments);\n      var assertionErrorObjectProperties = {\n          actual: actual\n        , expected: expected\n        , showDiff: showDiff\n      };\n\n      var operator = util.getOperator(this, arguments);\n      if (operator) {\n        assertionErrorObjectProperties.operator = operator;\n      }\n\n      throw new AssertionError(\n        msg,\n        assertionErrorObjectProperties,\n        (config.includeStack) ? this.assert : flag(this, 'ssfi'));\n    }\n  };\n\n  /*!\n   * ### ._obj\n   *\n   * Quick reference to stored `actual` value for plugin developers.\n   *\n   * @api private\n   */\n\n  Object.defineProperty(Assertion.prototype, '_obj',\n    { get: function () {\n        return flag(this, 'object');\n      }\n    , set: function (val) {\n        flag(this, 'object', val);\n      }\n  });\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/assertion.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/config.js":
/*!**********************************************!*\
  !*** ./node_modules/chai/lib/chai/config.js ***!
  \**********************************************/
/***/ ((module) => {

eval("module.exports = {\n\n  /**\n   * ### config.includeStack\n   *\n   * User configurable property, influences whether stack trace\n   * is included in Assertion error message. Default of false\n   * suppresses stack trace in the error message.\n   *\n   *     chai.config.includeStack = true;  // enable stack on error\n   *\n   * @param {Boolean}\n   * @api public\n   */\n\n  includeStack: false,\n\n  /**\n   * ### config.showDiff\n   *\n   * User configurable property, influences whether or not\n   * the `showDiff` flag should be included in the thrown\n   * AssertionErrors. `false` will always be `false`; `true`\n   * will be true when the assertion has requested a diff\n   * be shown.\n   *\n   * @param {Boolean}\n   * @api public\n   */\n\n  showDiff: true,\n\n  /**\n   * ### config.truncateThreshold\n   *\n   * User configurable property, sets length threshold for actual and\n   * expected values in assertion errors. If this threshold is exceeded, for\n   * example for large data structures, the value is replaced with something\n   * like `[ Array(3) ]` or `{ Object (prop1, prop2) }`.\n   *\n   * Set it to zero if you want to disable truncating altogether.\n   *\n   * This is especially userful when doing assertions on arrays: having this\n   * set to a reasonable large value makes the failure messages readily\n   * inspectable.\n   *\n   *     chai.config.truncateThreshold = 0;  // disable truncating\n   *\n   * @param {Number}\n   * @api public\n   */\n\n  truncateThreshold: 40,\n\n  /**\n   * ### config.useProxy\n   *\n   * User configurable property, defines if chai will use a Proxy to throw\n   * an error when a non-existent property is read, which protects users\n   * from typos when using property-based assertions.\n   *\n   * Set it to false if you want to disable this feature.\n   *\n   *     chai.config.useProxy = false;  // disable use of Proxy\n   *\n   * This feature is automatically disabled regardless of this config value\n   * in environments that don't support proxies.\n   *\n   * @param {Boolean}\n   * @api public\n   */\n\n  useProxy: true,\n\n  /**\n   * ### config.proxyExcludedKeys\n   *\n   * User configurable property, defines which properties should be ignored\n   * instead of throwing an error if they do not exist on the assertion.\n   * This is only applied if the environment Chai is running in supports proxies and\n   * if the `useProxy` configuration setting is enabled.\n   * By default, `then` and `inspect` will not throw an error if they do not exist on the\n   * assertion object because the `.inspect` property is read by `util.inspect` (for example, when\n   * using `console.log` on the assertion object) and `.then` is necessary for promise type-checking.\n   *\n   *     // By default these keys will not throw an error if they do not exist on the assertion object\n   *     chai.config.proxyExcludedKeys = ['then', 'inspect'];\n   *\n   * @param {Array}\n   * @api public\n   */\n\n  proxyExcludedKeys: ['then', 'catch', 'inspect', 'toJSON']\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/config.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/core/assertions.js":
/*!*******************************************************!*\
  !*** ./node_modules/chai/lib/chai/core/assertions.js ***!
  \*******************************************************/
/***/ ((module) => {

eval("/*!\n * chai\n * http://chaijs.com\n * Copyright(c) 2011-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\nmodule.exports = function (chai, _) {\n  var Assertion = chai.Assertion\n    , AssertionError = chai.AssertionError\n    , flag = _.flag;\n\n  /**\n   * ### Language Chains\n   *\n   * The following are provided as chainable getters to improve the readability\n   * of your assertions.\n   *\n   * **Chains**\n   *\n   * - to\n   * - be\n   * - been\n   * - is\n   * - that\n   * - which\n   * - and\n   * - has\n   * - have\n   * - with\n   * - at\n   * - of\n   * - same\n   * - but\n   * - does\n   * - still\n   * - also\n   *\n   * @name language chains\n   * @namespace BDD\n   * @api public\n   */\n\n  [ 'to', 'be', 'been', 'is'\n  , 'and', 'has', 'have', 'with'\n  , 'that', 'which', 'at', 'of'\n  , 'same', 'but', 'does', 'still', \"also\" ].forEach(function (chain) {\n    Assertion.addProperty(chain);\n  });\n\n  /**\n   * ### .not\n   *\n   * Negates all assertions that follow in the chain.\n   *\n   *     expect(function () {}).to.not.throw();\n   *     expect({a: 1}).to.not.have.property('b');\n   *     expect([1, 2]).to.be.an('array').that.does.not.include(3);\n   *\n   * Just because you can negate any assertion with `.not` doesn't mean you\n   * should. With great power comes great responsibility. It's often best to\n   * assert that the one expected output was produced, rather than asserting\n   * that one of countless unexpected outputs wasn't produced. See individual\n   * assertions for specific guidance.\n   *\n   *     expect(2).to.equal(2); // Recommended\n   *     expect(2).to.not.equal(1); // Not recommended\n   *\n   * @name not\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('not', function () {\n    flag(this, 'negate', true);\n  });\n\n  /**\n   * ### .deep\n   *\n   * Causes all `.equal`, `.include`, `.members`, `.keys`, and `.property`\n   * assertions that follow in the chain to use deep equality instead of strict\n   * (`===`) equality. See the `deep-eql` project page for info on the deep\n   * equality algorithm: https://github.com/chaijs/deep-eql.\n   *\n   *     // Target object deeply (but not strictly) equals `{a: 1}`\n   *     expect({a: 1}).to.deep.equal({a: 1});\n   *     expect({a: 1}).to.not.equal({a: 1});\n   *\n   *     // Target array deeply (but not strictly) includes `{a: 1}`\n   *     expect([{a: 1}]).to.deep.include({a: 1});\n   *     expect([{a: 1}]).to.not.include({a: 1});\n   *\n   *     // Target object deeply (but not strictly) includes `x: {a: 1}`\n   *     expect({x: {a: 1}}).to.deep.include({x: {a: 1}});\n   *     expect({x: {a: 1}}).to.not.include({x: {a: 1}});\n   *\n   *     // Target array deeply (but not strictly) has member `{a: 1}`\n   *     expect([{a: 1}]).to.have.deep.members([{a: 1}]);\n   *     expect([{a: 1}]).to.not.have.members([{a: 1}]);\n   *\n   *     // Target set deeply (but not strictly) has key `{a: 1}`\n   *     expect(new Set([{a: 1}])).to.have.deep.keys([{a: 1}]);\n   *     expect(new Set([{a: 1}])).to.not.have.keys([{a: 1}]);\n   *\n   *     // Target object deeply (but not strictly) has property `x: {a: 1}`\n   *     expect({x: {a: 1}}).to.have.deep.property('x', {a: 1});\n   *     expect({x: {a: 1}}).to.not.have.property('x', {a: 1});\n   *\n   * @name deep\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('deep', function () {\n    flag(this, 'deep', true);\n  });\n\n  /**\n   * ### .nested\n   *\n   * Enables dot- and bracket-notation in all `.property` and `.include`\n   * assertions that follow in the chain.\n   *\n   *     expect({a: {b: ['x', 'y']}}).to.have.nested.property('a.b[1]');\n   *     expect({a: {b: ['x', 'y']}}).to.nested.include({'a.b[1]': 'y'});\n   *\n   * If `.` or `[]` are part of an actual property name, they can be escaped by\n   * adding two backslashes before them.\n   *\n   *     expect({'.a': {'[b]': 'x'}}).to.have.nested.property('\\\\.a.\\\\[b\\\\]');\n   *     expect({'.a': {'[b]': 'x'}}).to.nested.include({'\\\\.a.\\\\[b\\\\]': 'x'});\n   *\n   * `.nested` cannot be combined with `.own`.\n   *\n   * @name nested\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('nested', function () {\n    flag(this, 'nested', true);\n  });\n\n  /**\n   * ### .own\n   *\n   * Causes all `.property` and `.include` assertions that follow in the chain\n   * to ignore inherited properties.\n   *\n   *     Object.prototype.b = 2;\n   *\n   *     expect({a: 1}).to.have.own.property('a');\n   *     expect({a: 1}).to.have.property('b');\n   *     expect({a: 1}).to.not.have.own.property('b');\n   *\n   *     expect({a: 1}).to.own.include({a: 1});\n   *     expect({a: 1}).to.include({b: 2}).but.not.own.include({b: 2});\n   *\n   * `.own` cannot be combined with `.nested`.\n   *\n   * @name own\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('own', function () {\n    flag(this, 'own', true);\n  });\n\n  /**\n   * ### .ordered\n   *\n   * Causes all `.members` assertions that follow in the chain to require that\n   * members be in the same order.\n   *\n   *     expect([1, 2]).to.have.ordered.members([1, 2])\n   *       .but.not.have.ordered.members([2, 1]);\n   *\n   * When `.include` and `.ordered` are combined, the ordering begins at the\n   * start of both arrays.\n   *\n   *     expect([1, 2, 3]).to.include.ordered.members([1, 2])\n   *       .but.not.include.ordered.members([2, 3]);\n   *\n   * @name ordered\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('ordered', function () {\n    flag(this, 'ordered', true);\n  });\n\n  /**\n   * ### .any\n   *\n   * Causes all `.keys` assertions that follow in the chain to only require that\n   * the target have at least one of the given keys. This is the opposite of\n   * `.all`, which requires that the target have all of the given keys.\n   *\n   *     expect({a: 1, b: 2}).to.not.have.any.keys('c', 'd');\n   *\n   * See the `.keys` doc for guidance on when to use `.any` or `.all`.\n   *\n   * @name any\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('any', function () {\n    flag(this, 'any', true);\n    flag(this, 'all', false);\n  });\n\n  /**\n   * ### .all\n   *\n   * Causes all `.keys` assertions that follow in the chain to require that the\n   * target have all of the given keys. This is the opposite of `.any`, which\n   * only requires that the target have at least one of the given keys.\n   *\n   *     expect({a: 1, b: 2}).to.have.all.keys('a', 'b');\n   *\n   * Note that `.all` is used by default when neither `.all` nor `.any` are\n   * added earlier in the chain. However, it's often best to add `.all` anyway\n   * because it improves readability.\n   *\n   * See the `.keys` doc for guidance on when to use `.any` or `.all`.\n   *\n   * @name all\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('all', function () {\n    flag(this, 'all', true);\n    flag(this, 'any', false);\n  });\n\n  /**\n   * ### .a(type[, msg])\n   *\n   * Asserts that the target's type is equal to the given string `type`. Types\n   * are case insensitive. See the `type-detect` project page for info on the\n   * type detection algorithm: https://github.com/chaijs/type-detect.\n   *\n   *     expect('foo').to.be.a('string');\n   *     expect({a: 1}).to.be.an('object');\n   *     expect(null).to.be.a('null');\n   *     expect(undefined).to.be.an('undefined');\n   *     expect(new Error).to.be.an('error');\n   *     expect(Promise.resolve()).to.be.a('promise');\n   *     expect(new Float32Array).to.be.a('float32array');\n   *     expect(Symbol()).to.be.a('symbol');\n   *\n   * `.a` supports objects that have a custom type set via `Symbol.toStringTag`.\n   *\n   *     var myObj = {\n   *       [Symbol.toStringTag]: 'myCustomType'\n   *     };\n   *\n   *     expect(myObj).to.be.a('myCustomType').but.not.an('object');\n   *\n   * It's often best to use `.a` to check a target's type before making more\n   * assertions on the same target. That way, you avoid unexpected behavior from\n   * any assertion that does different things based on the target's type.\n   *\n   *     expect([1, 2, 3]).to.be.an('array').that.includes(2);\n   *     expect([]).to.be.an('array').that.is.empty;\n   *\n   * Add `.not` earlier in the chain to negate `.a`. However, it's often best to\n   * assert that the target is the expected type, rather than asserting that it\n   * isn't one of many unexpected types.\n   *\n   *     expect('foo').to.be.a('string'); // Recommended\n   *     expect('foo').to.not.be.an('array'); // Not recommended\n   *\n   * `.a` accepts an optional `msg` argument which is a custom error message to\n   * show when the assertion fails. The message can also be given as the second\n   * argument to `expect`.\n   *\n   *     expect(1).to.be.a('string', 'nooo why fail??');\n   *     expect(1, 'nooo why fail??').to.be.a('string');\n   *\n   * `.a` can also be used as a language chain to improve the readability of\n   * your assertions.\n   *\n   *     expect({b: 2}).to.have.a.property('b');\n   *\n   * The alias `.an` can be used interchangeably with `.a`.\n   *\n   * @name a\n   * @alias an\n   * @param {String} type\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function an (type, msg) {\n    if (msg) flag(this, 'message', msg);\n    type = type.toLowerCase();\n    var obj = flag(this, 'object')\n      , article = ~[ 'a', 'e', 'i', 'o', 'u' ].indexOf(type.charAt(0)) ? 'an ' : 'a ';\n\n    this.assert(\n        type === _.type(obj).toLowerCase()\n      , 'expected #{this} to be ' + article + type\n      , 'expected #{this} not to be ' + article + type\n    );\n  }\n\n  Assertion.addChainableMethod('an', an);\n  Assertion.addChainableMethod('a', an);\n\n  /**\n   * ### .include(val[, msg])\n   *\n   * When the target is a string, `.include` asserts that the given string `val`\n   * is a substring of the target.\n   *\n   *     expect('foobar').to.include('foo');\n   *\n   * When the target is an array, `.include` asserts that the given `val` is a\n   * member of the target.\n   *\n   *     expect([1, 2, 3]).to.include(2);\n   *\n   * When the target is an object, `.include` asserts that the given object\n   * `val`'s properties are a subset of the target's properties.\n   *\n   *     expect({a: 1, b: 2, c: 3}).to.include({a: 1, b: 2});\n   *\n   * When the target is a Set or WeakSet, `.include` asserts that the given `val` is a\n   * member of the target. SameValueZero equality algorithm is used.\n   *\n   *     expect(new Set([1, 2])).to.include(2);\n   *\n   * When the target is a Map, `.include` asserts that the given `val` is one of\n   * the values of the target. SameValueZero equality algorithm is used.\n   *\n   *     expect(new Map([['a', 1], ['b', 2]])).to.include(2);\n   *\n   * Because `.include` does different things based on the target's type, it's\n   * important to check the target's type before using `.include`. See the `.a`\n   * doc for info on testing a target's type.\n   *\n   *     expect([1, 2, 3]).to.be.an('array').that.includes(2);\n   *\n   * By default, strict (`===`) equality is used to compare array members and\n   * object properties. Add `.deep` earlier in the chain to use deep equality\n   * instead (WeakSet targets are not supported). See the `deep-eql` project\n   * page for info on the deep equality algorithm: https://github.com/chaijs/deep-eql.\n   *\n   *     // Target array deeply (but not strictly) includes `{a: 1}`\n   *     expect([{a: 1}]).to.deep.include({a: 1});\n   *     expect([{a: 1}]).to.not.include({a: 1});\n   *\n   *     // Target object deeply (but not strictly) includes `x: {a: 1}`\n   *     expect({x: {a: 1}}).to.deep.include({x: {a: 1}});\n   *     expect({x: {a: 1}}).to.not.include({x: {a: 1}});\n   *\n   * By default, all of the target's properties are searched when working with\n   * objects. This includes properties that are inherited and/or non-enumerable.\n   * Add `.own` earlier in the chain to exclude the target's inherited\n   * properties from the search.\n   *\n   *     Object.prototype.b = 2;\n   *\n   *     expect({a: 1}).to.own.include({a: 1});\n   *     expect({a: 1}).to.include({b: 2}).but.not.own.include({b: 2});\n   *\n   * Note that a target object is always only searched for `val`'s own\n   * enumerable properties.\n   *\n   * `.deep` and `.own` can be combined.\n   *\n   *     expect({a: {b: 2}}).to.deep.own.include({a: {b: 2}});\n   *\n   * Add `.nested` earlier in the chain to enable dot- and bracket-notation when\n   * referencing nested properties.\n   *\n   *     expect({a: {b: ['x', 'y']}}).to.nested.include({'a.b[1]': 'y'});\n   *\n   * If `.` or `[]` are part of an actual property name, they can be escaped by\n   * adding two backslashes before them.\n   *\n   *     expect({'.a': {'[b]': 2}}).to.nested.include({'\\\\.a.\\\\[b\\\\]': 2});\n   *\n   * `.deep` and `.nested` can be combined.\n   *\n   *     expect({a: {b: [{c: 3}]}}).to.deep.nested.include({'a.b[0]': {c: 3}});\n   *\n   * `.own` and `.nested` cannot be combined.\n   *\n   * Add `.not` earlier in the chain to negate `.include`.\n   *\n   *     expect('foobar').to.not.include('taco');\n   *     expect([1, 2, 3]).to.not.include(4);\n   *\n   * However, it's dangerous to negate `.include` when the target is an object.\n   * The problem is that it creates uncertain expectations by asserting that the\n   * target object doesn't have all of `val`'s key/value pairs but may or may\n   * not have some of them. It's often best to identify the exact output that's\n   * expected, and then write an assertion that only accepts that exact output.\n   *\n   * When the target object isn't even expected to have `val`'s keys, it's\n   * often best to assert exactly that.\n   *\n   *     expect({c: 3}).to.not.have.any.keys('a', 'b'); // Recommended\n   *     expect({c: 3}).to.not.include({a: 1, b: 2}); // Not recommended\n   *\n   * When the target object is expected to have `val`'s keys, it's often best to\n   * assert that each of the properties has its expected value, rather than\n   * asserting that each property doesn't have one of many unexpected values.\n   *\n   *     expect({a: 3, b: 4}).to.include({a: 3, b: 4}); // Recommended\n   *     expect({a: 3, b: 4}).to.not.include({a: 1, b: 2}); // Not recommended\n   *\n   * `.include` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`.\n   *\n   *     expect([1, 2, 3]).to.include(4, 'nooo why fail??');\n   *     expect([1, 2, 3], 'nooo why fail??').to.include(4);\n   *\n   * `.include` can also be used as a language chain, causing all `.members` and\n   * `.keys` assertions that follow in the chain to require the target to be a\n   * superset of the expected set, rather than an identical set. Note that\n   * `.members` ignores duplicates in the subset when `.include` is added.\n   *\n   *     // Target object's keys are a superset of ['a', 'b'] but not identical\n   *     expect({a: 1, b: 2, c: 3}).to.include.all.keys('a', 'b');\n   *     expect({a: 1, b: 2, c: 3}).to.not.have.all.keys('a', 'b');\n   *\n   *     // Target array is a superset of [1, 2] but not identical\n   *     expect([1, 2, 3]).to.include.members([1, 2]);\n   *     expect([1, 2, 3]).to.not.have.members([1, 2]);\n   *\n   *     // Duplicates in the subset are ignored\n   *     expect([1, 2, 3]).to.include.members([1, 2, 2, 2]);\n   *\n   * Note that adding `.any` earlier in the chain causes the `.keys` assertion\n   * to ignore `.include`.\n   *\n   *     // Both assertions are identical\n   *     expect({a: 1}).to.include.any.keys('a', 'b');\n   *     expect({a: 1}).to.have.any.keys('a', 'b');\n   *\n   * The aliases `.includes`, `.contain`, and `.contains` can be used\n   * interchangeably with `.include`.\n   *\n   * @name include\n   * @alias contain\n   * @alias includes\n   * @alias contains\n   * @param {Mixed} val\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function SameValueZero(a, b) {\n    return (_.isNaN(a) && _.isNaN(b)) || a === b;\n  }\n\n  function includeChainingBehavior () {\n    flag(this, 'contains', true);\n  }\n\n  function include (val, msg) {\n    if (msg) flag(this, 'message', msg);\n\n    var obj = flag(this, 'object')\n      , objType = _.type(obj).toLowerCase()\n      , flagMsg = flag(this, 'message')\n      , negate = flag(this, 'negate')\n      , ssfi = flag(this, 'ssfi')\n      , isDeep = flag(this, 'deep')\n      , descriptor = isDeep ? 'deep ' : '';\n\n    flagMsg = flagMsg ? flagMsg + ': ' : '';\n\n    var included = false;\n\n    switch (objType) {\n      case 'string':\n        included = obj.indexOf(val) !== -1;\n        break;\n\n      case 'weakset':\n        if (isDeep) {\n          throw new AssertionError(\n            flagMsg + 'unable to use .deep.include with WeakSet',\n            undefined,\n            ssfi\n          );\n        }\n\n        included = obj.has(val);\n        break;\n\n      case 'map':\n        var isEql = isDeep ? _.eql : SameValueZero;\n        obj.forEach(function (item) {\n          included = included || isEql(item, val);\n        });\n        break;\n\n      case 'set':\n        if (isDeep) {\n          obj.forEach(function (item) {\n            included = included || _.eql(item, val);\n          });\n        } else {\n          included = obj.has(val);\n        }\n        break;\n\n      case 'array':\n        if (isDeep) {\n          included = obj.some(function (item) {\n            return _.eql(item, val);\n          })\n        } else {\n          included = obj.indexOf(val) !== -1;\n        }\n        break;\n\n      default:\n        // This block is for asserting a subset of properties in an object.\n        // `_.expectTypes` isn't used here because `.include` should work with\n        // objects with a custom `@@toStringTag`.\n        if (val !== Object(val)) {\n          throw new AssertionError(\n            flagMsg + 'the given combination of arguments ('\n            + objType + ' and '\n            + _.type(val).toLowerCase() + ')'\n            + ' is invalid for this assertion. '\n            + 'You can use an array, a map, an object, a set, a string, '\n            + 'or a weakset instead of a '\n            + _.type(val).toLowerCase(),\n            undefined,\n            ssfi\n          );\n        }\n\n        var props = Object.keys(val)\n          , firstErr = null\n          , numErrs = 0;\n\n        props.forEach(function (prop) {\n          var propAssertion = new Assertion(obj);\n          _.transferFlags(this, propAssertion, true);\n          flag(propAssertion, 'lockSsfi', true);\n\n          if (!negate || props.length === 1) {\n            propAssertion.property(prop, val[prop]);\n            return;\n          }\n\n          try {\n            propAssertion.property(prop, val[prop]);\n          } catch (err) {\n            if (!_.checkError.compatibleConstructor(err, AssertionError)) {\n              throw err;\n            }\n            if (firstErr === null) firstErr = err;\n            numErrs++;\n          }\n        }, this);\n\n        // When validating .not.include with multiple properties, we only want\n        // to throw an assertion error if all of the properties are included,\n        // in which case we throw the first property assertion error that we\n        // encountered.\n        if (negate && props.length > 1 && numErrs === props.length) {\n          throw firstErr;\n        }\n        return;\n    }\n\n    // Assert inclusion in collection or substring in a string.\n    this.assert(\n      included\n      , 'expected #{this} to ' + descriptor + 'include ' + _.inspect(val)\n      , 'expected #{this} to not ' + descriptor + 'include ' + _.inspect(val));\n  }\n\n  Assertion.addChainableMethod('include', include, includeChainingBehavior);\n  Assertion.addChainableMethod('contain', include, includeChainingBehavior);\n  Assertion.addChainableMethod('contains', include, includeChainingBehavior);\n  Assertion.addChainableMethod('includes', include, includeChainingBehavior);\n\n  /**\n   * ### .ok\n   *\n   * Asserts that the target is a truthy value (considered `true` in boolean context).\n   * However, it's often best to assert that the target is strictly (`===`) or\n   * deeply equal to its expected value.\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.be.ok; // Not recommended\n   *\n   *     expect(true).to.be.true; // Recommended\n   *     expect(true).to.be.ok; // Not recommended\n   *\n   * Add `.not` earlier in the chain to negate `.ok`.\n   *\n   *     expect(0).to.equal(0); // Recommended\n   *     expect(0).to.not.be.ok; // Not recommended\n   *\n   *     expect(false).to.be.false; // Recommended\n   *     expect(false).to.not.be.ok; // Not recommended\n   *\n   *     expect(null).to.be.null; // Recommended\n   *     expect(null).to.not.be.ok; // Not recommended\n   *\n   *     expect(undefined).to.be.undefined; // Recommended\n   *     expect(undefined).to.not.be.ok; // Not recommended\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect(false, 'nooo why fail??').to.be.ok;\n   *\n   * @name ok\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('ok', function () {\n    this.assert(\n        flag(this, 'object')\n      , 'expected #{this} to be truthy'\n      , 'expected #{this} to be falsy');\n  });\n\n  /**\n   * ### .true\n   *\n   * Asserts that the target is strictly (`===`) equal to `true`.\n   *\n   *     expect(true).to.be.true;\n   *\n   * Add `.not` earlier in the chain to negate `.true`. However, it's often best\n   * to assert that the target is equal to its expected value, rather than not\n   * equal to `true`.\n   *\n   *     expect(false).to.be.false; // Recommended\n   *     expect(false).to.not.be.true; // Not recommended\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.not.be.true; // Not recommended\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect(false, 'nooo why fail??').to.be.true;\n   *\n   * @name true\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('true', function () {\n    this.assert(\n        true === flag(this, 'object')\n      , 'expected #{this} to be true'\n      , 'expected #{this} to be false'\n      , flag(this, 'negate') ? false : true\n    );\n  });\n\n  /**\n   * ### .false\n   *\n   * Asserts that the target is strictly (`===`) equal to `false`.\n   *\n   *     expect(false).to.be.false;\n   *\n   * Add `.not` earlier in the chain to negate `.false`. However, it's often\n   * best to assert that the target is equal to its expected value, rather than\n   * not equal to `false`.\n   *\n   *     expect(true).to.be.true; // Recommended\n   *     expect(true).to.not.be.false; // Not recommended\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.not.be.false; // Not recommended\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect(true, 'nooo why fail??').to.be.false;\n   *\n   * @name false\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('false', function () {\n    this.assert(\n        false === flag(this, 'object')\n      , 'expected #{this} to be false'\n      , 'expected #{this} to be true'\n      , flag(this, 'negate') ? true : false\n    );\n  });\n\n  /**\n   * ### .null\n   *\n   * Asserts that the target is strictly (`===`) equal to `null`.\n   *\n   *     expect(null).to.be.null;\n   *\n   * Add `.not` earlier in the chain to negate `.null`. However, it's often best\n   * to assert that the target is equal to its expected value, rather than not\n   * equal to `null`.\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.not.be.null; // Not recommended\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect(42, 'nooo why fail??').to.be.null;\n   *\n   * @name null\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('null', function () {\n    this.assert(\n        null === flag(this, 'object')\n      , 'expected #{this} to be null'\n      , 'expected #{this} not to be null'\n    );\n  });\n\n  /**\n   * ### .undefined\n   *\n   * Asserts that the target is strictly (`===`) equal to `undefined`.\n   *\n   *     expect(undefined).to.be.undefined;\n   *\n   * Add `.not` earlier in the chain to negate `.undefined`. However, it's often\n   * best to assert that the target is equal to its expected value, rather than\n   * not equal to `undefined`.\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.not.be.undefined; // Not recommended\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect(42, 'nooo why fail??').to.be.undefined;\n   *\n   * @name undefined\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('undefined', function () {\n    this.assert(\n        undefined === flag(this, 'object')\n      , 'expected #{this} to be undefined'\n      , 'expected #{this} not to be undefined'\n    );\n  });\n\n  /**\n   * ### .NaN\n   *\n   * Asserts that the target is exactly `NaN`.\n   *\n   *     expect(NaN).to.be.NaN;\n   *\n   * Add `.not` earlier in the chain to negate `.NaN`. However, it's often best\n   * to assert that the target is equal to its expected value, rather than not\n   * equal to `NaN`.\n   *\n   *     expect('foo').to.equal('foo'); // Recommended\n   *     expect('foo').to.not.be.NaN; // Not recommended\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect(42, 'nooo why fail??').to.be.NaN;\n   *\n   * @name NaN\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('NaN', function () {\n    this.assert(\n        _.isNaN(flag(this, 'object'))\n        , 'expected #{this} to be NaN'\n        , 'expected #{this} not to be NaN'\n    );\n  });\n\n  /**\n   * ### .exist\n   *\n   * Asserts that the target is not strictly (`===`) equal to either `null` or\n   * `undefined`. However, it's often best to assert that the target is equal to\n   * its expected value.\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.exist; // Not recommended\n   *\n   *     expect(0).to.equal(0); // Recommended\n   *     expect(0).to.exist; // Not recommended\n   *\n   * Add `.not` earlier in the chain to negate `.exist`.\n   *\n   *     expect(null).to.be.null; // Recommended\n   *     expect(null).to.not.exist; // Not recommended\n   *\n   *     expect(undefined).to.be.undefined; // Recommended\n   *     expect(undefined).to.not.exist; // Not recommended\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect(null, 'nooo why fail??').to.exist;\n   *\n   * The alias `.exists` can be used interchangeably with `.exist`.\n   *\n   * @name exist\n   * @alias exists\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertExist () {\n    var val = flag(this, 'object');\n    this.assert(\n        val !== null && val !== undefined\n      , 'expected #{this} to exist'\n      , 'expected #{this} to not exist'\n    );\n  }\n\n  Assertion.addProperty('exist', assertExist);\n  Assertion.addProperty('exists', assertExist);\n\n  /**\n   * ### .empty\n   *\n   * When the target is a string or array, `.empty` asserts that the target's\n   * `length` property is strictly (`===`) equal to `0`.\n   *\n   *     expect([]).to.be.empty;\n   *     expect('').to.be.empty;\n   *\n   * When the target is a map or set, `.empty` asserts that the target's `size`\n   * property is strictly equal to `0`.\n   *\n   *     expect(new Set()).to.be.empty;\n   *     expect(new Map()).to.be.empty;\n   *\n   * When the target is a non-function object, `.empty` asserts that the target\n   * doesn't have any own enumerable properties. Properties with Symbol-based\n   * keys are excluded from the count.\n   *\n   *     expect({}).to.be.empty;\n   *\n   * Because `.empty` does different things based on the target's type, it's\n   * important to check the target's type before using `.empty`. See the `.a`\n   * doc for info on testing a target's type.\n   *\n   *     expect([]).to.be.an('array').that.is.empty;\n   *\n   * Add `.not` earlier in the chain to negate `.empty`. However, it's often\n   * best to assert that the target contains its expected number of values,\n   * rather than asserting that it's not empty.\n   *\n   *     expect([1, 2, 3]).to.have.lengthOf(3); // Recommended\n   *     expect([1, 2, 3]).to.not.be.empty; // Not recommended\n   *\n   *     expect(new Set([1, 2, 3])).to.have.property('size', 3); // Recommended\n   *     expect(new Set([1, 2, 3])).to.not.be.empty; // Not recommended\n   *\n   *     expect(Object.keys({a: 1})).to.have.lengthOf(1); // Recommended\n   *     expect({a: 1}).to.not.be.empty; // Not recommended\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect([1, 2, 3], 'nooo why fail??').to.be.empty;\n   *\n   * @name empty\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('empty', function () {\n    var val = flag(this, 'object')\n      , ssfi = flag(this, 'ssfi')\n      , flagMsg = flag(this, 'message')\n      , itemsCount;\n\n    flagMsg = flagMsg ? flagMsg + ': ' : '';\n\n    switch (_.type(val).toLowerCase()) {\n      case 'array':\n      case 'string':\n        itemsCount = val.length;\n        break;\n      case 'map':\n      case 'set':\n        itemsCount = val.size;\n        break;\n      case 'weakmap':\n      case 'weakset':\n        throw new AssertionError(\n          flagMsg + '.empty was passed a weak collection',\n          undefined,\n          ssfi\n        );\n      case 'function':\n        var msg = flagMsg + '.empty was passed a function ' + _.getName(val);\n        throw new AssertionError(msg.trim(), undefined, ssfi);\n      default:\n        if (val !== Object(val)) {\n          throw new AssertionError(\n            flagMsg + '.empty was passed non-string primitive ' + _.inspect(val),\n            undefined,\n            ssfi\n          );\n        }\n        itemsCount = Object.keys(val).length;\n    }\n\n    this.assert(\n        0 === itemsCount\n      , 'expected #{this} to be empty'\n      , 'expected #{this} not to be empty'\n    );\n  });\n\n  /**\n   * ### .arguments\n   *\n   * Asserts that the target is an `arguments` object.\n   *\n   *     function test () {\n   *       expect(arguments).to.be.arguments;\n   *     }\n   *\n   *     test();\n   *\n   * Add `.not` earlier in the chain to negate `.arguments`. However, it's often\n   * best to assert which type the target is expected to be, rather than\n   * asserting that it’s not an `arguments` object.\n   *\n   *     expect('foo').to.be.a('string'); // Recommended\n   *     expect('foo').to.not.be.arguments; // Not recommended\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect({}, 'nooo why fail??').to.be.arguments;\n   *\n   * The alias `.Arguments` can be used interchangeably with `.arguments`.\n   *\n   * @name arguments\n   * @alias Arguments\n   * @namespace BDD\n   * @api public\n   */\n\n  function checkArguments () {\n    var obj = flag(this, 'object')\n      , type = _.type(obj);\n    this.assert(\n        'Arguments' === type\n      , 'expected #{this} to be arguments but got ' + type\n      , 'expected #{this} to not be arguments'\n    );\n  }\n\n  Assertion.addProperty('arguments', checkArguments);\n  Assertion.addProperty('Arguments', checkArguments);\n\n  /**\n   * ### .equal(val[, msg])\n   *\n   * Asserts that the target is strictly (`===`) equal to the given `val`.\n   *\n   *     expect(1).to.equal(1);\n   *     expect('foo').to.equal('foo');\n   *\n   * Add `.deep` earlier in the chain to use deep equality instead. See the\n   * `deep-eql` project page for info on the deep equality algorithm:\n   * https://github.com/chaijs/deep-eql.\n   *\n   *     // Target object deeply (but not strictly) equals `{a: 1}`\n   *     expect({a: 1}).to.deep.equal({a: 1});\n   *     expect({a: 1}).to.not.equal({a: 1});\n   *\n   *     // Target array deeply (but not strictly) equals `[1, 2]`\n   *     expect([1, 2]).to.deep.equal([1, 2]);\n   *     expect([1, 2]).to.not.equal([1, 2]);\n   *\n   * Add `.not` earlier in the chain to negate `.equal`. However, it's often\n   * best to assert that the target is equal to its expected value, rather than\n   * not equal to one of countless unexpected values.\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.not.equal(2); // Not recommended\n   *\n   * `.equal` accepts an optional `msg` argument which is a custom error message\n   * to show when the assertion fails. The message can also be given as the\n   * second argument to `expect`.\n   *\n   *     expect(1).to.equal(2, 'nooo why fail??');\n   *     expect(1, 'nooo why fail??').to.equal(2);\n   *\n   * The aliases `.equals` and `eq` can be used interchangeably with `.equal`.\n   *\n   * @name equal\n   * @alias equals\n   * @alias eq\n   * @param {Mixed} val\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertEqual (val, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object');\n    if (flag(this, 'deep')) {\n      var prevLockSsfi = flag(this, 'lockSsfi');\n      flag(this, 'lockSsfi', true);\n      this.eql(val);\n      flag(this, 'lockSsfi', prevLockSsfi);\n    } else {\n      this.assert(\n          val === obj\n        , 'expected #{this} to equal #{exp}'\n        , 'expected #{this} to not equal #{exp}'\n        , val\n        , this._obj\n        , true\n      );\n    }\n  }\n\n  Assertion.addMethod('equal', assertEqual);\n  Assertion.addMethod('equals', assertEqual);\n  Assertion.addMethod('eq', assertEqual);\n\n  /**\n   * ### .eql(obj[, msg])\n   *\n   * Asserts that the target is deeply equal to the given `obj`. See the\n   * `deep-eql` project page for info on the deep equality algorithm:\n   * https://github.com/chaijs/deep-eql.\n   *\n   *     // Target object is deeply (but not strictly) equal to {a: 1}\n   *     expect({a: 1}).to.eql({a: 1}).but.not.equal({a: 1});\n   *\n   *     // Target array is deeply (but not strictly) equal to [1, 2]\n   *     expect([1, 2]).to.eql([1, 2]).but.not.equal([1, 2]);\n   *\n   * Add `.not` earlier in the chain to negate `.eql`. However, it's often best\n   * to assert that the target is deeply equal to its expected value, rather\n   * than not deeply equal to one of countless unexpected values.\n   *\n   *     expect({a: 1}).to.eql({a: 1}); // Recommended\n   *     expect({a: 1}).to.not.eql({b: 2}); // Not recommended\n   *\n   * `.eql` accepts an optional `msg` argument which is a custom error message\n   * to show when the assertion fails. The message can also be given as the\n   * second argument to `expect`.\n   *\n   *     expect({a: 1}).to.eql({b: 2}, 'nooo why fail??');\n   *     expect({a: 1}, 'nooo why fail??').to.eql({b: 2});\n   *\n   * The alias `.eqls` can be used interchangeably with `.eql`.\n   *\n   * The `.deep.equal` assertion is almost identical to `.eql` but with one\n   * difference: `.deep.equal` causes deep equality comparisons to also be used\n   * for any other assertions that follow in the chain.\n   *\n   * @name eql\n   * @alias eqls\n   * @param {Mixed} obj\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertEql(obj, msg) {\n    if (msg) flag(this, 'message', msg);\n    this.assert(\n        _.eql(obj, flag(this, 'object'))\n      , 'expected #{this} to deeply equal #{exp}'\n      , 'expected #{this} to not deeply equal #{exp}'\n      , obj\n      , this._obj\n      , true\n    );\n  }\n\n  Assertion.addMethod('eql', assertEql);\n  Assertion.addMethod('eqls', assertEql);\n\n  /**\n   * ### .above(n[, msg])\n   *\n   * Asserts that the target is a number or a date greater than the given number or date `n` respectively.\n   * However, it's often best to assert that the target is equal to its expected\n   * value.\n   *\n   *     expect(2).to.equal(2); // Recommended\n   *     expect(2).to.be.above(1); // Not recommended\n   *\n   * Add `.lengthOf` earlier in the chain to assert that the target's `length`\n   * or `size` is greater than the given number `n`.\n   *\n   *     expect('foo').to.have.lengthOf(3); // Recommended\n   *     expect('foo').to.have.lengthOf.above(2); // Not recommended\n   *\n   *     expect([1, 2, 3]).to.have.lengthOf(3); // Recommended\n   *     expect([1, 2, 3]).to.have.lengthOf.above(2); // Not recommended\n   *\n   * Add `.not` earlier in the chain to negate `.above`.\n   *\n   *     expect(2).to.equal(2); // Recommended\n   *     expect(1).to.not.be.above(2); // Not recommended\n   *\n   * `.above` accepts an optional `msg` argument which is a custom error message\n   * to show when the assertion fails. The message can also be given as the\n   * second argument to `expect`.\n   *\n   *     expect(1).to.be.above(2, 'nooo why fail??');\n   *     expect(1, 'nooo why fail??').to.be.above(2);\n   *\n   * The aliases `.gt` and `.greaterThan` can be used interchangeably with\n   * `.above`.\n   *\n   * @name above\n   * @alias gt\n   * @alias greaterThan\n   * @param {Number} n\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertAbove (n, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object')\n      , doLength = flag(this, 'doLength')\n      , flagMsg = flag(this, 'message')\n      , msgPrefix = ((flagMsg) ? flagMsg + ': ' : '')\n      , ssfi = flag(this, 'ssfi')\n      , objType = _.type(obj).toLowerCase()\n      , nType = _.type(n).toLowerCase()\n      , errorMessage\n      , shouldThrow = true;\n\n    if (doLength && objType !== 'map' && objType !== 'set') {\n      new Assertion(obj, flagMsg, ssfi, true).to.have.property('length');\n    }\n\n    if (!doLength && (objType === 'date' && nType !== 'date')) {\n      errorMessage = msgPrefix + 'the argument to above must be a date';\n    } else if (nType !== 'number' && (doLength || objType === 'number')) {\n      errorMessage = msgPrefix + 'the argument to above must be a number';\n    } else if (!doLength && (objType !== 'date' && objType !== 'number')) {\n      var printObj = (objType === 'string') ? \"'\" + obj + \"'\" : obj;\n      errorMessage = msgPrefix + 'expected ' + printObj + ' to be a number or a date';\n    } else {\n      shouldThrow = false;\n    }\n\n    if (shouldThrow) {\n      throw new AssertionError(errorMessage, undefined, ssfi);\n    }\n\n    if (doLength) {\n      var descriptor = 'length'\n        , itemsCount;\n      if (objType === 'map' || objType === 'set') {\n        descriptor = 'size';\n        itemsCount = obj.size;\n      } else {\n        itemsCount = obj.length;\n      }\n      this.assert(\n          itemsCount > n\n        , 'expected #{this} to have a ' + descriptor + ' above #{exp} but got #{act}'\n        , 'expected #{this} to not have a ' + descriptor + ' above #{exp}'\n        , n\n        , itemsCount\n      );\n    } else {\n      this.assert(\n          obj > n\n        , 'expected #{this} to be above #{exp}'\n        , 'expected #{this} to be at most #{exp}'\n        , n\n      );\n    }\n  }\n\n  Assertion.addMethod('above', assertAbove);\n  Assertion.addMethod('gt', assertAbove);\n  Assertion.addMethod('greaterThan', assertAbove);\n\n  /**\n   * ### .least(n[, msg])\n   *\n   * Asserts that the target is a number or a date greater than or equal to the given\n   * number or date `n` respectively. However, it's often best to assert that the target is equal to\n   * its expected value.\n   *\n   *     expect(2).to.equal(2); // Recommended\n   *     expect(2).to.be.at.least(1); // Not recommended\n   *     expect(2).to.be.at.least(2); // Not recommended\n   *\n   * Add `.lengthOf` earlier in the chain to assert that the target's `length`\n   * or `size` is greater than or equal to the given number `n`.\n   *\n   *     expect('foo').to.have.lengthOf(3); // Recommended\n   *     expect('foo').to.have.lengthOf.at.least(2); // Not recommended\n   *\n   *     expect([1, 2, 3]).to.have.lengthOf(3); // Recommended\n   *     expect([1, 2, 3]).to.have.lengthOf.at.least(2); // Not recommended\n   *\n   * Add `.not` earlier in the chain to negate `.least`.\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.not.be.at.least(2); // Not recommended\n   *\n   * `.least` accepts an optional `msg` argument which is a custom error message\n   * to show when the assertion fails. The message can also be given as the\n   * second argument to `expect`.\n   *\n   *     expect(1).to.be.at.least(2, 'nooo why fail??');\n   *     expect(1, 'nooo why fail??').to.be.at.least(2);\n   *\n   * The aliases `.gte` and `.greaterThanOrEqual` can be used interchangeably with\n   * `.least`.\n   *\n   * @name least\n   * @alias gte\n   * @alias greaterThanOrEqual\n   * @param {Number} n\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertLeast (n, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object')\n      , doLength = flag(this, 'doLength')\n      , flagMsg = flag(this, 'message')\n      , msgPrefix = ((flagMsg) ? flagMsg + ': ' : '')\n      , ssfi = flag(this, 'ssfi')\n      , objType = _.type(obj).toLowerCase()\n      , nType = _.type(n).toLowerCase()\n      , errorMessage\n      , shouldThrow = true;\n\n    if (doLength && objType !== 'map' && objType !== 'set') {\n      new Assertion(obj, flagMsg, ssfi, true).to.have.property('length');\n    }\n\n    if (!doLength && (objType === 'date' && nType !== 'date')) {\n      errorMessage = msgPrefix + 'the argument to least must be a date';\n    } else if (nType !== 'number' && (doLength || objType === 'number')) {\n      errorMessage = msgPrefix + 'the argument to least must be a number';\n    } else if (!doLength && (objType !== 'date' && objType !== 'number')) {\n      var printObj = (objType === 'string') ? \"'\" + obj + \"'\" : obj;\n      errorMessage = msgPrefix + 'expected ' + printObj + ' to be a number or a date';\n    } else {\n      shouldThrow = false;\n    }\n\n    if (shouldThrow) {\n      throw new AssertionError(errorMessage, undefined, ssfi);\n    }\n\n    if (doLength) {\n      var descriptor = 'length'\n        , itemsCount;\n      if (objType === 'map' || objType === 'set') {\n        descriptor = 'size';\n        itemsCount = obj.size;\n      } else {\n        itemsCount = obj.length;\n      }\n      this.assert(\n          itemsCount >= n\n        , 'expected #{this} to have a ' + descriptor + ' at least #{exp} but got #{act}'\n        , 'expected #{this} to have a ' + descriptor + ' below #{exp}'\n        , n\n        , itemsCount\n      );\n    } else {\n      this.assert(\n          obj >= n\n        , 'expected #{this} to be at least #{exp}'\n        , 'expected #{this} to be below #{exp}'\n        , n\n      );\n    }\n  }\n\n  Assertion.addMethod('least', assertLeast);\n  Assertion.addMethod('gte', assertLeast);\n  Assertion.addMethod('greaterThanOrEqual', assertLeast);\n\n  /**\n   * ### .below(n[, msg])\n   *\n   * Asserts that the target is a number or a date less than the given number or date `n` respectively.\n   * However, it's often best to assert that the target is equal to its expected\n   * value.\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.be.below(2); // Not recommended\n   *\n   * Add `.lengthOf` earlier in the chain to assert that the target's `length`\n   * or `size` is less than the given number `n`.\n   *\n   *     expect('foo').to.have.lengthOf(3); // Recommended\n   *     expect('foo').to.have.lengthOf.below(4); // Not recommended\n   *\n   *     expect([1, 2, 3]).to.have.length(3); // Recommended\n   *     expect([1, 2, 3]).to.have.lengthOf.below(4); // Not recommended\n   *\n   * Add `.not` earlier in the chain to negate `.below`.\n   *\n   *     expect(2).to.equal(2); // Recommended\n   *     expect(2).to.not.be.below(1); // Not recommended\n   *\n   * `.below` accepts an optional `msg` argument which is a custom error message\n   * to show when the assertion fails. The message can also be given as the\n   * second argument to `expect`.\n   *\n   *     expect(2).to.be.below(1, 'nooo why fail??');\n   *     expect(2, 'nooo why fail??').to.be.below(1);\n   *\n   * The aliases `.lt` and `.lessThan` can be used interchangeably with\n   * `.below`.\n   *\n   * @name below\n   * @alias lt\n   * @alias lessThan\n   * @param {Number} n\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertBelow (n, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object')\n      , doLength = flag(this, 'doLength')\n      , flagMsg = flag(this, 'message')\n      , msgPrefix = ((flagMsg) ? flagMsg + ': ' : '')\n      , ssfi = flag(this, 'ssfi')\n      , objType = _.type(obj).toLowerCase()\n      , nType = _.type(n).toLowerCase()\n      , errorMessage\n      , shouldThrow = true;\n\n    if (doLength && objType !== 'map' && objType !== 'set') {\n      new Assertion(obj, flagMsg, ssfi, true).to.have.property('length');\n    }\n\n    if (!doLength && (objType === 'date' && nType !== 'date')) {\n      errorMessage = msgPrefix + 'the argument to below must be a date';\n    } else if (nType !== 'number' && (doLength || objType === 'number')) {\n      errorMessage = msgPrefix + 'the argument to below must be a number';\n    } else if (!doLength && (objType !== 'date' && objType !== 'number')) {\n      var printObj = (objType === 'string') ? \"'\" + obj + \"'\" : obj;\n      errorMessage = msgPrefix + 'expected ' + printObj + ' to be a number or a date';\n    } else {\n      shouldThrow = false;\n    }\n\n    if (shouldThrow) {\n      throw new AssertionError(errorMessage, undefined, ssfi);\n    }\n\n    if (doLength) {\n      var descriptor = 'length'\n        , itemsCount;\n      if (objType === 'map' || objType === 'set') {\n        descriptor = 'size';\n        itemsCount = obj.size;\n      } else {\n        itemsCount = obj.length;\n      }\n      this.assert(\n          itemsCount < n\n        , 'expected #{this} to have a ' + descriptor + ' below #{exp} but got #{act}'\n        , 'expected #{this} to not have a ' + descriptor + ' below #{exp}'\n        , n\n        , itemsCount\n      );\n    } else {\n      this.assert(\n          obj < n\n        , 'expected #{this} to be below #{exp}'\n        , 'expected #{this} to be at least #{exp}'\n        , n\n      );\n    }\n  }\n\n  Assertion.addMethod('below', assertBelow);\n  Assertion.addMethod('lt', assertBelow);\n  Assertion.addMethod('lessThan', assertBelow);\n\n  /**\n   * ### .most(n[, msg])\n   *\n   * Asserts that the target is a number or a date less than or equal to the given number\n   * or date `n` respectively. However, it's often best to assert that the target is equal to its\n   * expected value.\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.be.at.most(2); // Not recommended\n   *     expect(1).to.be.at.most(1); // Not recommended\n   *\n   * Add `.lengthOf` earlier in the chain to assert that the target's `length`\n   * or `size` is less than or equal to the given number `n`.\n   *\n   *     expect('foo').to.have.lengthOf(3); // Recommended\n   *     expect('foo').to.have.lengthOf.at.most(4); // Not recommended\n   *\n   *     expect([1, 2, 3]).to.have.lengthOf(3); // Recommended\n   *     expect([1, 2, 3]).to.have.lengthOf.at.most(4); // Not recommended\n   *\n   * Add `.not` earlier in the chain to negate `.most`.\n   *\n   *     expect(2).to.equal(2); // Recommended\n   *     expect(2).to.not.be.at.most(1); // Not recommended\n   *\n   * `.most` accepts an optional `msg` argument which is a custom error message\n   * to show when the assertion fails. The message can also be given as the\n   * second argument to `expect`.\n   *\n   *     expect(2).to.be.at.most(1, 'nooo why fail??');\n   *     expect(2, 'nooo why fail??').to.be.at.most(1);\n   *\n   * The aliases `.lte` and `.lessThanOrEqual` can be used interchangeably with\n   * `.most`.\n   *\n   * @name most\n   * @alias lte\n   * @alias lessThanOrEqual\n   * @param {Number} n\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertMost (n, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object')\n      , doLength = flag(this, 'doLength')\n      , flagMsg = flag(this, 'message')\n      , msgPrefix = ((flagMsg) ? flagMsg + ': ' : '')\n      , ssfi = flag(this, 'ssfi')\n      , objType = _.type(obj).toLowerCase()\n      , nType = _.type(n).toLowerCase()\n      , errorMessage\n      , shouldThrow = true;\n\n    if (doLength && objType !== 'map' && objType !== 'set') {\n      new Assertion(obj, flagMsg, ssfi, true).to.have.property('length');\n    }\n\n    if (!doLength && (objType === 'date' && nType !== 'date')) {\n      errorMessage = msgPrefix + 'the argument to most must be a date';\n    } else if (nType !== 'number' && (doLength || objType === 'number')) {\n      errorMessage = msgPrefix + 'the argument to most must be a number';\n    } else if (!doLength && (objType !== 'date' && objType !== 'number')) {\n      var printObj = (objType === 'string') ? \"'\" + obj + \"'\" : obj;\n      errorMessage = msgPrefix + 'expected ' + printObj + ' to be a number or a date';\n    } else {\n      shouldThrow = false;\n    }\n\n    if (shouldThrow) {\n      throw new AssertionError(errorMessage, undefined, ssfi);\n    }\n\n    if (doLength) {\n      var descriptor = 'length'\n        , itemsCount;\n      if (objType === 'map' || objType === 'set') {\n        descriptor = 'size';\n        itemsCount = obj.size;\n      } else {\n        itemsCount = obj.length;\n      }\n      this.assert(\n          itemsCount <= n\n        , 'expected #{this} to have a ' + descriptor + ' at most #{exp} but got #{act}'\n        , 'expected #{this} to have a ' + descriptor + ' above #{exp}'\n        , n\n        , itemsCount\n      );\n    } else {\n      this.assert(\n          obj <= n\n        , 'expected #{this} to be at most #{exp}'\n        , 'expected #{this} to be above #{exp}'\n        , n\n      );\n    }\n  }\n\n  Assertion.addMethod('most', assertMost);\n  Assertion.addMethod('lte', assertMost);\n  Assertion.addMethod('lessThanOrEqual', assertMost);\n\n  /**\n   * ### .within(start, finish[, msg])\n   *\n   * Asserts that the target is a number or a date greater than or equal to the given\n   * number or date `start`, and less than or equal to the given number or date `finish` respectively.\n   * However, it's often best to assert that the target is equal to its expected\n   * value.\n   *\n   *     expect(2).to.equal(2); // Recommended\n   *     expect(2).to.be.within(1, 3); // Not recommended\n   *     expect(2).to.be.within(2, 3); // Not recommended\n   *     expect(2).to.be.within(1, 2); // Not recommended\n   *\n   * Add `.lengthOf` earlier in the chain to assert that the target's `length`\n   * or `size` is greater than or equal to the given number `start`, and less\n   * than or equal to the given number `finish`.\n   *\n   *     expect('foo').to.have.lengthOf(3); // Recommended\n   *     expect('foo').to.have.lengthOf.within(2, 4); // Not recommended\n   *\n   *     expect([1, 2, 3]).to.have.lengthOf(3); // Recommended\n   *     expect([1, 2, 3]).to.have.lengthOf.within(2, 4); // Not recommended\n   *\n   * Add `.not` earlier in the chain to negate `.within`.\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.not.be.within(2, 4); // Not recommended\n   *\n   * `.within` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`.\n   *\n   *     expect(4).to.be.within(1, 3, 'nooo why fail??');\n   *     expect(4, 'nooo why fail??').to.be.within(1, 3);\n   *\n   * @name within\n   * @param {Number} start lower bound inclusive\n   * @param {Number} finish upper bound inclusive\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addMethod('within', function (start, finish, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object')\n      , doLength = flag(this, 'doLength')\n      , flagMsg = flag(this, 'message')\n      , msgPrefix = ((flagMsg) ? flagMsg + ': ' : '')\n      , ssfi = flag(this, 'ssfi')\n      , objType = _.type(obj).toLowerCase()\n      , startType = _.type(start).toLowerCase()\n      , finishType = _.type(finish).toLowerCase()\n      , errorMessage\n      , shouldThrow = true\n      , range = (startType === 'date' && finishType === 'date')\n          ? start.toISOString() + '..' + finish.toISOString()\n          : start + '..' + finish;\n\n    if (doLength && objType !== 'map' && objType !== 'set') {\n      new Assertion(obj, flagMsg, ssfi, true).to.have.property('length');\n    }\n\n    if (!doLength && (objType === 'date' && (startType !== 'date' || finishType !== 'date'))) {\n      errorMessage = msgPrefix + 'the arguments to within must be dates';\n    } else if ((startType !== 'number' || finishType !== 'number') && (doLength || objType === 'number')) {\n      errorMessage = msgPrefix + 'the arguments to within must be numbers';\n    } else if (!doLength && (objType !== 'date' && objType !== 'number')) {\n      var printObj = (objType === 'string') ? \"'\" + obj + \"'\" : obj;\n      errorMessage = msgPrefix + 'expected ' + printObj + ' to be a number or a date';\n    } else {\n      shouldThrow = false;\n    }\n\n    if (shouldThrow) {\n      throw new AssertionError(errorMessage, undefined, ssfi);\n    }\n\n    if (doLength) {\n      var descriptor = 'length'\n        , itemsCount;\n      if (objType === 'map' || objType === 'set') {\n        descriptor = 'size';\n        itemsCount = obj.size;\n      } else {\n        itemsCount = obj.length;\n      }\n      this.assert(\n          itemsCount >= start && itemsCount <= finish\n        , 'expected #{this} to have a ' + descriptor + ' within ' + range\n        , 'expected #{this} to not have a ' + descriptor + ' within ' + range\n      );\n    } else {\n      this.assert(\n          obj >= start && obj <= finish\n        , 'expected #{this} to be within ' + range\n        , 'expected #{this} to not be within ' + range\n      );\n    }\n  });\n\n  /**\n   * ### .instanceof(constructor[, msg])\n   *\n   * Asserts that the target is an instance of the given `constructor`.\n   *\n   *     function Cat () { }\n   *\n   *     expect(new Cat()).to.be.an.instanceof(Cat);\n   *     expect([1, 2]).to.be.an.instanceof(Array);\n   *\n   * Add `.not` earlier in the chain to negate `.instanceof`.\n   *\n   *     expect({a: 1}).to.not.be.an.instanceof(Array);\n   *\n   * `.instanceof` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`.\n   *\n   *     expect(1).to.be.an.instanceof(Array, 'nooo why fail??');\n   *     expect(1, 'nooo why fail??').to.be.an.instanceof(Array);\n   *\n   * Due to limitations in ES5, `.instanceof` may not always work as expected\n   * when using a transpiler such as Babel or TypeScript. In particular, it may\n   * produce unexpected results when subclassing built-in object such as\n   * `Array`, `Error`, and `Map`. See your transpiler's docs for details:\n   *\n   * - ([Babel](https://babeljs.io/docs/usage/caveats/#classes))\n   * - ([TypeScript](https://github.com/Microsoft/TypeScript/wiki/Breaking-Changes#extending-built-ins-like-error-array-and-map-may-no-longer-work))\n   *\n   * The alias `.instanceOf` can be used interchangeably with `.instanceof`.\n   *\n   * @name instanceof\n   * @param {Constructor} constructor\n   * @param {String} msg _optional_\n   * @alias instanceOf\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertInstanceOf (constructor, msg) {\n    if (msg) flag(this, 'message', msg);\n\n    var target = flag(this, 'object')\n    var ssfi = flag(this, 'ssfi');\n    var flagMsg = flag(this, 'message');\n\n    try {\n      var isInstanceOf = target instanceof constructor;\n    } catch (err) {\n      if (err instanceof TypeError) {\n        flagMsg = flagMsg ? flagMsg + ': ' : '';\n        throw new AssertionError(\n          flagMsg + 'The instanceof assertion needs a constructor but '\n            + _.type(constructor) + ' was given.',\n          undefined,\n          ssfi\n        );\n      }\n      throw err;\n    }\n\n    var name = _.getName(constructor);\n    if (name === null) {\n      name = 'an unnamed constructor';\n    }\n\n    this.assert(\n        isInstanceOf\n      , 'expected #{this} to be an instance of ' + name\n      , 'expected #{this} to not be an instance of ' + name\n    );\n  };\n\n  Assertion.addMethod('instanceof', assertInstanceOf);\n  Assertion.addMethod('instanceOf', assertInstanceOf);\n\n  /**\n   * ### .property(name[, val[, msg]])\n   *\n   * Asserts that the target has a property with the given key `name`.\n   *\n   *     expect({a: 1}).to.have.property('a');\n   *\n   * When `val` is provided, `.property` also asserts that the property's value\n   * is equal to the given `val`.\n   *\n   *     expect({a: 1}).to.have.property('a', 1);\n   *\n   * By default, strict (`===`) equality is used. Add `.deep` earlier in the\n   * chain to use deep equality instead. See the `deep-eql` project page for\n   * info on the deep equality algorithm: https://github.com/chaijs/deep-eql.\n   *\n   *     // Target object deeply (but not strictly) has property `x: {a: 1}`\n   *     expect({x: {a: 1}}).to.have.deep.property('x', {a: 1});\n   *     expect({x: {a: 1}}).to.not.have.property('x', {a: 1});\n   *\n   * The target's enumerable and non-enumerable properties are always included\n   * in the search. By default, both own and inherited properties are included.\n   * Add `.own` earlier in the chain to exclude inherited properties from the\n   * search.\n   *\n   *     Object.prototype.b = 2;\n   *\n   *     expect({a: 1}).to.have.own.property('a');\n   *     expect({a: 1}).to.have.own.property('a', 1);\n   *     expect({a: 1}).to.have.property('b');\n   *     expect({a: 1}).to.not.have.own.property('b');\n   *\n   * `.deep` and `.own` can be combined.\n   *\n   *     expect({x: {a: 1}}).to.have.deep.own.property('x', {a: 1});\n   *\n   * Add `.nested` earlier in the chain to enable dot- and bracket-notation when\n   * referencing nested properties.\n   *\n   *     expect({a: {b: ['x', 'y']}}).to.have.nested.property('a.b[1]');\n   *     expect({a: {b: ['x', 'y']}}).to.have.nested.property('a.b[1]', 'y');\n   *\n   * If `.` or `[]` are part of an actual property name, they can be escaped by\n   * adding two backslashes before them.\n   *\n   *     expect({'.a': {'[b]': 'x'}}).to.have.nested.property('\\\\.a.\\\\[b\\\\]');\n   *\n   * `.deep` and `.nested` can be combined.\n   *\n   *     expect({a: {b: [{c: 3}]}})\n   *       .to.have.deep.nested.property('a.b[0]', {c: 3});\n   *\n   * `.own` and `.nested` cannot be combined.\n   *\n   * Add `.not` earlier in the chain to negate `.property`.\n   *\n   *     expect({a: 1}).to.not.have.property('b');\n   *\n   * However, it's dangerous to negate `.property` when providing `val`. The\n   * problem is that it creates uncertain expectations by asserting that the\n   * target either doesn't have a property with the given key `name`, or that it\n   * does have a property with the given key `name` but its value isn't equal to\n   * the given `val`. It's often best to identify the exact output that's\n   * expected, and then write an assertion that only accepts that exact output.\n   *\n   * When the target isn't expected to have a property with the given key\n   * `name`, it's often best to assert exactly that.\n   *\n   *     expect({b: 2}).to.not.have.property('a'); // Recommended\n   *     expect({b: 2}).to.not.have.property('a', 1); // Not recommended\n   *\n   * When the target is expected to have a property with the given key `name`,\n   * it's often best to assert that the property has its expected value, rather\n   * than asserting that it doesn't have one of many unexpected values.\n   *\n   *     expect({a: 3}).to.have.property('a', 3); // Recommended\n   *     expect({a: 3}).to.not.have.property('a', 1); // Not recommended\n   *\n   * `.property` changes the target of any assertions that follow in the chain\n   * to be the value of the property from the original target object.\n   *\n   *     expect({a: 1}).to.have.property('a').that.is.a('number');\n   *\n   * `.property` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`. When not providing `val`, only use the\n   * second form.\n   *\n   *     // Recommended\n   *     expect({a: 1}).to.have.property('a', 2, 'nooo why fail??');\n   *     expect({a: 1}, 'nooo why fail??').to.have.property('a', 2);\n   *     expect({a: 1}, 'nooo why fail??').to.have.property('b');\n   *\n   *     // Not recommended\n   *     expect({a: 1}).to.have.property('b', undefined, 'nooo why fail??');\n   *\n   * The above assertion isn't the same thing as not providing `val`. Instead,\n   * it's asserting that the target object has a `b` property that's equal to\n   * `undefined`.\n   *\n   * The assertions `.ownProperty` and `.haveOwnProperty` can be used\n   * interchangeably with `.own.property`.\n   *\n   * @name property\n   * @param {String} name\n   * @param {Mixed} val (optional)\n   * @param {String} msg _optional_\n   * @returns value of property for chaining\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertProperty (name, val, msg) {\n    if (msg) flag(this, 'message', msg);\n\n    var isNested = flag(this, 'nested')\n      , isOwn = flag(this, 'own')\n      , flagMsg = flag(this, 'message')\n      , obj = flag(this, 'object')\n      , ssfi = flag(this, 'ssfi')\n      , nameType = typeof name;\n\n    flagMsg = flagMsg ? flagMsg + ': ' : '';\n\n    if (isNested) {\n      if (nameType !== 'string') {\n        throw new AssertionError(\n          flagMsg + 'the argument to property must be a string when using nested syntax',\n          undefined,\n          ssfi\n        );\n      }\n    } else {\n      if (nameType !== 'string' && nameType !== 'number' && nameType !== 'symbol') {\n        throw new AssertionError(\n          flagMsg + 'the argument to property must be a string, number, or symbol',\n          undefined,\n          ssfi\n        );\n      }\n    }\n\n    if (isNested && isOwn) {\n      throw new AssertionError(\n        flagMsg + 'The \"nested\" and \"own\" flags cannot be combined.',\n        undefined,\n        ssfi\n      );\n    }\n\n    if (obj === null || obj === undefined) {\n      throw new AssertionError(\n        flagMsg + 'Target cannot be null or undefined.',\n        undefined,\n        ssfi\n      );\n    }\n\n    var isDeep = flag(this, 'deep')\n      , negate = flag(this, 'negate')\n      , pathInfo = isNested ? _.getPathInfo(obj, name) : null\n      , value = isNested ? pathInfo.value : obj[name];\n\n    var descriptor = '';\n    if (isDeep) descriptor += 'deep ';\n    if (isOwn) descriptor += 'own ';\n    if (isNested) descriptor += 'nested ';\n    descriptor += 'property ';\n\n    var hasProperty;\n    if (isOwn) hasProperty = Object.prototype.hasOwnProperty.call(obj, name);\n    else if (isNested) hasProperty = pathInfo.exists;\n    else hasProperty = _.hasProperty(obj, name);\n\n    // When performing a negated assertion for both name and val, merely having\n    // a property with the given name isn't enough to cause the assertion to\n    // fail. It must both have a property with the given name, and the value of\n    // that property must equal the given val. Therefore, skip this assertion in\n    // favor of the next.\n    if (!negate || arguments.length === 1) {\n      this.assert(\n          hasProperty\n        , 'expected #{this} to have ' + descriptor + _.inspect(name)\n        , 'expected #{this} to not have ' + descriptor + _.inspect(name));\n    }\n\n    if (arguments.length > 1) {\n      this.assert(\n          hasProperty && (isDeep ? _.eql(val, value) : val === value)\n        , 'expected #{this} to have ' + descriptor + _.inspect(name) + ' of #{exp}, but got #{act}'\n        , 'expected #{this} to not have ' + descriptor + _.inspect(name) + ' of #{act}'\n        , val\n        , value\n      );\n    }\n\n    flag(this, 'object', value);\n  }\n\n  Assertion.addMethod('property', assertProperty);\n\n  function assertOwnProperty (name, value, msg) {\n    flag(this, 'own', true);\n    assertProperty.apply(this, arguments);\n  }\n\n  Assertion.addMethod('ownProperty', assertOwnProperty);\n  Assertion.addMethod('haveOwnProperty', assertOwnProperty);\n\n  /**\n   * ### .ownPropertyDescriptor(name[, descriptor[, msg]])\n   *\n   * Asserts that the target has its own property descriptor with the given key\n   * `name`. Enumerable and non-enumerable properties are included in the\n   * search.\n   *\n   *     expect({a: 1}).to.have.ownPropertyDescriptor('a');\n   *\n   * When `descriptor` is provided, `.ownPropertyDescriptor` also asserts that\n   * the property's descriptor is deeply equal to the given `descriptor`. See\n   * the `deep-eql` project page for info on the deep equality algorithm:\n   * https://github.com/chaijs/deep-eql.\n   *\n   *     expect({a: 1}).to.have.ownPropertyDescriptor('a', {\n   *       configurable: true,\n   *       enumerable: true,\n   *       writable: true,\n   *       value: 1,\n   *     });\n   *\n   * Add `.not` earlier in the chain to negate `.ownPropertyDescriptor`.\n   *\n   *     expect({a: 1}).to.not.have.ownPropertyDescriptor('b');\n   *\n   * However, it's dangerous to negate `.ownPropertyDescriptor` when providing\n   * a `descriptor`. The problem is that it creates uncertain expectations by\n   * asserting that the target either doesn't have a property descriptor with\n   * the given key `name`, or that it does have a property descriptor with the\n   * given key `name` but it’s not deeply equal to the given `descriptor`. It's\n   * often best to identify the exact output that's expected, and then write an\n   * assertion that only accepts that exact output.\n   *\n   * When the target isn't expected to have a property descriptor with the given\n   * key `name`, it's often best to assert exactly that.\n   *\n   *     // Recommended\n   *     expect({b: 2}).to.not.have.ownPropertyDescriptor('a');\n   *\n   *     // Not recommended\n   *     expect({b: 2}).to.not.have.ownPropertyDescriptor('a', {\n   *       configurable: true,\n   *       enumerable: true,\n   *       writable: true,\n   *       value: 1,\n   *     });\n   *\n   * When the target is expected to have a property descriptor with the given\n   * key `name`, it's often best to assert that the property has its expected\n   * descriptor, rather than asserting that it doesn't have one of many\n   * unexpected descriptors.\n   *\n   *     // Recommended\n   *     expect({a: 3}).to.have.ownPropertyDescriptor('a', {\n   *       configurable: true,\n   *       enumerable: true,\n   *       writable: true,\n   *       value: 3,\n   *     });\n   *\n   *     // Not recommended\n   *     expect({a: 3}).to.not.have.ownPropertyDescriptor('a', {\n   *       configurable: true,\n   *       enumerable: true,\n   *       writable: true,\n   *       value: 1,\n   *     });\n   *\n   * `.ownPropertyDescriptor` changes the target of any assertions that follow\n   * in the chain to be the value of the property descriptor from the original\n   * target object.\n   *\n   *     expect({a: 1}).to.have.ownPropertyDescriptor('a')\n   *       .that.has.property('enumerable', true);\n   *\n   * `.ownPropertyDescriptor` accepts an optional `msg` argument which is a\n   * custom error message to show when the assertion fails. The message can also\n   * be given as the second argument to `expect`. When not providing\n   * `descriptor`, only use the second form.\n   *\n   *     // Recommended\n   *     expect({a: 1}).to.have.ownPropertyDescriptor('a', {\n   *       configurable: true,\n   *       enumerable: true,\n   *       writable: true,\n   *       value: 2,\n   *     }, 'nooo why fail??');\n   *\n   *     // Recommended\n   *     expect({a: 1}, 'nooo why fail??').to.have.ownPropertyDescriptor('a', {\n   *       configurable: true,\n   *       enumerable: true,\n   *       writable: true,\n   *       value: 2,\n   *     });\n   *\n   *     // Recommended\n   *     expect({a: 1}, 'nooo why fail??').to.have.ownPropertyDescriptor('b');\n   *\n   *     // Not recommended\n   *     expect({a: 1})\n   *       .to.have.ownPropertyDescriptor('b', undefined, 'nooo why fail??');\n   *\n   * The above assertion isn't the same thing as not providing `descriptor`.\n   * Instead, it's asserting that the target object has a `b` property\n   * descriptor that's deeply equal to `undefined`.\n   *\n   * The alias `.haveOwnPropertyDescriptor` can be used interchangeably with\n   * `.ownPropertyDescriptor`.\n   *\n   * @name ownPropertyDescriptor\n   * @alias haveOwnPropertyDescriptor\n   * @param {String} name\n   * @param {Object} descriptor _optional_\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertOwnPropertyDescriptor (name, descriptor, msg) {\n    if (typeof descriptor === 'string') {\n      msg = descriptor;\n      descriptor = null;\n    }\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object');\n    var actualDescriptor = Object.getOwnPropertyDescriptor(Object(obj), name);\n    if (actualDescriptor && descriptor) {\n      this.assert(\n          _.eql(descriptor, actualDescriptor)\n        , 'expected the own property descriptor for ' + _.inspect(name) + ' on #{this} to match ' + _.inspect(descriptor) + ', got ' + _.inspect(actualDescriptor)\n        , 'expected the own property descriptor for ' + _.inspect(name) + ' on #{this} to not match ' + _.inspect(descriptor)\n        , descriptor\n        , actualDescriptor\n        , true\n      );\n    } else {\n      this.assert(\n          actualDescriptor\n        , 'expected #{this} to have an own property descriptor for ' + _.inspect(name)\n        , 'expected #{this} to not have an own property descriptor for ' + _.inspect(name)\n      );\n    }\n    flag(this, 'object', actualDescriptor);\n  }\n\n  Assertion.addMethod('ownPropertyDescriptor', assertOwnPropertyDescriptor);\n  Assertion.addMethod('haveOwnPropertyDescriptor', assertOwnPropertyDescriptor);\n\n  /**\n   * ### .lengthOf(n[, msg])\n   *\n   * Asserts that the target's `length` or `size` is equal to the given number\n   * `n`.\n   *\n   *     expect([1, 2, 3]).to.have.lengthOf(3);\n   *     expect('foo').to.have.lengthOf(3);\n   *     expect(new Set([1, 2, 3])).to.have.lengthOf(3);\n   *     expect(new Map([['a', 1], ['b', 2], ['c', 3]])).to.have.lengthOf(3);\n   *\n   * Add `.not` earlier in the chain to negate `.lengthOf`. However, it's often\n   * best to assert that the target's `length` property is equal to its expected\n   * value, rather than not equal to one of many unexpected values.\n   *\n   *     expect('foo').to.have.lengthOf(3); // Recommended\n   *     expect('foo').to.not.have.lengthOf(4); // Not recommended\n   *\n   * `.lengthOf` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`.\n   *\n   *     expect([1, 2, 3]).to.have.lengthOf(2, 'nooo why fail??');\n   *     expect([1, 2, 3], 'nooo why fail??').to.have.lengthOf(2);\n   *\n   * `.lengthOf` can also be used as a language chain, causing all `.above`,\n   * `.below`, `.least`, `.most`, and `.within` assertions that follow in the\n   * chain to use the target's `length` property as the target. However, it's\n   * often best to assert that the target's `length` property is equal to its\n   * expected length, rather than asserting that its `length` property falls\n   * within some range of values.\n   *\n   *     // Recommended\n   *     expect([1, 2, 3]).to.have.lengthOf(3);\n   *\n   *     // Not recommended\n   *     expect([1, 2, 3]).to.have.lengthOf.above(2);\n   *     expect([1, 2, 3]).to.have.lengthOf.below(4);\n   *     expect([1, 2, 3]).to.have.lengthOf.at.least(3);\n   *     expect([1, 2, 3]).to.have.lengthOf.at.most(3);\n   *     expect([1, 2, 3]).to.have.lengthOf.within(2,4);\n   *\n   * Due to a compatibility issue, the alias `.length` can't be chained directly\n   * off of an uninvoked method such as `.a`. Therefore, `.length` can't be used\n   * interchangeably with `.lengthOf` in every situation. It's recommended to\n   * always use `.lengthOf` instead of `.length`.\n   *\n   *     expect([1, 2, 3]).to.have.a.length(3); // incompatible; throws error\n   *     expect([1, 2, 3]).to.have.a.lengthOf(3);  // passes as expected\n   *\n   * @name lengthOf\n   * @alias length\n   * @param {Number} n\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertLengthChain () {\n    flag(this, 'doLength', true);\n  }\n\n  function assertLength (n, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object')\n      , objType = _.type(obj).toLowerCase()\n      , flagMsg = flag(this, 'message')\n      , ssfi = flag(this, 'ssfi')\n      , descriptor = 'length'\n      , itemsCount;\n\n    switch (objType) {\n      case 'map':\n      case 'set':\n        descriptor = 'size';\n        itemsCount = obj.size;\n        break;\n      default:\n        new Assertion(obj, flagMsg, ssfi, true).to.have.property('length');\n        itemsCount = obj.length;\n    }\n\n    this.assert(\n        itemsCount == n\n      , 'expected #{this} to have a ' + descriptor + ' of #{exp} but got #{act}'\n      , 'expected #{this} to not have a ' + descriptor + ' of #{act}'\n      , n\n      , itemsCount\n    );\n  }\n\n  Assertion.addChainableMethod('length', assertLength, assertLengthChain);\n  Assertion.addChainableMethod('lengthOf', assertLength, assertLengthChain);\n\n  /**\n   * ### .match(re[, msg])\n   *\n   * Asserts that the target matches the given regular expression `re`.\n   *\n   *     expect('foobar').to.match(/^foo/);\n   *\n   * Add `.not` earlier in the chain to negate `.match`.\n   *\n   *     expect('foobar').to.not.match(/taco/);\n   *\n   * `.match` accepts an optional `msg` argument which is a custom error message\n   * to show when the assertion fails. The message can also be given as the\n   * second argument to `expect`.\n   *\n   *     expect('foobar').to.match(/taco/, 'nooo why fail??');\n   *     expect('foobar', 'nooo why fail??').to.match(/taco/);\n   *\n   * The alias `.matches` can be used interchangeably with `.match`.\n   *\n   * @name match\n   * @alias matches\n   * @param {RegExp} re\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n  function assertMatch(re, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object');\n    this.assert(\n        re.exec(obj)\n      , 'expected #{this} to match ' + re\n      , 'expected #{this} not to match ' + re\n    );\n  }\n\n  Assertion.addMethod('match', assertMatch);\n  Assertion.addMethod('matches', assertMatch);\n\n  /**\n   * ### .string(str[, msg])\n   *\n   * Asserts that the target string contains the given substring `str`.\n   *\n   *     expect('foobar').to.have.string('bar');\n   *\n   * Add `.not` earlier in the chain to negate `.string`.\n   *\n   *     expect('foobar').to.not.have.string('taco');\n   *\n   * `.string` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`.\n   *\n   *     expect('foobar').to.have.string('taco', 'nooo why fail??');\n   *     expect('foobar', 'nooo why fail??').to.have.string('taco');\n   *\n   * @name string\n   * @param {String} str\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addMethod('string', function (str, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object')\n      , flagMsg = flag(this, 'message')\n      , ssfi = flag(this, 'ssfi');\n    new Assertion(obj, flagMsg, ssfi, true).is.a('string');\n\n    this.assert(\n        ~obj.indexOf(str)\n      , 'expected #{this} to contain ' + _.inspect(str)\n      , 'expected #{this} to not contain ' + _.inspect(str)\n    );\n  });\n\n  /**\n   * ### .keys(key1[, key2[, ...]])\n   *\n   * Asserts that the target object, array, map, or set has the given keys. Only\n   * the target's own inherited properties are included in the search.\n   *\n   * When the target is an object or array, keys can be provided as one or more\n   * string arguments, a single array argument, or a single object argument. In\n   * the latter case, only the keys in the given object matter; the values are\n   * ignored.\n   *\n   *     expect({a: 1, b: 2}).to.have.all.keys('a', 'b');\n   *     expect(['x', 'y']).to.have.all.keys(0, 1);\n   *\n   *     expect({a: 1, b: 2}).to.have.all.keys(['a', 'b']);\n   *     expect(['x', 'y']).to.have.all.keys([0, 1]);\n   *\n   *     expect({a: 1, b: 2}).to.have.all.keys({a: 4, b: 5}); // ignore 4 and 5\n   *     expect(['x', 'y']).to.have.all.keys({0: 4, 1: 5}); // ignore 4 and 5\n   *\n   * When the target is a map or set, each key must be provided as a separate\n   * argument.\n   *\n   *     expect(new Map([['a', 1], ['b', 2]])).to.have.all.keys('a', 'b');\n   *     expect(new Set(['a', 'b'])).to.have.all.keys('a', 'b');\n   *\n   * Because `.keys` does different things based on the target's type, it's\n   * important to check the target's type before using `.keys`. See the `.a` doc\n   * for info on testing a target's type.\n   *\n   *     expect({a: 1, b: 2}).to.be.an('object').that.has.all.keys('a', 'b');\n   *\n   * By default, strict (`===`) equality is used to compare keys of maps and\n   * sets. Add `.deep` earlier in the chain to use deep equality instead. See\n   * the `deep-eql` project page for info on the deep equality algorithm:\n   * https://github.com/chaijs/deep-eql.\n   *\n   *     // Target set deeply (but not strictly) has key `{a: 1}`\n   *     expect(new Set([{a: 1}])).to.have.all.deep.keys([{a: 1}]);\n   *     expect(new Set([{a: 1}])).to.not.have.all.keys([{a: 1}]);\n   *\n   * By default, the target must have all of the given keys and no more. Add\n   * `.any` earlier in the chain to only require that the target have at least\n   * one of the given keys. Also, add `.not` earlier in the chain to negate\n   * `.keys`. It's often best to add `.any` when negating `.keys`, and to use\n   * `.all` when asserting `.keys` without negation.\n   *\n   * When negating `.keys`, `.any` is preferred because `.not.any.keys` asserts\n   * exactly what's expected of the output, whereas `.not.all.keys` creates\n   * uncertain expectations.\n   *\n   *     // Recommended; asserts that target doesn't have any of the given keys\n   *     expect({a: 1, b: 2}).to.not.have.any.keys('c', 'd');\n   *\n   *     // Not recommended; asserts that target doesn't have all of the given\n   *     // keys but may or may not have some of them\n   *     expect({a: 1, b: 2}).to.not.have.all.keys('c', 'd');\n   *\n   * When asserting `.keys` without negation, `.all` is preferred because\n   * `.all.keys` asserts exactly what's expected of the output, whereas\n   * `.any.keys` creates uncertain expectations.\n   *\n   *     // Recommended; asserts that target has all the given keys\n   *     expect({a: 1, b: 2}).to.have.all.keys('a', 'b');\n   *\n   *     // Not recommended; asserts that target has at least one of the given\n   *     // keys but may or may not have more of them\n   *     expect({a: 1, b: 2}).to.have.any.keys('a', 'b');\n   *\n   * Note that `.all` is used by default when neither `.all` nor `.any` appear\n   * earlier in the chain. However, it's often best to add `.all` anyway because\n   * it improves readability.\n   *\n   *     // Both assertions are identical\n   *     expect({a: 1, b: 2}).to.have.all.keys('a', 'b'); // Recommended\n   *     expect({a: 1, b: 2}).to.have.keys('a', 'b'); // Not recommended\n   *\n   * Add `.include` earlier in the chain to require that the target's keys be a\n   * superset of the expected keys, rather than identical sets.\n   *\n   *     // Target object's keys are a superset of ['a', 'b'] but not identical\n   *     expect({a: 1, b: 2, c: 3}).to.include.all.keys('a', 'b');\n   *     expect({a: 1, b: 2, c: 3}).to.not.have.all.keys('a', 'b');\n   *\n   * However, if `.any` and `.include` are combined, only the `.any` takes\n   * effect. The `.include` is ignored in this case.\n   *\n   *     // Both assertions are identical\n   *     expect({a: 1}).to.have.any.keys('a', 'b');\n   *     expect({a: 1}).to.include.any.keys('a', 'b');\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect({a: 1}, 'nooo why fail??').to.have.key('b');\n   *\n   * The alias `.key` can be used interchangeably with `.keys`.\n   *\n   * @name keys\n   * @alias key\n   * @param {...String|Array|Object} keys\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertKeys (keys) {\n    var obj = flag(this, 'object')\n      , objType = _.type(obj)\n      , keysType = _.type(keys)\n      , ssfi = flag(this, 'ssfi')\n      , isDeep = flag(this, 'deep')\n      , str\n      , deepStr = ''\n      , actual\n      , ok = true\n      , flagMsg = flag(this, 'message');\n\n    flagMsg = flagMsg ? flagMsg + ': ' : '';\n    var mixedArgsMsg = flagMsg + 'when testing keys against an object or an array you must give a single Array|Object|String argument or multiple String arguments';\n\n    if (objType === 'Map' || objType === 'Set') {\n      deepStr = isDeep ? 'deeply ' : '';\n      actual = [];\n\n      // Map and Set '.keys' aren't supported in IE 11. Therefore, use .forEach.\n      obj.forEach(function (val, key) { actual.push(key) });\n\n      if (keysType !== 'Array') {\n        keys = Array.prototype.slice.call(arguments);\n      }\n    } else {\n      actual = _.getOwnEnumerableProperties(obj);\n\n      switch (keysType) {\n        case 'Array':\n          if (arguments.length > 1) {\n            throw new AssertionError(mixedArgsMsg, undefined, ssfi);\n          }\n          break;\n        case 'Object':\n          if (arguments.length > 1) {\n            throw new AssertionError(mixedArgsMsg, undefined, ssfi);\n          }\n          keys = Object.keys(keys);\n          break;\n        default:\n          keys = Array.prototype.slice.call(arguments);\n      }\n\n      // Only stringify non-Symbols because Symbols would become \"Symbol()\"\n      keys = keys.map(function (val) {\n        return typeof val === 'symbol' ? val : String(val);\n      });\n    }\n\n    if (!keys.length) {\n      throw new AssertionError(flagMsg + 'keys required', undefined, ssfi);\n    }\n\n    var len = keys.length\n      , any = flag(this, 'any')\n      , all = flag(this, 'all')\n      , expected = keys;\n\n    if (!any && !all) {\n      all = true;\n    }\n\n    // Has any\n    if (any) {\n      ok = expected.some(function(expectedKey) {\n        return actual.some(function(actualKey) {\n          if (isDeep) {\n            return _.eql(expectedKey, actualKey);\n          } else {\n            return expectedKey === actualKey;\n          }\n        });\n      });\n    }\n\n    // Has all\n    if (all) {\n      ok = expected.every(function(expectedKey) {\n        return actual.some(function(actualKey) {\n          if (isDeep) {\n            return _.eql(expectedKey, actualKey);\n          } else {\n            return expectedKey === actualKey;\n          }\n        });\n      });\n\n      if (!flag(this, 'contains')) {\n        ok = ok && keys.length == actual.length;\n      }\n    }\n\n    // Key string\n    if (len > 1) {\n      keys = keys.map(function(key) {\n        return _.inspect(key);\n      });\n      var last = keys.pop();\n      if (all) {\n        str = keys.join(', ') + ', and ' + last;\n      }\n      if (any) {\n        str = keys.join(', ') + ', or ' + last;\n      }\n    } else {\n      str = _.inspect(keys[0]);\n    }\n\n    // Form\n    str = (len > 1 ? 'keys ' : 'key ') + str;\n\n    // Have / include\n    str = (flag(this, 'contains') ? 'contain ' : 'have ') + str;\n\n    // Assertion\n    this.assert(\n        ok\n      , 'expected #{this} to ' + deepStr + str\n      , 'expected #{this} to not ' + deepStr + str\n      , expected.slice(0).sort(_.compareByInspect)\n      , actual.sort(_.compareByInspect)\n      , true\n    );\n  }\n\n  Assertion.addMethod('keys', assertKeys);\n  Assertion.addMethod('key', assertKeys);\n\n  /**\n   * ### .throw([errorLike], [errMsgMatcher], [msg])\n   *\n   * When no arguments are provided, `.throw` invokes the target function and\n   * asserts that an error is thrown.\n   *\n   *     var badFn = function () { throw new TypeError('Illegal salmon!'); };\n   *\n   *     expect(badFn).to.throw();\n   *\n   * When one argument is provided, and it's an error constructor, `.throw`\n   * invokes the target function and asserts that an error is thrown that's an\n   * instance of that error constructor.\n   *\n   *     var badFn = function () { throw new TypeError('Illegal salmon!'); };\n   *\n   *     expect(badFn).to.throw(TypeError);\n   *\n   * When one argument is provided, and it's an error instance, `.throw` invokes\n   * the target function and asserts that an error is thrown that's strictly\n   * (`===`) equal to that error instance.\n   *\n   *     var err = new TypeError('Illegal salmon!');\n   *     var badFn = function () { throw err; };\n   *\n   *     expect(badFn).to.throw(err);\n   *\n   * When one argument is provided, and it's a string, `.throw` invokes the\n   * target function and asserts that an error is thrown with a message that\n   * contains that string.\n   *\n   *     var badFn = function () { throw new TypeError('Illegal salmon!'); };\n   *\n   *     expect(badFn).to.throw('salmon');\n   *\n   * When one argument is provided, and it's a regular expression, `.throw`\n   * invokes the target function and asserts that an error is thrown with a\n   * message that matches that regular expression.\n   *\n   *     var badFn = function () { throw new TypeError('Illegal salmon!'); };\n   *\n   *     expect(badFn).to.throw(/salmon/);\n   *\n   * When two arguments are provided, and the first is an error instance or\n   * constructor, and the second is a string or regular expression, `.throw`\n   * invokes the function and asserts that an error is thrown that fulfills both\n   * conditions as described above.\n   *\n   *     var err = new TypeError('Illegal salmon!');\n   *     var badFn = function () { throw err; };\n   *\n   *     expect(badFn).to.throw(TypeError, 'salmon');\n   *     expect(badFn).to.throw(TypeError, /salmon/);\n   *     expect(badFn).to.throw(err, 'salmon');\n   *     expect(badFn).to.throw(err, /salmon/);\n   *\n   * Add `.not` earlier in the chain to negate `.throw`.\n   *\n   *     var goodFn = function () {};\n   *\n   *     expect(goodFn).to.not.throw();\n   *\n   * However, it's dangerous to negate `.throw` when providing any arguments.\n   * The problem is that it creates uncertain expectations by asserting that the\n   * target either doesn't throw an error, or that it throws an error but of a\n   * different type than the given type, or that it throws an error of the given\n   * type but with a message that doesn't include the given string. It's often\n   * best to identify the exact output that's expected, and then write an\n   * assertion that only accepts that exact output.\n   *\n   * When the target isn't expected to throw an error, it's often best to assert\n   * exactly that.\n   *\n   *     var goodFn = function () {};\n   *\n   *     expect(goodFn).to.not.throw(); // Recommended\n   *     expect(goodFn).to.not.throw(ReferenceError, 'x'); // Not recommended\n   *\n   * When the target is expected to throw an error, it's often best to assert\n   * that the error is of its expected type, and has a message that includes an\n   * expected string, rather than asserting that it doesn't have one of many\n   * unexpected types, and doesn't have a message that includes some string.\n   *\n   *     var badFn = function () { throw new TypeError('Illegal salmon!'); };\n   *\n   *     expect(badFn).to.throw(TypeError, 'salmon'); // Recommended\n   *     expect(badFn).to.not.throw(ReferenceError, 'x'); // Not recommended\n   *\n   * `.throw` changes the target of any assertions that follow in the chain to\n   * be the error object that's thrown.\n   *\n   *     var err = new TypeError('Illegal salmon!');\n   *     err.code = 42;\n   *     var badFn = function () { throw err; };\n   *\n   *     expect(badFn).to.throw(TypeError).with.property('code', 42);\n   *\n   * `.throw` accepts an optional `msg` argument which is a custom error message\n   * to show when the assertion fails. The message can also be given as the\n   * second argument to `expect`. When not providing two arguments, always use\n   * the second form.\n   *\n   *     var goodFn = function () {};\n   *\n   *     expect(goodFn).to.throw(TypeError, 'x', 'nooo why fail??');\n   *     expect(goodFn, 'nooo why fail??').to.throw();\n   *\n   * Due to limitations in ES5, `.throw` may not always work as expected when\n   * using a transpiler such as Babel or TypeScript. In particular, it may\n   * produce unexpected results when subclassing the built-in `Error` object and\n   * then passing the subclassed constructor to `.throw`. See your transpiler's\n   * docs for details:\n   *\n   * - ([Babel](https://babeljs.io/docs/usage/caveats/#classes))\n   * - ([TypeScript](https://github.com/Microsoft/TypeScript/wiki/Breaking-Changes#extending-built-ins-like-error-array-and-map-may-no-longer-work))\n   *\n   * Beware of some common mistakes when using the `throw` assertion. One common\n   * mistake is to accidentally invoke the function yourself instead of letting\n   * the `throw` assertion invoke the function for you. For example, when\n   * testing if a function named `fn` throws, provide `fn` instead of `fn()` as\n   * the target for the assertion.\n   *\n   *     expect(fn).to.throw();     // Good! Tests `fn` as desired\n   *     expect(fn()).to.throw();   // Bad! Tests result of `fn()`, not `fn`\n   *\n   * If you need to assert that your function `fn` throws when passed certain\n   * arguments, then wrap a call to `fn` inside of another function.\n   *\n   *     expect(function () { fn(42); }).to.throw();  // Function expression\n   *     expect(() => fn(42)).to.throw();             // ES6 arrow function\n   *\n   * Another common mistake is to provide an object method (or any stand-alone\n   * function that relies on `this`) as the target of the assertion. Doing so is\n   * problematic because the `this` context will be lost when the function is\n   * invoked by `.throw`; there's no way for it to know what `this` is supposed\n   * to be. There are two ways around this problem. One solution is to wrap the\n   * method or function call inside of another function. Another solution is to\n   * use `bind`.\n   *\n   *     expect(function () { cat.meow(); }).to.throw();  // Function expression\n   *     expect(() => cat.meow()).to.throw();             // ES6 arrow function\n   *     expect(cat.meow.bind(cat)).to.throw();           // Bind\n   *\n   * Finally, it's worth mentioning that it's a best practice in JavaScript to\n   * only throw `Error` and derivatives of `Error` such as `ReferenceError`,\n   * `TypeError`, and user-defined objects that extend `Error`. No other type of\n   * value will generate a stack trace when initialized. With that said, the\n   * `throw` assertion does technically support any type of value being thrown,\n   * not just `Error` and its derivatives.\n   *\n   * The aliases `.throws` and `.Throw` can be used interchangeably with\n   * `.throw`.\n   *\n   * @name throw\n   * @alias throws\n   * @alias Throw\n   * @param {Error|ErrorConstructor} errorLike\n   * @param {String|RegExp} errMsgMatcher error message\n   * @param {String} msg _optional_\n   * @see https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Error#Error_types\n   * @returns error for chaining (null if no error)\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertThrows (errorLike, errMsgMatcher, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object')\n      , ssfi = flag(this, 'ssfi')\n      , flagMsg = flag(this, 'message')\n      , negate = flag(this, 'negate') || false;\n    new Assertion(obj, flagMsg, ssfi, true).is.a('function');\n\n    if (errorLike instanceof RegExp || typeof errorLike === 'string') {\n      errMsgMatcher = errorLike;\n      errorLike = null;\n    }\n\n    var caughtErr;\n    try {\n      obj();\n    } catch (err) {\n      caughtErr = err;\n    }\n\n    // If we have the negate flag enabled and at least one valid argument it means we do expect an error\n    // but we want it to match a given set of criteria\n    var everyArgIsUndefined = errorLike === undefined && errMsgMatcher === undefined;\n\n    // If we've got the negate flag enabled and both args, we should only fail if both aren't compatible\n    // See Issue #551 and PR #683@GitHub\n    var everyArgIsDefined = Boolean(errorLike && errMsgMatcher);\n    var errorLikeFail = false;\n    var errMsgMatcherFail = false;\n\n    // Checking if error was thrown\n    if (everyArgIsUndefined || !everyArgIsUndefined && !negate) {\n      // We need this to display results correctly according to their types\n      var errorLikeString = 'an error';\n      if (errorLike instanceof Error) {\n        errorLikeString = '#{exp}';\n      } else if (errorLike) {\n        errorLikeString = _.checkError.getConstructorName(errorLike);\n      }\n\n      this.assert(\n          caughtErr\n        , 'expected #{this} to throw ' + errorLikeString\n        , 'expected #{this} to not throw an error but #{act} was thrown'\n        , errorLike && errorLike.toString()\n        , (caughtErr instanceof Error ?\n            caughtErr.toString() : (typeof caughtErr === 'string' ? caughtErr : caughtErr &&\n                                    _.checkError.getConstructorName(caughtErr)))\n      );\n    }\n\n    if (errorLike && caughtErr) {\n      // We should compare instances only if `errorLike` is an instance of `Error`\n      if (errorLike instanceof Error) {\n        var isCompatibleInstance = _.checkError.compatibleInstance(caughtErr, errorLike);\n\n        if (isCompatibleInstance === negate) {\n          // These checks were created to ensure we won't fail too soon when we've got both args and a negate\n          // See Issue #551 and PR #683@GitHub\n          if (everyArgIsDefined && negate) {\n            errorLikeFail = true;\n          } else {\n            this.assert(\n                negate\n              , 'expected #{this} to throw #{exp} but #{act} was thrown'\n              , 'expected #{this} to not throw #{exp}' + (caughtErr && !negate ? ' but #{act} was thrown' : '')\n              , errorLike.toString()\n              , caughtErr.toString()\n            );\n          }\n        }\n      }\n\n      var isCompatibleConstructor = _.checkError.compatibleConstructor(caughtErr, errorLike);\n      if (isCompatibleConstructor === negate) {\n        if (everyArgIsDefined && negate) {\n            errorLikeFail = true;\n        } else {\n          this.assert(\n              negate\n            , 'expected #{this} to throw #{exp} but #{act} was thrown'\n            , 'expected #{this} to not throw #{exp}' + (caughtErr ? ' but #{act} was thrown' : '')\n            , (errorLike instanceof Error ? errorLike.toString() : errorLike && _.checkError.getConstructorName(errorLike))\n            , (caughtErr instanceof Error ? caughtErr.toString() : caughtErr && _.checkError.getConstructorName(caughtErr))\n          );\n        }\n      }\n    }\n\n    if (caughtErr && errMsgMatcher !== undefined && errMsgMatcher !== null) {\n      // Here we check compatible messages\n      var placeholder = 'including';\n      if (errMsgMatcher instanceof RegExp) {\n        placeholder = 'matching'\n      }\n\n      var isCompatibleMessage = _.checkError.compatibleMessage(caughtErr, errMsgMatcher);\n      if (isCompatibleMessage === negate) {\n        if (everyArgIsDefined && negate) {\n            errMsgMatcherFail = true;\n        } else {\n          this.assert(\n            negate\n            , 'expected #{this} to throw error ' + placeholder + ' #{exp} but got #{act}'\n            , 'expected #{this} to throw error not ' + placeholder + ' #{exp}'\n            ,  errMsgMatcher\n            ,  _.checkError.getMessage(caughtErr)\n          );\n        }\n      }\n    }\n\n    // If both assertions failed and both should've matched we throw an error\n    if (errorLikeFail && errMsgMatcherFail) {\n      this.assert(\n        negate\n        , 'expected #{this} to throw #{exp} but #{act} was thrown'\n        , 'expected #{this} to not throw #{exp}' + (caughtErr ? ' but #{act} was thrown' : '')\n        , (errorLike instanceof Error ? errorLike.toString() : errorLike && _.checkError.getConstructorName(errorLike))\n        , (caughtErr instanceof Error ? caughtErr.toString() : caughtErr && _.checkError.getConstructorName(caughtErr))\n      );\n    }\n\n    flag(this, 'object', caughtErr);\n  };\n\n  Assertion.addMethod('throw', assertThrows);\n  Assertion.addMethod('throws', assertThrows);\n  Assertion.addMethod('Throw', assertThrows);\n\n  /**\n   * ### .respondTo(method[, msg])\n   *\n   * When the target is a non-function object, `.respondTo` asserts that the\n   * target has a method with the given name `method`. The method can be own or\n   * inherited, and it can be enumerable or non-enumerable.\n   *\n   *     function Cat () {}\n   *     Cat.prototype.meow = function () {};\n   *\n   *     expect(new Cat()).to.respondTo('meow');\n   *\n   * When the target is a function, `.respondTo` asserts that the target's\n   * `prototype` property has a method with the given name `method`. Again, the\n   * method can be own or inherited, and it can be enumerable or non-enumerable.\n   *\n   *     function Cat () {}\n   *     Cat.prototype.meow = function () {};\n   *\n   *     expect(Cat).to.respondTo('meow');\n   *\n   * Add `.itself` earlier in the chain to force `.respondTo` to treat the\n   * target as a non-function object, even if it's a function. Thus, it asserts\n   * that the target has a method with the given name `method`, rather than\n   * asserting that the target's `prototype` property has a method with the\n   * given name `method`.\n   *\n   *     function Cat () {}\n   *     Cat.prototype.meow = function () {};\n   *     Cat.hiss = function () {};\n   *\n   *     expect(Cat).itself.to.respondTo('hiss').but.not.respondTo('meow');\n   *\n   * When not adding `.itself`, it's important to check the target's type before\n   * using `.respondTo`. See the `.a` doc for info on checking a target's type.\n   *\n   *     function Cat () {}\n   *     Cat.prototype.meow = function () {};\n   *\n   *     expect(new Cat()).to.be.an('object').that.respondsTo('meow');\n   *\n   * Add `.not` earlier in the chain to negate `.respondTo`.\n   *\n   *     function Dog () {}\n   *     Dog.prototype.bark = function () {};\n   *\n   *     expect(new Dog()).to.not.respondTo('meow');\n   *\n   * `.respondTo` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`.\n   *\n   *     expect({}).to.respondTo('meow', 'nooo why fail??');\n   *     expect({}, 'nooo why fail??').to.respondTo('meow');\n   *\n   * The alias `.respondsTo` can be used interchangeably with `.respondTo`.\n   *\n   * @name respondTo\n   * @alias respondsTo\n   * @param {String} method\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function respondTo (method, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object')\n      , itself = flag(this, 'itself')\n      , context = ('function' === typeof obj && !itself)\n        ? obj.prototype[method]\n        : obj[method];\n\n    this.assert(\n        'function' === typeof context\n      , 'expected #{this} to respond to ' + _.inspect(method)\n      , 'expected #{this} to not respond to ' + _.inspect(method)\n    );\n  }\n\n  Assertion.addMethod('respondTo', respondTo);\n  Assertion.addMethod('respondsTo', respondTo);\n\n  /**\n   * ### .itself\n   *\n   * Forces all `.respondTo` assertions that follow in the chain to behave as if\n   * the target is a non-function object, even if it's a function. Thus, it\n   * causes `.respondTo` to assert that the target has a method with the given\n   * name, rather than asserting that the target's `prototype` property has a\n   * method with the given name.\n   *\n   *     function Cat () {}\n   *     Cat.prototype.meow = function () {};\n   *     Cat.hiss = function () {};\n   *\n   *     expect(Cat).itself.to.respondTo('hiss').but.not.respondTo('meow');\n   *\n   * @name itself\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('itself', function () {\n    flag(this, 'itself', true);\n  });\n\n  /**\n   * ### .satisfy(matcher[, msg])\n   *\n   * Invokes the given `matcher` function with the target being passed as the\n   * first argument, and asserts that the value returned is truthy.\n   *\n   *     expect(1).to.satisfy(function(num) {\n   *       return num > 0;\n   *     });\n   *\n   * Add `.not` earlier in the chain to negate `.satisfy`.\n   *\n   *     expect(1).to.not.satisfy(function(num) {\n   *       return num > 2;\n   *     });\n   *\n   * `.satisfy` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`.\n   *\n   *     expect(1).to.satisfy(function(num) {\n   *       return num > 2;\n   *     }, 'nooo why fail??');\n   *\n   *     expect(1, 'nooo why fail??').to.satisfy(function(num) {\n   *       return num > 2;\n   *     });\n   *\n   * The alias `.satisfies` can be used interchangeably with `.satisfy`.\n   *\n   * @name satisfy\n   * @alias satisfies\n   * @param {Function} matcher\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function satisfy (matcher, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object');\n    var result = matcher(obj);\n    this.assert(\n        result\n      , 'expected #{this} to satisfy ' + _.objDisplay(matcher)\n      , 'expected #{this} to not satisfy' + _.objDisplay(matcher)\n      , flag(this, 'negate') ? false : true\n      , result\n    );\n  }\n\n  Assertion.addMethod('satisfy', satisfy);\n  Assertion.addMethod('satisfies', satisfy);\n\n  /**\n   * ### .closeTo(expected, delta[, msg])\n   *\n   * Asserts that the target is a number that's within a given +/- `delta` range\n   * of the given number `expected`. However, it's often best to assert that the\n   * target is equal to its expected value.\n   *\n   *     // Recommended\n   *     expect(1.5).to.equal(1.5);\n   *\n   *     // Not recommended\n   *     expect(1.5).to.be.closeTo(1, 0.5);\n   *     expect(1.5).to.be.closeTo(2, 0.5);\n   *     expect(1.5).to.be.closeTo(1, 1);\n   *\n   * Add `.not` earlier in the chain to negate `.closeTo`.\n   *\n   *     expect(1.5).to.equal(1.5); // Recommended\n   *     expect(1.5).to.not.be.closeTo(3, 1); // Not recommended\n   *\n   * `.closeTo` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`.\n   *\n   *     expect(1.5).to.be.closeTo(3, 1, 'nooo why fail??');\n   *     expect(1.5, 'nooo why fail??').to.be.closeTo(3, 1);\n   *\n   * The alias `.approximately` can be used interchangeably with `.closeTo`.\n   *\n   * @name closeTo\n   * @alias approximately\n   * @param {Number} expected\n   * @param {Number} delta\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function closeTo(expected, delta, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object')\n      , flagMsg = flag(this, 'message')\n      , ssfi = flag(this, 'ssfi');\n\n    new Assertion(obj, flagMsg, ssfi, true).is.a('number');\n    if (typeof expected !== 'number' || typeof delta !== 'number') {\n      flagMsg = flagMsg ? flagMsg + ': ' : '';\n      var deltaMessage = delta === undefined ? \", and a delta is required\" : \"\";\n      throw new AssertionError(\n          flagMsg + 'the arguments to closeTo or approximately must be numbers' + deltaMessage,\n          undefined,\n          ssfi\n      );\n    }\n\n    this.assert(\n        Math.abs(obj - expected) <= delta\n      , 'expected #{this} to be close to ' + expected + ' +/- ' + delta\n      , 'expected #{this} not to be close to ' + expected + ' +/- ' + delta\n    );\n  }\n\n  Assertion.addMethod('closeTo', closeTo);\n  Assertion.addMethod('approximately', closeTo);\n\n  // Note: Duplicates are ignored if testing for inclusion instead of sameness.\n  function isSubsetOf(subset, superset, cmp, contains, ordered) {\n    if (!contains) {\n      if (subset.length !== superset.length) return false;\n      superset = superset.slice();\n    }\n\n    return subset.every(function(elem, idx) {\n      if (ordered) return cmp ? cmp(elem, superset[idx]) : elem === superset[idx];\n\n      if (!cmp) {\n        var matchIdx = superset.indexOf(elem);\n        if (matchIdx === -1) return false;\n\n        // Remove match from superset so not counted twice if duplicate in subset.\n        if (!contains) superset.splice(matchIdx, 1);\n        return true;\n      }\n\n      return superset.some(function(elem2, matchIdx) {\n        if (!cmp(elem, elem2)) return false;\n\n        // Remove match from superset so not counted twice if duplicate in subset.\n        if (!contains) superset.splice(matchIdx, 1);\n        return true;\n      });\n    });\n  }\n\n  /**\n   * ### .members(set[, msg])\n   *\n   * Asserts that the target array has the same members as the given array\n   * `set`.\n   *\n   *     expect([1, 2, 3]).to.have.members([2, 1, 3]);\n   *     expect([1, 2, 2]).to.have.members([2, 1, 2]);\n   *\n   * By default, members are compared using strict (`===`) equality. Add `.deep`\n   * earlier in the chain to use deep equality instead. See the `deep-eql`\n   * project page for info on the deep equality algorithm:\n   * https://github.com/chaijs/deep-eql.\n   *\n   *     // Target array deeply (but not strictly) has member `{a: 1}`\n   *     expect([{a: 1}]).to.have.deep.members([{a: 1}]);\n   *     expect([{a: 1}]).to.not.have.members([{a: 1}]);\n   *\n   * By default, order doesn't matter. Add `.ordered` earlier in the chain to\n   * require that members appear in the same order.\n   *\n   *     expect([1, 2, 3]).to.have.ordered.members([1, 2, 3]);\n   *     expect([1, 2, 3]).to.have.members([2, 1, 3])\n   *       .but.not.ordered.members([2, 1, 3]);\n   *\n   * By default, both arrays must be the same size. Add `.include` earlier in\n   * the chain to require that the target's members be a superset of the\n   * expected members. Note that duplicates are ignored in the subset when\n   * `.include` is added.\n   *\n   *     // Target array is a superset of [1, 2] but not identical\n   *     expect([1, 2, 3]).to.include.members([1, 2]);\n   *     expect([1, 2, 3]).to.not.have.members([1, 2]);\n   *\n   *     // Duplicates in the subset are ignored\n   *     expect([1, 2, 3]).to.include.members([1, 2, 2, 2]);\n   *\n   * `.deep`, `.ordered`, and `.include` can all be combined. However, if\n   * `.include` and `.ordered` are combined, the ordering begins at the start of\n   * both arrays.\n   *\n   *     expect([{a: 1}, {b: 2}, {c: 3}])\n   *       .to.include.deep.ordered.members([{a: 1}, {b: 2}])\n   *       .but.not.include.deep.ordered.members([{b: 2}, {c: 3}]);\n   *\n   * Add `.not` earlier in the chain to negate `.members`. However, it's\n   * dangerous to do so. The problem is that it creates uncertain expectations\n   * by asserting that the target array doesn't have all of the same members as\n   * the given array `set` but may or may not have some of them. It's often best\n   * to identify the exact output that's expected, and then write an assertion\n   * that only accepts that exact output.\n   *\n   *     expect([1, 2]).to.not.include(3).and.not.include(4); // Recommended\n   *     expect([1, 2]).to.not.have.members([3, 4]); // Not recommended\n   *\n   * `.members` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`.\n   *\n   *     expect([1, 2]).to.have.members([1, 2, 3], 'nooo why fail??');\n   *     expect([1, 2], 'nooo why fail??').to.have.members([1, 2, 3]);\n   *\n   * @name members\n   * @param {Array} set\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addMethod('members', function (subset, msg) {\n    if (msg) flag(this, 'message', msg);\n    var obj = flag(this, 'object')\n      , flagMsg = flag(this, 'message')\n      , ssfi = flag(this, 'ssfi');\n\n    new Assertion(obj, flagMsg, ssfi, true).to.be.an('array');\n    new Assertion(subset, flagMsg, ssfi, true).to.be.an('array');\n\n    var contains = flag(this, 'contains');\n    var ordered = flag(this, 'ordered');\n\n    var subject, failMsg, failNegateMsg;\n\n    if (contains) {\n      subject = ordered ? 'an ordered superset' : 'a superset';\n      failMsg = 'expected #{this} to be ' + subject + ' of #{exp}';\n      failNegateMsg = 'expected #{this} to not be ' + subject + ' of #{exp}';\n    } else {\n      subject = ordered ? 'ordered members' : 'members';\n      failMsg = 'expected #{this} to have the same ' + subject + ' as #{exp}';\n      failNegateMsg = 'expected #{this} to not have the same ' + subject + ' as #{exp}';\n    }\n\n    var cmp = flag(this, 'deep') ? _.eql : undefined;\n\n    this.assert(\n        isSubsetOf(subset, obj, cmp, contains, ordered)\n      , failMsg\n      , failNegateMsg\n      , subset\n      , obj\n      , true\n    );\n  });\n\n  /**\n   * ### .oneOf(list[, msg])\n   *\n   * Asserts that the target is a member of the given array `list`. However,\n   * it's often best to assert that the target is equal to its expected value.\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.be.oneOf([1, 2, 3]); // Not recommended\n   *\n   * Comparisons are performed using strict (`===`) equality.\n   *\n   * Add `.not` earlier in the chain to negate `.oneOf`.\n   *\n   *     expect(1).to.equal(1); // Recommended\n   *     expect(1).to.not.be.oneOf([2, 3, 4]); // Not recommended\n   *\n   * It can also be chained with `.contain` or `.include`, which will work with\n   * both arrays and strings:\n   *\n   *     expect('Today is sunny').to.contain.oneOf(['sunny', 'cloudy'])\n   *     expect('Today is rainy').to.not.contain.oneOf(['sunny', 'cloudy'])\n   *     expect([1,2,3]).to.contain.oneOf([3,4,5])\n   *     expect([1,2,3]).to.not.contain.oneOf([4,5,6])\n   *\n   * `.oneOf` accepts an optional `msg` argument which is a custom error message\n   * to show when the assertion fails. The message can also be given as the\n   * second argument to `expect`.\n   *\n   *     expect(1).to.be.oneOf([2, 3, 4], 'nooo why fail??');\n   *     expect(1, 'nooo why fail??').to.be.oneOf([2, 3, 4]);\n   *\n   * @name oneOf\n   * @param {Array<*>} list\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function oneOf (list, msg) {\n    if (msg) flag(this, 'message', msg);\n    var expected = flag(this, 'object')\n      , flagMsg = flag(this, 'message')\n      , ssfi = flag(this, 'ssfi')\n      , contains = flag(this, 'contains')\n      , isDeep = flag(this, 'deep');\n    new Assertion(list, flagMsg, ssfi, true).to.be.an('array');\n\n    if (contains) {\n      this.assert(\n        list.some(function(possibility) { return expected.indexOf(possibility) > -1 })\n        , 'expected #{this} to contain one of #{exp}'\n        , 'expected #{this} to not contain one of #{exp}'\n        , list\n        , expected\n      );\n    } else {\n      if (isDeep) {\n        this.assert(\n          list.some(function(possibility) { return _.eql(expected, possibility) })\n          , 'expected #{this} to deeply equal one of #{exp}'\n          , 'expected #{this} to deeply equal one of #{exp}'\n          , list\n          , expected\n        );\n      } else {\n        this.assert(\n          list.indexOf(expected) > -1\n          , 'expected #{this} to be one of #{exp}'\n          , 'expected #{this} to not be one of #{exp}'\n          , list\n          , expected\n        );\n      }\n    }\n  }\n\n  Assertion.addMethod('oneOf', oneOf);\n\n  /**\n   * ### .change(subject[, prop[, msg]])\n   *\n   * When one argument is provided, `.change` asserts that the given function\n   * `subject` returns a different value when it's invoked before the target\n   * function compared to when it's invoked afterward. However, it's often best\n   * to assert that `subject` is equal to its expected value.\n   *\n   *     var dots = ''\n   *       , addDot = function () { dots += '.'; }\n   *       , getDots = function () { return dots; };\n   *\n   *     // Recommended\n   *     expect(getDots()).to.equal('');\n   *     addDot();\n   *     expect(getDots()).to.equal('.');\n   *\n   *     // Not recommended\n   *     expect(addDot).to.change(getDots);\n   *\n   * When two arguments are provided, `.change` asserts that the value of the\n   * given object `subject`'s `prop` property is different before invoking the\n   * target function compared to afterward.\n   *\n   *     var myObj = {dots: ''}\n   *       , addDot = function () { myObj.dots += '.'; };\n   *\n   *     // Recommended\n   *     expect(myObj).to.have.property('dots', '');\n   *     addDot();\n   *     expect(myObj).to.have.property('dots', '.');\n   *\n   *     // Not recommended\n   *     expect(addDot).to.change(myObj, 'dots');\n   *\n   * Strict (`===`) equality is used to compare before and after values.\n   *\n   * Add `.not` earlier in the chain to negate `.change`.\n   *\n   *     var dots = ''\n   *       , noop = function () {}\n   *       , getDots = function () { return dots; };\n   *\n   *     expect(noop).to.not.change(getDots);\n   *\n   *     var myObj = {dots: ''}\n   *       , noop = function () {};\n   *\n   *     expect(noop).to.not.change(myObj, 'dots');\n   *\n   * `.change` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`. When not providing two arguments, always\n   * use the second form.\n   *\n   *     var myObj = {dots: ''}\n   *       , addDot = function () { myObj.dots += '.'; };\n   *\n   *     expect(addDot).to.not.change(myObj, 'dots', 'nooo why fail??');\n   *\n   *     var dots = ''\n   *       , addDot = function () { dots += '.'; }\n   *       , getDots = function () { return dots; };\n   *\n   *     expect(addDot, 'nooo why fail??').to.not.change(getDots);\n   *\n   * `.change` also causes all `.by` assertions that follow in the chain to\n   * assert how much a numeric subject was increased or decreased by. However,\n   * it's dangerous to use `.change.by`. The problem is that it creates\n   * uncertain expectations by asserting that the subject either increases by\n   * the given delta, or that it decreases by the given delta. It's often best\n   * to identify the exact output that's expected, and then write an assertion\n   * that only accepts that exact output.\n   *\n   *     var myObj = {val: 1}\n   *       , addTwo = function () { myObj.val += 2; }\n   *       , subtractTwo = function () { myObj.val -= 2; };\n   *\n   *     expect(addTwo).to.increase(myObj, 'val').by(2); // Recommended\n   *     expect(addTwo).to.change(myObj, 'val').by(2); // Not recommended\n   *\n   *     expect(subtractTwo).to.decrease(myObj, 'val').by(2); // Recommended\n   *     expect(subtractTwo).to.change(myObj, 'val').by(2); // Not recommended\n   *\n   * The alias `.changes` can be used interchangeably with `.change`.\n   *\n   * @name change\n   * @alias changes\n   * @param {String} subject\n   * @param {String} prop name _optional_\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertChanges (subject, prop, msg) {\n    if (msg) flag(this, 'message', msg);\n    var fn = flag(this, 'object')\n      , flagMsg = flag(this, 'message')\n      , ssfi = flag(this, 'ssfi');\n    new Assertion(fn, flagMsg, ssfi, true).is.a('function');\n\n    var initial;\n    if (!prop) {\n      new Assertion(subject, flagMsg, ssfi, true).is.a('function');\n      initial = subject();\n    } else {\n      new Assertion(subject, flagMsg, ssfi, true).to.have.property(prop);\n      initial = subject[prop];\n    }\n\n    fn();\n\n    var final = prop === undefined || prop === null ? subject() : subject[prop];\n    var msgObj = prop === undefined || prop === null ? initial : '.' + prop;\n\n    // This gets flagged because of the .by(delta) assertion\n    flag(this, 'deltaMsgObj', msgObj);\n    flag(this, 'initialDeltaValue', initial);\n    flag(this, 'finalDeltaValue', final);\n    flag(this, 'deltaBehavior', 'change');\n    flag(this, 'realDelta', final !== initial);\n\n    this.assert(\n      initial !== final\n      , 'expected ' + msgObj + ' to change'\n      , 'expected ' + msgObj + ' to not change'\n    );\n  }\n\n  Assertion.addMethod('change', assertChanges);\n  Assertion.addMethod('changes', assertChanges);\n\n  /**\n   * ### .increase(subject[, prop[, msg]])\n   *\n   * When one argument is provided, `.increase` asserts that the given function\n   * `subject` returns a greater number when it's invoked after invoking the\n   * target function compared to when it's invoked beforehand. `.increase` also\n   * causes all `.by` assertions that follow in the chain to assert how much\n   * greater of a number is returned. It's often best to assert that the return\n   * value increased by the expected amount, rather than asserting it increased\n   * by any amount.\n   *\n   *     var val = 1\n   *       , addTwo = function () { val += 2; }\n   *       , getVal = function () { return val; };\n   *\n   *     expect(addTwo).to.increase(getVal).by(2); // Recommended\n   *     expect(addTwo).to.increase(getVal); // Not recommended\n   *\n   * When two arguments are provided, `.increase` asserts that the value of the\n   * given object `subject`'s `prop` property is greater after invoking the\n   * target function compared to beforehand.\n   *\n   *     var myObj = {val: 1}\n   *       , addTwo = function () { myObj.val += 2; };\n   *\n   *     expect(addTwo).to.increase(myObj, 'val').by(2); // Recommended\n   *     expect(addTwo).to.increase(myObj, 'val'); // Not recommended\n   *\n   * Add `.not` earlier in the chain to negate `.increase`. However, it's\n   * dangerous to do so. The problem is that it creates uncertain expectations\n   * by asserting that the subject either decreases, or that it stays the same.\n   * It's often best to identify the exact output that's expected, and then\n   * write an assertion that only accepts that exact output.\n   *\n   * When the subject is expected to decrease, it's often best to assert that it\n   * decreased by the expected amount.\n   *\n   *     var myObj = {val: 1}\n   *       , subtractTwo = function () { myObj.val -= 2; };\n   *\n   *     expect(subtractTwo).to.decrease(myObj, 'val').by(2); // Recommended\n   *     expect(subtractTwo).to.not.increase(myObj, 'val'); // Not recommended\n   *\n   * When the subject is expected to stay the same, it's often best to assert\n   * exactly that.\n   *\n   *     var myObj = {val: 1}\n   *       , noop = function () {};\n   *\n   *     expect(noop).to.not.change(myObj, 'val'); // Recommended\n   *     expect(noop).to.not.increase(myObj, 'val'); // Not recommended\n   *\n   * `.increase` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`. When not providing two arguments, always\n   * use the second form.\n   *\n   *     var myObj = {val: 1}\n   *       , noop = function () {};\n   *\n   *     expect(noop).to.increase(myObj, 'val', 'nooo why fail??');\n   *\n   *     var val = 1\n   *       , noop = function () {}\n   *       , getVal = function () { return val; };\n   *\n   *     expect(noop, 'nooo why fail??').to.increase(getVal);\n   *\n   * The alias `.increases` can be used interchangeably with `.increase`.\n   *\n   * @name increase\n   * @alias increases\n   * @param {String|Function} subject\n   * @param {String} prop name _optional_\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertIncreases (subject, prop, msg) {\n    if (msg) flag(this, 'message', msg);\n    var fn = flag(this, 'object')\n      , flagMsg = flag(this, 'message')\n      , ssfi = flag(this, 'ssfi');\n    new Assertion(fn, flagMsg, ssfi, true).is.a('function');\n\n    var initial;\n    if (!prop) {\n      new Assertion(subject, flagMsg, ssfi, true).is.a('function');\n      initial = subject();\n    } else {\n      new Assertion(subject, flagMsg, ssfi, true).to.have.property(prop);\n      initial = subject[prop];\n    }\n\n    // Make sure that the target is a number\n    new Assertion(initial, flagMsg, ssfi, true).is.a('number');\n\n    fn();\n\n    var final = prop === undefined || prop === null ? subject() : subject[prop];\n    var msgObj = prop === undefined || prop === null ? initial : '.' + prop;\n\n    flag(this, 'deltaMsgObj', msgObj);\n    flag(this, 'initialDeltaValue', initial);\n    flag(this, 'finalDeltaValue', final);\n    flag(this, 'deltaBehavior', 'increase');\n    flag(this, 'realDelta', final - initial);\n\n    this.assert(\n      final - initial > 0\n      , 'expected ' + msgObj + ' to increase'\n      , 'expected ' + msgObj + ' to not increase'\n    );\n  }\n\n  Assertion.addMethod('increase', assertIncreases);\n  Assertion.addMethod('increases', assertIncreases);\n\n  /**\n   * ### .decrease(subject[, prop[, msg]])\n   *\n   * When one argument is provided, `.decrease` asserts that the given function\n   * `subject` returns a lesser number when it's invoked after invoking the\n   * target function compared to when it's invoked beforehand. `.decrease` also\n   * causes all `.by` assertions that follow in the chain to assert how much\n   * lesser of a number is returned. It's often best to assert that the return\n   * value decreased by the expected amount, rather than asserting it decreased\n   * by any amount.\n   *\n   *     var val = 1\n   *       , subtractTwo = function () { val -= 2; }\n   *       , getVal = function () { return val; };\n   *\n   *     expect(subtractTwo).to.decrease(getVal).by(2); // Recommended\n   *     expect(subtractTwo).to.decrease(getVal); // Not recommended\n   *\n   * When two arguments are provided, `.decrease` asserts that the value of the\n   * given object `subject`'s `prop` property is lesser after invoking the\n   * target function compared to beforehand.\n   *\n   *     var myObj = {val: 1}\n   *       , subtractTwo = function () { myObj.val -= 2; };\n   *\n   *     expect(subtractTwo).to.decrease(myObj, 'val').by(2); // Recommended\n   *     expect(subtractTwo).to.decrease(myObj, 'val'); // Not recommended\n   *\n   * Add `.not` earlier in the chain to negate `.decrease`. However, it's\n   * dangerous to do so. The problem is that it creates uncertain expectations\n   * by asserting that the subject either increases, or that it stays the same.\n   * It's often best to identify the exact output that's expected, and then\n   * write an assertion that only accepts that exact output.\n   *\n   * When the subject is expected to increase, it's often best to assert that it\n   * increased by the expected amount.\n   *\n   *     var myObj = {val: 1}\n   *       , addTwo = function () { myObj.val += 2; };\n   *\n   *     expect(addTwo).to.increase(myObj, 'val').by(2); // Recommended\n   *     expect(addTwo).to.not.decrease(myObj, 'val'); // Not recommended\n   *\n   * When the subject is expected to stay the same, it's often best to assert\n   * exactly that.\n   *\n   *     var myObj = {val: 1}\n   *       , noop = function () {};\n   *\n   *     expect(noop).to.not.change(myObj, 'val'); // Recommended\n   *     expect(noop).to.not.decrease(myObj, 'val'); // Not recommended\n   *\n   * `.decrease` accepts an optional `msg` argument which is a custom error\n   * message to show when the assertion fails. The message can also be given as\n   * the second argument to `expect`. When not providing two arguments, always\n   * use the second form.\n   *\n   *     var myObj = {val: 1}\n   *       , noop = function () {};\n   *\n   *     expect(noop).to.decrease(myObj, 'val', 'nooo why fail??');\n   *\n   *     var val = 1\n   *       , noop = function () {}\n   *       , getVal = function () { return val; };\n   *\n   *     expect(noop, 'nooo why fail??').to.decrease(getVal);\n   *\n   * The alias `.decreases` can be used interchangeably with `.decrease`.\n   *\n   * @name decrease\n   * @alias decreases\n   * @param {String|Function} subject\n   * @param {String} prop name _optional_\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertDecreases (subject, prop, msg) {\n    if (msg) flag(this, 'message', msg);\n    var fn = flag(this, 'object')\n      , flagMsg = flag(this, 'message')\n      , ssfi = flag(this, 'ssfi');\n    new Assertion(fn, flagMsg, ssfi, true).is.a('function');\n\n    var initial;\n    if (!prop) {\n      new Assertion(subject, flagMsg, ssfi, true).is.a('function');\n      initial = subject();\n    } else {\n      new Assertion(subject, flagMsg, ssfi, true).to.have.property(prop);\n      initial = subject[prop];\n    }\n\n    // Make sure that the target is a number\n    new Assertion(initial, flagMsg, ssfi, true).is.a('number');\n\n    fn();\n\n    var final = prop === undefined || prop === null ? subject() : subject[prop];\n    var msgObj = prop === undefined || prop === null ? initial : '.' + prop;\n\n    flag(this, 'deltaMsgObj', msgObj);\n    flag(this, 'initialDeltaValue', initial);\n    flag(this, 'finalDeltaValue', final);\n    flag(this, 'deltaBehavior', 'decrease');\n    flag(this, 'realDelta', initial - final);\n\n    this.assert(\n      final - initial < 0\n      , 'expected ' + msgObj + ' to decrease'\n      , 'expected ' + msgObj + ' to not decrease'\n    );\n  }\n\n  Assertion.addMethod('decrease', assertDecreases);\n  Assertion.addMethod('decreases', assertDecreases);\n\n  /**\n   * ### .by(delta[, msg])\n   *\n   * When following an `.increase` assertion in the chain, `.by` asserts that\n   * the subject of the `.increase` assertion increased by the given `delta`.\n   *\n   *     var myObj = {val: 1}\n   *       , addTwo = function () { myObj.val += 2; };\n   *\n   *     expect(addTwo).to.increase(myObj, 'val').by(2);\n   *\n   * When following a `.decrease` assertion in the chain, `.by` asserts that the\n   * subject of the `.decrease` assertion decreased by the given `delta`.\n   *\n   *     var myObj = {val: 1}\n   *       , subtractTwo = function () { myObj.val -= 2; };\n   *\n   *     expect(subtractTwo).to.decrease(myObj, 'val').by(2);\n   *\n   * When following a `.change` assertion in the chain, `.by` asserts that the\n   * subject of the `.change` assertion either increased or decreased by the\n   * given `delta`. However, it's dangerous to use `.change.by`. The problem is\n   * that it creates uncertain expectations. It's often best to identify the\n   * exact output that's expected, and then write an assertion that only accepts\n   * that exact output.\n   *\n   *     var myObj = {val: 1}\n   *       , addTwo = function () { myObj.val += 2; }\n   *       , subtractTwo = function () { myObj.val -= 2; };\n   *\n   *     expect(addTwo).to.increase(myObj, 'val').by(2); // Recommended\n   *     expect(addTwo).to.change(myObj, 'val').by(2); // Not recommended\n   *\n   *     expect(subtractTwo).to.decrease(myObj, 'val').by(2); // Recommended\n   *     expect(subtractTwo).to.change(myObj, 'val').by(2); // Not recommended\n   *\n   * Add `.not` earlier in the chain to negate `.by`. However, it's often best\n   * to assert that the subject changed by its expected delta, rather than\n   * asserting that it didn't change by one of countless unexpected deltas.\n   *\n   *     var myObj = {val: 1}\n   *       , addTwo = function () { myObj.val += 2; };\n   *\n   *     // Recommended\n   *     expect(addTwo).to.increase(myObj, 'val').by(2);\n   *\n   *     // Not recommended\n   *     expect(addTwo).to.increase(myObj, 'val').but.not.by(3);\n   *\n   * `.by` accepts an optional `msg` argument which is a custom error message to\n   * show when the assertion fails. The message can also be given as the second\n   * argument to `expect`.\n   *\n   *     var myObj = {val: 1}\n   *       , addTwo = function () { myObj.val += 2; };\n   *\n   *     expect(addTwo).to.increase(myObj, 'val').by(3, 'nooo why fail??');\n   *     expect(addTwo, 'nooo why fail??').to.increase(myObj, 'val').by(3);\n   *\n   * @name by\n   * @param {Number} delta\n   * @param {String} msg _optional_\n   * @namespace BDD\n   * @api public\n   */\n\n  function assertDelta(delta, msg) {\n    if (msg) flag(this, 'message', msg);\n\n    var msgObj = flag(this, 'deltaMsgObj');\n    var initial = flag(this, 'initialDeltaValue');\n    var final = flag(this, 'finalDeltaValue');\n    var behavior = flag(this, 'deltaBehavior');\n    var realDelta = flag(this, 'realDelta');\n\n    var expression;\n    if (behavior === 'change') {\n      expression = Math.abs(final - initial) === Math.abs(delta);\n    } else {\n      expression = realDelta === Math.abs(delta);\n    }\n\n    this.assert(\n      expression\n      , 'expected ' + msgObj + ' to ' + behavior + ' by ' + delta\n      , 'expected ' + msgObj + ' to not ' + behavior + ' by ' + delta\n    );\n  }\n\n  Assertion.addMethod('by', assertDelta);\n\n  /**\n   * ### .extensible\n   *\n   * Asserts that the target is extensible, which means that new properties can\n   * be added to it. Primitives are never extensible.\n   *\n   *     expect({a: 1}).to.be.extensible;\n   *\n   * Add `.not` earlier in the chain to negate `.extensible`.\n   *\n   *     var nonExtensibleObject = Object.preventExtensions({})\n   *       , sealedObject = Object.seal({})\n   *       , frozenObject = Object.freeze({});\n   *\n   *     expect(nonExtensibleObject).to.not.be.extensible;\n   *     expect(sealedObject).to.not.be.extensible;\n   *     expect(frozenObject).to.not.be.extensible;\n   *     expect(1).to.not.be.extensible;\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect(1, 'nooo why fail??').to.be.extensible;\n   *\n   * @name extensible\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('extensible', function() {\n    var obj = flag(this, 'object');\n\n    // In ES5, if the argument to this method is a primitive, then it will cause a TypeError.\n    // In ES6, a non-object argument will be treated as if it was a non-extensible ordinary object, simply return false.\n    // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/isExtensible\n    // The following provides ES6 behavior for ES5 environments.\n\n    var isExtensible = obj === Object(obj) && Object.isExtensible(obj);\n\n    this.assert(\n      isExtensible\n      , 'expected #{this} to be extensible'\n      , 'expected #{this} to not be extensible'\n    );\n  });\n\n  /**\n   * ### .sealed\n   *\n   * Asserts that the target is sealed, which means that new properties can't be\n   * added to it, and its existing properties can't be reconfigured or deleted.\n   * However, it's possible that its existing properties can still be reassigned\n   * to different values. Primitives are always sealed.\n   *\n   *     var sealedObject = Object.seal({});\n   *     var frozenObject = Object.freeze({});\n   *\n   *     expect(sealedObject).to.be.sealed;\n   *     expect(frozenObject).to.be.sealed;\n   *     expect(1).to.be.sealed;\n   *\n   * Add `.not` earlier in the chain to negate `.sealed`.\n   *\n   *     expect({a: 1}).to.not.be.sealed;\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect({a: 1}, 'nooo why fail??').to.be.sealed;\n   *\n   * @name sealed\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('sealed', function() {\n    var obj = flag(this, 'object');\n\n    // In ES5, if the argument to this method is a primitive, then it will cause a TypeError.\n    // In ES6, a non-object argument will be treated as if it was a sealed ordinary object, simply return true.\n    // See https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/isSealed\n    // The following provides ES6 behavior for ES5 environments.\n\n    var isSealed = obj === Object(obj) ? Object.isSealed(obj) : true;\n\n    this.assert(\n      isSealed\n      , 'expected #{this} to be sealed'\n      , 'expected #{this} to not be sealed'\n    );\n  });\n\n  /**\n   * ### .frozen\n   *\n   * Asserts that the target is frozen, which means that new properties can't be\n   * added to it, and its existing properties can't be reassigned to different\n   * values, reconfigured, or deleted. Primitives are always frozen.\n   *\n   *     var frozenObject = Object.freeze({});\n   *\n   *     expect(frozenObject).to.be.frozen;\n   *     expect(1).to.be.frozen;\n   *\n   * Add `.not` earlier in the chain to negate `.frozen`.\n   *\n   *     expect({a: 1}).to.not.be.frozen;\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect({a: 1}, 'nooo why fail??').to.be.frozen;\n   *\n   * @name frozen\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('frozen', function() {\n    var obj = flag(this, 'object');\n\n    // In ES5, if the argument to this method is a primitive, then it will cause a TypeError.\n    // In ES6, a non-object argument will be treated as if it was a frozen ordinary object, simply return true.\n    // See https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/isFrozen\n    // The following provides ES6 behavior for ES5 environments.\n\n    var isFrozen = obj === Object(obj) ? Object.isFrozen(obj) : true;\n\n    this.assert(\n      isFrozen\n      , 'expected #{this} to be frozen'\n      , 'expected #{this} to not be frozen'\n    );\n  });\n\n  /**\n   * ### .finite\n   *\n   * Asserts that the target is a number, and isn't `NaN` or positive/negative\n   * `Infinity`.\n   *\n   *     expect(1).to.be.finite;\n   *\n   * Add `.not` earlier in the chain to negate `.finite`. However, it's\n   * dangerous to do so. The problem is that it creates uncertain expectations\n   * by asserting that the subject either isn't a number, or that it's `NaN`, or\n   * that it's positive `Infinity`, or that it's negative `Infinity`. It's often\n   * best to identify the exact output that's expected, and then write an\n   * assertion that only accepts that exact output.\n   *\n   * When the target isn't expected to be a number, it's often best to assert\n   * that it's the expected type, rather than asserting that it isn't one of\n   * many unexpected types.\n   *\n   *     expect('foo').to.be.a('string'); // Recommended\n   *     expect('foo').to.not.be.finite; // Not recommended\n   *\n   * When the target is expected to be `NaN`, it's often best to assert exactly\n   * that.\n   *\n   *     expect(NaN).to.be.NaN; // Recommended\n   *     expect(NaN).to.not.be.finite; // Not recommended\n   *\n   * When the target is expected to be positive infinity, it's often best to\n   * assert exactly that.\n   *\n   *     expect(Infinity).to.equal(Infinity); // Recommended\n   *     expect(Infinity).to.not.be.finite; // Not recommended\n   *\n   * When the target is expected to be negative infinity, it's often best to\n   * assert exactly that.\n   *\n   *     expect(-Infinity).to.equal(-Infinity); // Recommended\n   *     expect(-Infinity).to.not.be.finite; // Not recommended\n   *\n   * A custom error message can be given as the second argument to `expect`.\n   *\n   *     expect('foo', 'nooo why fail??').to.be.finite;\n   *\n   * @name finite\n   * @namespace BDD\n   * @api public\n   */\n\n  Assertion.addProperty('finite', function(msg) {\n    var obj = flag(this, 'object');\n\n    this.assert(\n        typeof obj === 'number' && isFinite(obj)\n      , 'expected #{this} to be a finite number'\n      , 'expected #{this} to not be a finite number'\n    );\n  });\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/core/assertions.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/interface/assert.js":
/*!********************************************************!*\
  !*** ./node_modules/chai/lib/chai/interface/assert.js ***!
  \********************************************************/
/***/ ((module) => {

eval("/*!\n * chai\n * Copyright(c) 2011-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\nmodule.exports = function (chai, util) {\n  /*!\n   * Chai dependencies.\n   */\n\n  var Assertion = chai.Assertion\n    , flag = util.flag;\n\n  /*!\n   * Module export.\n   */\n\n  /**\n   * ### assert(expression, message)\n   *\n   * Write your own test expressions.\n   *\n   *     assert('foo' !== 'bar', 'foo is not bar');\n   *     assert(Array.isArray([]), 'empty arrays are arrays');\n   *\n   * @param {Mixed} expression to test for truthiness\n   * @param {String} message to display on error\n   * @name assert\n   * @namespace Assert\n   * @api public\n   */\n\n  var assert = chai.assert = function (express, errmsg) {\n    var test = new Assertion(null, null, chai.assert, true);\n    test.assert(\n        express\n      , errmsg\n      , '[ negation message unavailable ]'\n    );\n  };\n\n  /**\n   * ### .fail([message])\n   * ### .fail(actual, expected, [message], [operator])\n   *\n   * Throw a failure. Node.js `assert` module-compatible.\n   *\n   *     assert.fail();\n   *     assert.fail(\"custom error message\");\n   *     assert.fail(1, 2);\n   *     assert.fail(1, 2, \"custom error message\");\n   *     assert.fail(1, 2, \"custom error message\", \">\");\n   *     assert.fail(1, 2, undefined, \">\");\n   *\n   * @name fail\n   * @param {Mixed} actual\n   * @param {Mixed} expected\n   * @param {String} message\n   * @param {String} operator\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.fail = function (actual, expected, message, operator) {\n    if (arguments.length < 2) {\n        // Comply with Node's fail([message]) interface\n\n        message = actual;\n        actual = undefined;\n    }\n\n    message = message || 'assert.fail()';\n    throw new chai.AssertionError(message, {\n        actual: actual\n      , expected: expected\n      , operator: operator\n    }, assert.fail);\n  };\n\n  /**\n   * ### .isOk(object, [message])\n   *\n   * Asserts that `object` is truthy.\n   *\n   *     assert.isOk('everything', 'everything is ok');\n   *     assert.isOk(false, 'this will fail');\n   *\n   * @name isOk\n   * @alias ok\n   * @param {Mixed} object to test\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isOk = function (val, msg) {\n    new Assertion(val, msg, assert.isOk, true).is.ok;\n  };\n\n  /**\n   * ### .isNotOk(object, [message])\n   *\n   * Asserts that `object` is falsy.\n   *\n   *     assert.isNotOk('everything', 'this will fail');\n   *     assert.isNotOk(false, 'this will pass');\n   *\n   * @name isNotOk\n   * @alias notOk\n   * @param {Mixed} object to test\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotOk = function (val, msg) {\n    new Assertion(val, msg, assert.isNotOk, true).is.not.ok;\n  };\n\n  /**\n   * ### .equal(actual, expected, [message])\n   *\n   * Asserts non-strict equality (`==`) of `actual` and `expected`.\n   *\n   *     assert.equal(3, '3', '== coerces values to strings');\n   *\n   * @name equal\n   * @param {Mixed} actual\n   * @param {Mixed} expected\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.equal = function (act, exp, msg) {\n    var test = new Assertion(act, msg, assert.equal, true);\n\n    test.assert(\n        exp == flag(test, 'object')\n      , 'expected #{this} to equal #{exp}'\n      , 'expected #{this} to not equal #{act}'\n      , exp\n      , act\n      , true\n    );\n  };\n\n  /**\n   * ### .notEqual(actual, expected, [message])\n   *\n   * Asserts non-strict inequality (`!=`) of `actual` and `expected`.\n   *\n   *     assert.notEqual(3, 4, 'these numbers are not equal');\n   *\n   * @name notEqual\n   * @param {Mixed} actual\n   * @param {Mixed} expected\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notEqual = function (act, exp, msg) {\n    var test = new Assertion(act, msg, assert.notEqual, true);\n\n    test.assert(\n        exp != flag(test, 'object')\n      , 'expected #{this} to not equal #{exp}'\n      , 'expected #{this} to equal #{act}'\n      , exp\n      , act\n      , true\n    );\n  };\n\n  /**\n   * ### .strictEqual(actual, expected, [message])\n   *\n   * Asserts strict equality (`===`) of `actual` and `expected`.\n   *\n   *     assert.strictEqual(true, true, 'these booleans are strictly equal');\n   *\n   * @name strictEqual\n   * @param {Mixed} actual\n   * @param {Mixed} expected\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.strictEqual = function (act, exp, msg) {\n    new Assertion(act, msg, assert.strictEqual, true).to.equal(exp);\n  };\n\n  /**\n   * ### .notStrictEqual(actual, expected, [message])\n   *\n   * Asserts strict inequality (`!==`) of `actual` and `expected`.\n   *\n   *     assert.notStrictEqual(3, '3', 'no coercion for strict equality');\n   *\n   * @name notStrictEqual\n   * @param {Mixed} actual\n   * @param {Mixed} expected\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notStrictEqual = function (act, exp, msg) {\n    new Assertion(act, msg, assert.notStrictEqual, true).to.not.equal(exp);\n  };\n\n  /**\n   * ### .deepEqual(actual, expected, [message])\n   *\n   * Asserts that `actual` is deeply equal to `expected`.\n   *\n   *     assert.deepEqual({ tea: 'green' }, { tea: 'green' });\n   *\n   * @name deepEqual\n   * @param {Mixed} actual\n   * @param {Mixed} expected\n   * @param {String} message\n   * @alias deepStrictEqual\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.deepEqual = assert.deepStrictEqual = function (act, exp, msg) {\n    new Assertion(act, msg, assert.deepEqual, true).to.eql(exp);\n  };\n\n  /**\n   * ### .notDeepEqual(actual, expected, [message])\n   *\n   * Assert that `actual` is not deeply equal to `expected`.\n   *\n   *     assert.notDeepEqual({ tea: 'green' }, { tea: 'jasmine' });\n   *\n   * @name notDeepEqual\n   * @param {Mixed} actual\n   * @param {Mixed} expected\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notDeepEqual = function (act, exp, msg) {\n    new Assertion(act, msg, assert.notDeepEqual, true).to.not.eql(exp);\n  };\n\n   /**\n   * ### .isAbove(valueToCheck, valueToBeAbove, [message])\n   *\n   * Asserts `valueToCheck` is strictly greater than (>) `valueToBeAbove`.\n   *\n   *     assert.isAbove(5, 2, '5 is strictly greater than 2');\n   *\n   * @name isAbove\n   * @param {Mixed} valueToCheck\n   * @param {Mixed} valueToBeAbove\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isAbove = function (val, abv, msg) {\n    new Assertion(val, msg, assert.isAbove, true).to.be.above(abv);\n  };\n\n   /**\n   * ### .isAtLeast(valueToCheck, valueToBeAtLeast, [message])\n   *\n   * Asserts `valueToCheck` is greater than or equal to (>=) `valueToBeAtLeast`.\n   *\n   *     assert.isAtLeast(5, 2, '5 is greater or equal to 2');\n   *     assert.isAtLeast(3, 3, '3 is greater or equal to 3');\n   *\n   * @name isAtLeast\n   * @param {Mixed} valueToCheck\n   * @param {Mixed} valueToBeAtLeast\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isAtLeast = function (val, atlst, msg) {\n    new Assertion(val, msg, assert.isAtLeast, true).to.be.least(atlst);\n  };\n\n   /**\n   * ### .isBelow(valueToCheck, valueToBeBelow, [message])\n   *\n   * Asserts `valueToCheck` is strictly less than (<) `valueToBeBelow`.\n   *\n   *     assert.isBelow(3, 6, '3 is strictly less than 6');\n   *\n   * @name isBelow\n   * @param {Mixed} valueToCheck\n   * @param {Mixed} valueToBeBelow\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isBelow = function (val, blw, msg) {\n    new Assertion(val, msg, assert.isBelow, true).to.be.below(blw);\n  };\n\n   /**\n   * ### .isAtMost(valueToCheck, valueToBeAtMost, [message])\n   *\n   * Asserts `valueToCheck` is less than or equal to (<=) `valueToBeAtMost`.\n   *\n   *     assert.isAtMost(3, 6, '3 is less than or equal to 6');\n   *     assert.isAtMost(4, 4, '4 is less than or equal to 4');\n   *\n   * @name isAtMost\n   * @param {Mixed} valueToCheck\n   * @param {Mixed} valueToBeAtMost\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isAtMost = function (val, atmst, msg) {\n    new Assertion(val, msg, assert.isAtMost, true).to.be.most(atmst);\n  };\n\n  /**\n   * ### .isTrue(value, [message])\n   *\n   * Asserts that `value` is true.\n   *\n   *     var teaServed = true;\n   *     assert.isTrue(teaServed, 'the tea has been served');\n   *\n   * @name isTrue\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isTrue = function (val, msg) {\n    new Assertion(val, msg, assert.isTrue, true).is['true'];\n  };\n\n  /**\n   * ### .isNotTrue(value, [message])\n   *\n   * Asserts that `value` is not true.\n   *\n   *     var tea = 'tasty chai';\n   *     assert.isNotTrue(tea, 'great, time for tea!');\n   *\n   * @name isNotTrue\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotTrue = function (val, msg) {\n    new Assertion(val, msg, assert.isNotTrue, true).to.not.equal(true);\n  };\n\n  /**\n   * ### .isFalse(value, [message])\n   *\n   * Asserts that `value` is false.\n   *\n   *     var teaServed = false;\n   *     assert.isFalse(teaServed, 'no tea yet? hmm...');\n   *\n   * @name isFalse\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isFalse = function (val, msg) {\n    new Assertion(val, msg, assert.isFalse, true).is['false'];\n  };\n\n  /**\n   * ### .isNotFalse(value, [message])\n   *\n   * Asserts that `value` is not false.\n   *\n   *     var tea = 'tasty chai';\n   *     assert.isNotFalse(tea, 'great, time for tea!');\n   *\n   * @name isNotFalse\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotFalse = function (val, msg) {\n    new Assertion(val, msg, assert.isNotFalse, true).to.not.equal(false);\n  };\n\n  /**\n   * ### .isNull(value, [message])\n   *\n   * Asserts that `value` is null.\n   *\n   *     assert.isNull(err, 'there was no error');\n   *\n   * @name isNull\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNull = function (val, msg) {\n    new Assertion(val, msg, assert.isNull, true).to.equal(null);\n  };\n\n  /**\n   * ### .isNotNull(value, [message])\n   *\n   * Asserts that `value` is not null.\n   *\n   *     var tea = 'tasty chai';\n   *     assert.isNotNull(tea, 'great, time for tea!');\n   *\n   * @name isNotNull\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotNull = function (val, msg) {\n    new Assertion(val, msg, assert.isNotNull, true).to.not.equal(null);\n  };\n\n  /**\n   * ### .isNaN\n   *\n   * Asserts that value is NaN.\n   *\n   *     assert.isNaN(NaN, 'NaN is NaN');\n   *\n   * @name isNaN\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNaN = function (val, msg) {\n    new Assertion(val, msg, assert.isNaN, true).to.be.NaN;\n  };\n\n  /**\n   * ### .isNotNaN\n   *\n   * Asserts that value is not NaN.\n   *\n   *     assert.isNotNaN(4, '4 is not NaN');\n   *\n   * @name isNotNaN\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n  assert.isNotNaN = function (val, msg) {\n    new Assertion(val, msg, assert.isNotNaN, true).not.to.be.NaN;\n  };\n\n  /**\n   * ### .exists\n   *\n   * Asserts that the target is neither `null` nor `undefined`.\n   *\n   *     var foo = 'hi';\n   *\n   *     assert.exists(foo, 'foo is neither `null` nor `undefined`');\n   *\n   * @name exists\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.exists = function (val, msg) {\n    new Assertion(val, msg, assert.exists, true).to.exist;\n  };\n\n  /**\n   * ### .notExists\n   *\n   * Asserts that the target is either `null` or `undefined`.\n   *\n   *     var bar = null\n   *       , baz;\n   *\n   *     assert.notExists(bar);\n   *     assert.notExists(baz, 'baz is either null or undefined');\n   *\n   * @name notExists\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notExists = function (val, msg) {\n    new Assertion(val, msg, assert.notExists, true).to.not.exist;\n  };\n\n  /**\n   * ### .isUndefined(value, [message])\n   *\n   * Asserts that `value` is `undefined`.\n   *\n   *     var tea;\n   *     assert.isUndefined(tea, 'no tea defined');\n   *\n   * @name isUndefined\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isUndefined = function (val, msg) {\n    new Assertion(val, msg, assert.isUndefined, true).to.equal(undefined);\n  };\n\n  /**\n   * ### .isDefined(value, [message])\n   *\n   * Asserts that `value` is not `undefined`.\n   *\n   *     var tea = 'cup of chai';\n   *     assert.isDefined(tea, 'tea has been defined');\n   *\n   * @name isDefined\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isDefined = function (val, msg) {\n    new Assertion(val, msg, assert.isDefined, true).to.not.equal(undefined);\n  };\n\n  /**\n   * ### .isFunction(value, [message])\n   *\n   * Asserts that `value` is a function.\n   *\n   *     function serveTea() { return 'cup of tea'; };\n   *     assert.isFunction(serveTea, 'great, we can have tea now');\n   *\n   * @name isFunction\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isFunction = function (val, msg) {\n    new Assertion(val, msg, assert.isFunction, true).to.be.a('function');\n  };\n\n  /**\n   * ### .isNotFunction(value, [message])\n   *\n   * Asserts that `value` is _not_ a function.\n   *\n   *     var serveTea = [ 'heat', 'pour', 'sip' ];\n   *     assert.isNotFunction(serveTea, 'great, we have listed the steps');\n   *\n   * @name isNotFunction\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotFunction = function (val, msg) {\n    new Assertion(val, msg, assert.isNotFunction, true).to.not.be.a('function');\n  };\n\n  /**\n   * ### .isObject(value, [message])\n   *\n   * Asserts that `value` is an object of type 'Object' (as revealed by `Object.prototype.toString`).\n   * _The assertion does not match subclassed objects._\n   *\n   *     var selection = { name: 'Chai', serve: 'with spices' };\n   *     assert.isObject(selection, 'tea selection is an object');\n   *\n   * @name isObject\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isObject = function (val, msg) {\n    new Assertion(val, msg, assert.isObject, true).to.be.a('object');\n  };\n\n  /**\n   * ### .isNotObject(value, [message])\n   *\n   * Asserts that `value` is _not_ an object of type 'Object' (as revealed by `Object.prototype.toString`).\n   *\n   *     var selection = 'chai'\n   *     assert.isNotObject(selection, 'tea selection is not an object');\n   *     assert.isNotObject(null, 'null is not an object');\n   *\n   * @name isNotObject\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotObject = function (val, msg) {\n    new Assertion(val, msg, assert.isNotObject, true).to.not.be.a('object');\n  };\n\n  /**\n   * ### .isArray(value, [message])\n   *\n   * Asserts that `value` is an array.\n   *\n   *     var menu = [ 'green', 'chai', 'oolong' ];\n   *     assert.isArray(menu, 'what kind of tea do we want?');\n   *\n   * @name isArray\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isArray = function (val, msg) {\n    new Assertion(val, msg, assert.isArray, true).to.be.an('array');\n  };\n\n  /**\n   * ### .isNotArray(value, [message])\n   *\n   * Asserts that `value` is _not_ an array.\n   *\n   *     var menu = 'green|chai|oolong';\n   *     assert.isNotArray(menu, 'what kind of tea do we want?');\n   *\n   * @name isNotArray\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotArray = function (val, msg) {\n    new Assertion(val, msg, assert.isNotArray, true).to.not.be.an('array');\n  };\n\n  /**\n   * ### .isString(value, [message])\n   *\n   * Asserts that `value` is a string.\n   *\n   *     var teaOrder = 'chai';\n   *     assert.isString(teaOrder, 'order placed');\n   *\n   * @name isString\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isString = function (val, msg) {\n    new Assertion(val, msg, assert.isString, true).to.be.a('string');\n  };\n\n  /**\n   * ### .isNotString(value, [message])\n   *\n   * Asserts that `value` is _not_ a string.\n   *\n   *     var teaOrder = 4;\n   *     assert.isNotString(teaOrder, 'order placed');\n   *\n   * @name isNotString\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotString = function (val, msg) {\n    new Assertion(val, msg, assert.isNotString, true).to.not.be.a('string');\n  };\n\n  /**\n   * ### .isNumber(value, [message])\n   *\n   * Asserts that `value` is a number.\n   *\n   *     var cups = 2;\n   *     assert.isNumber(cups, 'how many cups');\n   *\n   * @name isNumber\n   * @param {Number} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNumber = function (val, msg) {\n    new Assertion(val, msg, assert.isNumber, true).to.be.a('number');\n  };\n\n  /**\n   * ### .isNotNumber(value, [message])\n   *\n   * Asserts that `value` is _not_ a number.\n   *\n   *     var cups = '2 cups please';\n   *     assert.isNotNumber(cups, 'how many cups');\n   *\n   * @name isNotNumber\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotNumber = function (val, msg) {\n    new Assertion(val, msg, assert.isNotNumber, true).to.not.be.a('number');\n  };\n\n   /**\n   * ### .isFinite(value, [message])\n   *\n   * Asserts that `value` is a finite number. Unlike `.isNumber`, this will fail for `NaN` and `Infinity`.\n   *\n   *     var cups = 2;\n   *     assert.isFinite(cups, 'how many cups');\n   *\n   *     assert.isFinite(NaN); // throws\n   *\n   * @name isFinite\n   * @param {Number} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isFinite = function (val, msg) {\n    new Assertion(val, msg, assert.isFinite, true).to.be.finite;\n  };\n\n  /**\n   * ### .isBoolean(value, [message])\n   *\n   * Asserts that `value` is a boolean.\n   *\n   *     var teaReady = true\n   *       , teaServed = false;\n   *\n   *     assert.isBoolean(teaReady, 'is the tea ready');\n   *     assert.isBoolean(teaServed, 'has tea been served');\n   *\n   * @name isBoolean\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isBoolean = function (val, msg) {\n    new Assertion(val, msg, assert.isBoolean, true).to.be.a('boolean');\n  };\n\n  /**\n   * ### .isNotBoolean(value, [message])\n   *\n   * Asserts that `value` is _not_ a boolean.\n   *\n   *     var teaReady = 'yep'\n   *       , teaServed = 'nope';\n   *\n   *     assert.isNotBoolean(teaReady, 'is the tea ready');\n   *     assert.isNotBoolean(teaServed, 'has tea been served');\n   *\n   * @name isNotBoolean\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotBoolean = function (val, msg) {\n    new Assertion(val, msg, assert.isNotBoolean, true).to.not.be.a('boolean');\n  };\n\n  /**\n   * ### .typeOf(value, name, [message])\n   *\n   * Asserts that `value`'s type is `name`, as determined by\n   * `Object.prototype.toString`.\n   *\n   *     assert.typeOf({ tea: 'chai' }, 'object', 'we have an object');\n   *     assert.typeOf(['chai', 'jasmine'], 'array', 'we have an array');\n   *     assert.typeOf('tea', 'string', 'we have a string');\n   *     assert.typeOf(/tea/, 'regexp', 'we have a regular expression');\n   *     assert.typeOf(null, 'null', 'we have a null');\n   *     assert.typeOf(undefined, 'undefined', 'we have an undefined');\n   *\n   * @name typeOf\n   * @param {Mixed} value\n   * @param {String} name\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.typeOf = function (val, type, msg) {\n    new Assertion(val, msg, assert.typeOf, true).to.be.a(type);\n  };\n\n  /**\n   * ### .notTypeOf(value, name, [message])\n   *\n   * Asserts that `value`'s type is _not_ `name`, as determined by\n   * `Object.prototype.toString`.\n   *\n   *     assert.notTypeOf('tea', 'number', 'strings are not numbers');\n   *\n   * @name notTypeOf\n   * @param {Mixed} value\n   * @param {String} typeof name\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notTypeOf = function (val, type, msg) {\n    new Assertion(val, msg, assert.notTypeOf, true).to.not.be.a(type);\n  };\n\n  /**\n   * ### .instanceOf(object, constructor, [message])\n   *\n   * Asserts that `value` is an instance of `constructor`.\n   *\n   *     var Tea = function (name) { this.name = name; }\n   *       , chai = new Tea('chai');\n   *\n   *     assert.instanceOf(chai, Tea, 'chai is an instance of tea');\n   *\n   * @name instanceOf\n   * @param {Object} object\n   * @param {Constructor} constructor\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.instanceOf = function (val, type, msg) {\n    new Assertion(val, msg, assert.instanceOf, true).to.be.instanceOf(type);\n  };\n\n  /**\n   * ### .notInstanceOf(object, constructor, [message])\n   *\n   * Asserts `value` is not an instance of `constructor`.\n   *\n   *     var Tea = function (name) { this.name = name; }\n   *       , chai = new String('chai');\n   *\n   *     assert.notInstanceOf(chai, Tea, 'chai is not an instance of tea');\n   *\n   * @name notInstanceOf\n   * @param {Object} object\n   * @param {Constructor} constructor\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notInstanceOf = function (val, type, msg) {\n    new Assertion(val, msg, assert.notInstanceOf, true)\n      .to.not.be.instanceOf(type);\n  };\n\n  /**\n   * ### .include(haystack, needle, [message])\n   *\n   * Asserts that `haystack` includes `needle`. Can be used to assert the\n   * inclusion of a value in an array, a substring in a string, or a subset of\n   * properties in an object.\n   *\n   *     assert.include([1,2,3], 2, 'array contains value');\n   *     assert.include('foobar', 'foo', 'string contains substring');\n   *     assert.include({ foo: 'bar', hello: 'universe' }, { foo: 'bar' }, 'object contains property');\n   *\n   * Strict equality (===) is used. When asserting the inclusion of a value in\n   * an array, the array is searched for an element that's strictly equal to the\n   * given value. When asserting a subset of properties in an object, the object\n   * is searched for the given property keys, checking that each one is present\n   * and strictly equal to the given property value. For instance:\n   *\n   *     var obj1 = {a: 1}\n   *       , obj2 = {b: 2};\n   *     assert.include([obj1, obj2], obj1);\n   *     assert.include({foo: obj1, bar: obj2}, {foo: obj1});\n   *     assert.include({foo: obj1, bar: obj2}, {foo: obj1, bar: obj2});\n   *\n   * @name include\n   * @param {Array|String} haystack\n   * @param {Mixed} needle\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.include = function (exp, inc, msg) {\n    new Assertion(exp, msg, assert.include, true).include(inc);\n  };\n\n  /**\n   * ### .notInclude(haystack, needle, [message])\n   *\n   * Asserts that `haystack` does not include `needle`. Can be used to assert\n   * the absence of a value in an array, a substring in a string, or a subset of\n   * properties in an object.\n   *\n   *     assert.notInclude([1,2,3], 4, \"array doesn't contain value\");\n   *     assert.notInclude('foobar', 'baz', \"string doesn't contain substring\");\n   *     assert.notInclude({ foo: 'bar', hello: 'universe' }, { foo: 'baz' }, 'object doesn't contain property');\n   *\n   * Strict equality (===) is used. When asserting the absence of a value in an\n   * array, the array is searched to confirm the absence of an element that's\n   * strictly equal to the given value. When asserting a subset of properties in\n   * an object, the object is searched to confirm that at least one of the given\n   * property keys is either not present or not strictly equal to the given\n   * property value. For instance:\n   *\n   *     var obj1 = {a: 1}\n   *       , obj2 = {b: 2};\n   *     assert.notInclude([obj1, obj2], {a: 1});\n   *     assert.notInclude({foo: obj1, bar: obj2}, {foo: {a: 1}});\n   *     assert.notInclude({foo: obj1, bar: obj2}, {foo: obj1, bar: {b: 2}});\n   *\n   * @name notInclude\n   * @param {Array|String} haystack\n   * @param {Mixed} needle\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notInclude = function (exp, inc, msg) {\n    new Assertion(exp, msg, assert.notInclude, true).not.include(inc);\n  };\n\n  /**\n   * ### .deepInclude(haystack, needle, [message])\n   *\n   * Asserts that `haystack` includes `needle`. Can be used to assert the\n   * inclusion of a value in an array or a subset of properties in an object.\n   * Deep equality is used.\n   *\n   *     var obj1 = {a: 1}\n   *       , obj2 = {b: 2};\n   *     assert.deepInclude([obj1, obj2], {a: 1});\n   *     assert.deepInclude({foo: obj1, bar: obj2}, {foo: {a: 1}});\n   *     assert.deepInclude({foo: obj1, bar: obj2}, {foo: {a: 1}, bar: {b: 2}});\n   *\n   * @name deepInclude\n   * @param {Array|String} haystack\n   * @param {Mixed} needle\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.deepInclude = function (exp, inc, msg) {\n    new Assertion(exp, msg, assert.deepInclude, true).deep.include(inc);\n  };\n\n  /**\n   * ### .notDeepInclude(haystack, needle, [message])\n   *\n   * Asserts that `haystack` does not include `needle`. Can be used to assert\n   * the absence of a value in an array or a subset of properties in an object.\n   * Deep equality is used.\n   *\n   *     var obj1 = {a: 1}\n   *       , obj2 = {b: 2};\n   *     assert.notDeepInclude([obj1, obj2], {a: 9});\n   *     assert.notDeepInclude({foo: obj1, bar: obj2}, {foo: {a: 9}});\n   *     assert.notDeepInclude({foo: obj1, bar: obj2}, {foo: {a: 1}, bar: {b: 9}});\n   *\n   * @name notDeepInclude\n   * @param {Array|String} haystack\n   * @param {Mixed} needle\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notDeepInclude = function (exp, inc, msg) {\n    new Assertion(exp, msg, assert.notDeepInclude, true).not.deep.include(inc);\n  };\n\n  /**\n   * ### .nestedInclude(haystack, needle, [message])\n   *\n   * Asserts that 'haystack' includes 'needle'.\n   * Can be used to assert the inclusion of a subset of properties in an\n   * object.\n   * Enables the use of dot- and bracket-notation for referencing nested\n   * properties.\n   * '[]' and '.' in property names can be escaped using double backslashes.\n   *\n   *     assert.nestedInclude({'.a': {'b': 'x'}}, {'\\\\.a.[b]': 'x'});\n   *     assert.nestedInclude({'a': {'[b]': 'x'}}, {'a.\\\\[b\\\\]': 'x'});\n   *\n   * @name nestedInclude\n   * @param {Object} haystack\n   * @param {Object} needle\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.nestedInclude = function (exp, inc, msg) {\n    new Assertion(exp, msg, assert.nestedInclude, true).nested.include(inc);\n  };\n\n  /**\n   * ### .notNestedInclude(haystack, needle, [message])\n   *\n   * Asserts that 'haystack' does not include 'needle'.\n   * Can be used to assert the absence of a subset of properties in an\n   * object.\n   * Enables the use of dot- and bracket-notation for referencing nested\n   * properties.\n   * '[]' and '.' in property names can be escaped using double backslashes.\n   *\n   *     assert.notNestedInclude({'.a': {'b': 'x'}}, {'\\\\.a.b': 'y'});\n   *     assert.notNestedInclude({'a': {'[b]': 'x'}}, {'a.\\\\[b\\\\]': 'y'});\n   *\n   * @name notNestedInclude\n   * @param {Object} haystack\n   * @param {Object} needle\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notNestedInclude = function (exp, inc, msg) {\n    new Assertion(exp, msg, assert.notNestedInclude, true)\n      .not.nested.include(inc);\n  };\n\n  /**\n   * ### .deepNestedInclude(haystack, needle, [message])\n   *\n   * Asserts that 'haystack' includes 'needle'.\n   * Can be used to assert the inclusion of a subset of properties in an\n   * object while checking for deep equality.\n   * Enables the use of dot- and bracket-notation for referencing nested\n   * properties.\n   * '[]' and '.' in property names can be escaped using double backslashes.\n   *\n   *     assert.deepNestedInclude({a: {b: [{x: 1}]}}, {'a.b[0]': {x: 1}});\n   *     assert.deepNestedInclude({'.a': {'[b]': {x: 1}}}, {'\\\\.a.\\\\[b\\\\]': {x: 1}});\n   *\n   * @name deepNestedInclude\n   * @param {Object} haystack\n   * @param {Object} needle\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.deepNestedInclude = function(exp, inc, msg) {\n    new Assertion(exp, msg, assert.deepNestedInclude, true)\n      .deep.nested.include(inc);\n  };\n\n  /**\n   * ### .notDeepNestedInclude(haystack, needle, [message])\n   *\n   * Asserts that 'haystack' does not include 'needle'.\n   * Can be used to assert the absence of a subset of properties in an\n   * object while checking for deep equality.\n   * Enables the use of dot- and bracket-notation for referencing nested\n   * properties.\n   * '[]' and '.' in property names can be escaped using double backslashes.\n   *\n   *     assert.notDeepNestedInclude({a: {b: [{x: 1}]}}, {'a.b[0]': {y: 1}})\n   *     assert.notDeepNestedInclude({'.a': {'[b]': {x: 1}}}, {'\\\\.a.\\\\[b\\\\]': {y: 2}});\n   *\n   * @name notDeepNestedInclude\n   * @param {Object} haystack\n   * @param {Object} needle\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notDeepNestedInclude = function(exp, inc, msg) {\n    new Assertion(exp, msg, assert.notDeepNestedInclude, true)\n      .not.deep.nested.include(inc);\n  };\n\n  /**\n   * ### .ownInclude(haystack, needle, [message])\n   *\n   * Asserts that 'haystack' includes 'needle'.\n   * Can be used to assert the inclusion of a subset of properties in an\n   * object while ignoring inherited properties.\n   *\n   *     assert.ownInclude({ a: 1 }, { a: 1 });\n   *\n   * @name ownInclude\n   * @param {Object} haystack\n   * @param {Object} needle\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.ownInclude = function(exp, inc, msg) {\n    new Assertion(exp, msg, assert.ownInclude, true).own.include(inc);\n  };\n\n  /**\n   * ### .notOwnInclude(haystack, needle, [message])\n   *\n   * Asserts that 'haystack' includes 'needle'.\n   * Can be used to assert the absence of a subset of properties in an\n   * object while ignoring inherited properties.\n   *\n   *     Object.prototype.b = 2;\n   *\n   *     assert.notOwnInclude({ a: 1 }, { b: 2 });\n   *\n   * @name notOwnInclude\n   * @param {Object} haystack\n   * @param {Object} needle\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notOwnInclude = function(exp, inc, msg) {\n    new Assertion(exp, msg, assert.notOwnInclude, true).not.own.include(inc);\n  };\n\n  /**\n   * ### .deepOwnInclude(haystack, needle, [message])\n   *\n   * Asserts that 'haystack' includes 'needle'.\n   * Can be used to assert the inclusion of a subset of properties in an\n   * object while ignoring inherited properties and checking for deep equality.\n   *\n   *      assert.deepOwnInclude({a: {b: 2}}, {a: {b: 2}});\n   *\n   * @name deepOwnInclude\n   * @param {Object} haystack\n   * @param {Object} needle\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.deepOwnInclude = function(exp, inc, msg) {\n    new Assertion(exp, msg, assert.deepOwnInclude, true)\n      .deep.own.include(inc);\n  };\n\n   /**\n   * ### .notDeepOwnInclude(haystack, needle, [message])\n   *\n   * Asserts that 'haystack' includes 'needle'.\n   * Can be used to assert the absence of a subset of properties in an\n   * object while ignoring inherited properties and checking for deep equality.\n   *\n   *      assert.notDeepOwnInclude({a: {b: 2}}, {a: {c: 3}});\n   *\n   * @name notDeepOwnInclude\n   * @param {Object} haystack\n   * @param {Object} needle\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notDeepOwnInclude = function(exp, inc, msg) {\n    new Assertion(exp, msg, assert.notDeepOwnInclude, true)\n      .not.deep.own.include(inc);\n  };\n\n  /**\n   * ### .match(value, regexp, [message])\n   *\n   * Asserts that `value` matches the regular expression `regexp`.\n   *\n   *     assert.match('foobar', /^foo/, 'regexp matches');\n   *\n   * @name match\n   * @param {Mixed} value\n   * @param {RegExp} regexp\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.match = function (exp, re, msg) {\n    new Assertion(exp, msg, assert.match, true).to.match(re);\n  };\n\n  /**\n   * ### .notMatch(value, regexp, [message])\n   *\n   * Asserts that `value` does not match the regular expression `regexp`.\n   *\n   *     assert.notMatch('foobar', /^foo/, 'regexp does not match');\n   *\n   * @name notMatch\n   * @param {Mixed} value\n   * @param {RegExp} regexp\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notMatch = function (exp, re, msg) {\n    new Assertion(exp, msg, assert.notMatch, true).to.not.match(re);\n  };\n\n  /**\n   * ### .property(object, property, [message])\n   *\n   * Asserts that `object` has a direct or inherited property named by\n   * `property`.\n   *\n   *     assert.property({ tea: { green: 'matcha' }}, 'tea');\n   *     assert.property({ tea: { green: 'matcha' }}, 'toString');\n   *\n   * @name property\n   * @param {Object} object\n   * @param {String} property\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.property = function (obj, prop, msg) {\n    new Assertion(obj, msg, assert.property, true).to.have.property(prop);\n  };\n\n  /**\n   * ### .notProperty(object, property, [message])\n   *\n   * Asserts that `object` does _not_ have a direct or inherited property named\n   * by `property`.\n   *\n   *     assert.notProperty({ tea: { green: 'matcha' }}, 'coffee');\n   *\n   * @name notProperty\n   * @param {Object} object\n   * @param {String} property\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notProperty = function (obj, prop, msg) {\n    new Assertion(obj, msg, assert.notProperty, true)\n      .to.not.have.property(prop);\n  };\n\n  /**\n   * ### .propertyVal(object, property, value, [message])\n   *\n   * Asserts that `object` has a direct or inherited property named by\n   * `property` with a value given by `value`. Uses a strict equality check\n   * (===).\n   *\n   *     assert.propertyVal({ tea: 'is good' }, 'tea', 'is good');\n   *\n   * @name propertyVal\n   * @param {Object} object\n   * @param {String} property\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.propertyVal = function (obj, prop, val, msg) {\n    new Assertion(obj, msg, assert.propertyVal, true)\n      .to.have.property(prop, val);\n  };\n\n  /**\n   * ### .notPropertyVal(object, property, value, [message])\n   *\n   * Asserts that `object` does _not_ have a direct or inherited property named\n   * by `property` with value given by `value`. Uses a strict equality check\n   * (===).\n   *\n   *     assert.notPropertyVal({ tea: 'is good' }, 'tea', 'is bad');\n   *     assert.notPropertyVal({ tea: 'is good' }, 'coffee', 'is good');\n   *\n   * @name notPropertyVal\n   * @param {Object} object\n   * @param {String} property\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notPropertyVal = function (obj, prop, val, msg) {\n    new Assertion(obj, msg, assert.notPropertyVal, true)\n      .to.not.have.property(prop, val);\n  };\n\n  /**\n   * ### .deepPropertyVal(object, property, value, [message])\n   *\n   * Asserts that `object` has a direct or inherited property named by\n   * `property` with a value given by `value`. Uses a deep equality check.\n   *\n   *     assert.deepPropertyVal({ tea: { green: 'matcha' } }, 'tea', { green: 'matcha' });\n   *\n   * @name deepPropertyVal\n   * @param {Object} object\n   * @param {String} property\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.deepPropertyVal = function (obj, prop, val, msg) {\n    new Assertion(obj, msg, assert.deepPropertyVal, true)\n      .to.have.deep.property(prop, val);\n  };\n\n  /**\n   * ### .notDeepPropertyVal(object, property, value, [message])\n   *\n   * Asserts that `object` does _not_ have a direct or inherited property named\n   * by `property` with value given by `value`. Uses a deep equality check.\n   *\n   *     assert.notDeepPropertyVal({ tea: { green: 'matcha' } }, 'tea', { black: 'matcha' });\n   *     assert.notDeepPropertyVal({ tea: { green: 'matcha' } }, 'tea', { green: 'oolong' });\n   *     assert.notDeepPropertyVal({ tea: { green: 'matcha' } }, 'coffee', { green: 'matcha' });\n   *\n   * @name notDeepPropertyVal\n   * @param {Object} object\n   * @param {String} property\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notDeepPropertyVal = function (obj, prop, val, msg) {\n    new Assertion(obj, msg, assert.notDeepPropertyVal, true)\n      .to.not.have.deep.property(prop, val);\n  };\n\n  /**\n   * ### .ownProperty(object, property, [message])\n   *\n   * Asserts that `object` has a direct property named by `property`. Inherited\n   * properties aren't checked.\n   *\n   *     assert.ownProperty({ tea: { green: 'matcha' }}, 'tea');\n   *\n   * @name ownProperty\n   * @param {Object} object\n   * @param {String} property\n   * @param {String} message\n   * @api public\n   */\n\n  assert.ownProperty = function (obj, prop, msg) {\n    new Assertion(obj, msg, assert.ownProperty, true)\n      .to.have.own.property(prop);\n  };\n\n  /**\n   * ### .notOwnProperty(object, property, [message])\n   *\n   * Asserts that `object` does _not_ have a direct property named by\n   * `property`. Inherited properties aren't checked.\n   *\n   *     assert.notOwnProperty({ tea: { green: 'matcha' }}, 'coffee');\n   *     assert.notOwnProperty({}, 'toString');\n   *\n   * @name notOwnProperty\n   * @param {Object} object\n   * @param {String} property\n   * @param {String} message\n   * @api public\n   */\n\n  assert.notOwnProperty = function (obj, prop, msg) {\n    new Assertion(obj, msg, assert.notOwnProperty, true)\n      .to.not.have.own.property(prop);\n  };\n\n  /**\n   * ### .ownPropertyVal(object, property, value, [message])\n   *\n   * Asserts that `object` has a direct property named by `property` and a value\n   * equal to the provided `value`. Uses a strict equality check (===).\n   * Inherited properties aren't checked.\n   *\n   *     assert.ownPropertyVal({ coffee: 'is good'}, 'coffee', 'is good');\n   *\n   * @name ownPropertyVal\n   * @param {Object} object\n   * @param {String} property\n   * @param {Mixed} value\n   * @param {String} message\n   * @api public\n   */\n\n  assert.ownPropertyVal = function (obj, prop, value, msg) {\n    new Assertion(obj, msg, assert.ownPropertyVal, true)\n      .to.have.own.property(prop, value);\n  };\n\n  /**\n   * ### .notOwnPropertyVal(object, property, value, [message])\n   *\n   * Asserts that `object` does _not_ have a direct property named by `property`\n   * with a value equal to the provided `value`. Uses a strict equality check\n   * (===). Inherited properties aren't checked.\n   *\n   *     assert.notOwnPropertyVal({ tea: 'is better'}, 'tea', 'is worse');\n   *     assert.notOwnPropertyVal({}, 'toString', Object.prototype.toString);\n   *\n   * @name notOwnPropertyVal\n   * @param {Object} object\n   * @param {String} property\n   * @param {Mixed} value\n   * @param {String} message\n   * @api public\n   */\n\n  assert.notOwnPropertyVal = function (obj, prop, value, msg) {\n    new Assertion(obj, msg, assert.notOwnPropertyVal, true)\n      .to.not.have.own.property(prop, value);\n  };\n\n  /**\n   * ### .deepOwnPropertyVal(object, property, value, [message])\n   *\n   * Asserts that `object` has a direct property named by `property` and a value\n   * equal to the provided `value`. Uses a deep equality check. Inherited\n   * properties aren't checked.\n   *\n   *     assert.deepOwnPropertyVal({ tea: { green: 'matcha' } }, 'tea', { green: 'matcha' });\n   *\n   * @name deepOwnPropertyVal\n   * @param {Object} object\n   * @param {String} property\n   * @param {Mixed} value\n   * @param {String} message\n   * @api public\n   */\n\n  assert.deepOwnPropertyVal = function (obj, prop, value, msg) {\n    new Assertion(obj, msg, assert.deepOwnPropertyVal, true)\n      .to.have.deep.own.property(prop, value);\n  };\n\n  /**\n   * ### .notDeepOwnPropertyVal(object, property, value, [message])\n   *\n   * Asserts that `object` does _not_ have a direct property named by `property`\n   * with a value equal to the provided `value`. Uses a deep equality check.\n   * Inherited properties aren't checked.\n   *\n   *     assert.notDeepOwnPropertyVal({ tea: { green: 'matcha' } }, 'tea', { black: 'matcha' });\n   *     assert.notDeepOwnPropertyVal({ tea: { green: 'matcha' } }, 'tea', { green: 'oolong' });\n   *     assert.notDeepOwnPropertyVal({ tea: { green: 'matcha' } }, 'coffee', { green: 'matcha' });\n   *     assert.notDeepOwnPropertyVal({}, 'toString', Object.prototype.toString);\n   *\n   * @name notDeepOwnPropertyVal\n   * @param {Object} object\n   * @param {String} property\n   * @param {Mixed} value\n   * @param {String} message\n   * @api public\n   */\n\n  assert.notDeepOwnPropertyVal = function (obj, prop, value, msg) {\n    new Assertion(obj, msg, assert.notDeepOwnPropertyVal, true)\n      .to.not.have.deep.own.property(prop, value);\n  };\n\n  /**\n   * ### .nestedProperty(object, property, [message])\n   *\n   * Asserts that `object` has a direct or inherited property named by\n   * `property`, which can be a string using dot- and bracket-notation for\n   * nested reference.\n   *\n   *     assert.nestedProperty({ tea: { green: 'matcha' }}, 'tea.green');\n   *\n   * @name nestedProperty\n   * @param {Object} object\n   * @param {String} property\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.nestedProperty = function (obj, prop, msg) {\n    new Assertion(obj, msg, assert.nestedProperty, true)\n      .to.have.nested.property(prop);\n  };\n\n  /**\n   * ### .notNestedProperty(object, property, [message])\n   *\n   * Asserts that `object` does _not_ have a property named by `property`, which\n   * can be a string using dot- and bracket-notation for nested reference. The\n   * property cannot exist on the object nor anywhere in its prototype chain.\n   *\n   *     assert.notNestedProperty({ tea: { green: 'matcha' }}, 'tea.oolong');\n   *\n   * @name notNestedProperty\n   * @param {Object} object\n   * @param {String} property\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notNestedProperty = function (obj, prop, msg) {\n    new Assertion(obj, msg, assert.notNestedProperty, true)\n      .to.not.have.nested.property(prop);\n  };\n\n  /**\n   * ### .nestedPropertyVal(object, property, value, [message])\n   *\n   * Asserts that `object` has a property named by `property` with value given\n   * by `value`. `property` can use dot- and bracket-notation for nested\n   * reference. Uses a strict equality check (===).\n   *\n   *     assert.nestedPropertyVal({ tea: { green: 'matcha' }}, 'tea.green', 'matcha');\n   *\n   * @name nestedPropertyVal\n   * @param {Object} object\n   * @param {String} property\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.nestedPropertyVal = function (obj, prop, val, msg) {\n    new Assertion(obj, msg, assert.nestedPropertyVal, true)\n      .to.have.nested.property(prop, val);\n  };\n\n  /**\n   * ### .notNestedPropertyVal(object, property, value, [message])\n   *\n   * Asserts that `object` does _not_ have a property named by `property` with\n   * value given by `value`. `property` can use dot- and bracket-notation for\n   * nested reference. Uses a strict equality check (===).\n   *\n   *     assert.notNestedPropertyVal({ tea: { green: 'matcha' }}, 'tea.green', 'konacha');\n   *     assert.notNestedPropertyVal({ tea: { green: 'matcha' }}, 'coffee.green', 'matcha');\n   *\n   * @name notNestedPropertyVal\n   * @param {Object} object\n   * @param {String} property\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notNestedPropertyVal = function (obj, prop, val, msg) {\n    new Assertion(obj, msg, assert.notNestedPropertyVal, true)\n      .to.not.have.nested.property(prop, val);\n  };\n\n  /**\n   * ### .deepNestedPropertyVal(object, property, value, [message])\n   *\n   * Asserts that `object` has a property named by `property` with a value given\n   * by `value`. `property` can use dot- and bracket-notation for nested\n   * reference. Uses a deep equality check.\n   *\n   *     assert.deepNestedPropertyVal({ tea: { green: { matcha: 'yum' } } }, 'tea.green', { matcha: 'yum' });\n   *\n   * @name deepNestedPropertyVal\n   * @param {Object} object\n   * @param {String} property\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.deepNestedPropertyVal = function (obj, prop, val, msg) {\n    new Assertion(obj, msg, assert.deepNestedPropertyVal, true)\n      .to.have.deep.nested.property(prop, val);\n  };\n\n  /**\n   * ### .notDeepNestedPropertyVal(object, property, value, [message])\n   *\n   * Asserts that `object` does _not_ have a property named by `property` with\n   * value given by `value`. `property` can use dot- and bracket-notation for\n   * nested reference. Uses a deep equality check.\n   *\n   *     assert.notDeepNestedPropertyVal({ tea: { green: { matcha: 'yum' } } }, 'tea.green', { oolong: 'yum' });\n   *     assert.notDeepNestedPropertyVal({ tea: { green: { matcha: 'yum' } } }, 'tea.green', { matcha: 'yuck' });\n   *     assert.notDeepNestedPropertyVal({ tea: { green: { matcha: 'yum' } } }, 'tea.black', { matcha: 'yum' });\n   *\n   * @name notDeepNestedPropertyVal\n   * @param {Object} object\n   * @param {String} property\n   * @param {Mixed} value\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notDeepNestedPropertyVal = function (obj, prop, val, msg) {\n    new Assertion(obj, msg, assert.notDeepNestedPropertyVal, true)\n      .to.not.have.deep.nested.property(prop, val);\n  }\n\n  /**\n   * ### .lengthOf(object, length, [message])\n   *\n   * Asserts that `object` has a `length` or `size` with the expected value.\n   *\n   *     assert.lengthOf([1,2,3], 3, 'array has length of 3');\n   *     assert.lengthOf('foobar', 6, 'string has length of 6');\n   *     assert.lengthOf(new Set([1,2,3]), 3, 'set has size of 3');\n   *     assert.lengthOf(new Map([['a',1],['b',2],['c',3]]), 3, 'map has size of 3');\n   *\n   * @name lengthOf\n   * @param {Mixed} object\n   * @param {Number} length\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.lengthOf = function (exp, len, msg) {\n    new Assertion(exp, msg, assert.lengthOf, true).to.have.lengthOf(len);\n  };\n\n  /**\n   * ### .hasAnyKeys(object, [keys], [message])\n   *\n   * Asserts that `object` has at least one of the `keys` provided.\n   * You can also provide a single object instead of a `keys` array and its keys\n   * will be used as the expected set of keys.\n   *\n   *     assert.hasAnyKeys({foo: 1, bar: 2, baz: 3}, ['foo', 'iDontExist', 'baz']);\n   *     assert.hasAnyKeys({foo: 1, bar: 2, baz: 3}, {foo: 30, iDontExist: 99, baz: 1337});\n   *     assert.hasAnyKeys(new Map([[{foo: 1}, 'bar'], ['key', 'value']]), [{foo: 1}, 'key']);\n   *     assert.hasAnyKeys(new Set([{foo: 'bar'}, 'anotherKey']), [{foo: 'bar'}, 'anotherKey']);\n   *\n   * @name hasAnyKeys\n   * @param {Mixed} object\n   * @param {Array|Object} keys\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.hasAnyKeys = function (obj, keys, msg) {\n    new Assertion(obj, msg, assert.hasAnyKeys, true).to.have.any.keys(keys);\n  }\n\n  /**\n   * ### .hasAllKeys(object, [keys], [message])\n   *\n   * Asserts that `object` has all and only all of the `keys` provided.\n   * You can also provide a single object instead of a `keys` array and its keys\n   * will be used as the expected set of keys.\n   *\n   *     assert.hasAllKeys({foo: 1, bar: 2, baz: 3}, ['foo', 'bar', 'baz']);\n   *     assert.hasAllKeys({foo: 1, bar: 2, baz: 3}, {foo: 30, bar: 99, baz: 1337]);\n   *     assert.hasAllKeys(new Map([[{foo: 1}, 'bar'], ['key', 'value']]), [{foo: 1}, 'key']);\n   *     assert.hasAllKeys(new Set([{foo: 'bar'}, 'anotherKey'], [{foo: 'bar'}, 'anotherKey']);\n   *\n   * @name hasAllKeys\n   * @param {Mixed} object\n   * @param {String[]} keys\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.hasAllKeys = function (obj, keys, msg) {\n    new Assertion(obj, msg, assert.hasAllKeys, true).to.have.all.keys(keys);\n  }\n\n  /**\n   * ### .containsAllKeys(object, [keys], [message])\n   *\n   * Asserts that `object` has all of the `keys` provided but may have more keys not listed.\n   * You can also provide a single object instead of a `keys` array and its keys\n   * will be used as the expected set of keys.\n   *\n   *     assert.containsAllKeys({foo: 1, bar: 2, baz: 3}, ['foo', 'baz']);\n   *     assert.containsAllKeys({foo: 1, bar: 2, baz: 3}, ['foo', 'bar', 'baz']);\n   *     assert.containsAllKeys({foo: 1, bar: 2, baz: 3}, {foo: 30, baz: 1337});\n   *     assert.containsAllKeys({foo: 1, bar: 2, baz: 3}, {foo: 30, bar: 99, baz: 1337});\n   *     assert.containsAllKeys(new Map([[{foo: 1}, 'bar'], ['key', 'value']]), [{foo: 1}]);\n   *     assert.containsAllKeys(new Map([[{foo: 1}, 'bar'], ['key', 'value']]), [{foo: 1}, 'key']);\n   *     assert.containsAllKeys(new Set([{foo: 'bar'}, 'anotherKey'], [{foo: 'bar'}]);\n   *     assert.containsAllKeys(new Set([{foo: 'bar'}, 'anotherKey'], [{foo: 'bar'}, 'anotherKey']);\n   *\n   * @name containsAllKeys\n   * @param {Mixed} object\n   * @param {String[]} keys\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.containsAllKeys = function (obj, keys, msg) {\n    new Assertion(obj, msg, assert.containsAllKeys, true)\n      .to.contain.all.keys(keys);\n  }\n\n  /**\n   * ### .doesNotHaveAnyKeys(object, [keys], [message])\n   *\n   * Asserts that `object` has none of the `keys` provided.\n   * You can also provide a single object instead of a `keys` array and its keys\n   * will be used as the expected set of keys.\n   *\n   *     assert.doesNotHaveAnyKeys({foo: 1, bar: 2, baz: 3}, ['one', 'two', 'example']);\n   *     assert.doesNotHaveAnyKeys({foo: 1, bar: 2, baz: 3}, {one: 1, two: 2, example: 'foo'});\n   *     assert.doesNotHaveAnyKeys(new Map([[{foo: 1}, 'bar'], ['key', 'value']]), [{one: 'two'}, 'example']);\n   *     assert.doesNotHaveAnyKeys(new Set([{foo: 'bar'}, 'anotherKey'], [{one: 'two'}, 'example']);\n   *\n   * @name doesNotHaveAnyKeys\n   * @param {Mixed} object\n   * @param {String[]} keys\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.doesNotHaveAnyKeys = function (obj, keys, msg) {\n    new Assertion(obj, msg, assert.doesNotHaveAnyKeys, true)\n      .to.not.have.any.keys(keys);\n  }\n\n  /**\n   * ### .doesNotHaveAllKeys(object, [keys], [message])\n   *\n   * Asserts that `object` does not have at least one of the `keys` provided.\n   * You can also provide a single object instead of a `keys` array and its keys\n   * will be used as the expected set of keys.\n   *\n   *     assert.doesNotHaveAllKeys({foo: 1, bar: 2, baz: 3}, ['one', 'two', 'example']);\n   *     assert.doesNotHaveAllKeys({foo: 1, bar: 2, baz: 3}, {one: 1, two: 2, example: 'foo'});\n   *     assert.doesNotHaveAllKeys(new Map([[{foo: 1}, 'bar'], ['key', 'value']]), [{one: 'two'}, 'example']);\n   *     assert.doesNotHaveAllKeys(new Set([{foo: 'bar'}, 'anotherKey'], [{one: 'two'}, 'example']);\n   *\n   * @name doesNotHaveAllKeys\n   * @param {Mixed} object\n   * @param {String[]} keys\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.doesNotHaveAllKeys = function (obj, keys, msg) {\n    new Assertion(obj, msg, assert.doesNotHaveAllKeys, true)\n      .to.not.have.all.keys(keys);\n  }\n\n  /**\n   * ### .hasAnyDeepKeys(object, [keys], [message])\n   *\n   * Asserts that `object` has at least one of the `keys` provided.\n   * Since Sets and Maps can have objects as keys you can use this assertion to perform\n   * a deep comparison.\n   * You can also provide a single object instead of a `keys` array and its keys\n   * will be used as the expected set of keys.\n   *\n   *     assert.hasAnyDeepKeys(new Map([[{one: 'one'}, 'valueOne'], [1, 2]]), {one: 'one'});\n   *     assert.hasAnyDeepKeys(new Map([[{one: 'one'}, 'valueOne'], [1, 2]]), [{one: 'one'}, {two: 'two'}]);\n   *     assert.hasAnyDeepKeys(new Map([[{one: 'one'}, 'valueOne'], [{two: 'two'}, 'valueTwo']]), [{one: 'one'}, {two: 'two'}]);\n   *     assert.hasAnyDeepKeys(new Set([{one: 'one'}, {two: 'two'}]), {one: 'one'});\n   *     assert.hasAnyDeepKeys(new Set([{one: 'one'}, {two: 'two'}]), [{one: 'one'}, {three: 'three'}]);\n   *     assert.hasAnyDeepKeys(new Set([{one: 'one'}, {two: 'two'}]), [{one: 'one'}, {two: 'two'}]);\n   *\n   * @name hasAnyDeepKeys\n   * @param {Mixed} object\n   * @param {Array|Object} keys\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.hasAnyDeepKeys = function (obj, keys, msg) {\n    new Assertion(obj, msg, assert.hasAnyDeepKeys, true)\n      .to.have.any.deep.keys(keys);\n  }\n\n /**\n   * ### .hasAllDeepKeys(object, [keys], [message])\n   *\n   * Asserts that `object` has all and only all of the `keys` provided.\n   * Since Sets and Maps can have objects as keys you can use this assertion to perform\n   * a deep comparison.\n   * You can also provide a single object instead of a `keys` array and its keys\n   * will be used as the expected set of keys.\n   *\n   *     assert.hasAllDeepKeys(new Map([[{one: 'one'}, 'valueOne']]), {one: 'one'});\n   *     assert.hasAllDeepKeys(new Map([[{one: 'one'}, 'valueOne'], [{two: 'two'}, 'valueTwo']]), [{one: 'one'}, {two: 'two'}]);\n   *     assert.hasAllDeepKeys(new Set([{one: 'one'}]), {one: 'one'});\n   *     assert.hasAllDeepKeys(new Set([{one: 'one'}, {two: 'two'}]), [{one: 'one'}, {two: 'two'}]);\n   *\n   * @name hasAllDeepKeys\n   * @param {Mixed} object\n   * @param {Array|Object} keys\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.hasAllDeepKeys = function (obj, keys, msg) {\n    new Assertion(obj, msg, assert.hasAllDeepKeys, true)\n      .to.have.all.deep.keys(keys);\n  }\n\n /**\n   * ### .containsAllDeepKeys(object, [keys], [message])\n   *\n   * Asserts that `object` contains all of the `keys` provided.\n   * Since Sets and Maps can have objects as keys you can use this assertion to perform\n   * a deep comparison.\n   * You can also provide a single object instead of a `keys` array and its keys\n   * will be used as the expected set of keys.\n   *\n   *     assert.containsAllDeepKeys(new Map([[{one: 'one'}, 'valueOne'], [1, 2]]), {one: 'one'});\n   *     assert.containsAllDeepKeys(new Map([[{one: 'one'}, 'valueOne'], [{two: 'two'}, 'valueTwo']]), [{one: 'one'}, {two: 'two'}]);\n   *     assert.containsAllDeepKeys(new Set([{one: 'one'}, {two: 'two'}]), {one: 'one'});\n   *     assert.containsAllDeepKeys(new Set([{one: 'one'}, {two: 'two'}]), [{one: 'one'}, {two: 'two'}]);\n   *\n   * @name containsAllDeepKeys\n   * @param {Mixed} object\n   * @param {Array|Object} keys\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.containsAllDeepKeys = function (obj, keys, msg) {\n    new Assertion(obj, msg, assert.containsAllDeepKeys, true)\n      .to.contain.all.deep.keys(keys);\n  }\n\n /**\n   * ### .doesNotHaveAnyDeepKeys(object, [keys], [message])\n   *\n   * Asserts that `object` has none of the `keys` provided.\n   * Since Sets and Maps can have objects as keys you can use this assertion to perform\n   * a deep comparison.\n   * You can also provide a single object instead of a `keys` array and its keys\n   * will be used as the expected set of keys.\n   *\n   *     assert.doesNotHaveAnyDeepKeys(new Map([[{one: 'one'}, 'valueOne'], [1, 2]]), {thisDoesNot: 'exist'});\n   *     assert.doesNotHaveAnyDeepKeys(new Map([[{one: 'one'}, 'valueOne'], [{two: 'two'}, 'valueTwo']]), [{twenty: 'twenty'}, {fifty: 'fifty'}]);\n   *     assert.doesNotHaveAnyDeepKeys(new Set([{one: 'one'}, {two: 'two'}]), {twenty: 'twenty'});\n   *     assert.doesNotHaveAnyDeepKeys(new Set([{one: 'one'}, {two: 'two'}]), [{twenty: 'twenty'}, {fifty: 'fifty'}]);\n   *\n   * @name doesNotHaveAnyDeepKeys\n   * @param {Mixed} object\n   * @param {Array|Object} keys\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.doesNotHaveAnyDeepKeys = function (obj, keys, msg) {\n    new Assertion(obj, msg, assert.doesNotHaveAnyDeepKeys, true)\n      .to.not.have.any.deep.keys(keys);\n  }\n\n /**\n   * ### .doesNotHaveAllDeepKeys(object, [keys], [message])\n   *\n   * Asserts that `object` does not have at least one of the `keys` provided.\n   * Since Sets and Maps can have objects as keys you can use this assertion to perform\n   * a deep comparison.\n   * You can also provide a single object instead of a `keys` array and its keys\n   * will be used as the expected set of keys.\n   *\n   *     assert.doesNotHaveAllDeepKeys(new Map([[{one: 'one'}, 'valueOne'], [1, 2]]), {thisDoesNot: 'exist'});\n   *     assert.doesNotHaveAllDeepKeys(new Map([[{one: 'one'}, 'valueOne'], [{two: 'two'}, 'valueTwo']]), [{twenty: 'twenty'}, {one: 'one'}]);\n   *     assert.doesNotHaveAllDeepKeys(new Set([{one: 'one'}, {two: 'two'}]), {twenty: 'twenty'});\n   *     assert.doesNotHaveAllDeepKeys(new Set([{one: 'one'}, {two: 'two'}]), [{one: 'one'}, {fifty: 'fifty'}]);\n   *\n   * @name doesNotHaveAllDeepKeys\n   * @param {Mixed} object\n   * @param {Array|Object} keys\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.doesNotHaveAllDeepKeys = function (obj, keys, msg) {\n    new Assertion(obj, msg, assert.doesNotHaveAllDeepKeys, true)\n      .to.not.have.all.deep.keys(keys);\n  }\n\n /**\n   * ### .throws(fn, [errorLike/string/regexp], [string/regexp], [message])\n   *\n   * If `errorLike` is an `Error` constructor, asserts that `fn` will throw an error that is an\n   * instance of `errorLike`.\n   * If `errorLike` is an `Error` instance, asserts that the error thrown is the same\n   * instance as `errorLike`.\n   * If `errMsgMatcher` is provided, it also asserts that the error thrown will have a\n   * message matching `errMsgMatcher`.\n   *\n   *     assert.throws(fn, 'Error thrown must have this msg');\n   *     assert.throws(fn, /Error thrown must have a msg that matches this/);\n   *     assert.throws(fn, ReferenceError);\n   *     assert.throws(fn, errorInstance);\n   *     assert.throws(fn, ReferenceError, 'Error thrown must be a ReferenceError and have this msg');\n   *     assert.throws(fn, errorInstance, 'Error thrown must be the same errorInstance and have this msg');\n   *     assert.throws(fn, ReferenceError, /Error thrown must be a ReferenceError and match this/);\n   *     assert.throws(fn, errorInstance, /Error thrown must be the same errorInstance and match this/);\n   *\n   * @name throws\n   * @alias throw\n   * @alias Throw\n   * @param {Function} fn\n   * @param {ErrorConstructor|Error} errorLike\n   * @param {RegExp|String} errMsgMatcher\n   * @param {String} message\n   * @see https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Error#Error_types\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.throws = function (fn, errorLike, errMsgMatcher, msg) {\n    if ('string' === typeof errorLike || errorLike instanceof RegExp) {\n      errMsgMatcher = errorLike;\n      errorLike = null;\n    }\n\n    var assertErr = new Assertion(fn, msg, assert.throws, true)\n      .to.throw(errorLike, errMsgMatcher);\n    return flag(assertErr, 'object');\n  };\n\n  /**\n   * ### .doesNotThrow(fn, [errorLike/string/regexp], [string/regexp], [message])\n   *\n   * If `errorLike` is an `Error` constructor, asserts that `fn` will _not_ throw an error that is an\n   * instance of `errorLike`.\n   * If `errorLike` is an `Error` instance, asserts that the error thrown is _not_ the same\n   * instance as `errorLike`.\n   * If `errMsgMatcher` is provided, it also asserts that the error thrown will _not_ have a\n   * message matching `errMsgMatcher`.\n   *\n   *     assert.doesNotThrow(fn, 'Any Error thrown must not have this message');\n   *     assert.doesNotThrow(fn, /Any Error thrown must not match this/);\n   *     assert.doesNotThrow(fn, Error);\n   *     assert.doesNotThrow(fn, errorInstance);\n   *     assert.doesNotThrow(fn, Error, 'Error must not have this message');\n   *     assert.doesNotThrow(fn, errorInstance, 'Error must not have this message');\n   *     assert.doesNotThrow(fn, Error, /Error must not match this/);\n   *     assert.doesNotThrow(fn, errorInstance, /Error must not match this/);\n   *\n   * @name doesNotThrow\n   * @param {Function} fn\n   * @param {ErrorConstructor} errorLike\n   * @param {RegExp|String} errMsgMatcher\n   * @param {String} message\n   * @see https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Error#Error_types\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.doesNotThrow = function (fn, errorLike, errMsgMatcher, msg) {\n    if ('string' === typeof errorLike || errorLike instanceof RegExp) {\n      errMsgMatcher = errorLike;\n      errorLike = null;\n    }\n\n    new Assertion(fn, msg, assert.doesNotThrow, true)\n      .to.not.throw(errorLike, errMsgMatcher);\n  };\n\n  /**\n   * ### .operator(val1, operator, val2, [message])\n   *\n   * Compares two values using `operator`.\n   *\n   *     assert.operator(1, '<', 2, 'everything is ok');\n   *     assert.operator(1, '>', 2, 'this will fail');\n   *\n   * @name operator\n   * @param {Mixed} val1\n   * @param {String} operator\n   * @param {Mixed} val2\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.operator = function (val, operator, val2, msg) {\n    var ok;\n    switch(operator) {\n      case '==':\n        ok = val == val2;\n        break;\n      case '===':\n        ok = val === val2;\n        break;\n      case '>':\n        ok = val > val2;\n        break;\n      case '>=':\n        ok = val >= val2;\n        break;\n      case '<':\n        ok = val < val2;\n        break;\n      case '<=':\n        ok = val <= val2;\n        break;\n      case '!=':\n        ok = val != val2;\n        break;\n      case '!==':\n        ok = val !== val2;\n        break;\n      default:\n        msg = msg ? msg + ': ' : msg;\n        throw new chai.AssertionError(\n          msg + 'Invalid operator \"' + operator + '\"',\n          undefined,\n          assert.operator\n        );\n    }\n    var test = new Assertion(ok, msg, assert.operator, true);\n    test.assert(\n        true === flag(test, 'object')\n      , 'expected ' + util.inspect(val) + ' to be ' + operator + ' ' + util.inspect(val2)\n      , 'expected ' + util.inspect(val) + ' to not be ' + operator + ' ' + util.inspect(val2) );\n  };\n\n  /**\n   * ### .closeTo(actual, expected, delta, [message])\n   *\n   * Asserts that the target is equal `expected`, to within a +/- `delta` range.\n   *\n   *     assert.closeTo(1.5, 1, 0.5, 'numbers are close');\n   *\n   * @name closeTo\n   * @param {Number} actual\n   * @param {Number} expected\n   * @param {Number} delta\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.closeTo = function (act, exp, delta, msg) {\n    new Assertion(act, msg, assert.closeTo, true).to.be.closeTo(exp, delta);\n  };\n\n  /**\n   * ### .approximately(actual, expected, delta, [message])\n   *\n   * Asserts that the target is equal `expected`, to within a +/- `delta` range.\n   *\n   *     assert.approximately(1.5, 1, 0.5, 'numbers are close');\n   *\n   * @name approximately\n   * @param {Number} actual\n   * @param {Number} expected\n   * @param {Number} delta\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.approximately = function (act, exp, delta, msg) {\n    new Assertion(act, msg, assert.approximately, true)\n      .to.be.approximately(exp, delta);\n  };\n\n  /**\n   * ### .sameMembers(set1, set2, [message])\n   *\n   * Asserts that `set1` and `set2` have the same members in any order. Uses a\n   * strict equality check (===).\n   *\n   *     assert.sameMembers([ 1, 2, 3 ], [ 2, 1, 3 ], 'same members');\n   *\n   * @name sameMembers\n   * @param {Array} set1\n   * @param {Array} set2\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.sameMembers = function (set1, set2, msg) {\n    new Assertion(set1, msg, assert.sameMembers, true)\n      .to.have.same.members(set2);\n  }\n\n  /**\n   * ### .notSameMembers(set1, set2, [message])\n   *\n   * Asserts that `set1` and `set2` don't have the same members in any order.\n   * Uses a strict equality check (===).\n   *\n   *     assert.notSameMembers([ 1, 2, 3 ], [ 5, 1, 3 ], 'not same members');\n   *\n   * @name notSameMembers\n   * @param {Array} set1\n   * @param {Array} set2\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notSameMembers = function (set1, set2, msg) {\n    new Assertion(set1, msg, assert.notSameMembers, true)\n      .to.not.have.same.members(set2);\n  }\n\n  /**\n   * ### .sameDeepMembers(set1, set2, [message])\n   *\n   * Asserts that `set1` and `set2` have the same members in any order. Uses a\n   * deep equality check.\n   *\n   *     assert.sameDeepMembers([ { a: 1 }, { b: 2 }, { c: 3 } ], [{ b: 2 }, { a: 1 }, { c: 3 }], 'same deep members');\n   *\n   * @name sameDeepMembers\n   * @param {Array} set1\n   * @param {Array} set2\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.sameDeepMembers = function (set1, set2, msg) {\n    new Assertion(set1, msg, assert.sameDeepMembers, true)\n      .to.have.same.deep.members(set2);\n  }\n\n  /**\n   * ### .notSameDeepMembers(set1, set2, [message])\n   *\n   * Asserts that `set1` and `set2` don't have the same members in any order.\n   * Uses a deep equality check.\n   *\n   *     assert.notSameDeepMembers([ { a: 1 }, { b: 2 }, { c: 3 } ], [{ b: 2 }, { a: 1 }, { f: 5 }], 'not same deep members');\n   *\n   * @name notSameDeepMembers\n   * @param {Array} set1\n   * @param {Array} set2\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notSameDeepMembers = function (set1, set2, msg) {\n    new Assertion(set1, msg, assert.notSameDeepMembers, true)\n      .to.not.have.same.deep.members(set2);\n  }\n\n  /**\n   * ### .sameOrderedMembers(set1, set2, [message])\n   *\n   * Asserts that `set1` and `set2` have the same members in the same order.\n   * Uses a strict equality check (===).\n   *\n   *     assert.sameOrderedMembers([ 1, 2, 3 ], [ 1, 2, 3 ], 'same ordered members');\n   *\n   * @name sameOrderedMembers\n   * @param {Array} set1\n   * @param {Array} set2\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.sameOrderedMembers = function (set1, set2, msg) {\n    new Assertion(set1, msg, assert.sameOrderedMembers, true)\n      .to.have.same.ordered.members(set2);\n  }\n\n  /**\n   * ### .notSameOrderedMembers(set1, set2, [message])\n   *\n   * Asserts that `set1` and `set2` don't have the same members in the same\n   * order. Uses a strict equality check (===).\n   *\n   *     assert.notSameOrderedMembers([ 1, 2, 3 ], [ 2, 1, 3 ], 'not same ordered members');\n   *\n   * @name notSameOrderedMembers\n   * @param {Array} set1\n   * @param {Array} set2\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notSameOrderedMembers = function (set1, set2, msg) {\n    new Assertion(set1, msg, assert.notSameOrderedMembers, true)\n      .to.not.have.same.ordered.members(set2);\n  }\n\n  /**\n   * ### .sameDeepOrderedMembers(set1, set2, [message])\n   *\n   * Asserts that `set1` and `set2` have the same members in the same order.\n   * Uses a deep equality check.\n   *\n   *     assert.sameDeepOrderedMembers([ { a: 1 }, { b: 2 }, { c: 3 } ], [ { a: 1 }, { b: 2 }, { c: 3 } ], 'same deep ordered members');\n   *\n   * @name sameDeepOrderedMembers\n   * @param {Array} set1\n   * @param {Array} set2\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.sameDeepOrderedMembers = function (set1, set2, msg) {\n    new Assertion(set1, msg, assert.sameDeepOrderedMembers, true)\n      .to.have.same.deep.ordered.members(set2);\n  }\n\n  /**\n   * ### .notSameDeepOrderedMembers(set1, set2, [message])\n   *\n   * Asserts that `set1` and `set2` don't have the same members in the same\n   * order. Uses a deep equality check.\n   *\n   *     assert.notSameDeepOrderedMembers([ { a: 1 }, { b: 2 }, { c: 3 } ], [ { a: 1 }, { b: 2 }, { z: 5 } ], 'not same deep ordered members');\n   *     assert.notSameDeepOrderedMembers([ { a: 1 }, { b: 2 }, { c: 3 } ], [ { b: 2 }, { a: 1 }, { c: 3 } ], 'not same deep ordered members');\n   *\n   * @name notSameDeepOrderedMembers\n   * @param {Array} set1\n   * @param {Array} set2\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notSameDeepOrderedMembers = function (set1, set2, msg) {\n    new Assertion(set1, msg, assert.notSameDeepOrderedMembers, true)\n      .to.not.have.same.deep.ordered.members(set2);\n  }\n\n  /**\n   * ### .includeMembers(superset, subset, [message])\n   *\n   * Asserts that `subset` is included in `superset` in any order. Uses a\n   * strict equality check (===). Duplicates are ignored.\n   *\n   *     assert.includeMembers([ 1, 2, 3 ], [ 2, 1, 2 ], 'include members');\n   *\n   * @name includeMembers\n   * @param {Array} superset\n   * @param {Array} subset\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.includeMembers = function (superset, subset, msg) {\n    new Assertion(superset, msg, assert.includeMembers, true)\n      .to.include.members(subset);\n  }\n\n  /**\n   * ### .notIncludeMembers(superset, subset, [message])\n   *\n   * Asserts that `subset` isn't included in `superset` in any order. Uses a\n   * strict equality check (===). Duplicates are ignored.\n   *\n   *     assert.notIncludeMembers([ 1, 2, 3 ], [ 5, 1 ], 'not include members');\n   *\n   * @name notIncludeMembers\n   * @param {Array} superset\n   * @param {Array} subset\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notIncludeMembers = function (superset, subset, msg) {\n    new Assertion(superset, msg, assert.notIncludeMembers, true)\n      .to.not.include.members(subset);\n  }\n\n  /**\n   * ### .includeDeepMembers(superset, subset, [message])\n   *\n   * Asserts that `subset` is included in `superset` in any order. Uses a deep\n   * equality check. Duplicates are ignored.\n   *\n   *     assert.includeDeepMembers([ { a: 1 }, { b: 2 }, { c: 3 } ], [ { b: 2 }, { a: 1 }, { b: 2 } ], 'include deep members');\n   *\n   * @name includeDeepMembers\n   * @param {Array} superset\n   * @param {Array} subset\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.includeDeepMembers = function (superset, subset, msg) {\n    new Assertion(superset, msg, assert.includeDeepMembers, true)\n      .to.include.deep.members(subset);\n  }\n\n  /**\n   * ### .notIncludeDeepMembers(superset, subset, [message])\n   *\n   * Asserts that `subset` isn't included in `superset` in any order. Uses a\n   * deep equality check. Duplicates are ignored.\n   *\n   *     assert.notIncludeDeepMembers([ { a: 1 }, { b: 2 }, { c: 3 } ], [ { b: 2 }, { f: 5 } ], 'not include deep members');\n   *\n   * @name notIncludeDeepMembers\n   * @param {Array} superset\n   * @param {Array} subset\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notIncludeDeepMembers = function (superset, subset, msg) {\n    new Assertion(superset, msg, assert.notIncludeDeepMembers, true)\n      .to.not.include.deep.members(subset);\n  }\n\n  /**\n   * ### .includeOrderedMembers(superset, subset, [message])\n   *\n   * Asserts that `subset` is included in `superset` in the same order\n   * beginning with the first element in `superset`. Uses a strict equality\n   * check (===).\n   *\n   *     assert.includeOrderedMembers([ 1, 2, 3 ], [ 1, 2 ], 'include ordered members');\n   *\n   * @name includeOrderedMembers\n   * @param {Array} superset\n   * @param {Array} subset\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.includeOrderedMembers = function (superset, subset, msg) {\n    new Assertion(superset, msg, assert.includeOrderedMembers, true)\n      .to.include.ordered.members(subset);\n  }\n\n  /**\n   * ### .notIncludeOrderedMembers(superset, subset, [message])\n   *\n   * Asserts that `subset` isn't included in `superset` in the same order\n   * beginning with the first element in `superset`. Uses a strict equality\n   * check (===).\n   *\n   *     assert.notIncludeOrderedMembers([ 1, 2, 3 ], [ 2, 1 ], 'not include ordered members');\n   *     assert.notIncludeOrderedMembers([ 1, 2, 3 ], [ 2, 3 ], 'not include ordered members');\n   *\n   * @name notIncludeOrderedMembers\n   * @param {Array} superset\n   * @param {Array} subset\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notIncludeOrderedMembers = function (superset, subset, msg) {\n    new Assertion(superset, msg, assert.notIncludeOrderedMembers, true)\n      .to.not.include.ordered.members(subset);\n  }\n\n  /**\n   * ### .includeDeepOrderedMembers(superset, subset, [message])\n   *\n   * Asserts that `subset` is included in `superset` in the same order\n   * beginning with the first element in `superset`. Uses a deep equality\n   * check.\n   *\n   *     assert.includeDeepOrderedMembers([ { a: 1 }, { b: 2 }, { c: 3 } ], [ { a: 1 }, { b: 2 } ], 'include deep ordered members');\n   *\n   * @name includeDeepOrderedMembers\n   * @param {Array} superset\n   * @param {Array} subset\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.includeDeepOrderedMembers = function (superset, subset, msg) {\n    new Assertion(superset, msg, assert.includeDeepOrderedMembers, true)\n      .to.include.deep.ordered.members(subset);\n  }\n\n  /**\n   * ### .notIncludeDeepOrderedMembers(superset, subset, [message])\n   *\n   * Asserts that `subset` isn't included in `superset` in the same order\n   * beginning with the first element in `superset`. Uses a deep equality\n   * check.\n   *\n   *     assert.notIncludeDeepOrderedMembers([ { a: 1 }, { b: 2 }, { c: 3 } ], [ { a: 1 }, { f: 5 } ], 'not include deep ordered members');\n   *     assert.notIncludeDeepOrderedMembers([ { a: 1 }, { b: 2 }, { c: 3 } ], [ { b: 2 }, { a: 1 } ], 'not include deep ordered members');\n   *     assert.notIncludeDeepOrderedMembers([ { a: 1 }, { b: 2 }, { c: 3 } ], [ { b: 2 }, { c: 3 } ], 'not include deep ordered members');\n   *\n   * @name notIncludeDeepOrderedMembers\n   * @param {Array} superset\n   * @param {Array} subset\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.notIncludeDeepOrderedMembers = function (superset, subset, msg) {\n    new Assertion(superset, msg, assert.notIncludeDeepOrderedMembers, true)\n      .to.not.include.deep.ordered.members(subset);\n  }\n\n  /**\n   * ### .oneOf(inList, list, [message])\n   *\n   * Asserts that non-object, non-array value `inList` appears in the flat array `list`.\n   *\n   *     assert.oneOf(1, [ 2, 1 ], 'Not found in list');\n   *\n   * @name oneOf\n   * @param {*} inList\n   * @param {Array<*>} list\n   * @param {String} message\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.oneOf = function (inList, list, msg) {\n    new Assertion(inList, msg, assert.oneOf, true).to.be.oneOf(list);\n  }\n\n  /**\n   * ### .changes(function, object, property, [message])\n   *\n   * Asserts that a function changes the value of a property.\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { obj.val = 22 };\n   *     assert.changes(fn, obj, 'val');\n   *\n   * @name changes\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.changes = function (fn, obj, prop, msg) {\n    if (arguments.length === 3 && typeof obj === 'function') {\n      msg = prop;\n      prop = null;\n    }\n\n    new Assertion(fn, msg, assert.changes, true).to.change(obj, prop);\n  }\n\n   /**\n   * ### .changesBy(function, object, property, delta, [message])\n   *\n   * Asserts that a function changes the value of a property by an amount (delta).\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { obj.val += 2 };\n   *     assert.changesBy(fn, obj, 'val', 2);\n   *\n   * @name changesBy\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {Number} change amount (delta)\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.changesBy = function (fn, obj, prop, delta, msg) {\n    if (arguments.length === 4 && typeof obj === 'function') {\n      var tmpMsg = delta;\n      delta = prop;\n      msg = tmpMsg;\n    } else if (arguments.length === 3) {\n      delta = prop;\n      prop = null;\n    }\n\n    new Assertion(fn, msg, assert.changesBy, true)\n      .to.change(obj, prop).by(delta);\n  }\n\n   /**\n   * ### .doesNotChange(function, object, property, [message])\n   *\n   * Asserts that a function does not change the value of a property.\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { console.log('foo'); };\n   *     assert.doesNotChange(fn, obj, 'val');\n   *\n   * @name doesNotChange\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.doesNotChange = function (fn, obj, prop, msg) {\n    if (arguments.length === 3 && typeof obj === 'function') {\n      msg = prop;\n      prop = null;\n    }\n\n    return new Assertion(fn, msg, assert.doesNotChange, true)\n      .to.not.change(obj, prop);\n  }\n\n  /**\n   * ### .changesButNotBy(function, object, property, delta, [message])\n   *\n   * Asserts that a function does not change the value of a property or of a function's return value by an amount (delta)\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { obj.val += 10 };\n   *     assert.changesButNotBy(fn, obj, 'val', 5);\n   *\n   * @name changesButNotBy\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {Number} change amount (delta)\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.changesButNotBy = function (fn, obj, prop, delta, msg) {\n    if (arguments.length === 4 && typeof obj === 'function') {\n      var tmpMsg = delta;\n      delta = prop;\n      msg = tmpMsg;\n    } else if (arguments.length === 3) {\n      delta = prop;\n      prop = null;\n    }\n\n    new Assertion(fn, msg, assert.changesButNotBy, true)\n      .to.change(obj, prop).but.not.by(delta);\n  }\n\n  /**\n   * ### .increases(function, object, property, [message])\n   *\n   * Asserts that a function increases a numeric object property.\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { obj.val = 13 };\n   *     assert.increases(fn, obj, 'val');\n   *\n   * @name increases\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.increases = function (fn, obj, prop, msg) {\n    if (arguments.length === 3 && typeof obj === 'function') {\n      msg = prop;\n      prop = null;\n    }\n\n    return new Assertion(fn, msg, assert.increases, true)\n      .to.increase(obj, prop);\n  }\n\n  /**\n   * ### .increasesBy(function, object, property, delta, [message])\n   *\n   * Asserts that a function increases a numeric object property or a function's return value by an amount (delta).\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { obj.val += 10 };\n   *     assert.increasesBy(fn, obj, 'val', 10);\n   *\n   * @name increasesBy\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {Number} change amount (delta)\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.increasesBy = function (fn, obj, prop, delta, msg) {\n    if (arguments.length === 4 && typeof obj === 'function') {\n      var tmpMsg = delta;\n      delta = prop;\n      msg = tmpMsg;\n    } else if (arguments.length === 3) {\n      delta = prop;\n      prop = null;\n    }\n\n    new Assertion(fn, msg, assert.increasesBy, true)\n      .to.increase(obj, prop).by(delta);\n  }\n\n  /**\n   * ### .doesNotIncrease(function, object, property, [message])\n   *\n   * Asserts that a function does not increase a numeric object property.\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { obj.val = 8 };\n   *     assert.doesNotIncrease(fn, obj, 'val');\n   *\n   * @name doesNotIncrease\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.doesNotIncrease = function (fn, obj, prop, msg) {\n    if (arguments.length === 3 && typeof obj === 'function') {\n      msg = prop;\n      prop = null;\n    }\n\n    return new Assertion(fn, msg, assert.doesNotIncrease, true)\n      .to.not.increase(obj, prop);\n  }\n\n  /**\n   * ### .increasesButNotBy(function, object, property, delta, [message])\n   *\n   * Asserts that a function does not increase a numeric object property or function's return value by an amount (delta).\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { obj.val = 15 };\n   *     assert.increasesButNotBy(fn, obj, 'val', 10);\n   *\n   * @name increasesButNotBy\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {Number} change amount (delta)\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.increasesButNotBy = function (fn, obj, prop, delta, msg) {\n    if (arguments.length === 4 && typeof obj === 'function') {\n      var tmpMsg = delta;\n      delta = prop;\n      msg = tmpMsg;\n    } else if (arguments.length === 3) {\n      delta = prop;\n      prop = null;\n    }\n\n    new Assertion(fn, msg, assert.increasesButNotBy, true)\n      .to.increase(obj, prop).but.not.by(delta);\n  }\n\n  /**\n   * ### .decreases(function, object, property, [message])\n   *\n   * Asserts that a function decreases a numeric object property.\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { obj.val = 5 };\n   *     assert.decreases(fn, obj, 'val');\n   *\n   * @name decreases\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.decreases = function (fn, obj, prop, msg) {\n    if (arguments.length === 3 && typeof obj === 'function') {\n      msg = prop;\n      prop = null;\n    }\n\n    return new Assertion(fn, msg, assert.decreases, true)\n      .to.decrease(obj, prop);\n  }\n\n  /**\n   * ### .decreasesBy(function, object, property, delta, [message])\n   *\n   * Asserts that a function decreases a numeric object property or a function's return value by an amount (delta)\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { obj.val -= 5 };\n   *     assert.decreasesBy(fn, obj, 'val', 5);\n   *\n   * @name decreasesBy\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {Number} change amount (delta)\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.decreasesBy = function (fn, obj, prop, delta, msg) {\n    if (arguments.length === 4 && typeof obj === 'function') {\n      var tmpMsg = delta;\n      delta = prop;\n      msg = tmpMsg;\n    } else if (arguments.length === 3) {\n      delta = prop;\n      prop = null;\n    }\n\n    new Assertion(fn, msg, assert.decreasesBy, true)\n      .to.decrease(obj, prop).by(delta);\n  }\n\n  /**\n   * ### .doesNotDecrease(function, object, property, [message])\n   *\n   * Asserts that a function does not decreases a numeric object property.\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { obj.val = 15 };\n   *     assert.doesNotDecrease(fn, obj, 'val');\n   *\n   * @name doesNotDecrease\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.doesNotDecrease = function (fn, obj, prop, msg) {\n    if (arguments.length === 3 && typeof obj === 'function') {\n      msg = prop;\n      prop = null;\n    }\n\n    return new Assertion(fn, msg, assert.doesNotDecrease, true)\n      .to.not.decrease(obj, prop);\n  }\n\n  /**\n   * ### .doesNotDecreaseBy(function, object, property, delta, [message])\n   *\n   * Asserts that a function does not decreases a numeric object property or a function's return value by an amount (delta)\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { obj.val = 5 };\n   *     assert.doesNotDecreaseBy(fn, obj, 'val', 1);\n   *\n   * @name doesNotDecreaseBy\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {Number} change amount (delta)\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.doesNotDecreaseBy = function (fn, obj, prop, delta, msg) {\n    if (arguments.length === 4 && typeof obj === 'function') {\n      var tmpMsg = delta;\n      delta = prop;\n      msg = tmpMsg;\n    } else if (arguments.length === 3) {\n      delta = prop;\n      prop = null;\n    }\n\n    return new Assertion(fn, msg, assert.doesNotDecreaseBy, true)\n      .to.not.decrease(obj, prop).by(delta);\n  }\n\n  /**\n   * ### .decreasesButNotBy(function, object, property, delta, [message])\n   *\n   * Asserts that a function does not decreases a numeric object property or a function's return value by an amount (delta)\n   *\n   *     var obj = { val: 10 };\n   *     var fn = function() { obj.val = 5 };\n   *     assert.decreasesButNotBy(fn, obj, 'val', 1);\n   *\n   * @name decreasesButNotBy\n   * @param {Function} modifier function\n   * @param {Object} object or getter function\n   * @param {String} property name _optional_\n   * @param {Number} change amount (delta)\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.decreasesButNotBy = function (fn, obj, prop, delta, msg) {\n    if (arguments.length === 4 && typeof obj === 'function') {\n      var tmpMsg = delta;\n      delta = prop;\n      msg = tmpMsg;\n    } else if (arguments.length === 3) {\n      delta = prop;\n      prop = null;\n    }\n\n    new Assertion(fn, msg, assert.decreasesButNotBy, true)\n      .to.decrease(obj, prop).but.not.by(delta);\n  }\n\n  /*!\n   * ### .ifError(object)\n   *\n   * Asserts if value is not a false value, and throws if it is a true value.\n   * This is added to allow for chai to be a drop-in replacement for Node's\n   * assert class.\n   *\n   *     var err = new Error('I am a custom error');\n   *     assert.ifError(err); // Rethrows err!\n   *\n   * @name ifError\n   * @param {Object} object\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.ifError = function (val) {\n    if (val) {\n      throw(val);\n    }\n  };\n\n  /**\n   * ### .isExtensible(object)\n   *\n   * Asserts that `object` is extensible (can have new properties added to it).\n   *\n   *     assert.isExtensible({});\n   *\n   * @name isExtensible\n   * @alias extensible\n   * @param {Object} object\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isExtensible = function (obj, msg) {\n    new Assertion(obj, msg, assert.isExtensible, true).to.be.extensible;\n  };\n\n  /**\n   * ### .isNotExtensible(object)\n   *\n   * Asserts that `object` is _not_ extensible.\n   *\n   *     var nonExtensibleObject = Object.preventExtensions({});\n   *     var sealedObject = Object.seal({});\n   *     var frozenObject = Object.freeze({});\n   *\n   *     assert.isNotExtensible(nonExtensibleObject);\n   *     assert.isNotExtensible(sealedObject);\n   *     assert.isNotExtensible(frozenObject);\n   *\n   * @name isNotExtensible\n   * @alias notExtensible\n   * @param {Object} object\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotExtensible = function (obj, msg) {\n    new Assertion(obj, msg, assert.isNotExtensible, true).to.not.be.extensible;\n  };\n\n  /**\n   * ### .isSealed(object)\n   *\n   * Asserts that `object` is sealed (cannot have new properties added to it\n   * and its existing properties cannot be removed).\n   *\n   *     var sealedObject = Object.seal({});\n   *     var frozenObject = Object.seal({});\n   *\n   *     assert.isSealed(sealedObject);\n   *     assert.isSealed(frozenObject);\n   *\n   * @name isSealed\n   * @alias sealed\n   * @param {Object} object\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isSealed = function (obj, msg) {\n    new Assertion(obj, msg, assert.isSealed, true).to.be.sealed;\n  };\n\n  /**\n   * ### .isNotSealed(object)\n   *\n   * Asserts that `object` is _not_ sealed.\n   *\n   *     assert.isNotSealed({});\n   *\n   * @name isNotSealed\n   * @alias notSealed\n   * @param {Object} object\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotSealed = function (obj, msg) {\n    new Assertion(obj, msg, assert.isNotSealed, true).to.not.be.sealed;\n  };\n\n  /**\n   * ### .isFrozen(object)\n   *\n   * Asserts that `object` is frozen (cannot have new properties added to it\n   * and its existing properties cannot be modified).\n   *\n   *     var frozenObject = Object.freeze({});\n   *     assert.frozen(frozenObject);\n   *\n   * @name isFrozen\n   * @alias frozen\n   * @param {Object} object\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isFrozen = function (obj, msg) {\n    new Assertion(obj, msg, assert.isFrozen, true).to.be.frozen;\n  };\n\n  /**\n   * ### .isNotFrozen(object)\n   *\n   * Asserts that `object` is _not_ frozen.\n   *\n   *     assert.isNotFrozen({});\n   *\n   * @name isNotFrozen\n   * @alias notFrozen\n   * @param {Object} object\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotFrozen = function (obj, msg) {\n    new Assertion(obj, msg, assert.isNotFrozen, true).to.not.be.frozen;\n  };\n\n  /**\n   * ### .isEmpty(target)\n   *\n   * Asserts that the target does not contain any values.\n   * For arrays and strings, it checks the `length` property.\n   * For `Map` and `Set` instances, it checks the `size` property.\n   * For non-function objects, it gets the count of own\n   * enumerable string keys.\n   *\n   *     assert.isEmpty([]);\n   *     assert.isEmpty('');\n   *     assert.isEmpty(new Map);\n   *     assert.isEmpty({});\n   *\n   * @name isEmpty\n   * @alias empty\n   * @param {Object|Array|String|Map|Set} target\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isEmpty = function(val, msg) {\n    new Assertion(val, msg, assert.isEmpty, true).to.be.empty;\n  };\n\n  /**\n   * ### .isNotEmpty(target)\n   *\n   * Asserts that the target contains values.\n   * For arrays and strings, it checks the `length` property.\n   * For `Map` and `Set` instances, it checks the `size` property.\n   * For non-function objects, it gets the count of own\n   * enumerable string keys.\n   *\n   *     assert.isNotEmpty([1, 2]);\n   *     assert.isNotEmpty('34');\n   *     assert.isNotEmpty(new Set([5, 6]));\n   *     assert.isNotEmpty({ key: 7 });\n   *\n   * @name isNotEmpty\n   * @alias notEmpty\n   * @param {Object|Array|String|Map|Set} target\n   * @param {String} message _optional_\n   * @namespace Assert\n   * @api public\n   */\n\n  assert.isNotEmpty = function(val, msg) {\n    new Assertion(val, msg, assert.isNotEmpty, true).to.not.be.empty;\n  };\n\n  /*!\n   * Aliases.\n   */\n\n  (function alias(name, as){\n    assert[as] = assert[name];\n    return alias;\n  })\n  ('isOk', 'ok')\n  ('isNotOk', 'notOk')\n  ('throws', 'throw')\n  ('throws', 'Throw')\n  ('isExtensible', 'extensible')\n  ('isNotExtensible', 'notExtensible')\n  ('isSealed', 'sealed')\n  ('isNotSealed', 'notSealed')\n  ('isFrozen', 'frozen')\n  ('isNotFrozen', 'notFrozen')\n  ('isEmpty', 'empty')\n  ('isNotEmpty', 'notEmpty');\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/interface/assert.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/interface/expect.js":
/*!********************************************************!*\
  !*** ./node_modules/chai/lib/chai/interface/expect.js ***!
  \********************************************************/
/***/ ((module) => {

eval("/*!\n * chai\n * Copyright(c) 2011-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\nmodule.exports = function (chai, util) {\n  chai.expect = function (val, message) {\n    return new chai.Assertion(val, message);\n  };\n\n  /**\n   * ### .fail([message])\n   * ### .fail(actual, expected, [message], [operator])\n   *\n   * Throw a failure.\n   *\n   *     expect.fail();\n   *     expect.fail(\"custom error message\");\n   *     expect.fail(1, 2);\n   *     expect.fail(1, 2, \"custom error message\");\n   *     expect.fail(1, 2, \"custom error message\", \">\");\n   *     expect.fail(1, 2, undefined, \">\");\n   *\n   * @name fail\n   * @param {Mixed} actual\n   * @param {Mixed} expected\n   * @param {String} message\n   * @param {String} operator\n   * @namespace BDD\n   * @api public\n   */\n\n  chai.expect.fail = function (actual, expected, message, operator) {\n    if (arguments.length < 2) {\n        message = actual;\n        actual = undefined;\n    }\n\n    message = message || 'expect.fail()';\n    throw new chai.AssertionError(message, {\n        actual: actual\n      , expected: expected\n      , operator: operator\n    }, chai.expect.fail);\n  };\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/interface/expect.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/interface/should.js":
/*!********************************************************!*\
  !*** ./node_modules/chai/lib/chai/interface/should.js ***!
  \********************************************************/
/***/ ((module) => {

eval("/*!\n * chai\n * Copyright(c) 2011-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\nmodule.exports = function (chai, util) {\n  var Assertion = chai.Assertion;\n\n  function loadShould () {\n    // explicitly define this method as function as to have it's name to include as `ssfi`\n    function shouldGetter() {\n      if (this instanceof String\n          || this instanceof Number\n          || this instanceof Boolean\n          || typeof Symbol === 'function' && this instanceof Symbol\n          || typeof BigInt === 'function' && this instanceof BigInt) {\n        return new Assertion(this.valueOf(), null, shouldGetter);\n      }\n      return new Assertion(this, null, shouldGetter);\n    }\n    function shouldSetter(value) {\n      // See https://github.com/chaijs/chai/issues/86: this makes\n      // `whatever.should = someValue` actually set `someValue`, which is\n      // especially useful for `global.should = require('chai').should()`.\n      //\n      // Note that we have to use [[DefineProperty]] instead of [[Put]]\n      // since otherwise we would trigger this very setter!\n      Object.defineProperty(this, 'should', {\n        value: value,\n        enumerable: true,\n        configurable: true,\n        writable: true\n      });\n    }\n    // modify Object.prototype to have `should`\n    Object.defineProperty(Object.prototype, 'should', {\n      set: shouldSetter\n      , get: shouldGetter\n      , configurable: true\n    });\n\n    var should = {};\n\n    /**\n     * ### .fail([message])\n     * ### .fail(actual, expected, [message], [operator])\n     *\n     * Throw a failure.\n     *\n     *     should.fail();\n     *     should.fail(\"custom error message\");\n     *     should.fail(1, 2);\n     *     should.fail(1, 2, \"custom error message\");\n     *     should.fail(1, 2, \"custom error message\", \">\");\n     *     should.fail(1, 2, undefined, \">\");\n     *\n     *\n     * @name fail\n     * @param {Mixed} actual\n     * @param {Mixed} expected\n     * @param {String} message\n     * @param {String} operator\n     * @namespace BDD\n     * @api public\n     */\n\n    should.fail = function (actual, expected, message, operator) {\n      if (arguments.length < 2) {\n          message = actual;\n          actual = undefined;\n      }\n\n      message = message || 'should.fail()';\n      throw new chai.AssertionError(message, {\n          actual: actual\n        , expected: expected\n        , operator: operator\n      }, should.fail);\n    };\n\n    /**\n     * ### .equal(actual, expected, [message])\n     *\n     * Asserts non-strict equality (`==`) of `actual` and `expected`.\n     *\n     *     should.equal(3, '3', '== coerces values to strings');\n     *\n     * @name equal\n     * @param {Mixed} actual\n     * @param {Mixed} expected\n     * @param {String} message\n     * @namespace Should\n     * @api public\n     */\n\n    should.equal = function (val1, val2, msg) {\n      new Assertion(val1, msg).to.equal(val2);\n    };\n\n    /**\n     * ### .throw(function, [constructor/string/regexp], [string/regexp], [message])\n     *\n     * Asserts that `function` will throw an error that is an instance of\n     * `constructor`, or alternately that it will throw an error with message\n     * matching `regexp`.\n     *\n     *     should.throw(fn, 'function throws a reference error');\n     *     should.throw(fn, /function throws a reference error/);\n     *     should.throw(fn, ReferenceError);\n     *     should.throw(fn, ReferenceError, 'function throws a reference error');\n     *     should.throw(fn, ReferenceError, /function throws a reference error/);\n     *\n     * @name throw\n     * @alias Throw\n     * @param {Function} function\n     * @param {ErrorConstructor} constructor\n     * @param {RegExp} regexp\n     * @param {String} message\n     * @see https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Error#Error_types\n     * @namespace Should\n     * @api public\n     */\n\n    should.Throw = function (fn, errt, errs, msg) {\n      new Assertion(fn, msg).to.Throw(errt, errs);\n    };\n\n    /**\n     * ### .exist\n     *\n     * Asserts that the target is neither `null` nor `undefined`.\n     *\n     *     var foo = 'hi';\n     *\n     *     should.exist(foo, 'foo exists');\n     *\n     * @name exist\n     * @namespace Should\n     * @api public\n     */\n\n    should.exist = function (val, msg) {\n      new Assertion(val, msg).to.exist;\n    }\n\n    // negation\n    should.not = {}\n\n    /**\n     * ### .not.equal(actual, expected, [message])\n     *\n     * Asserts non-strict inequality (`!=`) of `actual` and `expected`.\n     *\n     *     should.not.equal(3, 4, 'these numbers are not equal');\n     *\n     * @name not.equal\n     * @param {Mixed} actual\n     * @param {Mixed} expected\n     * @param {String} message\n     * @namespace Should\n     * @api public\n     */\n\n    should.not.equal = function (val1, val2, msg) {\n      new Assertion(val1, msg).to.not.equal(val2);\n    };\n\n    /**\n     * ### .throw(function, [constructor/regexp], [message])\n     *\n     * Asserts that `function` will _not_ throw an error that is an instance of\n     * `constructor`, or alternately that it will not throw an error with message\n     * matching `regexp`.\n     *\n     *     should.not.throw(fn, Error, 'function does not throw');\n     *\n     * @name not.throw\n     * @alias not.Throw\n     * @param {Function} function\n     * @param {ErrorConstructor} constructor\n     * @param {RegExp} regexp\n     * @param {String} message\n     * @see https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Error#Error_types\n     * @namespace Should\n     * @api public\n     */\n\n    should.not.Throw = function (fn, errt, errs, msg) {\n      new Assertion(fn, msg).to.not.Throw(errt, errs);\n    };\n\n    /**\n     * ### .not.exist\n     *\n     * Asserts that the target is neither `null` nor `undefined`.\n     *\n     *     var bar = null;\n     *\n     *     should.not.exist(bar, 'bar does not exist');\n     *\n     * @name not.exist\n     * @namespace Should\n     * @api public\n     */\n\n    should.not.exist = function (val, msg) {\n      new Assertion(val, msg).to.not.exist;\n    }\n\n    should['throw'] = should['Throw'];\n    should.not['throw'] = should.not['Throw'];\n\n    return should;\n  };\n\n  chai.should = loadShould;\n  chai.Should = loadShould;\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/interface/should.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/addChainableMethod.js":
/*!****************************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/addChainableMethod.js ***!
  \****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * Chai - addChainingMethod utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/*!\n * Module dependencies\n */\n\nvar addLengthGuard = __webpack_require__(/*! ./addLengthGuard */ \"./node_modules/chai/lib/chai/utils/addLengthGuard.js\");\nvar chai = __webpack_require__(/*! ../../chai */ \"./node_modules/chai/lib/chai.js\");\nvar flag = __webpack_require__(/*! ./flag */ \"./node_modules/chai/lib/chai/utils/flag.js\");\nvar proxify = __webpack_require__(/*! ./proxify */ \"./node_modules/chai/lib/chai/utils/proxify.js\");\nvar transferFlags = __webpack_require__(/*! ./transferFlags */ \"./node_modules/chai/lib/chai/utils/transferFlags.js\");\n\n/*!\n * Module variables\n */\n\n// Check whether `Object.setPrototypeOf` is supported\nvar canSetPrototype = typeof Object.setPrototypeOf === 'function';\n\n// Without `Object.setPrototypeOf` support, this module will need to add properties to a function.\n// However, some of functions' own props are not configurable and should be skipped.\nvar testFn = function() {};\nvar excludeNames = Object.getOwnPropertyNames(testFn).filter(function(name) {\n  var propDesc = Object.getOwnPropertyDescriptor(testFn, name);\n\n  // Note: PhantomJS 1.x includes `callee` as one of `testFn`'s own properties,\n  // but then returns `undefined` as the property descriptor for `callee`. As a\n  // workaround, we perform an otherwise unnecessary type-check for `propDesc`,\n  // and then filter it out if it's not an object as it should be.\n  if (typeof propDesc !== 'object')\n    return true;\n\n  return !propDesc.configurable;\n});\n\n// Cache `Function` properties\nvar call  = Function.prototype.call,\n    apply = Function.prototype.apply;\n\n/**\n * ### .addChainableMethod(ctx, name, method, chainingBehavior)\n *\n * Adds a method to an object, such that the method can also be chained.\n *\n *     utils.addChainableMethod(chai.Assertion.prototype, 'foo', function (str) {\n *       var obj = utils.flag(this, 'object');\n *       new chai.Assertion(obj).to.be.equal(str);\n *     });\n *\n * Can also be accessed directly from `chai.Assertion`.\n *\n *     chai.Assertion.addChainableMethod('foo', fn, chainingBehavior);\n *\n * The result can then be used as both a method assertion, executing both `method` and\n * `chainingBehavior`, or as a language chain, which only executes `chainingBehavior`.\n *\n *     expect(fooStr).to.be.foo('bar');\n *     expect(fooStr).to.be.foo.equal('foo');\n *\n * @param {Object} ctx object to which the method is added\n * @param {String} name of method to add\n * @param {Function} method function to be used for `name`, when called\n * @param {Function} chainingBehavior function to be called every time the property is accessed\n * @namespace Utils\n * @name addChainableMethod\n * @api public\n */\n\nmodule.exports = function addChainableMethod(ctx, name, method, chainingBehavior) {\n  if (typeof chainingBehavior !== 'function') {\n    chainingBehavior = function () { };\n  }\n\n  var chainableBehavior = {\n      method: method\n    , chainingBehavior: chainingBehavior\n  };\n\n  // save the methods so we can overwrite them later, if we need to.\n  if (!ctx.__methods) {\n    ctx.__methods = {};\n  }\n  ctx.__methods[name] = chainableBehavior;\n\n  Object.defineProperty(ctx, name,\n    { get: function chainableMethodGetter() {\n        chainableBehavior.chainingBehavior.call(this);\n\n        var chainableMethodWrapper = function () {\n          // Setting the `ssfi` flag to `chainableMethodWrapper` causes this\n          // function to be the starting point for removing implementation\n          // frames from the stack trace of a failed assertion.\n          //\n          // However, we only want to use this function as the starting point if\n          // the `lockSsfi` flag isn't set.\n          //\n          // If the `lockSsfi` flag is set, then this assertion is being\n          // invoked from inside of another assertion. In this case, the `ssfi`\n          // flag has already been set by the outer assertion.\n          //\n          // Note that overwriting a chainable method merely replaces the saved\n          // methods in `ctx.__methods` instead of completely replacing the\n          // overwritten assertion. Therefore, an overwriting assertion won't\n          // set the `ssfi` or `lockSsfi` flags.\n          if (!flag(this, 'lockSsfi')) {\n            flag(this, 'ssfi', chainableMethodWrapper);\n          }\n\n          var result = chainableBehavior.method.apply(this, arguments);\n          if (result !== undefined) {\n            return result;\n          }\n\n          var newAssertion = new chai.Assertion();\n          transferFlags(this, newAssertion);\n          return newAssertion;\n        };\n\n        addLengthGuard(chainableMethodWrapper, name, true);\n\n        // Use `Object.setPrototypeOf` if available\n        if (canSetPrototype) {\n          // Inherit all properties from the object by replacing the `Function` prototype\n          var prototype = Object.create(this);\n          // Restore the `call` and `apply` methods from `Function`\n          prototype.call = call;\n          prototype.apply = apply;\n          Object.setPrototypeOf(chainableMethodWrapper, prototype);\n        }\n        // Otherwise, redefine all properties (slow!)\n        else {\n          var asserterNames = Object.getOwnPropertyNames(ctx);\n          asserterNames.forEach(function (asserterName) {\n            if (excludeNames.indexOf(asserterName) !== -1) {\n              return;\n            }\n\n            var pd = Object.getOwnPropertyDescriptor(ctx, asserterName);\n            Object.defineProperty(chainableMethodWrapper, asserterName, pd);\n          });\n        }\n\n        transferFlags(this, chainableMethodWrapper);\n        return proxify(chainableMethodWrapper);\n      }\n    , configurable: true\n  });\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/addChainableMethod.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/addLengthGuard.js":
/*!************************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/addLengthGuard.js ***!
  \************************************************************/
/***/ ((module) => {

eval("var fnLengthDesc = Object.getOwnPropertyDescriptor(function () {}, 'length');\n\n/*!\n * Chai - addLengthGuard utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/**\n * ### .addLengthGuard(fn, assertionName, isChainable)\n *\n * Define `length` as a getter on the given uninvoked method assertion. The\n * getter acts as a guard against chaining `length` directly off of an uninvoked\n * method assertion, which is a problem because it references `function`'s\n * built-in `length` property instead of Chai's `length` assertion. When the\n * getter catches the user making this mistake, it throws an error with a\n * helpful message.\n *\n * There are two ways in which this mistake can be made. The first way is by\n * chaining the `length` assertion directly off of an uninvoked chainable\n * method. In this case, Chai suggests that the user use `lengthOf` instead. The\n * second way is by chaining the `length` assertion directly off of an uninvoked\n * non-chainable method. Non-chainable methods must be invoked prior to\n * chaining. In this case, Chai suggests that the user consult the docs for the\n * given assertion.\n *\n * If the `length` property of functions is unconfigurable, then return `fn`\n * without modification.\n *\n * Note that in ES6, the function's `length` property is configurable, so once\n * support for legacy environments is dropped, Chai's `length` property can\n * replace the built-in function's `length` property, and this length guard will\n * no longer be necessary. In the mean time, maintaining consistency across all\n * environments is the priority.\n *\n * @param {Function} fn\n * @param {String} assertionName\n * @param {Boolean} isChainable\n * @namespace Utils\n * @name addLengthGuard\n */\n\nmodule.exports = function addLengthGuard (fn, assertionName, isChainable) {\n  if (!fnLengthDesc.configurable) return fn;\n\n  Object.defineProperty(fn, 'length', {\n    get: function () {\n      if (isChainable) {\n        throw Error('Invalid Chai property: ' + assertionName + '.length. Due' +\n          ' to a compatibility issue, \"length\" cannot directly follow \"' +\n          assertionName + '\". Use \"' + assertionName + '.lengthOf\" instead.');\n      }\n\n      throw Error('Invalid Chai property: ' + assertionName + '.length. See' +\n        ' docs for proper usage of \"' + assertionName + '\".');\n    }\n  });\n\n  return fn;\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/addLengthGuard.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/addMethod.js":
/*!*******************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/addMethod.js ***!
  \*******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * Chai - addMethod utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\nvar addLengthGuard = __webpack_require__(/*! ./addLengthGuard */ \"./node_modules/chai/lib/chai/utils/addLengthGuard.js\");\nvar chai = __webpack_require__(/*! ../../chai */ \"./node_modules/chai/lib/chai.js\");\nvar flag = __webpack_require__(/*! ./flag */ \"./node_modules/chai/lib/chai/utils/flag.js\");\nvar proxify = __webpack_require__(/*! ./proxify */ \"./node_modules/chai/lib/chai/utils/proxify.js\");\nvar transferFlags = __webpack_require__(/*! ./transferFlags */ \"./node_modules/chai/lib/chai/utils/transferFlags.js\");\n\n/**\n * ### .addMethod(ctx, name, method)\n *\n * Adds a method to the prototype of an object.\n *\n *     utils.addMethod(chai.Assertion.prototype, 'foo', function (str) {\n *       var obj = utils.flag(this, 'object');\n *       new chai.Assertion(obj).to.be.equal(str);\n *     });\n *\n * Can also be accessed directly from `chai.Assertion`.\n *\n *     chai.Assertion.addMethod('foo', fn);\n *\n * Then can be used as any other assertion.\n *\n *     expect(fooStr).to.be.foo('bar');\n *\n * @param {Object} ctx object to which the method is added\n * @param {String} name of method to add\n * @param {Function} method function to be used for name\n * @namespace Utils\n * @name addMethod\n * @api public\n */\n\nmodule.exports = function addMethod(ctx, name, method) {\n  var methodWrapper = function () {\n    // Setting the `ssfi` flag to `methodWrapper` causes this function to be the\n    // starting point for removing implementation frames from the stack trace of\n    // a failed assertion.\n    //\n    // However, we only want to use this function as the starting point if the\n    // `lockSsfi` flag isn't set.\n    //\n    // If the `lockSsfi` flag is set, then either this assertion has been\n    // overwritten by another assertion, or this assertion is being invoked from\n    // inside of another assertion. In the first case, the `ssfi` flag has\n    // already been set by the overwriting assertion. In the second case, the\n    // `ssfi` flag has already been set by the outer assertion.\n    if (!flag(this, 'lockSsfi')) {\n      flag(this, 'ssfi', methodWrapper);\n    }\n\n    var result = method.apply(this, arguments);\n    if (result !== undefined)\n      return result;\n\n    var newAssertion = new chai.Assertion();\n    transferFlags(this, newAssertion);\n    return newAssertion;\n  };\n\n  addLengthGuard(methodWrapper, name, false);\n  ctx[name] = proxify(methodWrapper, name);\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/addMethod.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/addProperty.js":
/*!*********************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/addProperty.js ***!
  \*********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * Chai - addProperty utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\nvar chai = __webpack_require__(/*! ../../chai */ \"./node_modules/chai/lib/chai.js\");\nvar flag = __webpack_require__(/*! ./flag */ \"./node_modules/chai/lib/chai/utils/flag.js\");\nvar isProxyEnabled = __webpack_require__(/*! ./isProxyEnabled */ \"./node_modules/chai/lib/chai/utils/isProxyEnabled.js\");\nvar transferFlags = __webpack_require__(/*! ./transferFlags */ \"./node_modules/chai/lib/chai/utils/transferFlags.js\");\n\n/**\n * ### .addProperty(ctx, name, getter)\n *\n * Adds a property to the prototype of an object.\n *\n *     utils.addProperty(chai.Assertion.prototype, 'foo', function () {\n *       var obj = utils.flag(this, 'object');\n *       new chai.Assertion(obj).to.be.instanceof(Foo);\n *     });\n *\n * Can also be accessed directly from `chai.Assertion`.\n *\n *     chai.Assertion.addProperty('foo', fn);\n *\n * Then can be used as any other assertion.\n *\n *     expect(myFoo).to.be.foo;\n *\n * @param {Object} ctx object to which the property is added\n * @param {String} name of property to add\n * @param {Function} getter function to be used for name\n * @namespace Utils\n * @name addProperty\n * @api public\n */\n\nmodule.exports = function addProperty(ctx, name, getter) {\n  getter = getter === undefined ? function () {} : getter;\n\n  Object.defineProperty(ctx, name,\n    { get: function propertyGetter() {\n        // Setting the `ssfi` flag to `propertyGetter` causes this function to\n        // be the starting point for removing implementation frames from the\n        // stack trace of a failed assertion.\n        //\n        // However, we only want to use this function as the starting point if\n        // the `lockSsfi` flag isn't set and proxy protection is disabled.\n        //\n        // If the `lockSsfi` flag is set, then either this assertion has been\n        // overwritten by another assertion, or this assertion is being invoked\n        // from inside of another assertion. In the first case, the `ssfi` flag\n        // has already been set by the overwriting assertion. In the second\n        // case, the `ssfi` flag has already been set by the outer assertion.\n        //\n        // If proxy protection is enabled, then the `ssfi` flag has already been\n        // set by the proxy getter.\n        if (!isProxyEnabled() && !flag(this, 'lockSsfi')) {\n          flag(this, 'ssfi', propertyGetter);\n        }\n\n        var result = getter.call(this);\n        if (result !== undefined)\n          return result;\n\n        var newAssertion = new chai.Assertion();\n        transferFlags(this, newAssertion);\n        return newAssertion;\n      }\n    , configurable: true\n  });\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/addProperty.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/compareByInspect.js":
/*!**************************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/compareByInspect.js ***!
  \**************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * Chai - compareByInspect utility\n * Copyright(c) 2011-2016 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/*!\n * Module dependencies\n */\n\nvar inspect = __webpack_require__(/*! ./inspect */ \"./node_modules/chai/lib/chai/utils/inspect.js\");\n\n/**\n * ### .compareByInspect(mixed, mixed)\n *\n * To be used as a compareFunction with Array.prototype.sort. Compares elements\n * using inspect instead of default behavior of using toString so that Symbols\n * and objects with irregular/missing toString can still be sorted without a\n * TypeError.\n *\n * @param {Mixed} first element to compare\n * @param {Mixed} second element to compare\n * @returns {Number} -1 if 'a' should come before 'b'; otherwise 1\n * @name compareByInspect\n * @namespace Utils\n * @api public\n */\n\nmodule.exports = function compareByInspect(a, b) {\n  return inspect(a) < inspect(b) ? -1 : 1;\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/compareByInspect.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/expectTypes.js":
/*!*********************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/expectTypes.js ***!
  \*********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * Chai - expectTypes utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/**\n * ### .expectTypes(obj, types)\n *\n * Ensures that the object being tested against is of a valid type.\n *\n *     utils.expectTypes(this, ['array', 'object', 'string']);\n *\n * @param {Mixed} obj constructed Assertion\n * @param {Array} type A list of allowed types for this assertion\n * @namespace Utils\n * @name expectTypes\n * @api public\n */\n\nvar AssertionError = __webpack_require__(/*! assertion-error */ \"./node_modules/assertion-error/index.js\");\nvar flag = __webpack_require__(/*! ./flag */ \"./node_modules/chai/lib/chai/utils/flag.js\");\nvar type = __webpack_require__(/*! type-detect */ \"./node_modules/type-detect/type-detect.js\");\n\nmodule.exports = function expectTypes(obj, types) {\n  var flagMsg = flag(obj, 'message');\n  var ssfi = flag(obj, 'ssfi');\n\n  flagMsg = flagMsg ? flagMsg + ': ' : '';\n\n  obj = flag(obj, 'object');\n  types = types.map(function (t) { return t.toLowerCase(); });\n  types.sort();\n\n  // Transforms ['lorem', 'ipsum'] into 'a lorem, or an ipsum'\n  var str = types.map(function (t, index) {\n    var art = ~[ 'a', 'e', 'i', 'o', 'u' ].indexOf(t.charAt(0)) ? 'an' : 'a';\n    var or = types.length > 1 && index === types.length - 1 ? 'or ' : '';\n    return or + art + ' ' + t;\n  }).join(', ');\n\n  var objType = type(obj).toLowerCase();\n\n  if (!types.some(function (expected) { return objType === expected; })) {\n    throw new AssertionError(\n      flagMsg + 'object tested must be ' + str + ', but ' + objType + ' given',\n      undefined,\n      ssfi\n    );\n  }\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/expectTypes.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/flag.js":
/*!**************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/flag.js ***!
  \**************************************************/
/***/ ((module) => {

eval("/*!\n * Chai - flag utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/**\n * ### .flag(object, key, [value])\n *\n * Get or set a flag value on an object. If a\n * value is provided it will be set, else it will\n * return the currently set value or `undefined` if\n * the value is not set.\n *\n *     utils.flag(this, 'foo', 'bar'); // setter\n *     utils.flag(this, 'foo'); // getter, returns `bar`\n *\n * @param {Object} object constructed Assertion\n * @param {String} key\n * @param {Mixed} value (optional)\n * @namespace Utils\n * @name flag\n * @api private\n */\n\nmodule.exports = function flag(obj, key, value) {\n  var flags = obj.__flags || (obj.__flags = Object.create(null));\n  if (arguments.length === 3) {\n    flags[key] = value;\n  } else {\n    return flags[key];\n  }\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/flag.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/getActual.js":
/*!*******************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/getActual.js ***!
  \*******************************************************/
/***/ ((module) => {

eval("/*!\n * Chai - getActual utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/**\n * ### .getActual(object, [actual])\n *\n * Returns the `actual` value for an Assertion.\n *\n * @param {Object} object (constructed Assertion)\n * @param {Arguments} chai.Assertion.prototype.assert arguments\n * @namespace Utils\n * @name getActual\n */\n\nmodule.exports = function getActual(obj, args) {\n  return args.length > 4 ? args[4] : obj._obj;\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/getActual.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/getMessage.js":
/*!********************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/getMessage.js ***!
  \********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * Chai - message composition utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/*!\n * Module dependencies\n */\n\nvar flag = __webpack_require__(/*! ./flag */ \"./node_modules/chai/lib/chai/utils/flag.js\")\n  , getActual = __webpack_require__(/*! ./getActual */ \"./node_modules/chai/lib/chai/utils/getActual.js\")\n  , objDisplay = __webpack_require__(/*! ./objDisplay */ \"./node_modules/chai/lib/chai/utils/objDisplay.js\");\n\n/**\n * ### .getMessage(object, message, negateMessage)\n *\n * Construct the error message based on flags\n * and template tags. Template tags will return\n * a stringified inspection of the object referenced.\n *\n * Message template tags:\n * - `#{this}` current asserted object\n * - `#{act}` actual value\n * - `#{exp}` expected value\n *\n * @param {Object} object (constructed Assertion)\n * @param {Arguments} chai.Assertion.prototype.assert arguments\n * @namespace Utils\n * @name getMessage\n * @api public\n */\n\nmodule.exports = function getMessage(obj, args) {\n  var negate = flag(obj, 'negate')\n    , val = flag(obj, 'object')\n    , expected = args[3]\n    , actual = getActual(obj, args)\n    , msg = negate ? args[2] : args[1]\n    , flagMsg = flag(obj, 'message');\n\n  if(typeof msg === \"function\") msg = msg();\n  msg = msg || '';\n  msg = msg\n    .replace(/#\\{this\\}/g, function () { return objDisplay(val); })\n    .replace(/#\\{act\\}/g, function () { return objDisplay(actual); })\n    .replace(/#\\{exp\\}/g, function () { return objDisplay(expected); });\n\n  return flagMsg ? flagMsg + ': ' + msg : msg;\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/getMessage.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/getOperator.js":
/*!*********************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/getOperator.js ***!
  \*********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("var type = __webpack_require__(/*! type-detect */ \"./node_modules/type-detect/type-detect.js\");\n\nvar flag = __webpack_require__(/*! ./flag */ \"./node_modules/chai/lib/chai/utils/flag.js\");\n\nfunction isObjectType(obj) {\n  var objectType = type(obj);\n  var objectTypes = ['Array', 'Object', 'function'];\n\n  return objectTypes.indexOf(objectType) !== -1;\n}\n\n/**\n * ### .getOperator(message)\n *\n * Extract the operator from error message.\n * Operator defined is based on below link\n * https://nodejs.org/api/assert.html#assert_assert.\n *\n * Returns the `operator` or `undefined` value for an Assertion.\n *\n * @param {Object} object (constructed Assertion)\n * @param {Arguments} chai.Assertion.prototype.assert arguments\n * @namespace Utils\n * @name getOperator\n * @api public\n */\n\nmodule.exports = function getOperator(obj, args) {\n  var operator = flag(obj, 'operator');\n  var negate = flag(obj, 'negate');\n  var expected = args[3];\n  var msg = negate ? args[2] : args[1];\n\n  if (operator) {\n    return operator;\n  }\n\n  if (typeof msg === 'function') msg = msg();\n\n  msg = msg || '';\n  if (!msg) {\n    return undefined;\n  }\n\n  if (/\\shave\\s/.test(msg)) {\n    return undefined;\n  }\n\n  var isObject = isObjectType(expected);\n  if (/\\snot\\s/.test(msg)) {\n    return isObject ? 'notDeepStrictEqual' : 'notStrictEqual';\n  }\n\n  return isObject ? 'deepStrictEqual' : 'strictEqual';\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/getOperator.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/getOwnEnumerableProperties.js":
/*!************************************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/getOwnEnumerableProperties.js ***!
  \************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * Chai - getOwnEnumerableProperties utility\n * Copyright(c) 2011-2016 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/*!\n * Module dependencies\n */\n\nvar getOwnEnumerablePropertySymbols = __webpack_require__(/*! ./getOwnEnumerablePropertySymbols */ \"./node_modules/chai/lib/chai/utils/getOwnEnumerablePropertySymbols.js\");\n\n/**\n * ### .getOwnEnumerableProperties(object)\n *\n * This allows the retrieval of directly-owned enumerable property names and\n * symbols of an object. This function is necessary because Object.keys only\n * returns enumerable property names, not enumerable property symbols.\n *\n * @param {Object} object\n * @returns {Array}\n * @namespace Utils\n * @name getOwnEnumerableProperties\n * @api public\n */\n\nmodule.exports = function getOwnEnumerableProperties(obj) {\n  return Object.keys(obj).concat(getOwnEnumerablePropertySymbols(obj));\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/getOwnEnumerableProperties.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/getOwnEnumerablePropertySymbols.js":
/*!*****************************************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/getOwnEnumerablePropertySymbols.js ***!
  \*****************************************************************************/
/***/ ((module) => {

eval("/*!\n * Chai - getOwnEnumerablePropertySymbols utility\n * Copyright(c) 2011-2016 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/**\n * ### .getOwnEnumerablePropertySymbols(object)\n *\n * This allows the retrieval of directly-owned enumerable property symbols of an\n * object. This function is necessary because Object.getOwnPropertySymbols\n * returns both enumerable and non-enumerable property symbols.\n *\n * @param {Object} object\n * @returns {Array}\n * @namespace Utils\n * @name getOwnEnumerablePropertySymbols\n * @api public\n */\n\nmodule.exports = function getOwnEnumerablePropertySymbols(obj) {\n  if (typeof Object.getOwnPropertySymbols !== 'function') return [];\n\n  return Object.getOwnPropertySymbols(obj).filter(function (sym) {\n    return Object.getOwnPropertyDescriptor(obj, sym).enumerable;\n  });\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/getOwnEnumerablePropertySymbols.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/getProperties.js":
/*!***********************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/getProperties.js ***!
  \***********************************************************/
/***/ ((module) => {

eval("/*!\n * Chai - getProperties utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/**\n * ### .getProperties(object)\n *\n * This allows the retrieval of property names of an object, enumerable or not,\n * inherited or not.\n *\n * @param {Object} object\n * @returns {Array}\n * @namespace Utils\n * @name getProperties\n * @api public\n */\n\nmodule.exports = function getProperties(object) {\n  var result = Object.getOwnPropertyNames(object);\n\n  function addProperty(property) {\n    if (result.indexOf(property) === -1) {\n      result.push(property);\n    }\n  }\n\n  var proto = Object.getPrototypeOf(object);\n  while (proto !== null) {\n    Object.getOwnPropertyNames(proto).forEach(addProperty);\n    proto = Object.getPrototypeOf(proto);\n  }\n\n  return result;\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/getProperties.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/index.js":
/*!***************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/index.js ***!
  \***************************************************/
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

eval("/*!\n * chai\n * Copyright(c) 2011 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/*!\n * Dependencies that are used for multiple exports are required here only once\n */\n\nvar pathval = __webpack_require__(/*! pathval */ \"./node_modules/pathval/index.js\");\n\n/*!\n * test utility\n */\n\nexports.test = __webpack_require__(/*! ./test */ \"./node_modules/chai/lib/chai/utils/test.js\");\n\n/*!\n * type utility\n */\n\nexports.type = __webpack_require__(/*! type-detect */ \"./node_modules/type-detect/type-detect.js\");\n\n/*!\n * expectTypes utility\n */\nexports.expectTypes = __webpack_require__(/*! ./expectTypes */ \"./node_modules/chai/lib/chai/utils/expectTypes.js\");\n\n/*!\n * message utility\n */\n\nexports.getMessage = __webpack_require__(/*! ./getMessage */ \"./node_modules/chai/lib/chai/utils/getMessage.js\");\n\n/*!\n * actual utility\n */\n\nexports.getActual = __webpack_require__(/*! ./getActual */ \"./node_modules/chai/lib/chai/utils/getActual.js\");\n\n/*!\n * Inspect util\n */\n\nexports.inspect = __webpack_require__(/*! ./inspect */ \"./node_modules/chai/lib/chai/utils/inspect.js\");\n\n/*!\n * Object Display util\n */\n\nexports.objDisplay = __webpack_require__(/*! ./objDisplay */ \"./node_modules/chai/lib/chai/utils/objDisplay.js\");\n\n/*!\n * Flag utility\n */\n\nexports.flag = __webpack_require__(/*! ./flag */ \"./node_modules/chai/lib/chai/utils/flag.js\");\n\n/*!\n * Flag transferring utility\n */\n\nexports.transferFlags = __webpack_require__(/*! ./transferFlags */ \"./node_modules/chai/lib/chai/utils/transferFlags.js\");\n\n/*!\n * Deep equal utility\n */\n\nexports.eql = __webpack_require__(/*! deep-eql */ \"./node_modules/deep-eql/index.js\");\n\n/*!\n * Deep path info\n */\n\nexports.getPathInfo = pathval.getPathInfo;\n\n/*!\n * Check if a property exists\n */\n\nexports.hasProperty = pathval.hasProperty;\n\n/*!\n * Function name\n */\n\nexports.getName = __webpack_require__(/*! get-func-name */ \"./node_modules/get-func-name/index.js\");\n\n/*!\n * add Property\n */\n\nexports.addProperty = __webpack_require__(/*! ./addProperty */ \"./node_modules/chai/lib/chai/utils/addProperty.js\");\n\n/*!\n * add Method\n */\n\nexports.addMethod = __webpack_require__(/*! ./addMethod */ \"./node_modules/chai/lib/chai/utils/addMethod.js\");\n\n/*!\n * overwrite Property\n */\n\nexports.overwriteProperty = __webpack_require__(/*! ./overwriteProperty */ \"./node_modules/chai/lib/chai/utils/overwriteProperty.js\");\n\n/*!\n * overwrite Method\n */\n\nexports.overwriteMethod = __webpack_require__(/*! ./overwriteMethod */ \"./node_modules/chai/lib/chai/utils/overwriteMethod.js\");\n\n/*!\n * Add a chainable method\n */\n\nexports.addChainableMethod = __webpack_require__(/*! ./addChainableMethod */ \"./node_modules/chai/lib/chai/utils/addChainableMethod.js\");\n\n/*!\n * Overwrite chainable method\n */\n\nexports.overwriteChainableMethod = __webpack_require__(/*! ./overwriteChainableMethod */ \"./node_modules/chai/lib/chai/utils/overwriteChainableMethod.js\");\n\n/*!\n * Compare by inspect method\n */\n\nexports.compareByInspect = __webpack_require__(/*! ./compareByInspect */ \"./node_modules/chai/lib/chai/utils/compareByInspect.js\");\n\n/*!\n * Get own enumerable property symbols method\n */\n\nexports.getOwnEnumerablePropertySymbols = __webpack_require__(/*! ./getOwnEnumerablePropertySymbols */ \"./node_modules/chai/lib/chai/utils/getOwnEnumerablePropertySymbols.js\");\n\n/*!\n * Get own enumerable properties method\n */\n\nexports.getOwnEnumerableProperties = __webpack_require__(/*! ./getOwnEnumerableProperties */ \"./node_modules/chai/lib/chai/utils/getOwnEnumerableProperties.js\");\n\n/*!\n * Checks error against a given set of criteria\n */\n\nexports.checkError = __webpack_require__(/*! check-error */ \"./node_modules/check-error/index.js\");\n\n/*!\n * Proxify util\n */\n\nexports.proxify = __webpack_require__(/*! ./proxify */ \"./node_modules/chai/lib/chai/utils/proxify.js\");\n\n/*!\n * addLengthGuard util\n */\n\nexports.addLengthGuard = __webpack_require__(/*! ./addLengthGuard */ \"./node_modules/chai/lib/chai/utils/addLengthGuard.js\");\n\n/*!\n * isProxyEnabled helper\n */\n\nexports.isProxyEnabled = __webpack_require__(/*! ./isProxyEnabled */ \"./node_modules/chai/lib/chai/utils/isProxyEnabled.js\");\n\n/*!\n * isNaN method\n */\n\nexports.isNaN = __webpack_require__(/*! ./isNaN */ \"./node_modules/chai/lib/chai/utils/isNaN.js\");\n\n/*!\n * getOperator method\n */\n\nexports.getOperator = __webpack_require__(/*! ./getOperator */ \"./node_modules/chai/lib/chai/utils/getOperator.js\");\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/index.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/inspect.js":
/*!*****************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/inspect.js ***!
  \*****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("// This is (almost) directly from Node.js utils\n// https://github.com/joyent/node/blob/f8c335d0caf47f16d31413f89aa28eda3878e3aa/lib/util.js\n\nvar getName = __webpack_require__(/*! get-func-name */ \"./node_modules/get-func-name/index.js\");\nvar loupe = __webpack_require__(/*! loupe */ \"./node_modules/loupe/loupe.js\");\nvar config = __webpack_require__(/*! ../config */ \"./node_modules/chai/lib/chai/config.js\");\n\nmodule.exports = inspect;\n\n/**\n * ### .inspect(obj, [showHidden], [depth], [colors])\n *\n * Echoes the value of a value. Tries to print the value out\n * in the best way possible given the different types.\n *\n * @param {Object} obj The object to print out.\n * @param {Boolean} showHidden Flag that shows hidden (not enumerable)\n *    properties of objects. Default is false.\n * @param {Number} depth Depth in which to descend in object. Default is 2.\n * @param {Boolean} colors Flag to turn on ANSI escape codes to color the\n *    output. Default is false (no coloring).\n * @namespace Utils\n * @name inspect\n */\nfunction inspect(obj, showHidden, depth, colors) {\n  var options = {\n    colors: colors,\n    depth: (typeof depth === 'undefined' ? 2 : depth),\n    showHidden: showHidden,\n    truncate: config.truncateThreshold ? config.truncateThreshold : Infinity,\n  };\n  return loupe.inspect(obj, options);\n}\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/inspect.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/isNaN.js":
/*!***************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/isNaN.js ***!
  \***************************************************/
/***/ ((module) => {

eval("/*!\n * Chai - isNaN utility\n * Copyright(c) 2012-2015 Sakthipriyan Vairamani <thechargingvolcano@gmail.com>\n * MIT Licensed\n */\n\n/**\n * ### .isNaN(value)\n *\n * Checks if the given value is NaN or not.\n *\n *     utils.isNaN(NaN); // true\n *\n * @param {Value} The value which has to be checked if it is NaN\n * @name isNaN\n * @api private\n */\n\nfunction isNaN(value) {\n  // Refer http://www.ecma-international.org/ecma-262/6.0/#sec-isnan-number\n  // section's NOTE.\n  return value !== value;\n}\n\n// If ECMAScript 6's Number.isNaN is present, prefer that.\nmodule.exports = Number.isNaN || isNaN;\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/isNaN.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/isProxyEnabled.js":
/*!************************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/isProxyEnabled.js ***!
  \************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("var config = __webpack_require__(/*! ../config */ \"./node_modules/chai/lib/chai/config.js\");\n\n/*!\n * Chai - isProxyEnabled helper\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/**\n * ### .isProxyEnabled()\n *\n * Helper function to check if Chai's proxy protection feature is enabled. If\n * proxies are unsupported or disabled via the user's Chai config, then return\n * false. Otherwise, return true.\n *\n * @namespace Utils\n * @name isProxyEnabled\n */\n\nmodule.exports = function isProxyEnabled() {\n  return config.useProxy &&\n    typeof Proxy !== 'undefined' &&\n    typeof Reflect !== 'undefined';\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/isProxyEnabled.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/objDisplay.js":
/*!********************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/objDisplay.js ***!
  \********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * Chai - flag utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/*!\n * Module dependencies\n */\n\nvar inspect = __webpack_require__(/*! ./inspect */ \"./node_modules/chai/lib/chai/utils/inspect.js\");\nvar config = __webpack_require__(/*! ../config */ \"./node_modules/chai/lib/chai/config.js\");\n\n/**\n * ### .objDisplay(object)\n *\n * Determines if an object or an array matches\n * criteria to be inspected in-line for error\n * messages or should be truncated.\n *\n * @param {Mixed} javascript object to inspect\n * @name objDisplay\n * @namespace Utils\n * @api public\n */\n\nmodule.exports = function objDisplay(obj) {\n  var str = inspect(obj)\n    , type = Object.prototype.toString.call(obj);\n\n  if (config.truncateThreshold && str.length >= config.truncateThreshold) {\n    if (type === '[object Function]') {\n      return !obj.name || obj.name === ''\n        ? '[Function]'\n        : '[Function: ' + obj.name + ']';\n    } else if (type === '[object Array]') {\n      return '[ Array(' + obj.length + ') ]';\n    } else if (type === '[object Object]') {\n      var keys = Object.keys(obj)\n        , kstr = keys.length > 2\n          ? keys.splice(0, 2).join(', ') + ', ...'\n          : keys.join(', ');\n      return '{ Object (' + kstr + ') }';\n    } else {\n      return str;\n    }\n  } else {\n    return str;\n  }\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/objDisplay.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/overwriteChainableMethod.js":
/*!**********************************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/overwriteChainableMethod.js ***!
  \**********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * Chai - overwriteChainableMethod utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\nvar chai = __webpack_require__(/*! ../../chai */ \"./node_modules/chai/lib/chai.js\");\nvar transferFlags = __webpack_require__(/*! ./transferFlags */ \"./node_modules/chai/lib/chai/utils/transferFlags.js\");\n\n/**\n * ### .overwriteChainableMethod(ctx, name, method, chainingBehavior)\n *\n * Overwrites an already existing chainable method\n * and provides access to the previous function or\n * property.  Must return functions to be used for\n * name.\n *\n *     utils.overwriteChainableMethod(chai.Assertion.prototype, 'lengthOf',\n *       function (_super) {\n *       }\n *     , function (_super) {\n *       }\n *     );\n *\n * Can also be accessed directly from `chai.Assertion`.\n *\n *     chai.Assertion.overwriteChainableMethod('foo', fn, fn);\n *\n * Then can be used as any other assertion.\n *\n *     expect(myFoo).to.have.lengthOf(3);\n *     expect(myFoo).to.have.lengthOf.above(3);\n *\n * @param {Object} ctx object whose method / property is to be overwritten\n * @param {String} name of method / property to overwrite\n * @param {Function} method function that returns a function to be used for name\n * @param {Function} chainingBehavior function that returns a function to be used for property\n * @namespace Utils\n * @name overwriteChainableMethod\n * @api public\n */\n\nmodule.exports = function overwriteChainableMethod(ctx, name, method, chainingBehavior) {\n  var chainableBehavior = ctx.__methods[name];\n\n  var _chainingBehavior = chainableBehavior.chainingBehavior;\n  chainableBehavior.chainingBehavior = function overwritingChainableMethodGetter() {\n    var result = chainingBehavior(_chainingBehavior).call(this);\n    if (result !== undefined) {\n      return result;\n    }\n\n    var newAssertion = new chai.Assertion();\n    transferFlags(this, newAssertion);\n    return newAssertion;\n  };\n\n  var _method = chainableBehavior.method;\n  chainableBehavior.method = function overwritingChainableMethodWrapper() {\n    var result = method(_method).apply(this, arguments);\n    if (result !== undefined) {\n      return result;\n    }\n\n    var newAssertion = new chai.Assertion();\n    transferFlags(this, newAssertion);\n    return newAssertion;\n  };\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/overwriteChainableMethod.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/overwriteMethod.js":
/*!*************************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/overwriteMethod.js ***!
  \*************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * Chai - overwriteMethod utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\nvar addLengthGuard = __webpack_require__(/*! ./addLengthGuard */ \"./node_modules/chai/lib/chai/utils/addLengthGuard.js\");\nvar chai = __webpack_require__(/*! ../../chai */ \"./node_modules/chai/lib/chai.js\");\nvar flag = __webpack_require__(/*! ./flag */ \"./node_modules/chai/lib/chai/utils/flag.js\");\nvar proxify = __webpack_require__(/*! ./proxify */ \"./node_modules/chai/lib/chai/utils/proxify.js\");\nvar transferFlags = __webpack_require__(/*! ./transferFlags */ \"./node_modules/chai/lib/chai/utils/transferFlags.js\");\n\n/**\n * ### .overwriteMethod(ctx, name, fn)\n *\n * Overwrites an already existing method and provides\n * access to previous function. Must return function\n * to be used for name.\n *\n *     utils.overwriteMethod(chai.Assertion.prototype, 'equal', function (_super) {\n *       return function (str) {\n *         var obj = utils.flag(this, 'object');\n *         if (obj instanceof Foo) {\n *           new chai.Assertion(obj.value).to.equal(str);\n *         } else {\n *           _super.apply(this, arguments);\n *         }\n *       }\n *     });\n *\n * Can also be accessed directly from `chai.Assertion`.\n *\n *     chai.Assertion.overwriteMethod('foo', fn);\n *\n * Then can be used as any other assertion.\n *\n *     expect(myFoo).to.equal('bar');\n *\n * @param {Object} ctx object whose method is to be overwritten\n * @param {String} name of method to overwrite\n * @param {Function} method function that returns a function to be used for name\n * @namespace Utils\n * @name overwriteMethod\n * @api public\n */\n\nmodule.exports = function overwriteMethod(ctx, name, method) {\n  var _method = ctx[name]\n    , _super = function () {\n      throw new Error(name + ' is not a function');\n    };\n\n  if (_method && 'function' === typeof _method)\n    _super = _method;\n\n  var overwritingMethodWrapper = function () {\n    // Setting the `ssfi` flag to `overwritingMethodWrapper` causes this\n    // function to be the starting point for removing implementation frames from\n    // the stack trace of a failed assertion.\n    //\n    // However, we only want to use this function as the starting point if the\n    // `lockSsfi` flag isn't set.\n    //\n    // If the `lockSsfi` flag is set, then either this assertion has been\n    // overwritten by another assertion, or this assertion is being invoked from\n    // inside of another assertion. In the first case, the `ssfi` flag has\n    // already been set by the overwriting assertion. In the second case, the\n    // `ssfi` flag has already been set by the outer assertion.\n    if (!flag(this, 'lockSsfi')) {\n      flag(this, 'ssfi', overwritingMethodWrapper);\n    }\n\n    // Setting the `lockSsfi` flag to `true` prevents the overwritten assertion\n    // from changing the `ssfi` flag. By this point, the `ssfi` flag is already\n    // set to the correct starting point for this assertion.\n    var origLockSsfi = flag(this, 'lockSsfi');\n    flag(this, 'lockSsfi', true);\n    var result = method(_super).apply(this, arguments);\n    flag(this, 'lockSsfi', origLockSsfi);\n\n    if (result !== undefined) {\n      return result;\n    }\n\n    var newAssertion = new chai.Assertion();\n    transferFlags(this, newAssertion);\n    return newAssertion;\n  }\n\n  addLengthGuard(overwritingMethodWrapper, name, false);\n  ctx[name] = proxify(overwritingMethodWrapper, name);\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/overwriteMethod.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/overwriteProperty.js":
/*!***************************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/overwriteProperty.js ***!
  \***************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * Chai - overwriteProperty utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\nvar chai = __webpack_require__(/*! ../../chai */ \"./node_modules/chai/lib/chai.js\");\nvar flag = __webpack_require__(/*! ./flag */ \"./node_modules/chai/lib/chai/utils/flag.js\");\nvar isProxyEnabled = __webpack_require__(/*! ./isProxyEnabled */ \"./node_modules/chai/lib/chai/utils/isProxyEnabled.js\");\nvar transferFlags = __webpack_require__(/*! ./transferFlags */ \"./node_modules/chai/lib/chai/utils/transferFlags.js\");\n\n/**\n * ### .overwriteProperty(ctx, name, fn)\n *\n * Overwrites an already existing property getter and provides\n * access to previous value. Must return function to use as getter.\n *\n *     utils.overwriteProperty(chai.Assertion.prototype, 'ok', function (_super) {\n *       return function () {\n *         var obj = utils.flag(this, 'object');\n *         if (obj instanceof Foo) {\n *           new chai.Assertion(obj.name).to.equal('bar');\n *         } else {\n *           _super.call(this);\n *         }\n *       }\n *     });\n *\n *\n * Can also be accessed directly from `chai.Assertion`.\n *\n *     chai.Assertion.overwriteProperty('foo', fn);\n *\n * Then can be used as any other assertion.\n *\n *     expect(myFoo).to.be.ok;\n *\n * @param {Object} ctx object whose property is to be overwritten\n * @param {String} name of property to overwrite\n * @param {Function} getter function that returns a getter function to be used for name\n * @namespace Utils\n * @name overwriteProperty\n * @api public\n */\n\nmodule.exports = function overwriteProperty(ctx, name, getter) {\n  var _get = Object.getOwnPropertyDescriptor(ctx, name)\n    , _super = function () {};\n\n  if (_get && 'function' === typeof _get.get)\n    _super = _get.get\n\n  Object.defineProperty(ctx, name,\n    { get: function overwritingPropertyGetter() {\n        // Setting the `ssfi` flag to `overwritingPropertyGetter` causes this\n        // function to be the starting point for removing implementation frames\n        // from the stack trace of a failed assertion.\n        //\n        // However, we only want to use this function as the starting point if\n        // the `lockSsfi` flag isn't set and proxy protection is disabled.\n        //\n        // If the `lockSsfi` flag is set, then either this assertion has been\n        // overwritten by another assertion, or this assertion is being invoked\n        // from inside of another assertion. In the first case, the `ssfi` flag\n        // has already been set by the overwriting assertion. In the second\n        // case, the `ssfi` flag has already been set by the outer assertion.\n        //\n        // If proxy protection is enabled, then the `ssfi` flag has already been\n        // set by the proxy getter.\n        if (!isProxyEnabled() && !flag(this, 'lockSsfi')) {\n          flag(this, 'ssfi', overwritingPropertyGetter);\n        }\n\n        // Setting the `lockSsfi` flag to `true` prevents the overwritten\n        // assertion from changing the `ssfi` flag. By this point, the `ssfi`\n        // flag is already set to the correct starting point for this assertion.\n        var origLockSsfi = flag(this, 'lockSsfi');\n        flag(this, 'lockSsfi', true);\n        var result = getter(_super).call(this);\n        flag(this, 'lockSsfi', origLockSsfi);\n\n        if (result !== undefined) {\n          return result;\n        }\n\n        var newAssertion = new chai.Assertion();\n        transferFlags(this, newAssertion);\n        return newAssertion;\n      }\n    , configurable: true\n  });\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/overwriteProperty.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/proxify.js":
/*!*****************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/proxify.js ***!
  \*****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("var config = __webpack_require__(/*! ../config */ \"./node_modules/chai/lib/chai/config.js\");\nvar flag = __webpack_require__(/*! ./flag */ \"./node_modules/chai/lib/chai/utils/flag.js\");\nvar getProperties = __webpack_require__(/*! ./getProperties */ \"./node_modules/chai/lib/chai/utils/getProperties.js\");\nvar isProxyEnabled = __webpack_require__(/*! ./isProxyEnabled */ \"./node_modules/chai/lib/chai/utils/isProxyEnabled.js\");\n\n/*!\n * Chai - proxify utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/**\n * ### .proxify(object)\n *\n * Return a proxy of given object that throws an error when a non-existent\n * property is read. By default, the root cause is assumed to be a misspelled\n * property, and thus an attempt is made to offer a reasonable suggestion from\n * the list of existing properties. However, if a nonChainableMethodName is\n * provided, then the root cause is instead a failure to invoke a non-chainable\n * method prior to reading the non-existent property.\n *\n * If proxies are unsupported or disabled via the user's Chai config, then\n * return object without modification.\n *\n * @param {Object} obj\n * @param {String} nonChainableMethodName\n * @namespace Utils\n * @name proxify\n */\n\nvar builtins = ['__flags', '__methods', '_obj', 'assert'];\n\nmodule.exports = function proxify(obj, nonChainableMethodName) {\n  if (!isProxyEnabled()) return obj;\n\n  return new Proxy(obj, {\n    get: function proxyGetter(target, property) {\n      // This check is here because we should not throw errors on Symbol properties\n      // such as `Symbol.toStringTag`.\n      // The values for which an error should be thrown can be configured using\n      // the `config.proxyExcludedKeys` setting.\n      if (typeof property === 'string' &&\n          config.proxyExcludedKeys.indexOf(property) === -1 &&\n          !Reflect.has(target, property)) {\n        // Special message for invalid property access of non-chainable methods.\n        if (nonChainableMethodName) {\n          throw Error('Invalid Chai property: ' + nonChainableMethodName + '.' +\n            property + '. See docs for proper usage of \"' +\n            nonChainableMethodName + '\".');\n        }\n\n        // If the property is reasonably close to an existing Chai property,\n        // suggest that property to the user. Only suggest properties with a\n        // distance less than 4.\n        var suggestion = null;\n        var suggestionDistance = 4;\n        getProperties(target).forEach(function(prop) {\n          if (\n            !Object.prototype.hasOwnProperty(prop) &&\n            builtins.indexOf(prop) === -1\n          ) {\n            var dist = stringDistanceCapped(\n              property,\n              prop,\n              suggestionDistance\n            );\n            if (dist < suggestionDistance) {\n              suggestion = prop;\n              suggestionDistance = dist;\n            }\n          }\n        });\n\n        if (suggestion !== null) {\n          throw Error('Invalid Chai property: ' + property +\n            '. Did you mean \"' + suggestion + '\"?');\n        } else {\n          throw Error('Invalid Chai property: ' + property);\n        }\n      }\n\n      // Use this proxy getter as the starting point for removing implementation\n      // frames from the stack trace of a failed assertion. For property\n      // assertions, this prevents the proxy getter from showing up in the stack\n      // trace since it's invoked before the property getter. For method and\n      // chainable method assertions, this flag will end up getting changed to\n      // the method wrapper, which is good since this frame will no longer be in\n      // the stack once the method is invoked. Note that Chai builtin assertion\n      // properties such as `__flags` are skipped since this is only meant to\n      // capture the starting point of an assertion. This step is also skipped\n      // if the `lockSsfi` flag is set, thus indicating that this assertion is\n      // being called from within another assertion. In that case, the `ssfi`\n      // flag is already set to the outer assertion's starting point.\n      if (builtins.indexOf(property) === -1 && !flag(target, 'lockSsfi')) {\n        flag(target, 'ssfi', proxyGetter);\n      }\n\n      return Reflect.get(target, property);\n    }\n  });\n};\n\n/**\n * # stringDistanceCapped(strA, strB, cap)\n * Return the Levenshtein distance between two strings, but no more than cap.\n * @param {string} strA\n * @param {string} strB\n * @param {number} number\n * @return {number} min(string distance between strA and strB, cap)\n * @api private\n */\n\nfunction stringDistanceCapped(strA, strB, cap) {\n  if (Math.abs(strA.length - strB.length) >= cap) {\n    return cap;\n  }\n\n  var memo = [];\n  // `memo` is a two-dimensional array containing distances.\n  // memo[i][j] is the distance between strA.slice(0, i) and\n  // strB.slice(0, j).\n  for (var i = 0; i <= strA.length; i++) {\n    memo[i] = Array(strB.length + 1).fill(0);\n    memo[i][0] = i;\n  }\n  for (var j = 0; j < strB.length; j++) {\n    memo[0][j] = j;\n  }\n\n  for (var i = 1; i <= strA.length; i++) {\n    var ch = strA.charCodeAt(i - 1);\n    for (var j = 1; j <= strB.length; j++) {\n      if (Math.abs(i - j) >= cap) {\n        memo[i][j] = cap;\n        continue;\n      }\n      memo[i][j] = Math.min(\n        memo[i - 1][j] + 1,\n        memo[i][j - 1] + 1,\n        memo[i - 1][j - 1] +\n          (ch === strB.charCodeAt(j - 1) ? 0 : 1)\n      );\n    }\n  }\n\n  return memo[strA.length][strB.length];\n}\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/proxify.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/test.js":
/*!**************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/test.js ***!
  \**************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("/*!\n * Chai - test utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/*!\n * Module dependencies\n */\n\nvar flag = __webpack_require__(/*! ./flag */ \"./node_modules/chai/lib/chai/utils/flag.js\");\n\n/**\n * ### .test(object, expression)\n *\n * Test and object for expression.\n *\n * @param {Object} object (constructed Assertion)\n * @param {Arguments} chai.Assertion.prototype.assert arguments\n * @namespace Utils\n * @name test\n */\n\nmodule.exports = function test(obj, args) {\n  var negate = flag(obj, 'negate')\n    , expr = args[0];\n  return negate ? !expr : expr;\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/test.js?");

/***/ }),

/***/ "./node_modules/chai/lib/chai/utils/transferFlags.js":
/*!***********************************************************!*\
  !*** ./node_modules/chai/lib/chai/utils/transferFlags.js ***!
  \***********************************************************/
/***/ ((module) => {

eval("/*!\n * Chai - transferFlags utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/**\n * ### .transferFlags(assertion, object, includeAll = true)\n *\n * Transfer all the flags for `assertion` to `object`. If\n * `includeAll` is set to `false`, then the base Chai\n * assertion flags (namely `object`, `ssfi`, `lockSsfi`,\n * and `message`) will not be transferred.\n *\n *\n *     var newAssertion = new Assertion();\n *     utils.transferFlags(assertion, newAssertion);\n *\n *     var anotherAssertion = new Assertion(myObj);\n *     utils.transferFlags(assertion, anotherAssertion, false);\n *\n * @param {Assertion} assertion the assertion to transfer the flags from\n * @param {Object} object the object to transfer the flags to; usually a new assertion\n * @param {Boolean} includeAll\n * @namespace Utils\n * @name transferFlags\n * @api private\n */\n\nmodule.exports = function transferFlags(assertion, object, includeAll) {\n  var flags = assertion.__flags || (assertion.__flags = Object.create(null));\n\n  if (!object.__flags) {\n    object.__flags = Object.create(null);\n  }\n\n  includeAll = arguments.length === 3 ? includeAll : true;\n\n  for (var flag in flags) {\n    if (includeAll ||\n        (flag !== 'object' && flag !== 'ssfi' && flag !== 'lockSsfi' && flag != 'message')) {\n      object.__flags[flag] = flags[flag];\n    }\n  }\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/lib/chai/utils/transferFlags.js?");

/***/ }),

/***/ "./node_modules/check-error/index.js":
/*!*******************************************!*\
  !*** ./node_modules/check-error/index.js ***!
  \*******************************************/
/***/ ((module) => {

"use strict";
eval("\n\n/* !\n * Chai - checkError utility\n * Copyright(c) 2012-2016 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/**\n * ### .checkError\n *\n * Checks that an error conforms to a given set of criteria and/or retrieves information about it.\n *\n * @api public\n */\n\n/**\n * ### .compatibleInstance(thrown, errorLike)\n *\n * Checks if two instances are compatible (strict equal).\n * Returns false if errorLike is not an instance of Error, because instances\n * can only be compatible if they're both error instances.\n *\n * @name compatibleInstance\n * @param {Error} thrown error\n * @param {Error|ErrorConstructor} errorLike object to compare against\n * @namespace Utils\n * @api public\n */\n\nfunction compatibleInstance(thrown, errorLike) {\n  return errorLike instanceof Error && thrown === errorLike;\n}\n\n/**\n * ### .compatibleConstructor(thrown, errorLike)\n *\n * Checks if two constructors are compatible.\n * This function can receive either an error constructor or\n * an error instance as the `errorLike` argument.\n * Constructors are compatible if they're the same or if one is\n * an instance of another.\n *\n * @name compatibleConstructor\n * @param {Error} thrown error\n * @param {Error|ErrorConstructor} errorLike object to compare against\n * @namespace Utils\n * @api public\n */\n\nfunction compatibleConstructor(thrown, errorLike) {\n  if (errorLike instanceof Error) {\n    // If `errorLike` is an instance of any error we compare their constructors\n    return thrown.constructor === errorLike.constructor || thrown instanceof errorLike.constructor;\n  } else if (errorLike.prototype instanceof Error || errorLike === Error) {\n    // If `errorLike` is a constructor that inherits from Error, we compare `thrown` to `errorLike` directly\n    return thrown.constructor === errorLike || thrown instanceof errorLike;\n  }\n\n  return false;\n}\n\n/**\n * ### .compatibleMessage(thrown, errMatcher)\n *\n * Checks if an error's message is compatible with a matcher (String or RegExp).\n * If the message contains the String or passes the RegExp test,\n * it is considered compatible.\n *\n * @name compatibleMessage\n * @param {Error} thrown error\n * @param {String|RegExp} errMatcher to look for into the message\n * @namespace Utils\n * @api public\n */\n\nfunction compatibleMessage(thrown, errMatcher) {\n  var comparisonString = typeof thrown === 'string' ? thrown : thrown.message;\n  if (errMatcher instanceof RegExp) {\n    return errMatcher.test(comparisonString);\n  } else if (typeof errMatcher === 'string') {\n    return comparisonString.indexOf(errMatcher) !== -1; // eslint-disable-line no-magic-numbers\n  }\n\n  return false;\n}\n\n/**\n * ### .getFunctionName(constructorFn)\n *\n * Returns the name of a function.\n * This also includes a polyfill function if `constructorFn.name` is not defined.\n *\n * @name getFunctionName\n * @param {Function} constructorFn\n * @namespace Utils\n * @api private\n */\n\nvar functionNameMatch = /\\s*function(?:\\s|\\s*\\/\\*[^(?:*\\/)]+\\*\\/\\s*)*([^\\(\\/]+)/;\nfunction getFunctionName(constructorFn) {\n  var name = '';\n  if (typeof constructorFn.name === 'undefined') {\n    // Here we run a polyfill if constructorFn.name is not defined\n    var match = String(constructorFn).match(functionNameMatch);\n    if (match) {\n      name = match[1];\n    }\n  } else {\n    name = constructorFn.name;\n  }\n\n  return name;\n}\n\n/**\n * ### .getConstructorName(errorLike)\n *\n * Gets the constructor name for an Error instance or constructor itself.\n *\n * @name getConstructorName\n * @param {Error|ErrorConstructor} errorLike\n * @namespace Utils\n * @api public\n */\n\nfunction getConstructorName(errorLike) {\n  var constructorName = errorLike;\n  if (errorLike instanceof Error) {\n    constructorName = getFunctionName(errorLike.constructor);\n  } else if (typeof errorLike === 'function') {\n    // If `err` is not an instance of Error it is an error constructor itself or another function.\n    // If we've got a common function we get its name, otherwise we may need to create a new instance\n    // of the error just in case it's a poorly-constructed error. Please see chaijs/chai/issues/45 to know more.\n    constructorName = getFunctionName(errorLike).trim() ||\n        getFunctionName(new errorLike()); // eslint-disable-line new-cap\n  }\n\n  return constructorName;\n}\n\n/**\n * ### .getMessage(errorLike)\n *\n * Gets the error message from an error.\n * If `err` is a String itself, we return it.\n * If the error has no message, we return an empty string.\n *\n * @name getMessage\n * @param {Error|String} errorLike\n * @namespace Utils\n * @api public\n */\n\nfunction getMessage(errorLike) {\n  var msg = '';\n  if (errorLike && errorLike.message) {\n    msg = errorLike.message;\n  } else if (typeof errorLike === 'string') {\n    msg = errorLike;\n  }\n\n  return msg;\n}\n\nmodule.exports = {\n  compatibleInstance: compatibleInstance,\n  compatibleConstructor: compatibleConstructor,\n  compatibleMessage: compatibleMessage,\n  getMessage: getMessage,\n  getConstructorName: getConstructorName,\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/check-error/index.js?");

/***/ }),

/***/ "./node_modules/deep-eql/index.js":
/*!****************************************!*\
  !*** ./node_modules/deep-eql/index.js ***!
  \****************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";
eval("\n/* globals Symbol: false, Uint8Array: false, WeakMap: false */\n/*!\n * deep-eql\n * Copyright(c) 2013 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\nvar type = __webpack_require__(/*! type-detect */ \"./node_modules/type-detect/type-detect.js\");\nfunction FakeMap() {\n  this._key = 'chai/deep-eql__' + Math.random() + Date.now();\n}\n\nFakeMap.prototype = {\n  get: function getMap(key) {\n    return key[this._key];\n  },\n  set: function setMap(key, value) {\n    if (Object.isExtensible(key)) {\n      Object.defineProperty(key, this._key, {\n        value: value,\n        configurable: true,\n      });\n    }\n  },\n};\n\nvar MemoizeMap = typeof WeakMap === 'function' ? WeakMap : FakeMap;\n/*!\n * Check to see if the MemoizeMap has recorded a result of the two operands\n *\n * @param {Mixed} leftHandOperand\n * @param {Mixed} rightHandOperand\n * @param {MemoizeMap} memoizeMap\n * @returns {Boolean|null} result\n*/\nfunction memoizeCompare(leftHandOperand, rightHandOperand, memoizeMap) {\n  // Technically, WeakMap keys can *only* be objects, not primitives.\n  if (!memoizeMap || isPrimitive(leftHandOperand) || isPrimitive(rightHandOperand)) {\n    return null;\n  }\n  var leftHandMap = memoizeMap.get(leftHandOperand);\n  if (leftHandMap) {\n    var result = leftHandMap.get(rightHandOperand);\n    if (typeof result === 'boolean') {\n      return result;\n    }\n  }\n  return null;\n}\n\n/*!\n * Set the result of the equality into the MemoizeMap\n *\n * @param {Mixed} leftHandOperand\n * @param {Mixed} rightHandOperand\n * @param {MemoizeMap} memoizeMap\n * @param {Boolean} result\n*/\nfunction memoizeSet(leftHandOperand, rightHandOperand, memoizeMap, result) {\n  // Technically, WeakMap keys can *only* be objects, not primitives.\n  if (!memoizeMap || isPrimitive(leftHandOperand) || isPrimitive(rightHandOperand)) {\n    return;\n  }\n  var leftHandMap = memoizeMap.get(leftHandOperand);\n  if (leftHandMap) {\n    leftHandMap.set(rightHandOperand, result);\n  } else {\n    leftHandMap = new MemoizeMap();\n    leftHandMap.set(rightHandOperand, result);\n    memoizeMap.set(leftHandOperand, leftHandMap);\n  }\n}\n\n/*!\n * Primary Export\n */\n\nmodule.exports = deepEqual;\nmodule.exports.MemoizeMap = MemoizeMap;\n\n/**\n * Assert deeply nested sameValue equality between two objects of any type.\n *\n * @param {Mixed} leftHandOperand\n * @param {Mixed} rightHandOperand\n * @param {Object} [options] (optional) Additional options\n * @param {Array} [options.comparator] (optional) Override default algorithm, determining custom equality.\n * @param {Array} [options.memoize] (optional) Provide a custom memoization object which will cache the results of\n    complex objects for a speed boost. By passing `false` you can disable memoization, but this will cause circular\n    references to blow the stack.\n * @return {Boolean} equal match\n */\nfunction deepEqual(leftHandOperand, rightHandOperand, options) {\n  // If we have a comparator, we can't assume anything; so bail to its check first.\n  if (options && options.comparator) {\n    return extensiveDeepEqual(leftHandOperand, rightHandOperand, options);\n  }\n\n  var simpleResult = simpleEqual(leftHandOperand, rightHandOperand);\n  if (simpleResult !== null) {\n    return simpleResult;\n  }\n\n  // Deeper comparisons are pushed through to a larger function\n  return extensiveDeepEqual(leftHandOperand, rightHandOperand, options);\n}\n\n/**\n * Many comparisons can be canceled out early via simple equality or primitive checks.\n * @param {Mixed} leftHandOperand\n * @param {Mixed} rightHandOperand\n * @return {Boolean|null} equal match\n */\nfunction simpleEqual(leftHandOperand, rightHandOperand) {\n  // Equal references (except for Numbers) can be returned early\n  if (leftHandOperand === rightHandOperand) {\n    // Handle +-0 cases\n    return leftHandOperand !== 0 || 1 / leftHandOperand === 1 / rightHandOperand;\n  }\n\n  // handle NaN cases\n  if (\n    leftHandOperand !== leftHandOperand && // eslint-disable-line no-self-compare\n    rightHandOperand !== rightHandOperand // eslint-disable-line no-self-compare\n  ) {\n    return true;\n  }\n\n  // Anything that is not an 'object', i.e. symbols, functions, booleans, numbers,\n  // strings, and undefined, can be compared by reference.\n  if (isPrimitive(leftHandOperand) || isPrimitive(rightHandOperand)) {\n    // Easy out b/c it would have passed the first equality check\n    return false;\n  }\n  return null;\n}\n\n/*!\n * The main logic of the `deepEqual` function.\n *\n * @param {Mixed} leftHandOperand\n * @param {Mixed} rightHandOperand\n * @param {Object} [options] (optional) Additional options\n * @param {Array} [options.comparator] (optional) Override default algorithm, determining custom equality.\n * @param {Array} [options.memoize] (optional) Provide a custom memoization object which will cache the results of\n    complex objects for a speed boost. By passing `false` you can disable memoization, but this will cause circular\n    references to blow the stack.\n * @return {Boolean} equal match\n*/\nfunction extensiveDeepEqual(leftHandOperand, rightHandOperand, options) {\n  options = options || {};\n  options.memoize = options.memoize === false ? false : options.memoize || new MemoizeMap();\n  var comparator = options && options.comparator;\n\n  // Check if a memoized result exists.\n  var memoizeResultLeft = memoizeCompare(leftHandOperand, rightHandOperand, options.memoize);\n  if (memoizeResultLeft !== null) {\n    return memoizeResultLeft;\n  }\n  var memoizeResultRight = memoizeCompare(rightHandOperand, leftHandOperand, options.memoize);\n  if (memoizeResultRight !== null) {\n    return memoizeResultRight;\n  }\n\n  // If a comparator is present, use it.\n  if (comparator) {\n    var comparatorResult = comparator(leftHandOperand, rightHandOperand);\n    // Comparators may return null, in which case we want to go back to default behavior.\n    if (comparatorResult === false || comparatorResult === true) {\n      memoizeSet(leftHandOperand, rightHandOperand, options.memoize, comparatorResult);\n      return comparatorResult;\n    }\n    // To allow comparators to override *any* behavior, we ran them first. Since it didn't decide\n    // what to do, we need to make sure to return the basic tests first before we move on.\n    var simpleResult = simpleEqual(leftHandOperand, rightHandOperand);\n    if (simpleResult !== null) {\n      // Don't memoize this, it takes longer to set/retrieve than to just compare.\n      return simpleResult;\n    }\n  }\n\n  var leftHandType = type(leftHandOperand);\n  if (leftHandType !== type(rightHandOperand)) {\n    memoizeSet(leftHandOperand, rightHandOperand, options.memoize, false);\n    return false;\n  }\n\n  // Temporarily set the operands in the memoize object to prevent blowing the stack\n  memoizeSet(leftHandOperand, rightHandOperand, options.memoize, true);\n\n  var result = extensiveDeepEqualByType(leftHandOperand, rightHandOperand, leftHandType, options);\n  memoizeSet(leftHandOperand, rightHandOperand, options.memoize, result);\n  return result;\n}\n\nfunction extensiveDeepEqualByType(leftHandOperand, rightHandOperand, leftHandType, options) {\n  switch (leftHandType) {\n    case 'String':\n    case 'Number':\n    case 'Boolean':\n    case 'Date':\n      // If these types are their instance types (e.g. `new Number`) then re-deepEqual against their values\n      return deepEqual(leftHandOperand.valueOf(), rightHandOperand.valueOf());\n    case 'Promise':\n    case 'Symbol':\n    case 'function':\n    case 'WeakMap':\n    case 'WeakSet':\n    case 'Error':\n      return leftHandOperand === rightHandOperand;\n    case 'Arguments':\n    case 'Int8Array':\n    case 'Uint8Array':\n    case 'Uint8ClampedArray':\n    case 'Int16Array':\n    case 'Uint16Array':\n    case 'Int32Array':\n    case 'Uint32Array':\n    case 'Float32Array':\n    case 'Float64Array':\n    case 'Array':\n      return iterableEqual(leftHandOperand, rightHandOperand, options);\n    case 'RegExp':\n      return regexpEqual(leftHandOperand, rightHandOperand);\n    case 'Generator':\n      return generatorEqual(leftHandOperand, rightHandOperand, options);\n    case 'DataView':\n      return iterableEqual(new Uint8Array(leftHandOperand.buffer), new Uint8Array(rightHandOperand.buffer), options);\n    case 'ArrayBuffer':\n      return iterableEqual(new Uint8Array(leftHandOperand), new Uint8Array(rightHandOperand), options);\n    case 'Set':\n      return entriesEqual(leftHandOperand, rightHandOperand, options);\n    case 'Map':\n      return entriesEqual(leftHandOperand, rightHandOperand, options);\n    default:\n      return objectEqual(leftHandOperand, rightHandOperand, options);\n  }\n}\n\n/*!\n * Compare two Regular Expressions for equality.\n *\n * @param {RegExp} leftHandOperand\n * @param {RegExp} rightHandOperand\n * @return {Boolean} result\n */\n\nfunction regexpEqual(leftHandOperand, rightHandOperand) {\n  return leftHandOperand.toString() === rightHandOperand.toString();\n}\n\n/*!\n * Compare two Sets/Maps for equality. Faster than other equality functions.\n *\n * @param {Set} leftHandOperand\n * @param {Set} rightHandOperand\n * @param {Object} [options] (Optional)\n * @return {Boolean} result\n */\n\nfunction entriesEqual(leftHandOperand, rightHandOperand, options) {\n  // IE11 doesn't support Set#entries or Set#@@iterator, so we need manually populate using Set#forEach\n  if (leftHandOperand.size !== rightHandOperand.size) {\n    return false;\n  }\n  if (leftHandOperand.size === 0) {\n    return true;\n  }\n  var leftHandItems = [];\n  var rightHandItems = [];\n  leftHandOperand.forEach(function gatherEntries(key, value) {\n    leftHandItems.push([ key, value ]);\n  });\n  rightHandOperand.forEach(function gatherEntries(key, value) {\n    rightHandItems.push([ key, value ]);\n  });\n  return iterableEqual(leftHandItems.sort(), rightHandItems.sort(), options);\n}\n\n/*!\n * Simple equality for flat iterable objects such as Arrays, TypedArrays or Node.js buffers.\n *\n * @param {Iterable} leftHandOperand\n * @param {Iterable} rightHandOperand\n * @param {Object} [options] (Optional)\n * @return {Boolean} result\n */\n\nfunction iterableEqual(leftHandOperand, rightHandOperand, options) {\n  var length = leftHandOperand.length;\n  if (length !== rightHandOperand.length) {\n    return false;\n  }\n  if (length === 0) {\n    return true;\n  }\n  var index = -1;\n  while (++index < length) {\n    if (deepEqual(leftHandOperand[index], rightHandOperand[index], options) === false) {\n      return false;\n    }\n  }\n  return true;\n}\n\n/*!\n * Simple equality for generator objects such as those returned by generator functions.\n *\n * @param {Iterable} leftHandOperand\n * @param {Iterable} rightHandOperand\n * @param {Object} [options] (Optional)\n * @return {Boolean} result\n */\n\nfunction generatorEqual(leftHandOperand, rightHandOperand, options) {\n  return iterableEqual(getGeneratorEntries(leftHandOperand), getGeneratorEntries(rightHandOperand), options);\n}\n\n/*!\n * Determine if the given object has an @@iterator function.\n *\n * @param {Object} target\n * @return {Boolean} `true` if the object has an @@iterator function.\n */\nfunction hasIteratorFunction(target) {\n  return typeof Symbol !== 'undefined' &&\n    typeof target === 'object' &&\n    typeof Symbol.iterator !== 'undefined' &&\n    typeof target[Symbol.iterator] === 'function';\n}\n\n/*!\n * Gets all iterator entries from the given Object. If the Object has no @@iterator function, returns an empty array.\n * This will consume the iterator - which could have side effects depending on the @@iterator implementation.\n *\n * @param {Object} target\n * @returns {Array} an array of entries from the @@iterator function\n */\nfunction getIteratorEntries(target) {\n  if (hasIteratorFunction(target)) {\n    try {\n      return getGeneratorEntries(target[Symbol.iterator]());\n    } catch (iteratorError) {\n      return [];\n    }\n  }\n  return [];\n}\n\n/*!\n * Gets all entries from a Generator. This will consume the generator - which could have side effects.\n *\n * @param {Generator} target\n * @returns {Array} an array of entries from the Generator.\n */\nfunction getGeneratorEntries(generator) {\n  var generatorResult = generator.next();\n  var accumulator = [ generatorResult.value ];\n  while (generatorResult.done === false) {\n    generatorResult = generator.next();\n    accumulator.push(generatorResult.value);\n  }\n  return accumulator;\n}\n\n/*!\n * Gets all own and inherited enumerable keys from a target.\n *\n * @param {Object} target\n * @returns {Array} an array of own and inherited enumerable keys from the target.\n */\nfunction getEnumerableKeys(target) {\n  var keys = [];\n  for (var key in target) {\n    keys.push(key);\n  }\n  return keys;\n}\n\n/*!\n * Determines if two objects have matching values, given a set of keys. Defers to deepEqual for the equality check of\n * each key. If any value of the given key is not equal, the function will return false (early).\n *\n * @param {Mixed} leftHandOperand\n * @param {Mixed} rightHandOperand\n * @param {Array} keys An array of keys to compare the values of leftHandOperand and rightHandOperand against\n * @param {Object} [options] (Optional)\n * @return {Boolean} result\n */\nfunction keysEqual(leftHandOperand, rightHandOperand, keys, options) {\n  var length = keys.length;\n  if (length === 0) {\n    return true;\n  }\n  for (var i = 0; i < length; i += 1) {\n    if (deepEqual(leftHandOperand[keys[i]], rightHandOperand[keys[i]], options) === false) {\n      return false;\n    }\n  }\n  return true;\n}\n\n/*!\n * Recursively check the equality of two Objects. Once basic sameness has been established it will defer to `deepEqual`\n * for each enumerable key in the object.\n *\n * @param {Mixed} leftHandOperand\n * @param {Mixed} rightHandOperand\n * @param {Object} [options] (Optional)\n * @return {Boolean} result\n */\n\nfunction objectEqual(leftHandOperand, rightHandOperand, options) {\n  var leftHandKeys = getEnumerableKeys(leftHandOperand);\n  var rightHandKeys = getEnumerableKeys(rightHandOperand);\n  if (leftHandKeys.length && leftHandKeys.length === rightHandKeys.length) {\n    leftHandKeys.sort();\n    rightHandKeys.sort();\n    if (iterableEqual(leftHandKeys, rightHandKeys) === false) {\n      return false;\n    }\n    return keysEqual(leftHandOperand, rightHandOperand, leftHandKeys, options);\n  }\n\n  var leftHandEntries = getIteratorEntries(leftHandOperand);\n  var rightHandEntries = getIteratorEntries(rightHandOperand);\n  if (leftHandEntries.length && leftHandEntries.length === rightHandEntries.length) {\n    leftHandEntries.sort();\n    rightHandEntries.sort();\n    return iterableEqual(leftHandEntries, rightHandEntries, options);\n  }\n\n  if (leftHandKeys.length === 0 &&\n      leftHandEntries.length === 0 &&\n      rightHandKeys.length === 0 &&\n      rightHandEntries.length === 0) {\n    return true;\n  }\n\n  return false;\n}\n\n/*!\n * Returns true if the argument is a primitive.\n *\n * This intentionally returns true for all objects that can be compared by reference,\n * including functions and symbols.\n *\n * @param {Mixed} value\n * @return {Boolean} result\n */\nfunction isPrimitive(value) {\n  return value === null || typeof value !== 'object';\n}\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/deep-eql/index.js?");

/***/ }),

/***/ "./node_modules/get-func-name/index.js":
/*!*********************************************!*\
  !*** ./node_modules/get-func-name/index.js ***!
  \*********************************************/
/***/ ((module) => {

"use strict";
eval("\n\n/* !\n * Chai - getFuncName utility\n * Copyright(c) 2012-2016 Jake Luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\n\n/**\n * ### .getFuncName(constructorFn)\n *\n * Returns the name of a function.\n * When a non-function instance is passed, returns `null`.\n * This also includes a polyfill function if `aFunc.name` is not defined.\n *\n * @name getFuncName\n * @param {Function} funct\n * @namespace Utils\n * @api public\n */\n\nvar toString = Function.prototype.toString;\nvar functionNameMatch = /\\s*function(?:\\s|\\s*\\/\\*[^(?:*\\/)]+\\*\\/\\s*)*([^\\s\\(\\/]+)/;\nfunction getFuncName(aFunc) {\n  if (typeof aFunc !== 'function') {\n    return null;\n  }\n\n  var name = '';\n  if (typeof Function.prototype.name === 'undefined' && typeof aFunc.name === 'undefined') {\n    // Here we run a polyfill if Function does not support the `name` property and if aFunc.name is not defined\n    var match = toString.call(aFunc).match(functionNameMatch);\n    if (match) {\n      name = match[1];\n    }\n  } else {\n    // If we've got a `name` property we just use it\n    name = aFunc.name;\n  }\n\n  return name;\n}\n\nmodule.exports = getFuncName;\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/get-func-name/index.js?");

/***/ }),

/***/ "./node_modules/loupe/loupe.js":
/*!*************************************!*\
  !*** ./node_modules/loupe/loupe.js ***!
  \*************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("(function (global, factory) {\n   true ? factory(exports) :\n  0;\n}(this, (function (exports) { 'use strict';\n\n  function _typeof(obj) {\n    \"@babel/helpers - typeof\";\n\n    if (typeof Symbol === \"function\" && typeof Symbol.iterator === \"symbol\") {\n      _typeof = function (obj) {\n        return typeof obj;\n      };\n    } else {\n      _typeof = function (obj) {\n        return obj && typeof Symbol === \"function\" && obj.constructor === Symbol && obj !== Symbol.prototype ? \"symbol\" : typeof obj;\n      };\n    }\n\n    return _typeof(obj);\n  }\n\n  function _slicedToArray(arr, i) {\n    return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest();\n  }\n\n  function _arrayWithHoles(arr) {\n    if (Array.isArray(arr)) return arr;\n  }\n\n  function _iterableToArrayLimit(arr, i) {\n    if (typeof Symbol === \"undefined\" || !(Symbol.iterator in Object(arr))) return;\n    var _arr = [];\n    var _n = true;\n    var _d = false;\n    var _e = undefined;\n\n    try {\n      for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) {\n        _arr.push(_s.value);\n\n        if (i && _arr.length === i) break;\n      }\n    } catch (err) {\n      _d = true;\n      _e = err;\n    } finally {\n      try {\n        if (!_n && _i[\"return\"] != null) _i[\"return\"]();\n      } finally {\n        if (_d) throw _e;\n      }\n    }\n\n    return _arr;\n  }\n\n  function _unsupportedIterableToArray(o, minLen) {\n    if (!o) return;\n    if (typeof o === \"string\") return _arrayLikeToArray(o, minLen);\n    var n = Object.prototype.toString.call(o).slice(8, -1);\n    if (n === \"Object\" && o.constructor) n = o.constructor.name;\n    if (n === \"Map\" || n === \"Set\") return Array.from(o);\n    if (n === \"Arguments\" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen);\n  }\n\n  function _arrayLikeToArray(arr, len) {\n    if (len == null || len > arr.length) len = arr.length;\n\n    for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i];\n\n    return arr2;\n  }\n\n  function _nonIterableRest() {\n    throw new TypeError(\"Invalid attempt to destructure non-iterable instance.\\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.\");\n  }\n\n  var ansiColors = {\n    bold: ['1', '22'],\n    dim: ['2', '22'],\n    italic: ['3', '23'],\n    underline: ['4', '24'],\n    // 5 & 6 are blinking\n    inverse: ['7', '27'],\n    hidden: ['8', '28'],\n    strike: ['9', '29'],\n    // 10-20 are fonts\n    // 21-29 are resets for 1-9\n    black: ['30', '39'],\n    red: ['31', '39'],\n    green: ['32', '39'],\n    yellow: ['33', '39'],\n    blue: ['34', '39'],\n    magenta: ['35', '39'],\n    cyan: ['36', '39'],\n    white: ['37', '39'],\n    brightblack: ['30;1', '39'],\n    brightred: ['31;1', '39'],\n    brightgreen: ['32;1', '39'],\n    brightyellow: ['33;1', '39'],\n    brightblue: ['34;1', '39'],\n    brightmagenta: ['35;1', '39'],\n    brightcyan: ['36;1', '39'],\n    brightwhite: ['37;1', '39'],\n    grey: ['90', '39']\n  };\n  var styles = {\n    special: 'cyan',\n    number: 'yellow',\n    bigint: 'yellow',\n    boolean: 'yellow',\n    undefined: 'grey',\n    null: 'bold',\n    string: 'green',\n    symbol: 'green',\n    date: 'magenta',\n    regexp: 'red'\n  };\n  var truncator = '…';\n\n  function colorise(value, styleType) {\n    var color = ansiColors[styles[styleType]] || ansiColors[styleType];\n\n    if (!color) {\n      return String(value);\n    }\n\n    return \"\\x1B[\".concat(color[0], \"m\").concat(String(value), \"\\x1B[\").concat(color[1], \"m\");\n  }\n\n  function normaliseOptions() {\n    var _ref = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},\n        _ref$showHidden = _ref.showHidden,\n        showHidden = _ref$showHidden === void 0 ? false : _ref$showHidden,\n        _ref$depth = _ref.depth,\n        depth = _ref$depth === void 0 ? 2 : _ref$depth,\n        _ref$colors = _ref.colors,\n        colors = _ref$colors === void 0 ? false : _ref$colors,\n        _ref$customInspect = _ref.customInspect,\n        customInspect = _ref$customInspect === void 0 ? true : _ref$customInspect,\n        _ref$showProxy = _ref.showProxy,\n        showProxy = _ref$showProxy === void 0 ? false : _ref$showProxy,\n        _ref$maxArrayLength = _ref.maxArrayLength,\n        maxArrayLength = _ref$maxArrayLength === void 0 ? Infinity : _ref$maxArrayLength,\n        _ref$breakLength = _ref.breakLength,\n        breakLength = _ref$breakLength === void 0 ? Infinity : _ref$breakLength,\n        _ref$seen = _ref.seen,\n        seen = _ref$seen === void 0 ? [] : _ref$seen,\n        _ref$truncate = _ref.truncate,\n        truncate = _ref$truncate === void 0 ? Infinity : _ref$truncate,\n        _ref$stylize = _ref.stylize,\n        stylize = _ref$stylize === void 0 ? String : _ref$stylize;\n\n    var options = {\n      showHidden: Boolean(showHidden),\n      depth: Number(depth),\n      colors: Boolean(colors),\n      customInspect: Boolean(customInspect),\n      showProxy: Boolean(showProxy),\n      maxArrayLength: Number(maxArrayLength),\n      breakLength: Number(breakLength),\n      truncate: Number(truncate),\n      seen: seen,\n      stylize: stylize\n    };\n\n    if (options.colors) {\n      options.stylize = colorise;\n    }\n\n    return options;\n  }\n  function truncate(string, length) {\n    var tail = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : truncator;\n    string = String(string);\n    var tailLength = tail.length;\n    var stringLength = string.length;\n\n    if (tailLength > length && stringLength > tailLength) {\n      return tail;\n    }\n\n    if (stringLength > length && stringLength > tailLength) {\n      return \"\".concat(string.slice(0, length - tailLength)).concat(tail);\n    }\n\n    return string;\n  } // eslint-disable-next-line complexity\n\n  function inspectList(list, options, inspectItem) {\n    var separator = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : ', ';\n    inspectItem = inspectItem || options.inspect;\n    var size = list.length;\n    if (size === 0) return '';\n    var originalLength = options.truncate;\n    var output = '';\n    var peek = '';\n    var truncated = '';\n\n    for (var i = 0; i < size; i += 1) {\n      var last = i + 1 === list.length;\n      var secondToLast = i + 2 === list.length;\n      truncated = \"\".concat(truncator, \"(\").concat(list.length - i, \")\");\n      var value = list[i]; // If there is more than one remaining we need to account for a separator of `, `\n\n      options.truncate = originalLength - output.length - (last ? 0 : separator.length);\n      var string = peek || inspectItem(value, options) + (last ? '' : separator);\n      var nextLength = output.length + string.length;\n      var truncatedLength = nextLength + truncated.length; // If this is the last element, and adding it would\n      // take us over length, but adding the truncator wouldn't - then break now\n\n      if (last && nextLength > originalLength && output.length + truncated.length <= originalLength) {\n        break;\n      } // If this isn't the last or second to last element to scan,\n      // but the string is already over length then break here\n\n\n      if (!last && !secondToLast && truncatedLength > originalLength) {\n        break;\n      } // Peek at the next string to determine if we should\n      // break early before adding this item to the output\n\n\n      peek = last ? '' : inspectItem(list[i + 1], options) + (secondToLast ? '' : separator); // If we have one element left, but this element and\n      // the next takes over length, the break early\n\n      if (!last && secondToLast && truncatedLength > originalLength && nextLength + peek.length > originalLength) {\n        break;\n      }\n\n      output += string; // If the next element takes us to length -\n      // but there are more after that, then we should truncate now\n\n      if (!last && !secondToLast && nextLength + peek.length >= originalLength) {\n        truncated = \"\".concat(truncator, \"(\").concat(list.length - i - 1, \")\");\n        break;\n      }\n\n      truncated = '';\n    }\n\n    return \"\".concat(output).concat(truncated);\n  }\n\n  function quoteComplexKey(key) {\n    if (key.match(/^[a-zA-Z_][a-zA-Z_0-9]*$/)) {\n      return key;\n    }\n\n    return JSON.stringify(key).replace(/'/g, \"\\\\'\").replace(/\\\\\"/g, '\"').replace(/(^\"|\"$)/g, \"'\");\n  }\n\n  function inspectProperty(_ref2, options) {\n    var _ref3 = _slicedToArray(_ref2, 2),\n        key = _ref3[0],\n        value = _ref3[1];\n\n    options.truncate -= 2;\n\n    if (typeof key === 'string') {\n      key = quoteComplexKey(key);\n    } else if (typeof key !== 'number') {\n      key = \"[\".concat(options.inspect(key, options), \"]\");\n    }\n\n    options.truncate -= key.length;\n    value = options.inspect(value, options);\n    return \"\".concat(key, \": \").concat(value);\n  }\n\n  function inspectArray(array, options) {\n    // Object.keys will always output the Array indices first, so we can slice by\n    // `array.length` to get non-index properties\n    var nonIndexProperties = Object.keys(array).slice(array.length);\n    if (!array.length && !nonIndexProperties.length) return '[]';\n    options.truncate -= 4;\n    var listContents = inspectList(array, options);\n    options.truncate -= listContents.length;\n    var propertyContents = '';\n\n    if (nonIndexProperties.length) {\n      propertyContents = inspectList(nonIndexProperties.map(function (key) {\n        return [key, array[key]];\n      }), options, inspectProperty);\n    }\n\n    return \"[ \".concat(listContents).concat(propertyContents ? \", \".concat(propertyContents) : '', \" ]\");\n  }\n\n  /* !\n   * Chai - getFuncName utility\n   * Copyright(c) 2012-2016 Jake Luer <jake@alogicalparadox.com>\n   * MIT Licensed\n   */\n\n  /**\n   * ### .getFuncName(constructorFn)\n   *\n   * Returns the name of a function.\n   * When a non-function instance is passed, returns `null`.\n   * This also includes a polyfill function if `aFunc.name` is not defined.\n   *\n   * @name getFuncName\n   * @param {Function} funct\n   * @namespace Utils\n   * @api public\n   */\n\n  var toString = Function.prototype.toString;\n  var functionNameMatch = /\\s*function(?:\\s|\\s*\\/\\*[^(?:*\\/)]+\\*\\/\\s*)*([^\\s\\(\\/]+)/;\n  function getFuncName(aFunc) {\n    if (typeof aFunc !== 'function') {\n      return null;\n    }\n\n    var name = '';\n    if (typeof Function.prototype.name === 'undefined' && typeof aFunc.name === 'undefined') {\n      // Here we run a polyfill if Function does not support the `name` property and if aFunc.name is not defined\n      var match = toString.call(aFunc).match(functionNameMatch);\n      if (match) {\n        name = match[1];\n      }\n    } else {\n      // If we've got a `name` property we just use it\n      name = aFunc.name;\n    }\n\n    return name;\n  }\n\n  var getFuncName_1 = getFuncName;\n\n  var getArrayName = function getArrayName(array) {\n    // We need to special case Node.js' Buffers, which report to be Uint8Array\n    if (typeof Buffer === 'function' && array instanceof Buffer) {\n      return 'Buffer';\n    }\n\n    if (array[Symbol.toStringTag]) {\n      return array[Symbol.toStringTag];\n    }\n\n    return getFuncName_1(array.constructor);\n  };\n\n  function inspectTypedArray(array, options) {\n    var name = getArrayName(array);\n    options.truncate -= name.length + 4; // Object.keys will always output the Array indices first, so we can slice by\n    // `array.length` to get non-index properties\n\n    var nonIndexProperties = Object.keys(array).slice(array.length);\n    if (!array.length && !nonIndexProperties.length) return \"\".concat(name, \"[]\"); // As we know TypedArrays only contain Unsigned Integers, we can skip inspecting each one and simply\n    // stylise the toString() value of them\n\n    var output = '';\n\n    for (var i = 0; i < array.length; i++) {\n      var string = \"\".concat(options.stylize(truncate(array[i], options.truncate), 'number')).concat(i === array.length - 1 ? '' : ', ');\n      options.truncate -= string.length;\n\n      if (array[i] !== array.length && options.truncate <= 3) {\n        output += \"\".concat(truncator, \"(\").concat(array.length - array[i] + 1, \")\");\n        break;\n      }\n\n      output += string;\n    }\n\n    var propertyContents = '';\n\n    if (nonIndexProperties.length) {\n      propertyContents = inspectList(nonIndexProperties.map(function (key) {\n        return [key, array[key]];\n      }), options, inspectProperty);\n    }\n\n    return \"\".concat(name, \"[ \").concat(output).concat(propertyContents ? \", \".concat(propertyContents) : '', \" ]\");\n  }\n\n  function inspectDate(dateObject, options) {\n    // If we need to - truncate the time portion, but never the date\n    var split = dateObject.toJSON().split('T');\n    var date = split[0];\n    return options.stylize(\"\".concat(date, \"T\").concat(truncate(split[1], options.truncate - date.length - 1)), 'date');\n  }\n\n  function inspectFunction(func, options) {\n    var name = getFuncName_1(func);\n\n    if (!name) {\n      return options.stylize('[Function]', 'special');\n    }\n\n    return options.stylize(\"[Function \".concat(truncate(name, options.truncate - 11), \"]\"), 'special');\n  }\n\n  function inspectMapEntry(_ref, options) {\n    var _ref2 = _slicedToArray(_ref, 2),\n        key = _ref2[0],\n        value = _ref2[1];\n\n    options.truncate -= 4;\n    key = options.inspect(key, options);\n    options.truncate -= key.length;\n    value = options.inspect(value, options);\n    return \"\".concat(key, \" => \").concat(value);\n  } // IE11 doesn't support `map.entries()`\n\n\n  function mapToEntries(map) {\n    var entries = [];\n    map.forEach(function (value, key) {\n      entries.push([key, value]);\n    });\n    return entries;\n  }\n\n  function inspectMap(map, options) {\n    var size = map.size - 1;\n\n    if (size <= 0) {\n      return 'Map{}';\n    }\n\n    options.truncate -= 7;\n    return \"Map{ \".concat(inspectList(mapToEntries(map), options, inspectMapEntry), \" }\");\n  }\n\n  var isNaN = Number.isNaN || function (i) {\n    return i !== i;\n  }; // eslint-disable-line no-self-compare\n\n\n  function inspectNumber(number, options) {\n    if (isNaN(number)) {\n      return options.stylize('NaN', 'number');\n    }\n\n    if (number === Infinity) {\n      return options.stylize('Infinity', 'number');\n    }\n\n    if (number === -Infinity) {\n      return options.stylize('-Infinity', 'number');\n    }\n\n    if (number === 0) {\n      return options.stylize(1 / number === Infinity ? '+0' : '-0', 'number');\n    }\n\n    return options.stylize(truncate(number, options.truncate), 'number');\n  }\n\n  function inspectBigInt(number, options) {\n    var nums = truncate(number.toString(), options.truncate - 1);\n    if (nums !== truncator) nums += 'n';\n    return options.stylize(nums, 'bigint');\n  }\n\n  function inspectRegExp(value, options) {\n    var flags = value.toString().split('/')[2];\n    var sourceLength = options.truncate - (2 + flags.length);\n    var source = value.source;\n    return options.stylize(\"/\".concat(truncate(source, sourceLength), \"/\").concat(flags), 'regexp');\n  }\n\n  function arrayFromSet(set) {\n    var values = [];\n    set.forEach(function (value) {\n      values.push(value);\n    });\n    return values;\n  }\n\n  function inspectSet(set, options) {\n    if (set.size === 0) return 'Set{}';\n    options.truncate -= 7;\n    return \"Set{ \".concat(inspectList(arrayFromSet(set), options), \" }\");\n  }\n\n  var stringEscapeChars = new RegExp(\"['\\\\u0000-\\\\u001f\\\\u007f-\\\\u009f\\\\u00ad\\\\u0600-\\\\u0604\\\\u070f\\\\u17b4\\\\u17b5\" + \"\\\\u200c-\\\\u200f\\\\u2028-\\\\u202f\\\\u2060-\\\\u206f\\\\ufeff\\\\ufff0-\\\\uffff]\", 'g');\n  var escapeCharacters = {\n    '\\b': '\\\\b',\n    '\\t': '\\\\t',\n    '\\n': '\\\\n',\n    '\\f': '\\\\f',\n    '\\r': '\\\\r',\n    \"'\": \"\\\\'\",\n    '\\\\': '\\\\\\\\'\n  };\n  var hex = 16;\n  var unicodeLength = 4;\n\n  function escape(char) {\n    return escapeCharacters[char] || \"\\\\u\".concat(\"0000\".concat(char.charCodeAt(0).toString(hex)).slice(-unicodeLength));\n  }\n\n  function inspectString(string, options) {\n    if (stringEscapeChars.test(string)) {\n      string = string.replace(stringEscapeChars, escape);\n    }\n\n    return options.stylize(\"'\".concat(truncate(string, options.truncate - 2), \"'\"), 'string');\n  }\n\n  function inspectSymbol(value) {\n    if ('description' in Symbol.prototype) {\n      return value.description ? \"Symbol(\".concat(value.description, \")\") : 'Symbol()';\n    }\n\n    return value.toString();\n  }\n\n  var getPromiseValue = function getPromiseValue() {\n    return 'Promise{…}';\n  };\n\n  try {\n    var _process$binding = process.binding('util'),\n        getPromiseDetails = _process$binding.getPromiseDetails,\n        kPending = _process$binding.kPending,\n        kRejected = _process$binding.kRejected;\n\n    if (Array.isArray(getPromiseDetails(Promise.resolve()))) {\n      getPromiseValue = function getPromiseValue(value, options) {\n        var _getPromiseDetails = getPromiseDetails(value),\n            _getPromiseDetails2 = _slicedToArray(_getPromiseDetails, 2),\n            state = _getPromiseDetails2[0],\n            innerValue = _getPromiseDetails2[1];\n\n        if (state === kPending) {\n          return 'Promise{<pending>}';\n        }\n\n        return \"Promise\".concat(state === kRejected ? '!' : '', \"{\").concat(options.inspect(innerValue, options), \"}\");\n      };\n    }\n  } catch (notNode) {\n    /* ignore */\n  }\n\n  var inspectPromise = getPromiseValue;\n\n  function inspectObject(object, options) {\n    var properties = Object.getOwnPropertyNames(object);\n    var symbols = Object.getOwnPropertySymbols ? Object.getOwnPropertySymbols(object) : [];\n\n    if (properties.length === 0 && symbols.length === 0) {\n      return '{}';\n    }\n\n    options.truncate -= 4;\n    options.seen = options.seen || [];\n\n    if (options.seen.indexOf(object) >= 0) {\n      return '[Circular]';\n    }\n\n    options.seen.push(object);\n    var propertyContents = inspectList(properties.map(function (key) {\n      return [key, object[key]];\n    }), options, inspectProperty);\n    var symbolContents = inspectList(symbols.map(function (key) {\n      return [key, object[key]];\n    }), options, inspectProperty);\n    options.seen.pop();\n    var sep = '';\n\n    if (propertyContents && symbolContents) {\n      sep = ', ';\n    }\n\n    return \"{ \".concat(propertyContents).concat(sep).concat(symbolContents, \" }\");\n  }\n\n  var toStringTag = typeof Symbol !== 'undefined' && Symbol.toStringTag ? Symbol.toStringTag : false;\n  function inspectClass(value, options) {\n    var name = '';\n\n    if (toStringTag && toStringTag in value) {\n      name = value[toStringTag];\n    }\n\n    name = name || getFuncName_1(value.constructor); // Babel transforms anonymous classes to the name `_class`\n\n    if (!name || name === '_class') {\n      name = '<Anonymous Class>';\n    }\n\n    options.truncate -= name.length;\n    return \"\".concat(name).concat(inspectObject(value, options));\n  }\n\n  function inspectArguments(args, options) {\n    if (args.length === 0) return 'Arguments[]';\n    options.truncate -= 13;\n    return \"Arguments[ \".concat(inspectList(args, options), \" ]\");\n  }\n\n  var errorKeys = ['stack', 'line', 'column', 'name', 'message', 'fileName', 'lineNumber', 'columnNumber', 'number', 'description'];\n  function inspectObject$1(error, options) {\n    var properties = Object.getOwnPropertyNames(error).filter(function (key) {\n      return errorKeys.indexOf(key) === -1;\n    });\n    var name = error.name;\n    options.truncate -= name.length;\n    var message = '';\n\n    if (typeof error.message === 'string') {\n      message = truncate(error.message, options.truncate);\n    } else {\n      properties.unshift('message');\n    }\n\n    message = message ? \": \".concat(message) : '';\n    options.truncate -= message.length + 5;\n    var propertyContents = inspectList(properties.map(function (key) {\n      return [key, error[key]];\n    }), options, inspectProperty);\n    return \"\".concat(name).concat(message).concat(propertyContents ? \" { \".concat(propertyContents, \" }\") : '');\n  }\n\n  function inspectAttribute(_ref, options) {\n    var _ref2 = _slicedToArray(_ref, 2),\n        key = _ref2[0],\n        value = _ref2[1];\n\n    options.truncate -= 3;\n\n    if (!value) {\n      return \"\".concat(options.stylize(key, 'yellow'));\n    }\n\n    return \"\".concat(options.stylize(key, 'yellow'), \"=\").concat(options.stylize(\"\\\"\".concat(value, \"\\\"\"), 'string'));\n  }\n  function inspectHTMLCollection(collection, options) {\n    // eslint-disable-next-line no-use-before-define\n    return inspectList(collection, options, inspectHTML, '\\n');\n  }\n  function inspectHTML(element, options) {\n    var properties = element.getAttributeNames();\n    var name = element.tagName.toLowerCase();\n    var head = options.stylize(\"<\".concat(name), 'special');\n    var headClose = options.stylize(\">\", 'special');\n    var tail = options.stylize(\"</\".concat(name, \">\"), 'special');\n    options.truncate -= name.length * 2 + 5;\n    var propertyContents = '';\n\n    if (properties.length > 0) {\n      propertyContents += ' ';\n      propertyContents += inspectList(properties.map(function (key) {\n        return [key, element.getAttribute(key)];\n      }), options, inspectAttribute, ' ');\n    }\n\n    options.truncate -= propertyContents.length;\n    var truncate = options.truncate;\n    var children = inspectHTMLCollection(element.children, options);\n\n    if (children && children.length > truncate) {\n      children = \"\".concat(truncator, \"(\").concat(element.children.length, \")\");\n    }\n\n    return \"\".concat(head).concat(propertyContents).concat(headClose).concat(children).concat(tail);\n  }\n\n  var symbolsSupported = typeof Symbol === 'function' && typeof Symbol.for === 'function';\n  var chaiInspect = symbolsSupported ? Symbol.for('chai/inspect') : '@@chai/inspect';\n  var nodeInspect = false;\n\n  try {\n    // eslint-disable-next-line global-require\n    var nodeUtil = __webpack_require__(/*! util */ \"?0ac7\");\n\n    nodeInspect = nodeUtil.inspect ? nodeUtil.inspect.custom : false;\n  } catch (noNodeInspect) {\n    nodeInspect = false;\n  }\n\n  var constructorMap = new WeakMap();\n  var stringTagMap = {};\n  var baseTypesMap = {\n    undefined: function undefined$1(value, options) {\n      return options.stylize('undefined', 'undefined');\n    },\n    null: function _null(value, options) {\n      return options.stylize(null, 'null');\n    },\n    boolean: function boolean(value, options) {\n      return options.stylize(value, 'boolean');\n    },\n    Boolean: function Boolean(value, options) {\n      return options.stylize(value, 'boolean');\n    },\n    number: inspectNumber,\n    Number: inspectNumber,\n    bigint: inspectBigInt,\n    BigInt: inspectBigInt,\n    string: inspectString,\n    String: inspectString,\n    function: inspectFunction,\n    Function: inspectFunction,\n    symbol: inspectSymbol,\n    // A Symbol polyfill will return `Symbol` not `symbol` from typedetect\n    Symbol: inspectSymbol,\n    Array: inspectArray,\n    Date: inspectDate,\n    Map: inspectMap,\n    Set: inspectSet,\n    RegExp: inspectRegExp,\n    Promise: inspectPromise,\n    // WeakSet, WeakMap are totally opaque to us\n    WeakSet: function WeakSet(value, options) {\n      return options.stylize('WeakSet{…}', 'special');\n    },\n    WeakMap: function WeakMap(value, options) {\n      return options.stylize('WeakMap{…}', 'special');\n    },\n    Arguments: inspectArguments,\n    Int8Array: inspectTypedArray,\n    Uint8Array: inspectTypedArray,\n    Uint8ClampedArray: inspectTypedArray,\n    Int16Array: inspectTypedArray,\n    Uint16Array: inspectTypedArray,\n    Int32Array: inspectTypedArray,\n    Uint32Array: inspectTypedArray,\n    Float32Array: inspectTypedArray,\n    Float64Array: inspectTypedArray,\n    Generator: function Generator() {\n      return '';\n    },\n    DataView: function DataView() {\n      return '';\n    },\n    ArrayBuffer: function ArrayBuffer() {\n      return '';\n    },\n    Error: inspectObject$1,\n    HTMLCollection: inspectHTMLCollection,\n    NodeList: inspectHTMLCollection\n  }; // eslint-disable-next-line complexity\n\n  var inspectCustom = function inspectCustom(value, options, type) {\n    if (chaiInspect in value && typeof value[chaiInspect] === 'function') {\n      return value[chaiInspect](options);\n    }\n\n    if (nodeInspect && nodeInspect in value && typeof value[nodeInspect] === 'function') {\n      return value[nodeInspect](options.depth, options);\n    }\n\n    if ('inspect' in value && typeof value.inspect === 'function') {\n      return value.inspect(options.depth, options);\n    }\n\n    if ('constructor' in value && constructorMap.has(value.constructor)) {\n      return constructorMap.get(value.constructor)(value, options);\n    }\n\n    if (stringTagMap[type]) {\n      return stringTagMap[type](value, options);\n    }\n\n    return '';\n  };\n\n  var toString$1 = Object.prototype.toString; // eslint-disable-next-line complexity\n\n  function inspect(value, options) {\n    options = normaliseOptions(options);\n    options.inspect = inspect;\n    var _options = options,\n        customInspect = _options.customInspect;\n    var type = value === null ? 'null' : _typeof(value);\n\n    if (type === 'object') {\n      type = toString$1.call(value).slice(8, -1);\n    } // If it is a base value that we already support, then use Loupe's inspector\n\n\n    if (baseTypesMap[type]) {\n      return baseTypesMap[type](value, options);\n    } // If `options.customInspect` is set to true then try to use the custom inspector\n\n\n    if (customInspect && value) {\n      var output = inspectCustom(value, options, type);\n\n      if (output) {\n        if (typeof output === 'string') return output;\n        return inspect(output, options);\n      }\n    }\n\n    var proto = value ? Object.getPrototypeOf(value) : false; // If it's a plain Object then use Loupe's inspector\n\n    if (proto === Object.prototype || proto === null) {\n      return inspectObject(value, options);\n    } // Specifically account for HTMLElements\n    // eslint-disable-next-line no-undef\n\n\n    if (value && typeof HTMLElement === 'function' && value instanceof HTMLElement) {\n      return inspectHTML(value, options);\n    }\n\n    if ('constructor' in value) {\n      // If it is a class, inspect it like an object but add the constructor name\n      if (value.constructor !== Object) {\n        return inspectClass(value, options);\n      } // If it is an object with an anonymous prototype, display it as an object.\n\n\n      return inspectObject(value, options);\n    } // last chance to check if it's an object\n\n\n    if (value === Object(value)) {\n      return inspectObject(value, options);\n    } // We have run out of options! Just stringify the value\n\n\n    return options.stylize(String(value), type);\n  }\n  function registerConstructor(constructor, inspector) {\n    if (constructorMap.has(constructor)) {\n      return false;\n    }\n\n    constructorMap.add(constructor, inspector);\n    return true;\n  }\n  function registerStringTag(stringTag, inspector) {\n    if (stringTag in stringTagMap) {\n      return false;\n    }\n\n    stringTagMap[stringTag] = inspector;\n    return true;\n  }\n  var custom = chaiInspect;\n\n  exports.custom = custom;\n  exports.default = inspect;\n  exports.inspect = inspect;\n  exports.registerConstructor = registerConstructor;\n  exports.registerStringTag = registerStringTag;\n\n  Object.defineProperty(exports, '__esModule', { value: true });\n\n})));\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/loupe/loupe.js?");

/***/ }),

/***/ "./node_modules/pathval/index.js":
/*!***************************************!*\
  !*** ./node_modules/pathval/index.js ***!
  \***************************************/
/***/ ((module) => {

"use strict";
eval("\n\n/* !\n * Chai - pathval utility\n * Copyright(c) 2012-2014 Jake Luer <jake@alogicalparadox.com>\n * @see https://github.com/logicalparadox/filtr\n * MIT Licensed\n */\n\n/**\n * ### .hasProperty(object, name)\n *\n * This allows checking whether an object has own\n * or inherited from prototype chain named property.\n *\n * Basically does the same thing as the `in`\n * operator but works properly with null/undefined values\n * and other primitives.\n *\n *     var obj = {\n *         arr: ['a', 'b', 'c']\n *       , str: 'Hello'\n *     }\n *\n * The following would be the results.\n *\n *     hasProperty(obj, 'str');  // true\n *     hasProperty(obj, 'constructor');  // true\n *     hasProperty(obj, 'bar');  // false\n *\n *     hasProperty(obj.str, 'length'); // true\n *     hasProperty(obj.str, 1);  // true\n *     hasProperty(obj.str, 5);  // false\n *\n *     hasProperty(obj.arr, 'length');  // true\n *     hasProperty(obj.arr, 2);  // true\n *     hasProperty(obj.arr, 3);  // false\n *\n * @param {Object} object\n * @param {String|Symbol} name\n * @returns {Boolean} whether it exists\n * @namespace Utils\n * @name hasProperty\n * @api public\n */\n\nfunction hasProperty(obj, name) {\n  if (typeof obj === 'undefined' || obj === null) {\n    return false;\n  }\n\n  // The `in` operator does not work with primitives.\n  return name in Object(obj);\n}\n\n/* !\n * ## parsePath(path)\n *\n * Helper function used to parse string object\n * paths. Use in conjunction with `internalGetPathValue`.\n *\n *      var parsed = parsePath('myobject.property.subprop');\n *\n * ### Paths:\n *\n * * Can be infinitely deep and nested.\n * * Arrays are also valid using the formal `myobject.document[3].property`.\n * * Literal dots and brackets (not delimiter) must be backslash-escaped.\n *\n * @param {String} path\n * @returns {Object} parsed\n * @api private\n */\n\nfunction parsePath(path) {\n  var str = path.replace(/([^\\\\])\\[/g, '$1.[');\n  var parts = str.match(/(\\\\\\.|[^.]+?)+/g);\n  return parts.map(function mapMatches(value) {\n    if (\n      value === 'constructor' ||\n      value === '__proto__' ||\n      value === 'prototype'\n    ) {\n      return {};\n    }\n    var regexp = /^\\[(\\d+)\\]$/;\n    var mArr = regexp.exec(value);\n    var parsed = null;\n    if (mArr) {\n      parsed = { i: parseFloat(mArr[1]) };\n    } else {\n      parsed = { p: value.replace(/\\\\([.[\\]])/g, '$1') };\n    }\n\n    return parsed;\n  });\n}\n\n/* !\n * ## internalGetPathValue(obj, parsed[, pathDepth])\n *\n * Helper companion function for `.parsePath` that returns\n * the value located at the parsed address.\n *\n *      var value = getPathValue(obj, parsed);\n *\n * @param {Object} object to search against\n * @param {Object} parsed definition from `parsePath`.\n * @param {Number} depth (nesting level) of the property we want to retrieve\n * @returns {Object|Undefined} value\n * @api private\n */\n\nfunction internalGetPathValue(obj, parsed, pathDepth) {\n  var temporaryValue = obj;\n  var res = null;\n  pathDepth = typeof pathDepth === 'undefined' ? parsed.length : pathDepth;\n\n  for (var i = 0; i < pathDepth; i++) {\n    var part = parsed[i];\n    if (temporaryValue) {\n      if (typeof part.p === 'undefined') {\n        temporaryValue = temporaryValue[part.i];\n      } else {\n        temporaryValue = temporaryValue[part.p];\n      }\n\n      if (i === pathDepth - 1) {\n        res = temporaryValue;\n      }\n    }\n  }\n\n  return res;\n}\n\n/* !\n * ## internalSetPathValue(obj, value, parsed)\n *\n * Companion function for `parsePath` that sets\n * the value located at a parsed address.\n *\n *  internalSetPathValue(obj, 'value', parsed);\n *\n * @param {Object} object to search and define on\n * @param {*} value to use upon set\n * @param {Object} parsed definition from `parsePath`\n * @api private\n */\n\nfunction internalSetPathValue(obj, val, parsed) {\n  var tempObj = obj;\n  var pathDepth = parsed.length;\n  var part = null;\n  // Here we iterate through every part of the path\n  for (var i = 0; i < pathDepth; i++) {\n    var propName = null;\n    var propVal = null;\n    part = parsed[i];\n\n    // If it's the last part of the path, we set the 'propName' value with the property name\n    if (i === pathDepth - 1) {\n      propName = typeof part.p === 'undefined' ? part.i : part.p;\n      // Now we set the property with the name held by 'propName' on object with the desired val\n      tempObj[propName] = val;\n    } else if (typeof part.p !== 'undefined' && tempObj[part.p]) {\n      tempObj = tempObj[part.p];\n    } else if (typeof part.i !== 'undefined' && tempObj[part.i]) {\n      tempObj = tempObj[part.i];\n    } else {\n      // If the obj doesn't have the property we create one with that name to define it\n      var next = parsed[i + 1];\n      // Here we set the name of the property which will be defined\n      propName = typeof part.p === 'undefined' ? part.i : part.p;\n      // Here we decide if this property will be an array or a new object\n      propVal = typeof next.p === 'undefined' ? [] : {};\n      tempObj[propName] = propVal;\n      tempObj = tempObj[propName];\n    }\n  }\n}\n\n/**\n * ### .getPathInfo(object, path)\n *\n * This allows the retrieval of property info in an\n * object given a string path.\n *\n * The path info consists of an object with the\n * following properties:\n *\n * * parent - The parent object of the property referenced by `path`\n * * name - The name of the final property, a number if it was an array indexer\n * * value - The value of the property, if it exists, otherwise `undefined`\n * * exists - Whether the property exists or not\n *\n * @param {Object} object\n * @param {String} path\n * @returns {Object} info\n * @namespace Utils\n * @name getPathInfo\n * @api public\n */\n\nfunction getPathInfo(obj, path) {\n  var parsed = parsePath(path);\n  var last = parsed[parsed.length - 1];\n  var info = {\n    parent:\n      parsed.length > 1 ?\n        internalGetPathValue(obj, parsed, parsed.length - 1) :\n        obj,\n    name: last.p || last.i,\n    value: internalGetPathValue(obj, parsed),\n  };\n  info.exists = hasProperty(info.parent, info.name);\n\n  return info;\n}\n\n/**\n * ### .getPathValue(object, path)\n *\n * This allows the retrieval of values in an\n * object given a string path.\n *\n *     var obj = {\n *         prop1: {\n *             arr: ['a', 'b', 'c']\n *           , str: 'Hello'\n *         }\n *       , prop2: {\n *             arr: [ { nested: 'Universe' } ]\n *           , str: 'Hello again!'\n *         }\n *     }\n *\n * The following would be the results.\n *\n *     getPathValue(obj, 'prop1.str'); // Hello\n *     getPathValue(obj, 'prop1.att[2]'); // b\n *     getPathValue(obj, 'prop2.arr[0].nested'); // Universe\n *\n * @param {Object} object\n * @param {String} path\n * @returns {Object} value or `undefined`\n * @namespace Utils\n * @name getPathValue\n * @api public\n */\n\nfunction getPathValue(obj, path) {\n  var info = getPathInfo(obj, path);\n  return info.value;\n}\n\n/**\n * ### .setPathValue(object, path, value)\n *\n * Define the value in an object at a given string path.\n *\n * ```js\n * var obj = {\n *     prop1: {\n *         arr: ['a', 'b', 'c']\n *       , str: 'Hello'\n *     }\n *   , prop2: {\n *         arr: [ { nested: 'Universe' } ]\n *       , str: 'Hello again!'\n *     }\n * };\n * ```\n *\n * The following would be acceptable.\n *\n * ```js\n * var properties = require('tea-properties');\n * properties.set(obj, 'prop1.str', 'Hello Universe!');\n * properties.set(obj, 'prop1.arr[2]', 'B');\n * properties.set(obj, 'prop2.arr[0].nested.value', { hello: 'universe' });\n * ```\n *\n * @param {Object} object\n * @param {String} path\n * @param {Mixed} value\n * @api private\n */\n\nfunction setPathValue(obj, path, val) {\n  var parsed = parsePath(path);\n  internalSetPathValue(obj, val, parsed);\n  return obj;\n}\n\nmodule.exports = {\n  hasProperty: hasProperty,\n  getPathInfo: getPathInfo,\n  getPathValue: getPathValue,\n  setPathValue: setPathValue,\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/pathval/index.js?");

/***/ }),

/***/ "./node_modules/type-detect/type-detect.js":
/*!*************************************************!*\
  !*** ./node_modules/type-detect/type-detect.js ***!
  \*************************************************/
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

eval("(function (global, factory) {\n\t true ? module.exports = factory() :\n\t0;\n}(this, (function () { 'use strict';\n\n/* !\n * type-detect\n * Copyright(c) 2013 jake luer <jake@alogicalparadox.com>\n * MIT Licensed\n */\nvar promiseExists = typeof Promise === 'function';\n\n/* eslint-disable no-undef */\nvar globalObject = typeof self === 'object' ? self : __webpack_require__.g; // eslint-disable-line id-blacklist\n\nvar symbolExists = typeof Symbol !== 'undefined';\nvar mapExists = typeof Map !== 'undefined';\nvar setExists = typeof Set !== 'undefined';\nvar weakMapExists = typeof WeakMap !== 'undefined';\nvar weakSetExists = typeof WeakSet !== 'undefined';\nvar dataViewExists = typeof DataView !== 'undefined';\nvar symbolIteratorExists = symbolExists && typeof Symbol.iterator !== 'undefined';\nvar symbolToStringTagExists = symbolExists && typeof Symbol.toStringTag !== 'undefined';\nvar setEntriesExists = setExists && typeof Set.prototype.entries === 'function';\nvar mapEntriesExists = mapExists && typeof Map.prototype.entries === 'function';\nvar setIteratorPrototype = setEntriesExists && Object.getPrototypeOf(new Set().entries());\nvar mapIteratorPrototype = mapEntriesExists && Object.getPrototypeOf(new Map().entries());\nvar arrayIteratorExists = symbolIteratorExists && typeof Array.prototype[Symbol.iterator] === 'function';\nvar arrayIteratorPrototype = arrayIteratorExists && Object.getPrototypeOf([][Symbol.iterator]());\nvar stringIteratorExists = symbolIteratorExists && typeof String.prototype[Symbol.iterator] === 'function';\nvar stringIteratorPrototype = stringIteratorExists && Object.getPrototypeOf(''[Symbol.iterator]());\nvar toStringLeftSliceLength = 8;\nvar toStringRightSliceLength = -1;\n/**\n * ### typeOf (obj)\n *\n * Uses `Object.prototype.toString` to determine the type of an object,\n * normalising behaviour across engine versions & well optimised.\n *\n * @param {Mixed} object\n * @return {String} object type\n * @api public\n */\nfunction typeDetect(obj) {\n  /* ! Speed optimisation\n   * Pre:\n   *   string literal     x 3,039,035 ops/sec ±1.62% (78 runs sampled)\n   *   boolean literal    x 1,424,138 ops/sec ±4.54% (75 runs sampled)\n   *   number literal     x 1,653,153 ops/sec ±1.91% (82 runs sampled)\n   *   undefined          x 9,978,660 ops/sec ±1.92% (75 runs sampled)\n   *   function           x 2,556,769 ops/sec ±1.73% (77 runs sampled)\n   * Post:\n   *   string literal     x 38,564,796 ops/sec ±1.15% (79 runs sampled)\n   *   boolean literal    x 31,148,940 ops/sec ±1.10% (79 runs sampled)\n   *   number literal     x 32,679,330 ops/sec ±1.90% (78 runs sampled)\n   *   undefined          x 32,363,368 ops/sec ±1.07% (82 runs sampled)\n   *   function           x 31,296,870 ops/sec ±0.96% (83 runs sampled)\n   */\n  var typeofObj = typeof obj;\n  if (typeofObj !== 'object') {\n    return typeofObj;\n  }\n\n  /* ! Speed optimisation\n   * Pre:\n   *   null               x 28,645,765 ops/sec ±1.17% (82 runs sampled)\n   * Post:\n   *   null               x 36,428,962 ops/sec ±1.37% (84 runs sampled)\n   */\n  if (obj === null) {\n    return 'null';\n  }\n\n  /* ! Spec Conformance\n   * Test: `Object.prototype.toString.call(window)``\n   *  - Node === \"[object global]\"\n   *  - Chrome === \"[object global]\"\n   *  - Firefox === \"[object Window]\"\n   *  - PhantomJS === \"[object Window]\"\n   *  - Safari === \"[object Window]\"\n   *  - IE 11 === \"[object Window]\"\n   *  - IE Edge === \"[object Window]\"\n   * Test: `Object.prototype.toString.call(this)``\n   *  - Chrome Worker === \"[object global]\"\n   *  - Firefox Worker === \"[object DedicatedWorkerGlobalScope]\"\n   *  - Safari Worker === \"[object DedicatedWorkerGlobalScope]\"\n   *  - IE 11 Worker === \"[object WorkerGlobalScope]\"\n   *  - IE Edge Worker === \"[object WorkerGlobalScope]\"\n   */\n  if (obj === globalObject) {\n    return 'global';\n  }\n\n  /* ! Speed optimisation\n   * Pre:\n   *   array literal      x 2,888,352 ops/sec ±0.67% (82 runs sampled)\n   * Post:\n   *   array literal      x 22,479,650 ops/sec ±0.96% (81 runs sampled)\n   */\n  if (\n    Array.isArray(obj) &&\n    (symbolToStringTagExists === false || !(Symbol.toStringTag in obj))\n  ) {\n    return 'Array';\n  }\n\n  // Not caching existence of `window` and related properties due to potential\n  // for `window` to be unset before tests in quasi-browser environments.\n  if (typeof window === 'object' && window !== null) {\n    /* ! Spec Conformance\n     * (https://html.spec.whatwg.org/multipage/browsers.html#location)\n     * WhatWG HTML$7.7.3 - The `Location` interface\n     * Test: `Object.prototype.toString.call(window.location)``\n     *  - IE <=11 === \"[object Object]\"\n     *  - IE Edge <=13 === \"[object Object]\"\n     */\n    if (typeof window.location === 'object' && obj === window.location) {\n      return 'Location';\n    }\n\n    /* ! Spec Conformance\n     * (https://html.spec.whatwg.org/#document)\n     * WhatWG HTML$3.1.1 - The `Document` object\n     * Note: Most browsers currently adher to the W3C DOM Level 2 spec\n     *       (https://www.w3.org/TR/DOM-Level-2-HTML/html.html#ID-26809268)\n     *       which suggests that browsers should use HTMLTableCellElement for\n     *       both TD and TH elements. WhatWG separates these.\n     *       WhatWG HTML states:\n     *         > For historical reasons, Window objects must also have a\n     *         > writable, configurable, non-enumerable property named\n     *         > HTMLDocument whose value is the Document interface object.\n     * Test: `Object.prototype.toString.call(document)``\n     *  - Chrome === \"[object HTMLDocument]\"\n     *  - Firefox === \"[object HTMLDocument]\"\n     *  - Safari === \"[object HTMLDocument]\"\n     *  - IE <=10 === \"[object Document]\"\n     *  - IE 11 === \"[object HTMLDocument]\"\n     *  - IE Edge <=13 === \"[object HTMLDocument]\"\n     */\n    if (typeof window.document === 'object' && obj === window.document) {\n      return 'Document';\n    }\n\n    if (typeof window.navigator === 'object') {\n      /* ! Spec Conformance\n       * (https://html.spec.whatwg.org/multipage/webappapis.html#mimetypearray)\n       * WhatWG HTML$8.6.1.5 - Plugins - Interface MimeTypeArray\n       * Test: `Object.prototype.toString.call(navigator.mimeTypes)``\n       *  - IE <=10 === \"[object MSMimeTypesCollection]\"\n       */\n      if (typeof window.navigator.mimeTypes === 'object' &&\n          obj === window.navigator.mimeTypes) {\n        return 'MimeTypeArray';\n      }\n\n      /* ! Spec Conformance\n       * (https://html.spec.whatwg.org/multipage/webappapis.html#pluginarray)\n       * WhatWG HTML$8.6.1.5 - Plugins - Interface PluginArray\n       * Test: `Object.prototype.toString.call(navigator.plugins)``\n       *  - IE <=10 === \"[object MSPluginsCollection]\"\n       */\n      if (typeof window.navigator.plugins === 'object' &&\n          obj === window.navigator.plugins) {\n        return 'PluginArray';\n      }\n    }\n\n    if ((typeof window.HTMLElement === 'function' ||\n        typeof window.HTMLElement === 'object') &&\n        obj instanceof window.HTMLElement) {\n      /* ! Spec Conformance\n      * (https://html.spec.whatwg.org/multipage/webappapis.html#pluginarray)\n      * WhatWG HTML$4.4.4 - The `blockquote` element - Interface `HTMLQuoteElement`\n      * Test: `Object.prototype.toString.call(document.createElement('blockquote'))``\n      *  - IE <=10 === \"[object HTMLBlockElement]\"\n      */\n      if (obj.tagName === 'BLOCKQUOTE') {\n        return 'HTMLQuoteElement';\n      }\n\n      /* ! Spec Conformance\n       * (https://html.spec.whatwg.org/#htmltabledatacellelement)\n       * WhatWG HTML$4.9.9 - The `td` element - Interface `HTMLTableDataCellElement`\n       * Note: Most browsers currently adher to the W3C DOM Level 2 spec\n       *       (https://www.w3.org/TR/DOM-Level-2-HTML/html.html#ID-82915075)\n       *       which suggests that browsers should use HTMLTableCellElement for\n       *       both TD and TH elements. WhatWG separates these.\n       * Test: Object.prototype.toString.call(document.createElement('td'))\n       *  - Chrome === \"[object HTMLTableCellElement]\"\n       *  - Firefox === \"[object HTMLTableCellElement]\"\n       *  - Safari === \"[object HTMLTableCellElement]\"\n       */\n      if (obj.tagName === 'TD') {\n        return 'HTMLTableDataCellElement';\n      }\n\n      /* ! Spec Conformance\n       * (https://html.spec.whatwg.org/#htmltableheadercellelement)\n       * WhatWG HTML$4.9.9 - The `td` element - Interface `HTMLTableHeaderCellElement`\n       * Note: Most browsers currently adher to the W3C DOM Level 2 spec\n       *       (https://www.w3.org/TR/DOM-Level-2-HTML/html.html#ID-82915075)\n       *       which suggests that browsers should use HTMLTableCellElement for\n       *       both TD and TH elements. WhatWG separates these.\n       * Test: Object.prototype.toString.call(document.createElement('th'))\n       *  - Chrome === \"[object HTMLTableCellElement]\"\n       *  - Firefox === \"[object HTMLTableCellElement]\"\n       *  - Safari === \"[object HTMLTableCellElement]\"\n       */\n      if (obj.tagName === 'TH') {\n        return 'HTMLTableHeaderCellElement';\n      }\n    }\n  }\n\n  /* ! Speed optimisation\n  * Pre:\n  *   Float64Array       x 625,644 ops/sec ±1.58% (80 runs sampled)\n  *   Float32Array       x 1,279,852 ops/sec ±2.91% (77 runs sampled)\n  *   Uint32Array        x 1,178,185 ops/sec ±1.95% (83 runs sampled)\n  *   Uint16Array        x 1,008,380 ops/sec ±2.25% (80 runs sampled)\n  *   Uint8Array         x 1,128,040 ops/sec ±2.11% (81 runs sampled)\n  *   Int32Array         x 1,170,119 ops/sec ±2.88% (80 runs sampled)\n  *   Int16Array         x 1,176,348 ops/sec ±5.79% (86 runs sampled)\n  *   Int8Array          x 1,058,707 ops/sec ±4.94% (77 runs sampled)\n  *   Uint8ClampedArray  x 1,110,633 ops/sec ±4.20% (80 runs sampled)\n  * Post:\n  *   Float64Array       x 7,105,671 ops/sec ±13.47% (64 runs sampled)\n  *   Float32Array       x 5,887,912 ops/sec ±1.46% (82 runs sampled)\n  *   Uint32Array        x 6,491,661 ops/sec ±1.76% (79 runs sampled)\n  *   Uint16Array        x 6,559,795 ops/sec ±1.67% (82 runs sampled)\n  *   Uint8Array         x 6,463,966 ops/sec ±1.43% (85 runs sampled)\n  *   Int32Array         x 5,641,841 ops/sec ±3.49% (81 runs sampled)\n  *   Int16Array         x 6,583,511 ops/sec ±1.98% (80 runs sampled)\n  *   Int8Array          x 6,606,078 ops/sec ±1.74% (81 runs sampled)\n  *   Uint8ClampedArray  x 6,602,224 ops/sec ±1.77% (83 runs sampled)\n  */\n  var stringTag = (symbolToStringTagExists && obj[Symbol.toStringTag]);\n  if (typeof stringTag === 'string') {\n    return stringTag;\n  }\n\n  var objPrototype = Object.getPrototypeOf(obj);\n  /* ! Speed optimisation\n  * Pre:\n  *   regex literal      x 1,772,385 ops/sec ±1.85% (77 runs sampled)\n  *   regex constructor  x 2,143,634 ops/sec ±2.46% (78 runs sampled)\n  * Post:\n  *   regex literal      x 3,928,009 ops/sec ±0.65% (78 runs sampled)\n  *   regex constructor  x 3,931,108 ops/sec ±0.58% (84 runs sampled)\n  */\n  if (objPrototype === RegExp.prototype) {\n    return 'RegExp';\n  }\n\n  /* ! Speed optimisation\n  * Pre:\n  *   date               x 2,130,074 ops/sec ±4.42% (68 runs sampled)\n  * Post:\n  *   date               x 3,953,779 ops/sec ±1.35% (77 runs sampled)\n  */\n  if (objPrototype === Date.prototype) {\n    return 'Date';\n  }\n\n  /* ! Spec Conformance\n   * (http://www.ecma-international.org/ecma-262/6.0/index.html#sec-promise.prototype-@@tostringtag)\n   * ES6$25.4.5.4 - Promise.prototype[@@toStringTag] should be \"Promise\":\n   * Test: `Object.prototype.toString.call(Promise.resolve())``\n   *  - Chrome <=47 === \"[object Object]\"\n   *  - Edge <=20 === \"[object Object]\"\n   *  - Firefox 29-Latest === \"[object Promise]\"\n   *  - Safari 7.1-Latest === \"[object Promise]\"\n   */\n  if (promiseExists && objPrototype === Promise.prototype) {\n    return 'Promise';\n  }\n\n  /* ! Speed optimisation\n  * Pre:\n  *   set                x 2,222,186 ops/sec ±1.31% (82 runs sampled)\n  * Post:\n  *   set                x 4,545,879 ops/sec ±1.13% (83 runs sampled)\n  */\n  if (setExists && objPrototype === Set.prototype) {\n    return 'Set';\n  }\n\n  /* ! Speed optimisation\n  * Pre:\n  *   map                x 2,396,842 ops/sec ±1.59% (81 runs sampled)\n  * Post:\n  *   map                x 4,183,945 ops/sec ±6.59% (82 runs sampled)\n  */\n  if (mapExists && objPrototype === Map.prototype) {\n    return 'Map';\n  }\n\n  /* ! Speed optimisation\n  * Pre:\n  *   weakset            x 1,323,220 ops/sec ±2.17% (76 runs sampled)\n  * Post:\n  *   weakset            x 4,237,510 ops/sec ±2.01% (77 runs sampled)\n  */\n  if (weakSetExists && objPrototype === WeakSet.prototype) {\n    return 'WeakSet';\n  }\n\n  /* ! Speed optimisation\n  * Pre:\n  *   weakmap            x 1,500,260 ops/sec ±2.02% (78 runs sampled)\n  * Post:\n  *   weakmap            x 3,881,384 ops/sec ±1.45% (82 runs sampled)\n  */\n  if (weakMapExists && objPrototype === WeakMap.prototype) {\n    return 'WeakMap';\n  }\n\n  /* ! Spec Conformance\n   * (http://www.ecma-international.org/ecma-262/6.0/index.html#sec-dataview.prototype-@@tostringtag)\n   * ES6$24.2.4.21 - DataView.prototype[@@toStringTag] should be \"DataView\":\n   * Test: `Object.prototype.toString.call(new DataView(new ArrayBuffer(1)))``\n   *  - Edge <=13 === \"[object Object]\"\n   */\n  if (dataViewExists && objPrototype === DataView.prototype) {\n    return 'DataView';\n  }\n\n  /* ! Spec Conformance\n   * (http://www.ecma-international.org/ecma-262/6.0/index.html#sec-%mapiteratorprototype%-@@tostringtag)\n   * ES6$23.1.5.2.2 - %MapIteratorPrototype%[@@toStringTag] should be \"Map Iterator\":\n   * Test: `Object.prototype.toString.call(new Map().entries())``\n   *  - Edge <=13 === \"[object Object]\"\n   */\n  if (mapExists && objPrototype === mapIteratorPrototype) {\n    return 'Map Iterator';\n  }\n\n  /* ! Spec Conformance\n   * (http://www.ecma-international.org/ecma-262/6.0/index.html#sec-%setiteratorprototype%-@@tostringtag)\n   * ES6$23.2.5.2.2 - %SetIteratorPrototype%[@@toStringTag] should be \"Set Iterator\":\n   * Test: `Object.prototype.toString.call(new Set().entries())``\n   *  - Edge <=13 === \"[object Object]\"\n   */\n  if (setExists && objPrototype === setIteratorPrototype) {\n    return 'Set Iterator';\n  }\n\n  /* ! Spec Conformance\n   * (http://www.ecma-international.org/ecma-262/6.0/index.html#sec-%arrayiteratorprototype%-@@tostringtag)\n   * ES6$22.1.5.2.2 - %ArrayIteratorPrototype%[@@toStringTag] should be \"Array Iterator\":\n   * Test: `Object.prototype.toString.call([][Symbol.iterator]())``\n   *  - Edge <=13 === \"[object Object]\"\n   */\n  if (arrayIteratorExists && objPrototype === arrayIteratorPrototype) {\n    return 'Array Iterator';\n  }\n\n  /* ! Spec Conformance\n   * (http://www.ecma-international.org/ecma-262/6.0/index.html#sec-%stringiteratorprototype%-@@tostringtag)\n   * ES6$21.1.5.2.2 - %StringIteratorPrototype%[@@toStringTag] should be \"String Iterator\":\n   * Test: `Object.prototype.toString.call(''[Symbol.iterator]())``\n   *  - Edge <=13 === \"[object Object]\"\n   */\n  if (stringIteratorExists && objPrototype === stringIteratorPrototype) {\n    return 'String Iterator';\n  }\n\n  /* ! Speed optimisation\n  * Pre:\n  *   object from null   x 2,424,320 ops/sec ±1.67% (76 runs sampled)\n  * Post:\n  *   object from null   x 5,838,000 ops/sec ±0.99% (84 runs sampled)\n  */\n  if (objPrototype === null) {\n    return 'Object';\n  }\n\n  return Object\n    .prototype\n    .toString\n    .call(obj)\n    .slice(toStringLeftSliceLength, toStringRightSliceLength);\n}\n\nreturn typeDetect;\n\n})));\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/type-detect/type-detect.js?");

/***/ }),

/***/ "./test/boot-test.lispx":
/*!******************************!*\
  !*** ./test/boot-test.lispx ***!
  \******************************/
/***/ ((module) => {

"use strict";
eval("module.exports = \";;; LispX Main Test Suite\\n\\n;; The order of the tests is haphazard.  It should be brought in line\\n;; with how the operators appear in boot.lispx.\\n\\n(defsuite signals-error\\n\\n  (deftest |SIGNALS-ERROR returns true if the expression signals.|\\n    (signals-error\\n     x\\n     unbound-symbol-error))\\n\\n  (deftest |SIGNALS-ERROR returns true if the expression signals a subclass.|\\n    (signals-error\\n     x\\n     error))\\n\\n  (deftest |SIGNALS-ERROR returns true if the expression signals and slots match.|\\n    (signals-error\\n     x\\n     unbound-symbol-error :symbol 'x))\\n\\n  (deftest |SIGNALS-ERROR returns true if the expression signals a subclass and slots match.|\\n    (signals-error\\n     x\\n     error :symbol 'x))\\n\\n  (deftest |SIGNALS-ERROR returns false if the expression signals but slots don't match.|\\n    (signals-error\\n     x\\n     unbound-symbol-error :symbol 'y)\\n    #f)\\n\\n  (deftest |SIGNALS-ERROR returns false if the expression doesn't signal.|\\n    (signals-error\\n     1\\n     error)\\n    #f)\\n\\n  (deftest |SIGNALS-ERROR returns false if the expression signals another condition type.|\\n    (signals-error\\n     x\\n     match-error)\\n    #f))\\n\\n(defsuite assert\\n\\n  (deftest assert.1\\n    (assert (= 1 1))\\n    #void)\\n\\n  (deftest assert.2\\n    (signals-error\\n     (assert (= 1 2))\\n     assertion-error))\\n\\n  (deftest assert.3\\n    (signals-error\\n     (assert)\\n     match-error))\\n\\n  (deftest assert.4\\n    (signals-error\\n     (assert 1)\\n     type-error :datum 1 :expected-type 'boolean))\\n\\n  (deftest assert.5\\n    (signals-error\\n     (assert #t #t)\\n     match-error)))\\n\\n(defsuite error\\n\\n  (deftest error.1\\n    (signals-error\\n     (error (make-instance #^standard-object :x 1 :y 2))\\n     standard-object :x 1 :y 2))\\n\\n  (deftest error.2\\n    (signals-error\\n     (error)\\n     match-error))\\n\\n  (deftest error.3\\n    (handler-bind ((simple-error (lambda (e)\\n                                   (invoke-restart 'continue 12))))\\n      (error (make-simple-error \\\"Foo!\\\")\\n        (abort (lambda (value) (* value 10)))\\n        (continue (lambda (value) (* value 2)))))\\n    24)\\n\\n  (deftest error.3b\\n    (handler-bind ((simple-error (lambda (e)\\n                                   (invoke-restart 'abort 12))))\\n      (error (make-simple-error \\\"Foo!\\\")\\n        (abort (lambda (value) (* value 10)))\\n        (continue (lambda (value) (* value 2)))))\\n    120)\\n\\n  (deftest error.4\\n    (restart-case ((abort (lambda (value) (* 1000 value))))\\n      (handler-bind ((simple-error (lambda (e)\\n                                     (invoke-restart 'abort 12))))\\n        (error (error (make-simple-error \\\"Foo!\\\"))\\n          (abort (lambda (value) (* value 10)))\\n          (continue (lambda (value) (* value 2))))))\\n    12000)\\n\\n  (deftest error.4b\\n    (block b\\n      (restart-case ((abort (lambda (value) (* 1000 value))))\\n        (handler-bind ((simple-error (lambda (e)\\n                                       (invoke-restart 'abort 12))))\\n          (error (error (make-simple-error \\\"Foo!\\\")\\n                   (abort (lambda (value) (return-from b (* 10000 value)))))\\n            (abort (lambda (value) (* value 10)))\\n            (continue (lambda (value) (* value 2)))))))\\n    120000))\\n\\n(defsuite assert-type\\n\\n  (deftest assert-type.1\\n    (assert-type #t #^boolean))\\n\\n  (deftest assert-type.2\\n    (signals-error\\n     (assert-type #t #^string)\\n    type-error :datum #t :expected-type 'string))\\n\\n  (deftest assert-type.3\\n    (signals-error\\n     (assert-type #t #f)\\n     type-error :datum #f :expected-type 'class))\\n\\n  (deftest assert-type.4\\n    (signals-error\\n     (assert-type)\\n     match-error))\\n\\n  (deftest assert-type.5\\n    (signals-error\\n     (assert-type #t)\\n     match-error)))\\n\\n(defsuite the\\n\\n  (deftest the.1\\n    (the boolean (= 1 1))\\n    #t)\\n\\n  (deftest the.2\\n    (signals-error\\n     (the string #t)\\n     type-error :datum #t :expected-type 'string))\\n\\n  (deftest the.3\\n    (signals-error\\n     (the #t #f)\\n     type-error :datum #t :expected-type 'symbol))\\n\\n  (deftest the.4\\n    (signals-error\\n     (the)\\n     match-error))\\n\\n  (deftest the.5\\n    (signals-error\\n     (the #t)\\n     match-error)))\\n\\n(defsuite make-type-error\\n\\n  (deftest make-type-error.1\\n    (let ((e (make-type-error \\\"foo\\\" 'number)))\\n      (assert (typep e #^type-error))\\n      (assert (typep e #^error))\\n      (assert (typep e #^condition))\\n      (assert (= \\\"foo\\\" (slot-value e 'datum)))\\n      (assert (= 'number (slot-value e 'expected-type))))\\n    #void))\\n\\n(defsuite simple-error\\n\\n  (deftest make-simple-error.1\\n    (let ((e (make-simple-error \\\"foo\\\")))\\n      (assert (typep e #^simple-error))\\n      (assert (typep e #^error))\\n      (assert (typep e #^condition))\\n      (assert (= \\\"foo\\\" (slot-value e 'message))))\\n    #void)\\n\\n  (deftest simple-error.1\\n    (signals-error\\n     (simple-error)\\n     match-error))\\n\\n  (deftest simple-error.2\\n    (signals-error\\n     (simple-error \\\"foo\\\")\\n     simple-error :message \\\"foo\\\")))\\n\\n(defsuite make-restart-error\\n\\n  (deftest make-restart-error.1\\n    (let ((e (make-restart-error 'continue)))\\n      (assert (typep e #^restart-error))\\n      (assert (typep e #^error))\\n      (assert (typep e #^condition))\\n      (assert (= 'continue (slot-value e 'restart-name))))\\n    #void))\\n\\n(defsuite vau\\n\\n  (deftest |A fexpr without body expression results in #VOID.|\\n    ((vau #ignore #ignore))\\n    #void)\\n\\n  (deftest |A fexpr returns its operands unevaluated.|\\n    ((vau ops #ignore ops) x y z)\\n    '(x y z))\\n\\n  (deftest |A fexpr can have multiple body expressions.|\\n    (progn\\n      (def env (the-environment))\\n      (assert (= ((vau ops #ignore (set env var 1) ops) x y z)\\n                 '(x y z)))\\n      (assert (= (eval 'var env) 1)))\\n    #void)\\n\\n  (deftest |The environment parameter receives the current environment.|\\n    (progn\\n      (def env (the-environment))\\n      (eq ((vau #ignore e e)) env)))\\n\\n  (deftest |Fexprs are instances of FEXPR, OPERATOR, OBJECT.|\\n    (let ((#'fexpr (vau #ignore #ignore 12)))\\n      (typep #'fexpr #^fexpr)\\n      (typep #'fexpr #^operator)\\n      (typep #'fexpr #^object)\\n      (fexpr))\\n    12)\\n\\n  (deftest |Arity check 1.|\\n    (signals-error\\n     (vau)\\n     match-error))\\n\\n  (deftest |Arity check 2.|\\n    (signals-error\\n     (vau x)\\n     match-error)))\\n\\n(defsuite quote\\n\\n  (deftest |Quoted symbols are pointer identical.|\\n    (and\\n     (eq 'foo 'foo)\\n     (eq '#'foo '#'foo)\\n     (eq (class-symbol 'foo) (class-symbol 'foo))))\\n\\n  (deftest |Ordinary symbols can be quoted.|\\n    (eq 'foo (variable-symbol 'foo)))\\n\\n  (deftest |Function symbols can be quoted.|\\n    (eq '#'foo (function-symbol 'foo)))\\n\\n  (deftest |Lists can be quoted.|\\n    (= '(1 2 3) (list 1 2 3)))\\n\\n  (deftest |Numbers can be quoted.|\\n    (= '1 1))\\n\\n  (deftest |Strings can be quoted.|\\n    (= '\\\"foo\\\" \\\"foo\\\"))\\n\\n  (deftest |QUOTE requires at least one argument.|\\n    (signals-error\\n     (quote)\\n     match-error))\\n\\n  (deftest |QUOTE requires at most one argument.|\\n    (signals-error\\n     (quote 1 2 3)\\n     match-error)))\\n\\n(defsuite list\\n\\n  (deftest list.1\\n    (eq #nil (list)))\\n\\n  (deftest list.2\\n    (= '(1) (list 1)))\\n\\n  (deftest list.3\\n    (= '((1) (2)) (list (list 1) (list 2)))))\\n\\n(defsuite list*\\n\\n  (deftest list*.1\\n    (list*)\\n    #nil)\\n\\n  (deftest list*.2\\n    (list* 1 2 '(3))\\n    '(1 2 3))\\n\\n  (deftest list*.3\\n    (list* 1 2 3)\\n    '(1 2 . 3))\\n\\n  (deftest list*.4\\n    (list* 1 2)\\n    '(1 . 2))\\n\\n  (deftest list*.5\\n    (list* 2)\\n    '2))\\n\\n(defsuite the-environment\\n\\n  (deftest the-environment.1\\n    (let ((x 1))\\n      (eval 'x (the-environment)))\\n    1)\\n\\n  (deftest the-environment.2\\n    (let ((x 1)\\n          (env (the-environment)))\\n      (boundp 'x env))\\n    #f)\\n\\n  (deftest the-environment.3\\n    (let* ((x 1)\\n           (env (the-environment)))\\n      (boundp 'x env))\\n    #t)\\n\\n  (deftest the-environment.4\\n    (signals-error\\n     (the-environment #t)\\n     match-error)))\\n\\n(defsuite boundp\\n\\n  (deftest boundp.1\\n    (boundp 'nope (the-environment))\\n    #f)\\n\\n  (deftest boundp.2\\n    (boundp '#'defun (the-environment)))\\n\\n  (deftest boundp.3\\n    (boundp (class-symbol 'standard-object) (the-environment)))\\n\\n  (deftest boundp.4\\n    (signals-error\\n     (boundp)\\n     match-error))\\n\\n  (deftest boundp.5\\n    (signals-error\\n     (boundp 'x)\\n     match-error))\\n\\n  (deftest boundp.6\\n    (signals-error\\n     (boundp 12 (the-environment))\\n     type-error :datum 12 :expected-type 'symbol))\\n\\n  (deftest boundp.7\\n    (signals-error\\n     (boundp 'x 33)\\n     type-error :datum 33 :expected-type 'environment))\\n\\n  (deftest boundp.8\\n    (signals-error\\n     (boundp 12 (the-environment) #t)\\n     match-error)))\\n\\n(defsuite macro\\n\\n  (deftest macro.0\\n    ((macro ()))\\n    #void)\\n\\n  (deftest macro.1\\n    (let ((z 1))\\n      ((macro () 'x 'y 'z)))\\n    1)\\n\\n  (deftest macro.2\\n    ((macro () (list #'= 1 1)))\\n    #t)\\n\\n  (deftest macro.3\\n    (signals-error\\n     (macro)\\n     match-error)\\n    #t))\\n\\n(defsuite defmacro\\n\\n  (deftest defmacro.1\\n    (progn\\n      (defmacro myprogn body\\n        (list* #'progn body))\\n      (myprogn 1 2 3))\\n    3)\\n\\n  (deftest defmacro.2\\n    (signals-error\\n     (defmacro)\\n     match-error))\\n\\n  (deftest defmacro.3\\n    (signals-error\\n     (defmacro foo)\\n     match-error))\\n\\n  (deftest defmacro.4\\n    (progn\\n      (defmacro foo ())\\n      (foo))\\n    #void))\\n\\n(defsuite defexpr\\n\\n  (deftest defexpr.1\\n    (progn\\n      (defexpr myprogn body env\\n        (eval (list* #'progn body) env))\\n      (myprogn 1 2 3))\\n    3)\\n\\n  (deftest defexpr.2\\n    (signals-error\\n     (defexpr)\\n     match-error))\\n\\n  (deftest defexpr.3\\n    (signals-error\\n     (defexpr foo)\\n     match-error))\\n\\n  (deftest defexpr.3b\\n    (signals-error\\n     (defexpr foo ())\\n     match-error))\\n\\n  (deftest defexpr.4\\n    (progn\\n      (defexpr foo () #ignore)\\n      (foo))\\n    #void))\\n\\n(defsuite def\\n\\n  (deftest def.1\\n    (progn\\n      (def (x y z) (list 1 2 3))\\n      (assert (= x 1))\\n      (assert (= y 2))\\n      (assert (= z 3)))\\n    #void)\\n\\n  (deftest def.2\\n    (def :key :key)\\n    :key)\\n\\n  (deftest def.2b\\n    (signals-error\\n     (def :key :key2)\\n     match-error :definiend :key :value :key2))\\n\\n  (deftest def.3\\n    (def #ignore 2)\\n    2)\\n\\n  (deftest def.4\\n    (def x 1)\\n    1)\\n\\n  (deftest def.5\\n    (def () ())\\n    ())\\n\\n  (deftest def.6\\n    (signals-error\\n     (def)\\n     match-error))\\n\\n  (deftest def.7\\n    (signals-error\\n     (def x)\\n     match-error))\\n\\n  (deftest def.8\\n    (signals-error\\n     (def #t 12)\\n     type-error :datum #t :expected-type '(or symbol ignore list)))\\n\\n  (deftest def.9\\n    (def x 1 \\\"docstring\\\")\\n    1))\\n\\n(defsuite defconstant\\n\\n  (deftest defconstant.1\\n    (progn\\n      (defconstant x 1)\\n      x)\\n    1)\\n\\n  (deftest defconstant.2\\n    (signals-error\\n     (defconstant)\\n     match-error))\\n\\n  (deftest defconstant.3\\n    (signals-error\\n     (defconstant x)\\n     match-error))\\n\\n  (deftest defconstant.4\\n    (progn\\n      (defconstant x 1)\\n      x)\\n    1))\\n\\n(defsuite lambda\\n\\n  (deftest lambda.1\\n    ((lambda ()))\\n    #void)\\n\\n  (deftest lambda.2\\n    ((lambda args 'whatever args) 1 2 (intern \\\"foo\\\"))\\n    '(1 2 foo))\\n\\n  (deftest lambda.3\\n    (signals-error\\n     (lambda)\\n     match-error))\\n\\n  (deftest lambda.4\\n    (signals-error\\n     ((lambda #t))\\n     type-error :datum #t :expected-type '(or symbol ignore list))))\\n\\n(defsuite defun\\n\\n  (deftest defun.1\\n    (progn\\n      (defun foo () 'foo)\\n      (defun bar (x) (list (foo) x))\\n      (bar 'bar))\\n    '(foo bar))\\n\\n  (deftest defun.2\\n    (progn\\n      (defun nope ())\\n      (nope))\\n    #void)\\n\\n  (deftest defun.3\\n    (signals-error\\n     (defun)\\n     match-error))\\n\\n  (deftest defun.4\\n    (signals-error\\n     (defun foo)\\n     match-error))\\n\\n  (deftest defun.5\\n    (signals-error\\n     (defun #t ())\\n     type-error :datum #t :expected-type 'symbol)))\\n\\n(defsuite progn\\n\\n  (deftest progn.1\\n    (progn)\\n    #void)\\n\\n  (deftest progn.2\\n    (progn (list))\\n    (list))\\n\\n  (deftest progn.3\\n    (progn\\n      (def env (the-environment))\\n      (progn (set env x 1) (set env x 2) x))\\n    2))\\n\\n(defsuite prog1\\n\\n  (deftest prog1.1\\n    (prog1 1)\\n    1)\\n\\n  (deftest prog1.2\\n    (prog1 1 2 3)\\n    1)\\n\\n  (deftest prog1.3\\n    (signals-error\\n     (prog1)\\n     match-error))\\n\\n  (deftest prog1.4\\n    (progn\\n      (def env (the-environment))\\n      (assert (= 4 (prog1 4 (set env x 2))))\\n      x)\\n    2))\\n\\n(defsuite eval\\n\\n  (deftest eval.1\\n    (eval #nil (the-environment))\\n    #nil)\\n\\n  (deftest eval.2\\n    (eval '(= 1 2) (the-environment))\\n    #f)\\n\\n  (deftest eval.3\\n    (signals-error\\n     (eval)\\n     match-error))\\n\\n  (deftest eval.4\\n    (signals-error\\n     (eval '1)\\n     match-error))\\n\\n  (deftest eval.5\\n    (signals-error\\n     (eval 'x (the-environment))\\n     unbound-symbol-error\\n     :symbol 'x))\\n\\n  (deftest eval.6\\n    (signals-error\\n     (eval '1 (the-environment) #t)\\n     match-error)))\\n\\n(defsuite cons\\n\\n  (deftest cons.1\\n    (cons (list 1) (list 2))\\n    '((1) . (2)))\\n\\n  (deftest cons.2\\n    (cons (list 1) #nil)\\n    '((1)))\\n\\n  (deftest cons.3\\n    (signals-error\\n     (cons)\\n     match-error))\\n\\n  (deftest cons.4\\n    (signals-error\\n     (cons 1)\\n     match-error))\\n\\n  (deftest cons.5\\n    (signals-error\\n     (cons 1 2 3)\\n     match-error)))\\n\\n(defsuite car\\n\\n  (deftest car.1\\n    (car (list 1 2))\\n    1)\\n\\n  (deftest car.2\\n    (signals-error\\n     (car)\\n     match-error))\\n\\n  (deftest car.3\\n    (signals-error\\n     (car 1)\\n     type-error :datum 1 :expected-type 'cons))\\n\\n  (deftest car.4\\n    (signals-error\\n     (car 1 2)\\n     match-error)))\\n\\n(defsuite cdr\\n\\n  (deftest cdr.1\\n    (cdr (list 1 2))\\n    '(2))\\n\\n  (deftest cdr.2\\n    (signals-error\\n     (cdr)\\n     match-error))\\n\\n  (deftest cdr.3\\n    (signals-error\\n     (cdr 1)\\n     type-error :datum 1 :expected-type 'cons))\\n\\n  (deftest car.4\\n    (signals-error\\n     (cdr 1 2)\\n     match-error)))\\n\\n(defsuite symbol-name\\n\\n  (deftest symbol-name.1\\n    (= \\\"foo\\\" (symbol-name 'foo)))\\n\\n  (deftest symbol-name.2\\n    (= \\\"foo\\\" (symbol-name '#'foo)))\\n\\n  (deftest symbol-name.3\\n    (= \\\"foo\\\" (symbol-name (class-symbol 'foo))))\\n\\n  (deftest symbol-name.4\\n    (= \\\"foo\\\" (symbol-name :foo)))\\n\\n  (deftest symbol-name.5\\n    (signals-error\\n     (symbol-name)\\n     match-error))\\n\\n  (deftest symbol-name.6\\n    (signals-error\\n     (symbol-name 12)\\n     type-error :datum 12 :expected-type 'symbol)))\\n\\n(defsuite variable-symbol\\n\\n  (deftest variable-symbol.1\\n    (variable-symbol '#'foo)\\n    'foo)\\n\\n  (deftest variable-symbol.2\\n    (variable-symbol :foo)\\n    'foo)\\n\\n  (deftest variable-symbol.4\\n    (variable-symbol (class-symbol 'foo))\\n    'foo)\\n\\n  (deftest variable-symbol.5\\n    (signals-error\\n     (variable-symbol)\\n     match-error))\\n\\n  (deftest variable-symbol.6\\n    (signals-error\\n     (variable-symbol 5)\\n     type-error :datum 5 :expected-type 'symbol)))\\n\\n(defsuite function-symbol\\n\\n  (deftest function-symbol.1\\n    (function-symbol 'foo)\\n    '#'foo)\\n\\n  (deftest function-symbol.2\\n    (function-symbol :foo)\\n    '#'foo)\\n\\n  (deftest function-symbol.4\\n    (function-symbol (class-symbol 'foo))\\n    '#'foo)\\n\\n  (deftest function-symbol.5\\n    (signals-error\\n     (function-symbol)\\n     match-error))\\n\\n  (deftest function-symbol.6\\n    (signals-error\\n     (function-symbol 5)\\n     type-error :datum 5 :expected-type 'symbol)))\\n\\n(defsuite class-symbol\\n\\n  (deftest class-symbol.1\\n    (class-symbol 'foo)\\n    (class-symbol 'foo))\\n\\n  (deftest class-symbol.2\\n    (class-symbol :foo)\\n    (class-symbol 'foo))\\n\\n  (deftest class-symbol.3\\n    (class-symbol '#'foo)\\n    (class-symbol 'foo))\\n\\n  (deftest class-symbol.5\\n    (signals-error\\n     (class-symbol)\\n     match-error))\\n\\n  (deftest class-symbol.6\\n    (signals-error\\n     (class-symbol 5)\\n     type-error :datum 5 :expected-type 'symbol)))\\n\\n(defsuite keyword-symbol\\n\\n  (deftest keyword-symbol.1\\n    (keyword-symbol 'foo)\\n    :foo)\\n\\n  (deftest keyword-symbol.2\\n    (keyword-symbol '#'foo)\\n    :foo)\\n\\n  (deftest keyword-symbol.3\\n    (keyword-symbol (class-symbol 'foo))\\n    :foo)\\n\\n  (deftest keyword-symbol.5\\n    (signals-error\\n     (keyword-symbol)\\n     match-error))\\n\\n  (deftest keyword-symbol.6\\n    (signals-error\\n     (keyword-symbol 5)\\n     type-error :datum 5 :expected-type 'symbol)))\\n\\n(defsuite wrap\\n\\n  (deftest wrap.1\\n    ((wrap (vau (x) #ignore x)) (list 1 2 3))\\n    '(1 2 3))\\n\\n  (deftest wrap.2\\n    (signals-error\\n     (wrap)\\n     match-error))\\n\\n  (deftest wrap.3\\n    (signals-error\\n     (wrap 33)\\n     type-error :datum 33 :expected-type 'operator)))\\n\\n(defsuite unwrap\\n\\n  (deftest unwrap.1\\n    ((unwrap (lambda (x) x)) (list 1 2 3))\\n    '(list 1 2 3))\\n\\n  (deftest unwrap.2\\n    (signals-error\\n     (unwrap)\\n     match-error))\\n\\n  (deftest unwrap.3\\n    (signals-error\\n     (unwrap 33)\\n     type-error :datum 33 :expected-type 'function)))\\n\\n(defsuite if\\n\\n  (deftest if.1\\n    (if #t (= 1 1) (= 1 2))\\n    #t)\\n\\n  (deftest if.2\\n    (if #f (= 1 1) (= 1 2))\\n    #f)\\n\\n  (deftest if.3\\n    (signals-error\\n     (if)\\n     match-error))\\n\\n  (deftest if.4\\n    (signals-error\\n     (if #t)\\n     match-error))\\n\\n  (deftest if.5\\n    (signals-error\\n     (if #t #f)\\n     match-error))\\n\\n  (deftest if.6\\n    (signals-error\\n     (if 1 #f #t)\\n     type-error :datum 1 :expected-type 'boolean)))\\n\\n(defsuite cond\\n\\n  (deftest cond.1\\n    (cond)\\n    #void)\\n\\n  (deftest cond.2\\n    (cond (#f 1 2 3))\\n    #void)\\n\\n  (deftest cond.3\\n    (cond (#f 1 2 3)\\n          ((= 1 1) 4 5 (= 1 1)))\\n    #t)\\n\\n  (deftest cond.4\\n    (signals-error\\n     (cond (1 #t))\\n     type-error :datum 1 :expected-type 'boolean)))\\n\\n(defsuite typecase\\n\\n  (deftest typecase.1\\n    (signals-error\\n     (typecase)\\n     match-error))\\n\\n  (deftest typecase.2\\n    (typecase (+ 2 2))\\n    #void)\\n\\n  (deftest typecase.3\\n    (typecase (+ 2 2)\\n      (object 1)\\n      (number 2))\\n    1)\\n\\n  (deftest typecase.4\\n    (typecase (+ 2 2)\\n      (number 2)\\n      (object 1))\\n    2))\\n\\n(defsuite not\\n\\n  (deftest not.1\\n    (not #t)\\n    #f)\\n\\n  (deftest not.2\\n    (not #f)\\n    #t)\\n\\n  (deftest not.3\\n    (signals-error\\n     (not)\\n     match-error))\\n\\n  (deftest not.4\\n    (signals-error\\n     (not 1)\\n     type-error :datum 1 :expected-type 'boolean)))\\n\\n(defsuite eq\\n\\n  (deftest eq.1\\n    (eq 'foo 'foo)\\n    #t)\\n\\n  (deftest eq.2\\n    (eq 'foo 'bar)\\n    #f)\\n\\n  (deftest eq.3\\n    (eq #t #t)\\n    #t)\\n\\n  (deftest eq.4\\n    (eq #f #f)\\n    #t)\\n\\n  (deftest eq.5\\n    (eq #void #void)\\n    #t)\\n\\n  (deftest eq.6\\n    (eq #ignore #ignore)\\n    #t)\\n\\n  (deftest eq.7\\n    (eq #nil #nil)\\n    #t)\\n\\n  (deftest eq.8\\n    (eq #nil ())\\n    #t)\\n\\n  (deftest eq.9\\n    (eq #nil '())\\n    #t)\\n\\n  (deftest eq.10\\n    (signals-error\\n     (eq)\\n     match-error))\\n\\n  (deftest eq.11\\n    (signals-error\\n     (eq 11)\\n     match-error)))\\n\\n(defsuite =\\n\\n  (deftest =.1\\n    (= 1 1 1)\\n    #t)\\n\\n  (deftest =.2\\n    (= 1 1 2)\\n    #f)\\n\\n  (deftest =.3\\n    (= #t #t #t)\\n    #t)\\n\\n  (deftest =.4\\n    (= #t #t #f)\\n    #f)\\n\\n  (deftest =.5\\n    (= \\\"foo\\\" \\\"foo\\\" \\\"foo\\\")\\n    #t)\\n\\n  (deftest =.6\\n    (= (list (list 1)) (list (list 1)))\\n    #t)\\n\\n  (deftest =.7\\n    (signals-error\\n     (=)\\n     match-error))\\n\\n  (deftest =.8\\n    (signals-error\\n     (= 1)\\n     match-error)))\\n\\n(defsuite /=\\n\\n  (deftest /=.1\\n    (/= 1 1 1)\\n    #f)\\n\\n  (deftest /=.2\\n    (/= 1 1 2)\\n    #f)\\n\\n  (deftest /=.3\\n    (/= 1 2 3)\\n    #t)\\n\\n  (deftest /=.4\\n    (/= #t #t #f)\\n    #f)\\n\\n  (deftest /=.4b\\n    (/= #t #f)\\n    #t)\\n\\n  (deftest /=.5\\n    (/= \\\"foo\\\" \\\"foo\\\" \\\"foo\\\")\\n    #f)\\n\\n  (deftest /=.5b\\n    (/= \\\"foo\\\" \\\"bar\\\" \\\"quux\\\")\\n    #t)\\n\\n  (deftest /=.6\\n    (/= (list (list 1)) (list (list 2)))\\n    #t)\\n\\n  (deftest /=.7\\n    (signals-error\\n     (/=)\\n     match-error))\\n\\n  (deftest /=.8\\n    (/= 1)\\n    #t)\\n\\n  (deftest /=.9\\n    (/= 1 2 3 3)\\n    #f)\\n\\n  (deftest /=.9b\\n    (/= 3 1 2 3)\\n    #f)\\n\\n  (deftest /=.9c\\n    (/= 3 1 2 3 4)\\n    #f)\\n\\n  (deftest /=.9d\\n    (/= 1 2 3 3 4)\\n    #f)\\n\\n  (deftest /=.9e\\n    (/= 3 3 4)\\n    #f)\\n\\n  (deftest /=.10\\n    (/= #t \\\"a\\\")\\n    #t))\\n\\n(defsuite <\\n\\n  (deftest <.1\\n    (< 1 1 1)\\n    #f)\\n\\n  (deftest <.2\\n    (< 1 1 2)\\n    #f)\\n\\n  (deftest <.3\\n    (< -3 -2 -1 0 1 2 3)\\n    #t)\\n\\n  (deftest <.4\\n    (signals-error\\n     (< #t #t #f)\\n    error))\\n\\n  (deftest <.5\\n    (signals-error\\n     (< \\\"foo\\\" \\\"foo\\\" \\\"foo\\\")\\n     error))\\n\\n  (deftest <.6\\n    (signals-error\\n     (< (list (list 1)) (list (list 2)))\\n     error))\\n\\n  (deftest <.7\\n    (signals-error\\n     (<)\\n     match-error))\\n\\n  (deftest <.8\\n    (signals-error\\n     (< 1)\\n     match-error))\\n\\n  (deftest <.9\\n    (< 1 2 3 3)\\n    #f)\\n\\n  (deftest <.different-types\\n    (signals-error\\n     (< 1 2 3 \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type 'number)))\\n\\n(defsuite >\\n\\n  (deftest >.1\\n    (> 1 1 1)\\n    #f)\\n\\n  (deftest >.2\\n    (> 2 1 1)\\n    #f)\\n\\n  (deftest >.3\\n    (> 3 2 1 0 -1 -2 -3)\\n    #t)\\n\\n  (deftest >.4\\n    (signals-error\\n     (> #t #t #f)\\n    error))\\n\\n  (deftest >.5\\n    (signals-error\\n     (> \\\"foo\\\" \\\"foo\\\" \\\"foo\\\")\\n     error))\\n\\n  (deftest >.6\\n    (signals-error\\n     (> (list (list 1)) (list (list 2)))\\n     error))\\n\\n  (deftest >.7\\n    (signals-error\\n     (>)\\n     match-error))\\n\\n  (deftest >.8\\n    (signals-error\\n     (> 1)\\n     match-error))\\n\\n  (deftest >.9\\n    (> 3 2 1 1)\\n    #f)\\n\\n  (deftest >.different-types\\n    (signals-error\\n     (> 3 2 1 \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type 'number)))\\n\\n(defsuite <=\\n\\n  (deftest <=.1\\n    (<= 1 1 1)\\n    #t)\\n\\n  (deftest <=.2\\n    (<= 1 1 2)\\n    #t)\\n\\n  (deftest <=.3\\n    (<= -3 -2 -1 0 1 2 3)\\n    #t)\\n\\n  (deftest <=.4\\n    (signals-error\\n     (<= #t #t #f)\\n    error))\\n\\n  (deftest <=.5\\n    (signals-error\\n     (<= \\\"foo\\\" \\\"foo\\\" \\\"foo\\\")\\n     error))\\n\\n  (deftest <=.6\\n    (signals-error\\n     (<= (list (list 1)) (list (list 2)))\\n     error))\\n\\n  (deftest <=.7\\n    (signals-error\\n     (<=)\\n     match-error))\\n\\n  (deftest <=.8\\n    (signals-error\\n     (<= 1)\\n     match-error))\\n\\n  (deftest <=.9\\n    (<= 1 2 3 3)\\n    #t)\\n\\n  (deftest <=.10\\n    (<= 1 2 3 3 -1)\\n    #f)\\n\\n  (deftest <=.different-types\\n    (signals-error\\n     (<= 1 1 3 \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type 'number)))\\n\\n(defsuite >=\\n\\n  (deftest >=.1\\n    (>= 1 1 1)\\n    #t)\\n\\n  (deftest >=.2\\n    (>= 2 1 1)\\n    #t)\\n\\n  (deftest >=.3\\n    (>= 3 2 1 1 0 0 -1 -2 -3 -3)\\n    #t)\\n\\n  (deftest >=.4\\n    (signals-error\\n     (>= #t #t #f)\\n    error))\\n\\n  (deftest >=.5\\n    (signals-error\\n     (>= \\\"foo\\\" \\\"foo\\\" \\\"foo\\\")\\n     error))\\n\\n  (deftest >=.6\\n    (signals-error\\n     (>= (list (list 1)) (list (list 2)))\\n     error))\\n\\n  (deftest >=.7\\n    (signals-error\\n     (>=)\\n     match-error))\\n\\n  (deftest >=.8\\n    (signals-error\\n     (>= 1)\\n     match-error))\\n\\n  (deftest >=.9\\n    (>= 3 3 2 1)\\n    #t)\\n\\n  (deftest >=.10\\n    (>= 3 3 2 100)\\n    #f)\\n\\n  (deftest >=.different-types\\n    (signals-error\\n     (>= 3 3 2 \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type 'number)))\\n\\n(defsuite +\\n\\n  (deftest +.1\\n    (+)\\n    0)\\n\\n  (deftest +.2\\n    (+ 10)\\n    10)\\n\\n  (deftest +.3\\n    (+ 10 10 10)\\n    30))\\n\\n(defsuite -\\n\\n  (deftest -.1\\n    (signals-error\\n     (-)\\n     match-error))\\n\\n  (deftest -.2\\n    (- 1)\\n    -1)\\n\\n  (deftest -.3\\n    (- -1)\\n    1)\\n\\n  (deftest -.4\\n    (- 100 10 1)\\n    89))\\n\\n(defsuite *\\n\\n  (deftest *.1\\n    (*)\\n    1)\\n\\n  (deftest *.2\\n    (* 10)\\n    10)\\n\\n  (deftest *.3\\n    (* 10 -10 10)\\n    -1000))\\n\\n(defsuite /\\n\\n  (deftest /.1\\n    (signals-error\\n     (/)\\n     match-error))\\n\\n  (deftest /.2\\n    (/ 10)\\n    0.1)\\n\\n  (deftest /.3\\n    (/ 100 10 2)\\n    5)\\n\\n  (deftest /.4\\n    (/ -100 10 2)\\n    -5))\\n\\n(defsuite typep\\n\\n  (deftest typep.1\\n    (and\\n     (typep #t #^boolean)\\n     (typep #f #^boolean)\\n     (typep 12 #^number)\\n     (typep \\\"foo\\\" #^string)\\n     (typep #nil #^nil)\\n     (typep #^object #^class)))\\n\\n  (deftest typep.2\\n    (and\\n     (typep #^condition #^standard-class)\\n     (typep #^condition #^class)\\n     (typep #^condition #^object)))\\n\\n  (deftest typep.3\\n    (signals-error\\n     (typep)\\n     match-error))\\n\\n  (deftest typep.3\\n    (signals-error\\n     (typep \\\"foo\\\")\\n     match-error)))\\n\\n(defsuite intern\\n\\n  (deftest intern.1\\n    (eq 'foo (intern \\\"foo\\\")))\\n\\n  (deftest intern.2\\n    (signals-error\\n     (intern 12)\\n     type-error :datum 12 :expected-type 'string))\\n\\n  (deftest intern.3\\n    (signals-error\\n     (intern)\\n     match-error)))\\n\\n(defsuite apply\\n\\n  (deftest apply.1\\n    (apply #'list ())\\n    '())\\n\\n  (deftest apply.2\\n    (apply #'list '(1))\\n    '(1))\\n\\n  (deftest apply.3\\n    (signals-error\\n     (apply)\\n     match-error))\\n\\n  (deftest apply.4\\n    (signals-error\\n     (apply #'list)\\n     match-error))\\n\\n  (deftest apply.5\\n    (signals-error\\n     (apply 12 '())\\n     type-error :datum 12 :expected-type 'function)))\\n\\n(defsuite class\\n\\n  (deftest class.1\\n    (eq #^string (class-of \\\"foo\\\")))\\n\\n  (deftest class.2\\n    (signals-error\\n     (class)\\n     match-error))\\n\\n  (deftest class.3\\n    (signals-error\\n     (class \\\"string\\\")\\n     type-error :datum \\\"string\\\" :expected-type 'symbol))\\n\\n  (deftest class.4\\n    (signals-error\\n     #^does-not-exist\\n     unbound-symbol-error :symbol (class-symbol 'does-not-exist))))\\n\\n(defsuite find-class\\n\\n  (deftest find-class.1\\n    (eq (find-class 'string (the-environment))\\n        (class-of \\\"foo\\\")))\\n\\n  (deftest find-class.2a\\n    (signals-error\\n     (find-class)\\n     match-error))\\n\\n  (deftest find-class.2b\\n    (signals-error\\n     (find-class 'foo)\\n     match-error))\\n\\n  (deftest find-class.3\\n    (signals-error\\n     (find-class \\\"string\\\" (the-environment))\\n     type-error :datum \\\"string\\\" :expected-type 'symbol))\\n\\n  (deftest find-class.4\\n    (signals-error\\n     (find-class 'does-not-exist (the-environment))\\n     unbound-symbol-error :symbol (class-symbol 'does-not-exist))))\\n\\n(defsuite make-environment\\n\\n  (deftest make-environment.1\\n    (let ((env (make-environment)))\\n      (boundp '#'def env))\\n    #f)\\n\\n  (deftest make-environment.2\\n    (let* ((env (make-environment (the-environment)))\\n           (child (make-environment env)))\\n      (assert (not (boundp 'x env)))\\n      (assert (not (boundp 'x child)))\\n      (eval '(def x 1) env)\\n      (assert (boundp 'x env))\\n      (assert (boundp 'x child))\\n      (assert (= 1 (eval 'x env)))\\n      (assert (= 1 (eval 'x child))))\\n    #void)\\n\\n  (deftest make-environment.3\\n    (signals-error\\n     (make-environment 12)\\n     type-error :datum 12 :expected-type 'environment)))\\n\\n(defsuite cxxr\\n\\n  (deftest caar.1\\n    (caar '((1 2) (3 4) (5 6)))\\n    1)\\n\\n  (deftest caar.2\\n    (signals-error\\n     (caar)\\n     match-error))\\n\\n  (deftest caar.3\\n    (signals-error\\n     (caar 1)\\n     type-error :datum 1 :expected-type 'cons))\\n\\n  (deftest cadr.1\\n    (cadr '((1 2) (3 4) (5 6)))\\n    '(3 4))\\n\\n  (deftest cadr.2\\n    (signals-error\\n     (cadr)\\n     match-error))\\n\\n  (deftest cadr.3\\n    (signals-error\\n     (cadr 1)\\n     type-error :datum 1 :expected-type 'cons))\\n\\n  (deftest cddr.1\\n    (cddr '((1 2) (3 4) (5 6)))\\n    '((5 6)))\\n\\n  (deftest cddr.2\\n    (signals-error\\n     (cddr)\\n     match-error))\\n\\n  (deftest cddr.3\\n    (signals-error\\n     (cddr 1)\\n     type-error :datum 1 :expected-type 'cons))\\n\\n  (deftest cdar.1\\n    (cdar '((1 2) (3 4) (5 6)))\\n    '(2))\\n\\n  (deftest cdar.2\\n    (signals-error\\n     (cdar)\\n     match-error))\\n\\n  (deftest cdar.3\\n    (signals-error\\n     (cdar 1)\\n     type-error :datum 1 :expected-type 'cons))\\n\\n(defsuite when\\n\\n  (deftest when.1\\n    (when #t)\\n    #void)\\n\\n  (deftest when.2\\n    (when #f)\\n    #void)\\n\\n  (deftest when.3\\n    (when #t 1 2 (= 1 1))\\n    #t)\\n\\n  (deftest when.4\\n    (when #f 1 2 3)\\n    #void)\\n\\n  (deftest when.5\\n    (signals-error\\n     (when)\\n     match-error))\\n\\n  (deftest when.6\\n    (signals-error\\n     (when 1)\\n     type-error :datum 1 :expected-type 'boolean))))\\n\\n(defsuite unless\\n\\n  (deftest unless.1\\n    (unless #t)\\n    #void)\\n\\n  (deftest unless.2\\n    (unless #f)\\n    #void)\\n\\n  (deftest unless.3\\n    (unless #f 1 2 (= 1 1))\\n    #t)\\n\\n  (deftest unless.4\\n    (unless #t 1 2 3)\\n    #void)\\n\\n  (deftest unless.5\\n    (signals-error\\n     (unless)\\n     match-error))\\n\\n  (deftest unless.6\\n    (signals-error\\n     (unless 1)\\n     type-error :datum 1 :expected-type 'boolean)))\\n\\n(defsuite null\\n\\n  (deftest null.1\\n    (null #nil)\\n    #t)\\n\\n  (deftest null.2\\n    (null '(1))\\n    #f)\\n\\n  (deftest null.3\\n    (signals-error\\n     (null)\\n     match-error))\\n\\n  (deftest null.4\\n    (null 1)\\n    #f))\\n\\n(defsuite mapcar\\n\\n  (deftest mapcar.1\\n    (mapcar #'intern ())\\n    ())\\n\\n  (deftest mapcar.2\\n    (mapcar #'intern '(\\\"foo\\\" \\\"bar\\\"))\\n    '(foo bar))\\n\\n  (deftest mapcar.3\\n    (signals-error\\n     (mapcar)\\n     match-error))\\n\\n  (deftest mapcar.4\\n    (signals-error\\n     (mapcar #'intern)\\n     match-error))\\n\\n  (deftest mapcar.5\\n    (signals-error\\n     (mapcar #'intern 12)\\n     type-error :datum 12 :expected-type 'cons))\\n\\n  (deftest mapcar.6\\n    (signals-error\\n     (mapcar 12 '(1))\\n     type-error :datum 12 :expected-type 'operator)))\\n\\n(defsuite reduce\\n\\n  (deftest reduce.1\\n    (reduce #'%* (list 1 2 3) :initial-value 10)\\n    60)\\n\\n  (deftest reduce.2\\n    (reduce #'%* (list) :initial-value 10)\\n    10)\\n\\n  (deftest reduce.3\\n    (signals-error\\n     (reduce)\\n     match-error))\\n\\n  (deftest reduce.4\\n    (signals-error\\n     (reduce #'%*)\\n     match-error))\\n\\n  (deftest reduce.5\\n    (signals-error\\n     (reduce #'%* (list))\\n     match-error))\\n\\n  (deftest reduce.ansi.3\\n    (reduce #'cons '(a b c d e f) :initial-value 'z)\\n    '((((((z . a) . b) . c) . d) . e) . f))\\n\\n  (deftest reduce.clhs.1\\n    (reduce #'list '(1 2 3 4) :initial-value 'foo)\\n    '((((foo 1) 2) 3) 4)))\\n\\n(defsuite mapc\\n\\n  (deftest mapc.1\\n    (signals-error\\n     (mapc)\\n     match-error))\\n\\n  (deftest mapc.2\\n    (signals-error\\n     (mapc (lambda (x) x))\\n     match-error))\\n\\n  (deftest mapc.3\\n    (signals-error\\n     (mapc \\\"foo\\\" '(1 2 3))\\n     type-error :datum \\\"foo\\\" :expected-type 'operator))\\n\\n  (deftest mapc.4\\n    (signals-error\\n     (mapc (lambda (x) x) \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type 'cons))\\n\\n  (deftest mapc.5\\n    (let ((ct 0))\\n      (def env (the-environment))\\n      (mapc (lambda (x) (set env ct (+ ct x)))\\n            '(1 2 3 4))\\n      ct)\\n    10)\\n\\n  (deftest mapc.6\\n    (let ((list (list 1 2 3)))\\n      (mapc (lambda (x)) list))\\n    (list 1 2 3)))\\n\\n(defsuite dolist\\n\\n  (deftest dolist.1\\n    (signals-error\\n     (dolist)\\n     match-error))\\n\\n  (deftest dolist.2\\n    (signals-error\\n     (dolist ())\\n     match-error))\\n\\n  (deftest dolist.3\\n    (signals-error\\n     (dolist (x))\\n     match-error))\\n\\n  (deftest dolist.4\\n    (dolist (x '(1 2 3)))\\n    #void)\\n\\n  (deftest dolist.5\\n    (signals-error\\n     (dolist (x \\\"not-a-list\\\"))\\n     type-error :datum \\\"not-a-list\\\" :expected-type 'cons))\\n\\n  (deftest dolist.6\\n    (signals-error\\n     (dolist (\\\"not-a-symbol\\\" '(1 2 3)))\\n     type-error\\n     :datum \\\"not-a-symbol\\\"\\n     :expected-type '(or symbol ignore list)))\\n\\n  (deftest dolist.7\\n    (let ((ct 0))\\n      (def env (the-environment))\\n      (assert (= #void (dolist (x '(1 2 3 4))\\n                         'bla\\n                         (set env ct (+ ct x)))))\\n      ct)\\n    10)\\n\\n  (deftest dolist.8\\n    (let ((ct 0))\\n      (def env (the-environment))\\n      (dolist (x '(1 2 3 4) (+ ct 100))\\n        'bla\\n        (set env ct (+ ct x))))\\n    110)\\n\\n  (deftest dolist.9\\n    (dolist (x '(1 2 3) x))\\n    '()))\\n\\n(defsuite nth\\n\\n  (deftest nth.1\\n    (signals-error\\n     (nth)\\n     match-error))\\n\\n  (deftest nth.2\\n    (signals-error\\n     (nth '())\\n     match-error))\\n\\n  (deftest nth.3\\n    (signals-error\\n     (nth #t '(1 2))\\n     type-error :datum #t :expected-type 'number))\\n\\n  (deftest nth.3a\\n    (signals-error\\n     (nth 1 '())\\n     type-error :datum '() :expected-type 'cons))\\n\\n  (deftest nth.4\\n    (signals-error\\n     (nth 0 \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type 'cons))\\n\\n  (deftest nth.5\\n    (nth 0 '(1 2))\\n    1)\\n\\n  (deftest nth.6\\n    (nth 1 '(1 2))\\n    2)\\n\\n  (deftest nth.7\\n    (signals-error\\n     (nth 2 '(1 2))\\n     type-error :datum '() :expected-type 'cons)))\\n\\n(defsuite reverse\\n\\n  (deftest reverse.1\\n    (signals-error\\n     (reverse)\\n     match-error))\\n\\n  (deftest reverse.2\\n    (signals-error\\n     (reverse \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type 'list))\\n\\n  (deftest reverse.3\\n    (signals-error\\n     (reverse \\\"foo\\\" \\\"bar\\\")\\n     match-error))\\n\\n  (deftest reverse.4\\n    (reverse ())\\n    ())\\n\\n  (deftest reverse.5\\n    (reverse '(1))\\n    '(1))\\n\\n  (deftest reverse.6\\n    (reverse '(1 2 3))\\n    '(3 2 1)))\\n\\n(defsuite append\\n\\n  (deftest append.1\\n    (signals-error\\n     (append)\\n     match-error))\\n\\n  (deftest append.2\\n    (signals-error\\n     (append '())\\n     match-error))\\n\\n  (deftest append.3\\n    (signals-error\\n     (append 1 '())\\n     type-error :datum 1 :expected-type 'list))\\n\\n  (deftest append.4\\n    (append '() '())\\n    '())\\n\\n  (deftest append.5\\n    (append '() 12)\\n    12)\\n\\n  (deftest append.6\\n    (append '(1) 12)\\n    '(1 . 12))\\n\\n  (deftest append.7\\n    (append '(1 2) 12)\\n    '(1 2 . 12))\\n\\n  (deftest append.8\\n    (append '(1 2) '(3 4))\\n    '(1 2 3 4)))\\n\\n(defsuite length\\n\\n  (deftest length.1\\n    (signals-error\\n     (length)\\n     error))\\n\\n  (deftest length.2\\n    (signals-error\\n     (length #f)\\n     error)))\\n\\n(defsuite list-length\\n\\n  (deftest list-length.1\\n    (length '())\\n    0)\\n\\n  (deftest list-length.2\\n    (length '(1 2 3))\\n    3))\\n\\n(defsuite nthcdr\\n\\n  (deftest nthcdr.1\\n    (signals-error\\n     (nthcdr)\\n     match-error))\\n\\n  (deftest nthcdr.2\\n    (signals-error\\n     (nthcdr 1)\\n     match-error))\\n\\n  (deftest nthcdr.3\\n    (signals-error\\n     (nthcdr \\\"foo\\\" '())\\n     type-error :datum \\\"foo\\\" :expected-type 'number))\\n\\n  (deftest nthcdr.4\\n    (signals-error\\n     (nthcdr 12 \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type 'list))\\n\\n  (deftest nthcdr.5\\n    (nthcdr 0 '())\\n    '())\\n\\n  (deftest nthcdr.6\\n    (signals-error\\n     (nthcdr 1 '())\\n     out-of-bounds-error))\\n\\n  (deftest nthcdr.7\\n    (signals-error\\n     (nthcdr 2 '(1))\\n     out-of-bounds-error))\\n\\n  (deftest nthcdr.8\\n    (signals-error\\n     (nthcdr 3 '(1))\\n     out-of-bounds-error))\\n\\n  (deftest nthcdr.9\\n    (nthcdr 0 '(1 2 3))\\n    '(1 2 3))\\n\\n  (deftest nthcdr.10\\n    (nthcdr 1 '(1 2 3))\\n    '(2 3))\\n\\n  (deftest nthcdr.11\\n    (nthcdr 2 '(1 2 3))\\n    '(3))\\n\\n  (deftest nthcdr.12\\n    (nthcdr 3 '(1 2 3))\\n    '())\\n\\n  (deftest nthcdr.13\\n    (signals-error\\n     (nthcdr 4 '(1 2 3))\\n     out-of-bounds-error)))\\n\\n(defsuite subseq\\n\\n  (deftest subseq.1\\n    (signals-error\\n     (subseq)\\n     error))\\n\\n  (deftest subseq.2\\n    (signals-error\\n     (subseq '())\\n     match-error))\\n\\n  (deftest subseq.3\\n    (signals-error\\n     (subseq 12 1 2)\\n     unbound-method-error :class #^number :method-name 'subseq)))\\n\\n(defsuite list-subseq\\n\\n  (deftest list-subseq.4\\n    (signals-error\\n     (subseq '() \\\"a\\\" 2)\\n     type-error :datum \\\"a\\\" :expected-type 'number))\\n\\n  (deftest list-subseq.5\\n    (signals-error\\n     (subseq '() 1 \\\"b\\\")\\n     type-error :datum \\\"b\\\" :expected-type 'number))\\n\\n  (deftest list-subseq.6\\n    (signals-error\\n     (subseq '() 1)\\n     out-of-bounds-error))\\n\\n  (deftest list-subseq.7\\n    (signals-error\\n     (subseq '() 0 1)\\n     out-of-bounds-error))\\n\\n  (deftest list-subseq.8\\n    (subseq '() 0)\\n    '())\\n\\n  (deftest list-subseq.9\\n    (subseq '() 0 0)\\n    '())\\n\\n  (deftest list-subseq.10\\n    (subseq '() 0 #void)\\n    '())\\n\\n  (deftest list-subseq.11\\n    (subseq '(1 2 3) 0)\\n    '(1 2 3))\\n\\n  (deftest list-subseq.12\\n    (subseq '(1 2 3) 0 #void)\\n    '(1 2 3))\\n\\n  (deftest list-subseq.13\\n    (subseq '(0 1 2 3 4 5) 2)\\n    '(2 3 4 5))\\n\\n  (deftest list-subseq.14\\n    (subseq '(0 1 2 3 4 5) 3 5)\\n    '(3 4))\\n\\n  (deftest list-subseq.15\\n    (subseq '(0 1 2 3 4 5) 0 6)\\n    '(0 1 2 3 4 5))\\n\\n  (deftest list-subseq.16\\n    (signals-error\\n     (subseq '(0 1 2 3 4 5) 0 7)\\n     out-of-bounds-error))\\n\\n  (deftest list-subseq.17\\n    (signals-error\\n     (subseq '(0 1 2 3 4 5) 7 10)\\n     out-of-bounds-error))\\n\\n  (deftest list-subseq.18\\n    (subseq '(0 1 2 3 4 5) 6)\\n    '()))\\n\\n(defsuite string-subseq\\n\\n  (deftest string-subseq.2\\n    (signals-error\\n     (subseq \\\"\\\")\\n     match-error))\\n\\n  (deftest string-subseq.4\\n    (signals-error\\n     (subseq \\\"\\\" \\\"a\\\" 2)\\n     type-error :datum \\\"a\\\" :expected-type 'number))\\n\\n  (deftest string-subseq.5\\n    (signals-error\\n     (subseq \\\"foo\\\" 1 \\\"b\\\")\\n     type-error :datum \\\"b\\\" :expected-type 'number))\\n\\n  (deftest string-subseq.6\\n    (signals-error\\n     (subseq \\\"\\\" 1)\\n     out-of-bounds-error))\\n\\n  (deftest string-subseq.7\\n    (signals-error\\n     (subseq \\\"\\\" 0 1)\\n     out-of-bounds-error))\\n\\n  (deftest string-subseq.8\\n    (subseq \\\"\\\" 0)\\n    \\\"\\\")\\n\\n  (deftest string-subseq.9\\n    (subseq \\\"\\\" 0 0)\\n    \\\"\\\")\\n\\n  (deftest string-subseq.10\\n    (subseq \\\"\\\" 0 #void)\\n    \\\"\\\")\\n\\n  (deftest string-subseq.10.1\\n    (signals-error\\n     (subseq \\\"\\\" 1 #void)\\n     out-of-bounds-error))\\n\\n  (deftest string-subseq.11\\n    (subseq \\\"123\\\" 0)\\n    \\\"123\\\")\\n\\n  (deftest string-subseq.12\\n    (subseq \\\"123\\\" 0 #void)\\n    \\\"123\\\")\\n\\n  (deftest string-subseq.13\\n    (subseq \\\"012345\\\" 2)\\n    \\\"2345\\\")\\n\\n  (deftest string-subseq.14\\n    (subseq \\\"012345\\\" 3 5)\\n    \\\"34\\\")\\n\\n  (deftest string-subseq.15\\n    (subseq \\\"012345\\\" 0 6)\\n    \\\"012345\\\")\\n\\n  (deftest string-subseq.16\\n    (signals-error\\n     (subseq \\\"012345\\\" 0 7)\\n     out-of-bounds-error))\\n\\n  (deftest string-subseq.17\\n    (signals-error\\n     (subseq \\\"012345\\\" 7 10)\\n     out-of-bounds-error))\\n\\n  (deftest string-subseq.18\\n    (subseq \\\"012345\\\" 6)\\n     \\\"\\\"))\\n\\n(defsuite elt\\n\\n  (deftest elt.1\\n    (signals-error\\n     (elt)\\n     error))\\n\\n  (deftest elt.2\\n    (signals-error\\n     (elt #t 0)\\n     error)))\\n\\n(defsuite list-elt\\n\\n  (deftest list-elt.2\\n    (signals-error\\n     (elt '())\\n     match-error))\\n\\n  (deftest list-elt.3a\\n    (signals-error\\n     (elt '() 1)\\n     type-error :datum '() :expected-type 'cons))\\n\\n  (deftest list-elt.5\\n    (elt '(1 2) 0)\\n    1)\\n\\n  (deftest list-elt.6\\n    (elt '(1 2) 1)\\n    2)\\n\\n  (deftest list-elt.7\\n    (signals-error\\n     (elt '(1 2) 2)\\n     type-error :datum '() :expected-type 'cons)))\\n\\n(defsuite memberp\\n\\n  (deftest memberp.1\\n    (signals-error\\n     (memberp)\\n     match-error))\\n\\n  (deftest memberp.2\\n    (signals-error\\n     (memberp 'a)\\n     match-error))\\n\\n  (deftest memberp.3\\n    (memberp 'a '() :test #'eq)\\n    #f)\\n\\n  (deftest memberp.4\\n    (memberp 'b '(a) :test #'eq)\\n    #f)\\n\\n  (deftest memberp.5\\n    (memberp 'b '(a b) :test #'eq)\\n    #t)\\n\\n  (deftest memberp.5b\\n    (memberp  'x '(a b) :test #'eq)\\n    #f)\\n\\n  (deftest memberp.6\\n    (memberp 'b '(a b c) :test #'eq)\\n    #t)\\n\\n  (deftest memberp.7\\n    (memberp \\\"b\\\" '(\\\"a\\\" \\\"b\\\" \\\"c\\\") :test #'eq)\\n    #f)\\n\\n  (deftest memberp.8\\n    (memberp \\\"b\\\" '(\\\"a\\\" \\\"b\\\" \\\"c\\\") :test #'=)\\n    #t))\\n\\n(defsuite remove-if\\n\\n  (deftest remove-if.1\\n    (signals-error\\n     (remove-if)\\n     match-error))\\n\\n  (deftest remove-if.2\\n    (signals-error\\n     (remove-if (lambda (item) #t))\\n     match-error))\\n\\n  (deftest remove-if.3\\n    (signals-error\\n     (remove-if (lambda (item) #t) 12)\\n     type-error :datum 12 :expected-type 'cons))\\n\\n  (deftest remove-if.4\\n    (signals-error\\n     (remove-if 1 '(1 2))\\n     type-error :datum 1 :expected-type 'operator))\\n\\n  (deftest remove-if.5\\n    (remove-if (lambda (item) (= 1 item)) '(1 2 3 1 2 3))\\n    '(2 3 2 3))\\n\\n  (deftest remove-if.6\\n    (remove-if (lambda (item) (= 1 item)) '())\\n    '()))\\n\\n(defsuite get?\\n\\n  (deftest get?.1\\n    (get? '(:foo 1 :bar 2) :foo)\\n    (some 1))\\n\\n  (deftest get?.2\\n    (get? '(:foo 1 :bar 2) :bar)\\n    (some 2))\\n\\n  (deftest get?.3\\n    (get? '() :bar)\\n    #nil)\\n\\n  (deftest get?.4\\n    (get? '(:foo 1 :bar 2) :quux)\\n    #nil)\\n\\n  (deftest get?.5\\n    (signals-error\\n     (get?)\\n     match-error)\\n    #t)\\n\\n  (deftest get?.6\\n    (signals-error\\n     (get? '())\\n     match-error)\\n    #t)\\n\\n  (deftest get?.7\\n    (signals-error\\n     (get? 12 :quux)\\n     match-error)\\n    #t))\\n\\n(defsuite and\\n\\n  (deftest and.1\\n    (and)\\n    #t)\\n\\n  (deftest and.2\\n    (and #t #f)\\n    #f)\\n\\n  (deftest and.3\\n    (and #t (= 1 1) #t)\\n    #t)\\n\\n  (deftest and.4\\n    (signals-error\\n     (and #t #t 1)\\n     type-error :datum 1 :expected-type 'boolean))\\n\\n  (deftest and.5\\n    (progn\\n      (def x 1)\\n      (def env (the-environment))\\n      (and #f (set env x 2))\\n      x)\\n    1))\\n\\n(defsuite or\\n\\n  (deftest or.1\\n    (or)\\n    #f)\\n\\n  (deftest or.2\\n    (or #t #f)\\n    #t)\\n\\n  (deftest or.3\\n    (or #t #t #t)\\n    #t)\\n\\n  (deftest or.4\\n    (or #f #f #f)\\n    #f)\\n\\n  (deftest or.5\\n    (signals-error\\n     (or #f #f 1)\\n     type-error :datum 1 :expected-type 'boolean))\\n\\n  (deftest or.6\\n    (progn\\n      (def x 1)\\n      (def env (the-environment))\\n      (or #t (set env x 2))\\n      x)\\n    1))\\n\\n(defsuite make-instance\\n\\n  (deftest make-instance.1\\n    (progn\\n      (def obj (make-instance #^type-error :datum 1 :expected-type 'string))\\n      (assert (= (slot-value obj 'datum) 1))\\n      (assert (= (slot-value obj :datum) 1))\\n      (assert (= (slot-value obj 'expected-type) 'string))\\n      (assert (= (slot-value obj :expected-type) 'string)))\\n    #void)\\n\\n  (deftest make-instance.2\\n    (signals-error\\n     (make-instance)\\n     match-error))\\n\\n  (deftest make-instance.3\\n    (signals-error\\n     (make-instance 3)\\n     type-error :datum 3 :expected-type 'standard-class))\\n\\n  (deftest make-instance.4\\n    (signals-error\\n     (make-instance #^object)\\n     type-error :datum #^object :expected-type 'standard-class))\\n\\n  (deftest make-instance.5\\n    (signals-error\\n     (make-instance #^standard-object :x)\\n     assertion-error)))\\n\\n(defsuite slots\\n\\n  (deftest slots.1\\n    (progn\\n      (def obj (make-instance #^standard-object))\\n      (assert (not (slot-bound-p obj 'x)))\\n      (assert (not (slot-bound-p obj :x)))\\n      (assert (signals-error (slot-value obj 'x)\\n                             unbound-slot-error :object obj :slot-name 'x))\\n      (assert (signals-error (slot-value obj :x)\\n                             unbound-slot-error :object obj :slot-name :x))\\n      (set-slot-value obj 'x 1)\\n      (assert (slot-bound-p obj 'x))\\n      (assert (slot-bound-p obj :x))\\n      (assert (= (slot-value obj 'x) 1))\\n      (assert (= (slot-value obj :x) 1))\\n      (set-slot-value obj 'x 2)\\n      (assert (= (slot-value obj 'x) 2))\\n      (assert (= (slot-value obj :x) 2)))\\n    #void)\\n\\n  (deftest slot-value.1\\n    (signals-error\\n     (slot-value \\\"foo\\\" 'x)\\n     type-error :datum \\\"foo\\\" :expected-type 'standard-object))\\n\\n  (deftest slot-value.1a\\n    (signals-error\\n     (slot-value (make-instance #^standard-object) \\\"x\\\")\\n     type-error :datum \\\"x\\\" :expected-type 'symbol))\\n\\n  (deftest slot-value.2\\n    (signals-error\\n     (slot-value (make-instance #^standard-object))\\n     match-error))\\n\\n  (deftest slot-value.3\\n    (signals-error\\n     (slot-value)\\n     match-error))\\n\\n  (deftest set-slot-value.1\\n    (signals-error\\n     (set-slot-value \\\"foo\\\" 'x 1)\\n     type-error :datum \\\"foo\\\" :expected-type 'standard-object))\\n\\n  (deftest set-slot-value.1a\\n    (signals-error\\n     (set-slot-value (make-instance #^standard-object) \\\"x\\\" 1)\\n     type-error :datum \\\"x\\\" :expected-type 'symbol))\\n\\n  (deftest set-slot-value.2\\n    (signals-error\\n     (set-slot-value (make-instance #^standard-object))\\n     match-error))\\n\\n  (deftest set-slot-value.3\\n    (signals-error\\n     (set-slot-value (make-instance #^standard-object) 'x)\\n     match-error))\\n\\n  (deftest slot-bound-p.1\\n    (signals-error\\n     (slot-bound-p \\\"foo\\\" 'x)\\n     type-error :datum \\\"foo\\\" :expected-type 'standard-object))\\n\\n  (deftest slot-bound-p.1a\\n    (signals-error\\n     (slot-bound-p (make-instance #^standard-object) \\\"x\\\")\\n     type-error :datum \\\"x\\\" :expected-type 'symbol))\\n\\n  (deftest slot-bound-p.2\\n    (signals-error\\n     (slot-bound-p)\\n     match-error))\\n\\n  (deftest slot-bound-p.3\\n    (signals-error\\n     (slot-bound-p (make-instance #^standard-object))\\n     match-error)))\\n\\n(defsuite let\\n\\n  (deftest let.1\\n    (let ((x 1))\\n      x)\\n    1)\\n\\n  (deftest let.2\\n    (let ((x 1))\\n      (let ((x 2))\\n        x))\\n    2)\\n\\n  (deftest let.3\\n    (let ((x 1))\\n      (let ((x 2)))\\n      x)\\n    1)\\n\\n  (deftest let.4\\n    (let ((x 1))\\n      (let ((x 2)\\n            (it x))\\n        it))\\n    1)\\n\\n  (deftest let.5\\n    (let ())\\n    #void)\\n\\n  (deftest let.6\\n    (signals-error\\n     (let)\\n     match-error))\\n\\n  (deftest let.7\\n    (signals-error\\n     (let #t)\\n     type-error :datum #t :expected-type 'cons))\\n\\n  (deftest let.8\\n    (signals-error\\n     (let ((#t 1)))\\n     type-error :datum #t :expected-type '(or symbol ignore list))))\\n\\n(defsuite let*\\n\\n  (deftest let*.1\\n    (let* ((x 1)\\n           (y x)\\n           (z y))\\n      (list x y z))\\n    '(1 1 1))\\n\\n  (deftest let*.2\\n    (let* ())\\n    #void)\\n\\n  (deftest let*.3\\n    (signals-error\\n     (let*)\\n     match-error))\\n\\n  (deftest let*.4\\n    (signals-error\\n     (let* #t)\\n     type-error :datum #t :expected-type 'cons))\\n\\n  (deftest let*.5\\n    (signals-error\\n     (let* ((#t 1)))\\n     type-error :datum #t :expected-type '(or symbol ignore list))))\\n\\n(defsuite flet\\n\\n  (deftest flet.1\\n    (flet ((f () 1))\\n          (flet ((f () 2)\\n                 (g () (f)))\\n                (g)))\\n    1)\\n\\n  (deftest flet.2\\n    (flet ((foo ()))\\n          (foo))\\n    #void)\\n\\n  (deftest flet.2\\n    (flet ((foo (x) x))\\n          (foo 1))\\n    1)\\n\\n  (deftest flet.3\\n    (signals-error\\n     (flet)\\n     match-error))\\n\\n  (deftest flet.4\\n    (signals-error\\n     (flet #t)\\n     type-error :datum #t :expected-type 'cons))\\n\\n  (deftest flet.5\\n    (flet ())\\n    #void))\\n\\n(defsuite labels\\n\\n  (deftest labels.1\\n    (labels ((f () 1))\\n            (labels ((f () 2)\\n                     (g () (f)))\\n                    (g)))\\n    2)\\n\\n  (deftest labels.2\\n    (labels ((foo ()))\\n            (foo))\\n    #void)\\n\\n  (deftest labels.3\\n    (labels ((foo (x) x))\\n            (foo 1))\\n    1)\\n\\n  (deftest labels.4\\n    (signals-error\\n     (labels)\\n     match-error))\\n\\n  (deftest labels.5\\n    (signals-error\\n     (labels #t)\\n     type-error :datum #t :expected-type 'cons))\\n\\n  (deftest labels.6\\n    (labels ())\\n    #void))\\n\\n(defsuite loop-let\\n\\n  (deftest loop-let.1\\n    (signals-error\\n     (loop-let)\\n     match-error))\\n\\n  (deftest loop-let.2\\n    (signals-error\\n     (loop-let -name-)\\n     match-error))\\n\\n  (deftest loop-let.3\\n    (loop-let -name- ())\\n    #void)\\n\\n  (deftest loop-let.4\\n    (loop-let -sum- ((as (list 1 2 3))\\n                     (bs (list 4 5 6)))\\n      (if (null as)\\n          #nil\\n          (cons (+ (car as) (car bs))\\n                (-sum- (cdr as) (cdr bs)))))\\n    '(5 7 9)))\\n\\n(defsuite while\\n\\n  (deftest while.1\\n    (signals-error\\n     (while)\\n     match-error))\\n\\n  (deftest while.2\\n    (while (= 1 2))\\n    #void)\\n\\n  (deftest while.3\\n    (signals-error\\n     (while 12)\\n     type-error :datum 12 :expected-type 'boolean))\\n\\n  (deftest while.4\\n    (progn\\n      (def x 0)\\n      (def y 0)\\n      (def env (the-environment))\\n      (while (< x 5)\\n        (set env x (+ x 1))\\n        (set env y (+ y 1)))\\n      (list x y))\\n    '(5 5)))\\n\\n(defsuite until\\n\\n  (deftest until.1\\n    (signals-error\\n     (until)\\n     match-error))\\n\\n  (deftest until.2\\n    (until (/= 1 2))\\n    #void)\\n\\n  (deftest until.3\\n    (signals-error\\n     (until 12)\\n     type-error :datum 12 :expected-type 'boolean))\\n\\n  (deftest until.4\\n    (progn\\n      (def x 0)\\n      (def y 0)\\n      (def env (the-environment))\\n      (until (> x 4)\\n        (set env x (+ x 1))\\n        (set env y (+ y 1)))\\n      (list x y))\\n    '(5 5)))\\n\\n(defsuite dotimes\\n\\n  (deftest dotimes.1\\n    (dotimes (temp-one 10 temp-one))\\n    10)\\n\\n  (deftest dotimes.2\\n    (progn\\n      (def temp-two 0)\\n      (def env (the-environment))\\n      (dotimes (temp-one 10 #t) (set env temp-two (+ temp-two 1)))\\n      temp-two)\\n    10)\\n\\n  (deftest dotimes.3\\n    (signals-error\\n     (dotimes)\\n     match-error))\\n\\n  (deftest dotimes.4\\n    (signals-error\\n     (dotimes (x))\\n     match-error))\\n\\n  (deftest dotimes.5\\n    (dotimes (x 0))\\n    #void)\\n\\n  (deftest dotimes.6\\n    (dotimes (x 1))\\n    #void)\\n\\n  (deftest dotimes.7\\n    (dotimes (x 1) 1 2 3)\\n    #void)\\n\\n  (deftest dotimes.8\\n    (signals-error\\n     (dotimes (x \\\"foo\\\") 1 2 3)\\n     type-error :datum \\\"foo\\\" :expected-type 'number))\\n\\n  (deftest dotimes.9\\n    (progn\\n      (def x 0)\\n      (def y 0)\\n      (def env (the-environment))\\n      (dotimes (i 5 (list x y))\\n        (set env x (+ x 1))\\n        (set env y (+ y 1))))\\n    '(5 5)))\\n\\n(defsuite optional\\n\\n  (deftest optional.1\\n    (optional '())\\n    #void)\\n\\n  (deftest optional.2\\n    (optional '() \\\"default\\\")\\n    \\\"default\\\")\\n\\n  (deftest optional.3\\n    (optional (some 1))\\n    1)\\n\\n  (deftest optional.4\\n    (optional (some 1) \\\"default\\\")\\n    1)\\n\\n  (deftest optional.5\\n    (signals-error\\n     (optional)\\n     match-error))\\n\\n  (deftest optional.6\\n    (signals-error\\n     (optional 1)\\n     type-error :datum 1 :expected-type 'cons))\\n\\n  (deftest optional.lazy.1\\n    (optional (some 1) (error (make-simple-error \\\"foo\\\")))\\n    1)\\n\\n  (deftest optional.lazy.2\\n    (signals-error\\n     (optional #nil (error (make-simple-error \\\"foo\\\")))\\n     simple-error :message \\\"foo\\\")))\\n\\n(defsuite optionals\\n\\n  (deftest optionals.1\\n    (optionals '())\\n    '())\\n\\n  (deftest optionals.2\\n    (optionals '() 1)\\n    '(1))\\n\\n  (deftest optionals.3\\n    (optionals '() 1 2)\\n    '(1 2))\\n\\n  (deftest optionals.4\\n    (optionals '(4) 1 2)\\n    '(4 2))\\n\\n  (deftest optionals.5\\n    (optionals '(4 5) 1 2)\\n    '(4 5))\\n\\n  (deftest optionals.6\\n    (optionals '(4 5 6) 1 2)\\n    '(4 5 6))\\n\\n  (deftest optionals.7\\n    (optionals '(4 5 6))\\n    '(4 5 6)))\\n\\n(defsuite get-option\\n\\n  (deftest get-option.1\\n    (signals-error\\n     (get-option)\\n     match-error))\\n\\n  (deftest get-option.2\\n    (get-option (some 2))\\n    2)\\n\\n  (deftest get-option.3\\n    (signals-error\\n     (get-option #nil)\\n     simple-error :message \\\"Option is nil\\\")))\\n\\n(defsuite some\\n\\n  (deftest some.1\\n    (some 1)\\n    (list 1))\\n\\n  (deftest some.2\\n    (signals-error\\n     (some)\\n     match-error))\\n\\n  (deftest some.3\\n    (signals-error\\n     (some 1 2)\\n     match-error)))\\n\\n(defsuite if-option\\n\\n  (deftest if-option.1\\n    (if-option (x (some 12))\\n               x\\n               14)\\n    12)\\n\\n  (deftest if-option.2\\n    (if-option (x #nil)\\n               x\\n               14)\\n    14)\\n\\n  (deftest if-option.3\\n    (signals-error\\n     (if-option)\\n     match-error))\\n\\n  (deftest if-option.4\\n    (signals-error\\n     (if-option (name))\\n     match-error))\\n\\n  (deftest if-option.4.2\\n    (signals-error\\n     (if-option (name) then)\\n     match-error))\\n\\n  (deftest if-option.5\\n    (signals-error\\n     (if-option (name option))\\n     match-error))\\n\\n  (deftest if-option.6\\n    (signals-error\\n     (if-option (name option) then)\\n     match-error)))\\n\\n(defsuite when-option\\n\\n  (deftest when-option.0\\n    (when-option (x (some 12)))\\n    #void)\\n\\n  (deftest when-option.1\\n    (when-option (x (some 12))\\n                 x)\\n    12)\\n\\n  (deftest when-option.2\\n    (when-option (x #nil)\\n                 x)\\n    #nil)\\n\\n  (deftest when-option.3\\n    (signals-error\\n     (when-option)\\n     match-error))\\n\\n  (deftest when-option.4\\n    (signals-error\\n     (when-option (name))\\n     match-error)))\\n\\n(defsuite unless-option\\n\\n  (deftest unless-option.0\\n    (unless-option (some 12) 2 3 (+ 2 2))\\n    #nil)\\n\\n  (deftest unless-option.1\\n    (unless-option #nil 2 3 (+ 2 2))\\n    4)\\n\\n  (deftest unless-option.2\\n    (unless-option #nil)\\n    #void)\\n\\n  (deftest unless-option.3\\n    (signals-error\\n     (unless-option)\\n     match-error)))\\n\\n(defsuite class-of\\n\\n  (deftest class-of.1\\n    (progn\\n      (assert (eq (class-of \\\"foo\\\") #^string))\\n      (assert (eq (class-of 12) #^number))\\n      (assert (eq (class-of #^string) #^built-in-class)))\\n    #void)\\n\\n  (deftest class-of.2\\n    (signals-error\\n     (class-of)\\n     match-error)))\\n\\n(defsuite set\\n\\n  (deftest set.1\\n    (progn\\n      (set (the-environment) x 1)\\n      x)\\n    1)\\n\\n  (deftest set.2\\n    (progn\\n      (def x 0)\\n      (set (the-environment) x 1)\\n      x)\\n    1)\\n\\n  (deftest set.3\\n    (progn\\n      (set (the-environment) (x y) (list 1 2))\\n      (list x y))\\n    '(1 2))\\n\\n  (deftest set.4\\n    (signals-error\\n     (set)\\n     match-error))\\n\\n  (deftest set.5\\n    (signals-error\\n     (set (the-environment))\\n     match-error))\\n\\n  (deftest set.6\\n    (signals-error\\n     (set (the-environment) x)\\n     match-error))\\n\\n  (deftest set.7\\n    (signals-error\\n     (set 33 x y)\\n     type-error :datum 33 :expected-type 'environment)))\\n\\n(defsuite class-name\\n\\n  (deftest class-name.1\\n    (signals-error\\n     (class-name)\\n     match-error))\\n\\n  (deftest class-name.2\\n    (signals-error\\n     (class-name \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type 'class))\\n\\n  (deftest class-name.3\\n    (class-name #^object)\\n    'object))\\n\\n(defsuite subclassp\\n\\n  (deftest subclassp.1\\n    (signals-error\\n     (subclassp)\\n     match-error))\\n\\n  (deftest subclassp.2\\n    (signals-error\\n     (subclassp #^object)\\n     match-error))\\n\\n  (deftest subclassp.3\\n    (signals-error\\n     (subclassp #^object \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type 'class))\\n\\n  (deftest subclassp.4\\n    (signals-error\\n     (subclassp \\\"foo\\\" #^object)\\n     type-error :datum \\\"foo\\\" :expected-type 'class))\\n\\n  (deftest subclassp.5\\n    (and (subclassp #^object #^object)\\n         (subclassp #^class #^object)\\n         (subclassp #^standard-class #^class)\\n         (subclassp #^standard-class #^object))\\n    #t))\\n\\n;; Utility used by `%make-standard-class' and `defclass' tests.\\n\\n(defun classes-test (c d p q)\\n  (assert (typep c #^standard-class))\\n  (assert (typep c #^class))\\n  (assert (typep c #^object))\\n  (assert (= (class-name c) 'point-2d))\\n  (assert (subclassp c #^standard-object))\\n  (assert (subclassp c #^object))\\n\\n  (assert (typep p c))\\n  (assert (typep p #^standard-object))\\n  (assert (typep p #^object))\\n  (assert (= (slot-value p 'x) 1))\\n  (assert (= (slot-value p 'y) 2))\\n\\n  (assert (typep d #^standard-class))\\n  (assert (typep d #^class))\\n  (assert (typep d #^object))\\n  (assert (= (class-name d) 'point-3d))\\n  (assert (subclassp d c))\\n  (assert (subclassp d #^standard-object))\\n  (assert (subclassp d #^object))\\n\\n  (assert (typep q d))\\n  (assert (typep q c))\\n  (assert (typep q #^standard-object))\\n  (assert (typep q #^object))\\n  (assert (= (slot-value q 'x) 1))\\n  (assert (= (slot-value q 'y) 2))\\n  (assert (= (slot-value q 'z) 3)))\\n\\n(defsuite %make-standard-class\\n\\n  (deftest %make-standard-class.1\\n    (let* ((c (%make-standard-class 'point-2d #^standard-object))\\n           (d (%make-standard-class 'point-3d c))\\n           (p (make-instance c :x 1 :y 2))\\n           (q (make-instance d :x 1 :y 2 :z 3)))\\n      (classes-test c d p q))\\n    #void)\\n\\n  (deftest %make-standard-class.2\\n    (signals-error\\n     (%make-standard-class 'foo #^object)\\n     type-error :datum #^object :expected-type 'standard-class))\\n\\n  #|\\n  (deftest %make-standard-class.3\\n    (signals-error\\n     (%make-standard-class 'foo)\\n     match-error))\\n\\n  (deftest %make-standard-class.4\\n    (signals-error\\n     (%make-standard-class)\\n     match-error))\\n  |#\\n\\n  (deftest %make-standard-class.5\\n    (signals-error\\n     (%make-standard-class \\\"foo\\\" #^standard-object)\\n     type-error :datum \\\"foo\\\" :expected-type 'symbol)))\\n\\n(defsuite %reinitialize-standard-class\\n\\n  (deftest* %reinitialize-standard-class.1\\n    (let* ((a (%make-standard-class 'a #^standard-object))\\n           (b (%make-standard-class 'b a))\\n           (c (%make-standard-class 'c b))\\n           (d (%make-standard-class 'd #^standard-object))\\n           (e (%make-standard-class 'e d)))\\n      (assert (subclassp b a))\\n      (assert (subclassp c a))\\n      (assert (subclassp c b))\\n      (%reinitialize-standard-class b e)\\n      (assert (subclassp b e))\\n      (assert (subclassp b d))\\n      (assert (subclassp c e))\\n      (assert (subclassp c d))\\n      (assert (subclassp c b)))))\\n\\n(defsuite defclass\\n\\n  (deftest defclass.1\\n    (signals-error\\n     (defclass foo () (\\\"a\\\"))\\n     type-error :datum \\\"a\\\" :expected-type 'symbol))\\n\\n  (deftest defclass.2\\n    (signals-error\\n     (defclass)\\n     match-error))\\n\\n  (deftest defclass.3\\n    (signals-error\\n     (defclass foo)\\n     match-error))\\n\\n  (deftest defclass.4\\n    (signals-error\\n     (defclass foo ())\\n     match-error))\\n\\n  (deftest defclass.5\\n    (defclass c () ())\\n    'c)\\n\\n  (deftest defclass.6\\n    (progn\\n      (defclass point-2d () (x y))\\n      (defclass point-3d (point-2d) (z))\\n      (def p (make-instance #^point-2d :x 1 :y 2))\\n      (def q (make-instance #^point-3d :x 1 :y 2 :z 3))\\n      (classes-test #^point-2d #^point-3d p q))\\n    #void)\\n\\n  (deftest* defclass.redefine.1\\n    (defclass a () ())\\n    (defclass b (a) ())\\n    (defgeneric foo (x))\\n    (defmethod foo ((a a)) \\\"foo\\\")\\n    (def b (make-instance #^b))\\n    (assert (subclassp #^b #^a))\\n    (assert (typep b #^b))\\n    (assert (typep b #^a))\\n    (assert (= \\\"foo\\\" (foo b)))\\n    (defclass c () ())\\n    (defmethod foo ((c c)) \\\"bar\\\")\\n    (defclass b (c) ())\\n    (assert (subclassp #^b #^c))\\n    (assert (eq (class-of b) #^b))\\n    (assert (typep b #^b))\\n    (assert (typep b #^c))\\n    (assert (= \\\"bar\\\" (foo b)))))\\n\\n(defsuite generics\\n\\n  (deftest generics.1\\n    (progn\\n      (defclass foo () ())\\n      (defclass bar (foo) ())\\n      (def foo (make-instance #^foo))\\n      (def bar (make-instance #^bar))\\n\\n      (defgeneric g1 (obj param))\\n      (assert (typep #'g1 #^function))\\n\\n      (assert (signals-error\\n               (g1 12 (+ 1 1))\\n               unbound-method-error :class #^number :method-name 'g1))\\n      (assert (signals-error\\n               (g1 foo (+ 1 1))\\n               unbound-method-error :class #^foo :method-name 'g1))\\n      (assert (signals-error\\n               (g1 bar (+ 1 1))\\n               unbound-method-error :class #^bar :method-name 'g1))\\n\\n      (defmethod g1 ((obj foo) param)\\n        (+ param 100))\\n\\n      (assert (signals-error\\n               (g1 12 (+ 1 1))\\n               unbound-method-error :class #^number :method-name 'g1))\\n      (assert (= 102 (g1 foo (+ 1 1))))\\n      (assert (= 102 (g1 bar (+ 1 1))))\\n\\n      (defmethod g1 ((obj bar) param)\\n        (+ param 1000))\\n\\n      (assert (signals-error\\n               (g1 12 (+ 1 1))\\n               unbound-method-error :class #^number :method-name 'g1))\\n      (assert (= 102 (g1 foo (+ 1 1))))\\n      (assert (= 1002 (g1 bar (+ 1 1))))\\n\\n      (defmethod g1 ((obj number) param)\\n        (+ param 10000))\\n\\n      (assert (= 10002 (g1 12 (+ 1 1))))\\n      (assert (= 102 (g1 foo (+ 1 1))))\\n      (assert (= 1002 (g1 bar (+ 1 1))))\\n\\n      (defmethod g1 ((obj number) param)\\n        (+ param 100000))\\n\\n      (assert (= 100002 (g1 12 (+ 1 1))))\\n      (assert (= 102 (g1 foo (+ 1 1))))\\n      (assert (= 1002 (g1 bar (+ 1 1))))\\n\\n      (defmethod g1 ((obj object) param)\\n        (- param))\\n\\n      (assert (= 100002 (g1 12 (+ 1 1))))\\n      (assert (= 102 (g1 foo (+ 1 1))))\\n      (assert (= 1002 (g1 bar (+ 1 1))))\\n      (assert (= -2 (g1 #t (+ 1 1))))\\n      (assert (= -2 (g1 #void (+ 1 1))))\\n      (assert (= -2 (g1 \\\"hello\\\" (+ 1 1)))))\\n    #void))\\n\\n(defsuite handler-bind\\n\\n  (defclass test-error (error) ())\\n\\n  (deftest handler-bind.0\\n    (signals-error\\n     (handler-bind)\\n     match-error))\\n\\n  (deftest handler-bind.0b\\n    (handler-bind ())\\n    #void)\\n\\n  (deftest handler-bind.1\\n    (handler-bind ()\\n      1 2 (+ 1 2))\\n    3)\\n\\n  (deftest handler-bind.2\\n    (handler-bind ((simple-error (lambda (e) 'whatever)))\\n      1 2 (+ 1 2))\\n    3)\\n\\n  (deftest handler-bind.3\\n    (handler-bind ((simple-error (lambda (e) 'whatever))\\n                   (test-error (lambda (e) 'whatever)))\\n      1 2 (+ 1 2))\\n    3)\\n\\n  (deftest handler-bind.no-handler\\n    (handler-bind ((test-error (lambda (e) 'whatever)))\\n      (signal (make-simple-error \\\"foo\\\")))\\n    #void)\\n\\n  (deftest handler-bind.unhandled\\n    (handler-bind ((simple-error (lambda (e) 'whatever)))\\n      (signal (make-simple-error \\\"foo\\\")))\\n    #void)\\n\\n  (deftest handler-bind.decline\\n    (block b\\n      (handler-bind ((simple-error (lambda (e) (return-from b 1))))\\n        (handler-bind ((simple-error (lambda (e) 'decline)))\\n          (signal (make-simple-error \\\"foo\\\")))))\\n    1)\\n\\n  (deftest handler-bind.handled\\n    (block b\\n      (handler-bind ((simple-error (lambda (e)\\n                                     (assert (= \\\"foo\\\" (slot-value e 'message)))\\n                                     (return-from b 1))))\\n        (signal (make-simple-error \\\"foo\\\"))))\\n    1)\\n\\n  (deftest handler-bind.nested\\n    (block b\\n      (handler-bind ((simple-error (lambda (e) (return-from b 'outer))))\\n        (handler-bind ((simple-error (lambda (e) (return-from b 'inner))))\\n          (signal (make-simple-error \\\"foo\\\")))))\\n    'inner)\\n\\n  (deftest handler-bind.resignal\\n    (block b\\n      (handler-bind ((simple-error (lambda (e) (return-from b 'outer))))\\n        (handler-bind ((simple-error (lambda (e) (signal e))))\\n          (signal (make-simple-error \\\"foo\\\")))))\\n    'outer)\\n\\n  (deftest handler-bind.firewall\\n    (block b\\n      (handler-bind ((test-error (lambda (e) (return-from b 'outer))))\\n        (handler-bind ((simple-error (lambda (e)\\n                                       (signal (make-instance #^test-error)))))\\n          (handler-bind ((test-error (lambda (e) (return-from b 'inner))))\\n            (signal (make-simple-error \\\"foo\\\"))))))\\n    'outer)\\n\\n  (deftest handler-bind.multiple.1\\n    (block b\\n      (handler-bind ((test-error (lambda (e) (return-from b 'test)))\\n                     (simple-error (lambda (e) (return-from b 'simple))))\\n        (signal (make-simple-error \\\"foo\\\"))))\\n    'simple)\\n\\n  (deftest handler-bind.multiple.2\\n    (block b\\n      (handler-bind ((test-error (lambda (e) (return-from b 'test)))\\n                     (simple-error (lambda (e) (return-from b 'simple))))\\n        (signal (make-instance #^test-error))))\\n    'test)\\n\\n  (deftest handler-bind.anything\\n    (block b\\n      (handler-bind ((object (lambda (e) (return-from b e))))\\n        (signal \\\"foo\\\")))\\n    \\\"foo\\\"))\\n\\n(defsuite handler-case\\n\\n  (defclass test-error (error) ())\\n\\n  (deftest handler-case.0\\n    (signals-error\\n     (handler-case)\\n     match-error))\\n\\n  (deftest handler-case.0b\\n    (handler-case ())\\n    #void)\\n\\n  (deftest handler-case.1\\n    (handler-case ()\\n      1 2 (+ 1 2))\\n    3)\\n\\n  (deftest handler-case.2\\n    (handler-case ((simple-error (lambda (e) 'whatever)))\\n      1 2 (+ 1 2))\\n    3)\\n\\n  (deftest handler-case.3\\n    (handler-case ((simple-error (lambda (e) 'whatever))\\n                   (test-error (lambda (e) 'whatever)))\\n      1 2 (+ 1 2))\\n    3)\\n\\n  (deftest handler-case.no-handler\\n    (handler-case ((test-error (lambda (e) 'whatever)))\\n      (signal (make-simple-error \\\"foo\\\")))\\n    #void)\\n\\n  (deftest handler-case.handled\\n    (handler-case ((simple-error (lambda (e)\\n                                   (assert (= \\\"foo\\\" (slot-value e 'message)))\\n                                   'whatever)))\\n      (signal (make-simple-error \\\"foo\\\")))\\n    'whatever)\\n\\n  (deftest handler-case.nested\\n    (handler-case ((simple-error (lambda (e) 'outer)))\\n      (handler-case ((simple-error (lambda (e) 'inner)))\\n        (signal (make-simple-error \\\"foo\\\"))))\\n    'inner)\\n\\n  (deftest handler-case.resignal\\n    (handler-case ((simple-error (lambda (e) 'outer)))\\n      (handler-case ((simple-error (lambda (e) (signal e))))\\n        (signal (make-simple-error \\\"foo\\\"))))\\n    'outer)\\n\\n  (deftest handler-case.multiple.1\\n    (handler-case ((test-error (lambda (e) 'test))\\n                   (simple-error (lambda (e) 'simple)))\\n      (signal (make-simple-error \\\"foo\\\")))\\n    'simple)\\n\\n  (deftest handler-case.multiple.2\\n    (handler-case ((test-error (lambda (e) 'test))\\n                   (simple-error (lambda (e) 'simple)))\\n      (signal (make-instance #^test-error)))\\n    'test)\\n\\n  (deftest handler-case.anything\\n    (handler-case ((object (lambda (e) e)))\\n      (signal \\\"foo\\\"))\\n    \\\"foo\\\"))\\n\\n(defsuite restart-bind\\n\\n  (deftest restart-bind.0\\n    (signals-error\\n     (restart-bind)\\n     match-error))\\n\\n  (deftest restart-bind.0b\\n    (restart-bind ())\\n    #void)\\n\\n  (deftest restart-bind.1\\n    (restart-bind ()\\n      1 2 (+ 1 2))\\n    3)\\n\\n  (deftest restart-bind.2\\n    (restart-bind ((continue (lambda () 'whatever)))\\n      1 2 (+ 1 2))\\n    3)\\n\\n  (deftest restart-bind.3\\n    (restart-bind ((continue (lambda () 'whatever))\\n                   (abort (lambda () 'whatever)))\\n      1 2 (+ 1 2))\\n    3)\\n\\n  (deftest restart-bind.no-handler.1\\n    (signals-error\\n     (invoke-restart 'continue)\\n     restart-error :restart-name 'continue))\\n\\n  (deftest restart-bind.no-handler.2\\n    (signals-error\\n     (restart-bind ((continue (lambda () 'whatever)))\\n       (invoke-restart 'abort))\\n     restart-error :restart-name 'abort))\\n\\n  (deftest restart-bind.no-handler.1a\\n    (signals-error\\n     (invoke-restart-interactively 'continue)\\n     restart-error :restart-name 'continue))\\n\\n  (deftest restart-bind.no-handler.2a\\n    (signals-error\\n     (restart-bind ((continue (lambda () 'whatever)))\\n       (invoke-restart-interactively 'abort))\\n     restart-error :restart-name 'abort))\\n\\n  (deftest restart-bind.no-exit.1\\n    (restart-bind ((continue (lambda () 'whatever)))\\n      (invoke-restart 'continue))\\n    'whatever)\\n\\n  (deftest restart-bind.no-exit.2\\n    (restart-bind ((continue (lambda () 'whatever)))\\n      (if-option (restart-handler (find-restart? 'continue))\\n        (invoke-restart restart-handler)\\n        (assert #f)))\\n    'whatever)\\n\\n  (deftest restart-bind.handled\\n    (block b\\n      (restart-bind ((abort (lambda (arg1 arg2)\\n                              (assert (= 1 arg1))\\n                              (assert (= 2 arg2))\\n                              (return-from b 3))))\\n        (invoke-restart 'abort (+ 0 1) (+ 1 1))))\\n    3)\\n\\n  (deftest restart-bind.nested\\n    (block b\\n      (restart-bind ((continue (lambda () (return-from b 'outer))))\\n        (restart-bind ((continue (lambda () (return-from b 'inner))))\\n          (invoke-restart 'continue))))\\n    'inner)\\n\\n  (deftest restart-bind.reinvoke\\n    (block b\\n      (restart-bind ((y (lambda (val) (return-from b (+ val 1)))))\\n        (restart-bind ((x (lambda (val) (invoke-restart 'y (+ val 1)))))\\n          (invoke-restart 'x 1))))\\n    3)\\n\\n  (deftest restart-bind.no-firewall\\n    (block b\\n      (restart-bind ((r1 (lambda () (return-from b 'outer))))\\n        (restart-bind ((r2 (lambda () (invoke-restart 'r1))))\\n          (restart-bind ((r1 (lambda () (return-from b 'inner))))\\n            (invoke-restart 'r2)))))\\n    'inner)\\n\\n  (deftest restart-bind.multiple.1\\n    (block b\\n      (restart-bind ((r1 (lambda (arg) (return-from b (- 100 arg))))\\n                     (r2 (lambda (arg) (return-from b (+ 100 arg)))))\\n        (invoke-restart 'r2 1)))\\n    101)\\n\\n  (deftest restart-bind.multiple.2\\n    (block b\\n      (restart-bind ((r1 (lambda (arg) (return-from b (- 100 arg))))\\n                     (r2 (lambda (arg) (return-from b (+ 100 arg)))))\\n        (invoke-restart 'r1 1)))\\n    99))\\n\\n(defsuite restart-case\\n\\n  (deftest restart-case.0\\n    (signals-error\\n     (restart-case)\\n     match-error))\\n\\n  (deftest restart-case.0b\\n    (restart-case ())\\n    #void)\\n\\n  (deftest restart-case.1\\n    (restart-case ()\\n      1 2 (+ 1 2))\\n    3)\\n\\n  (deftest restart-case.2\\n    (restart-case ((continue (lambda () 'whatever)))\\n      1 2 (+ 1 2))\\n    3)\\n\\n  (deftest restart-case.3\\n    (restart-case ((continue (lambda () 'whatever))\\n                   (abort (lambda () 'whatever)))\\n      1 2 (+ 1 2))\\n    3)\\n\\n  (deftest restart-case.no-handler.1\\n    (signals-error\\n     (invoke-restart 'continue)\\n     restart-error :restart-name 'continue))\\n\\n  (deftest restart-case.no-handler.2\\n    (signals-error\\n     (restart-case ((continue (lambda () 'whatever)))\\n       (invoke-restart 'abort))\\n     restart-error :restart-name 'abort))\\n\\n  (deftest restart-case.no-exit.1\\n    (restart-case ((continue (lambda () 'whatever)))\\n      (invoke-restart 'continue))\\n    'whatever)\\n\\n  (deftest restart-case.no-exit.2\\n    (restart-case ((continue (lambda () 'whatever)))\\n      (if-option (restart-handler (find-restart? 'continue))\\n        (invoke-restart restart-handler)\\n        (assert #f)))\\n    'whatever)\\n\\n  (deftest restart-case.handled\\n    (restart-case ((abort (lambda (arg1 arg2)\\n                            (assert (= 1 arg1))\\n                            (assert (= 2 arg2))\\n                            3)))\\n      (invoke-restart 'abort (+ 0 1) (+ 1 1)))\\n    3)\\n\\n  (deftest restart-case.nested\\n    (restart-case ((continue (lambda () 'outer)))\\n      (restart-case ((continue (lambda () 'inner)))\\n        (invoke-restart 'continue)))\\n    'inner)\\n\\n  (deftest restart-case.nested2\\n    (restart-case ((r1 (lambda () 'outer)))\\n      (restart-case ((r1 (lambda ()  (invoke-restart 'r1)))\\n                     (r2 (lambda ())))\\n        (invoke-restart 'r1)))\\n    'outer)\\n\\n  (deftest restart-case.reinvoke\\n    (restart-case ((y (lambda (val) (+ val 1))))\\n      (restart-case ((x (lambda (val) (invoke-restart 'y (+ val 1)))))\\n        (invoke-restart 'x 1)))\\n    3)\\n\\n  (deftest restart-case.no-firewall\\n    (restart-case ((r1 (lambda () 'outer)))\\n      (restart-case ((r2 (lambda () (invoke-restart 'r1))))\\n        (restart-case ((r1 (lambda () 'inner)))\\n          (invoke-restart 'r2))))\\n    'outer)\\n\\n  (deftest restart-case.parallel\\n    (restart-case ((r1 (lambda () 'outer)))\\n      (restart-case ((r1 (lambda () 'inner))\\n                     (r2 (lambda () (invoke-restart 'r1))))\\n        (invoke-restart 'r2)))\\n    'outer)\\n\\n  (deftest restart-case.multiple.1\\n    (restart-case ((r1 (lambda (arg) (- 100 arg)))\\n                   (r2 (lambda (arg) (+ 100 arg))))\\n      (invoke-restart 'r2 1))\\n    101)\\n\\n  (deftest restart-case.multiple.2\\n    (restart-case ((r1 (lambda (arg) (- 100 arg)))\\n                   (r2 (lambda (arg) (+ 100 arg))))\\n      (invoke-restart 'r1 1))\\n    99))\\n\\n(defsuite find-restart?\\n\\n  (deftest find-restart?.0\\n    (signals-error\\n     (find-restart?)\\n     match-error))\\n\\n  (deftest find-restart?.1\\n    (find-restart? 'foo)\\n    #nil)\\n\\n  (deftest find-restart?.1b\\n    (restart-bind ((bar (lambda ())))\\n      (find-restart? 'foo))\\n    #nil)\\n\\n  (deftest find-restart?.2\\n    (find-restart? 'foo (make-simple-error \\\"foo\\\"))\\n    #nil)\\n\\n  (deftest find-restart?.3\\n    (block exit\\n      (restart-bind ((x (lambda (arg) (return-from exit (+ 1000 arg)))))\\n        (if-option (r (find-restart? 'x))\\n          (progn\\n            (assert (= (slot-value r 'restart-name) 'x))\\n            (assert (= (slot-value r 'associated-conditions) '()))\\n            (invoke-restart r 1))\\n          (assert #f))))\\n    1001)\\n\\n  (deftest find-restart?.3b\\n    (restart-case ((x (lambda (arg) (+ 1000 arg))))\\n      (if-option (r (find-restart? 'x))\\n        (progn\\n          (assert (= (slot-value r 'restart-name) 'x))\\n          (assert (= (slot-value r 'associated-conditions) '()))\\n          (invoke-restart r 1))\\n        (assert #f)))\\n    1001)\\n\\n  (deftest find-restart?.4\\n    (block exit\\n      (def c (make-simple-error \\\"foo\\\"))\\n      (restart-bind ((x (lambda (arg) (return-from exit (+ 1000 arg)))\\n                        :associated-conditions (list c)))\\n        (if-option (r (find-restart? 'x c))\\n          (progn\\n            (assert (= (slot-value r 'restart-name) 'x))\\n            (assert (= (slot-value r 'associated-conditions) (list c)))\\n            (invoke-restart r 1))\\n          (assert #f))))\\n    1001)\\n\\n  (deftest find-restart?.4b\\n    (progn\\n      (def c (make-simple-error \\\"foo\\\"))\\n      (restart-bind ((x (lambda (arg) (+ 1000 arg))\\n                        :associated-conditions (list c)))\\n        (if-option (r (find-restart? 'x c))\\n          (progn\\n            (assert (= (slot-value r 'restart-name) 'x))\\n            (assert (= (slot-value r 'associated-conditions) (list c)))\\n            (invoke-restart r 1))\\n          (assert #f))))\\n    1001)\\n\\n  (deftest find-restart?.5\\n    (block exit\\n      (def c (make-simple-error \\\"foo\\\"))\\n      (restart-bind ((x (lambda (arg) (return-from exit (+ 1000 arg)))\\n                        :associated-conditions (list c)))\\n        (restart-bind ((x (lambda (arg) (return-from exit (- 1000 arg)))))\\n          (if-option (r (find-restart? 'x c))\\n            (invoke-restart r 1)\\n            (assert #f)))))\\n    999)\\n\\n  (deftest find-restart?.5b\\n    (progn\\n      (def c (make-simple-error \\\"foo\\\"))\\n      (restart-case ((x (lambda (arg) (+ 1000 arg))\\n                        :associated-conditions (list c)))\\n        (restart-case ((x (lambda (arg) (- 1000 arg))))\\n          (if-option (r (find-restart? 'x c))\\n            (invoke-restart r 1)\\n            (assert #f)))))\\n    999)\\n\\n  (deftest find-restart?.6\\n    (block exit\\n      (def c (make-simple-error \\\"foo\\\"))\\n      (def d (make-simple-error \\\"bar\\\"))\\n      (restart-bind ((x (lambda (arg) (return-from exit (+ 1000 arg)))\\n                        :associated-conditions (list c)))\\n        (restart-bind ((x (lambda (arg) (return-from exit (- 1000 arg)))\\n                          :associated-conditions (list d)))\\n          (if-option (r (find-restart? 'x c))\\n            (invoke-restart r 1)\\n            (assert #f)))))\\n    1001)\\n\\n  (deftest find-restart?.6b\\n    (progn\\n      (def c (make-simple-error \\\"foo\\\"))\\n      (def d (make-simple-error \\\"bar\\\"))\\n      (restart-case ((x (lambda (arg) (+ 1000 arg))\\n                        :associated-conditions (list c)))\\n        (restart-case ((x (lambda (arg) (- 1000 arg))\\n                          :associated-conditions (list d)))\\n          (if-option (r (find-restart? 'x c))\\n            (invoke-restart r 1)\\n            (assert #f)))))\\n    1001))\\n\\n(defsuite compute-restarts\\n\\n  (deftest compute-restarts.1\\n    (compute-restarts)\\n    '())\\n\\n  (defun restart-names condition?\\n    (let ((restarts (apply #'compute-restarts condition?)))\\n      (mapcar (lambda (restart)\\n                (slot-value restart 'restart-name))\\n              restarts)))\\n\\n  (deftest compute-restarts.1\\n    (restart-case ((outer1 (lambda ()))\\n                   (outer2 (lambda ())))\\n      (restart-case ((middle1 (lambda ()))\\n                     (middle2 (lambda ())))\\n        (restart-case ((inner1 (lambda ()))\\n                       (inner2 (lambda ())))\\n          (assert (= (restart-names)\\n                     '(inner1\\n                       inner2\\n                       middle1\\n                       middle2\\n                       outer1\\n                       outer2)))\\n          #t))))\\n\\n  (deftest compute-restarts.2\\n    (progn\\n      (def c (make-simple-error \\\"foo\\\"))\\n      (def d (make-simple-error \\\"bar\\\"))\\n      (restart-case ((outer1 (lambda ()))\\n                     (outer2 (lambda ()) :associated-conditions (list c)))\\n        (restart-case ((middle1 (lambda ()) :associated-conditions (list c))\\n                       (middle2 (lambda ())))\\n          (restart-case ((inner1 (lambda ()) :associated-conditions (list c d))\\n                         (inner2 (lambda ())))\\n            (assert (= (restart-names)\\n                       '(inner1\\n                         inner2\\n                         middle1\\n                         middle2\\n                         outer1\\n                         outer2)))\\n            (assert (= (restart-names c)\\n                       '(inner1\\n                         inner2\\n                         middle1\\n                         middle2\\n                         outer1\\n                         outer2)))\\n            (assert (= (restart-names d)\\n                       '(inner1\\n                         inner2\\n                         middle2\\n                         outer1)))\\n            #t))))))\\n\\n(defsuite signal\\n\\n  (deftest signal.0\\n    (signals-error\\n     (signal)\\n     match-error))\\n\\n  (deftest signal.1\\n    (signal (make-simple-error \\\"foo\\\"))\\n    #void)\\n\\n  (deftest signal.2\\n    (signals-error\\n     (signal (make-instance #^standard-object :x 1 :y 2))\\n     standard-object :x 1 :y 2))\\n\\n  (deftest signal.3\\n    (handler-bind ((simple-error (lambda (e)\\n                                   (invoke-restart 'continue 12))))\\n      (signal (make-simple-error \\\"Foo!\\\")\\n        (abort (lambda (value) (* value 10)))\\n        (continue (lambda (value) (* value 2)))))\\n    24)\\n\\n  (deftest signal.3b\\n    (handler-bind ((simple-error (lambda (e)\\n                                   (invoke-restart 'abort 12))))\\n      (signal (make-simple-error \\\"Foo!\\\")\\n        (abort (lambda (value) (* value 10)))\\n        (continue (lambda (value) (* value 2)))))\\n    120)\\n\\n  (deftest signal.4\\n    (restart-case ((abort (lambda (value) (* 1000 value))))\\n      (handler-bind ((simple-error (lambda (e)\\n                                     (invoke-restart 'abort 12))))\\n        ;; Silly?  Yeah, probably.\\n        (signal (signal (make-simple-error \\\"Foo!\\\"))\\n          (abort (lambda (value) (* value 10)))\\n          (continue (lambda (value) (* value 2))))))\\n    12000)\\n\\n  (deftest signal.4b\\n    (block b\\n      (restart-case ((abort (lambda (value) (* 1000 value))))\\n        (handler-bind ((simple-error (lambda (e)\\n                                       (invoke-restart 'abort 12))))\\n          (signal (signal (make-simple-error \\\"Foo!\\\")\\n                    (abort (lambda (value) (return-from b (* 10000 value)))))\\n            (abort (lambda (value) (* value 10)))\\n            (continue (lambda (value) (* value 2)))))))\\n    120000)\\n\\n  ;;; Test that `signal' (and `error' likewise) associates the\\n  ;;; restarts it binds with the signalled error.\\n  (deftest signal.associations\\n    (dolist (#'signal-operator (list #'signal #'error) #t)\\n      (block ok ; so that the test doesn't fail when testing `error'.\\n        (handler-bind ((simple-error\\n                        (lambda (e)\\n                          (let* ((handler (optional (find-restart? 'r e)))\\n                                 (a-cs (slot-value handler 'associated-conditions)))\\n                            (assert (memberp e a-cs :test #'eq))\\n                            (return-from ok)))))\\n          (signal-operator (make-simple-error \\\"error!\\\")\\n            (r (lambda () 1))))))))\\n\\n(defsuite invoke-restart\\n\\n  (deftest invoke-restart.0\\n    (signals-error\\n     (invoke-restart)\\n     match-error))\\n\\n  (deftest invoke-restart.1\\n    (signals-error\\n     (invoke-restart \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type '(or symbol restart-handler))))\\n\\n(defsuite invoke-restart-interactively\\n\\n  (deftest invoke-restart-interactively.1\\n    (restart-bind ((r1 (lambda (arg1 arg2) (+ arg1 arg2))\\n                       :interactive-function (lambda () (list 1 2))))\\n      (invoke-restart-interactively 'r1))\\n    3)\\n\\n  (deftest invoke-restart-interactively.1a\\n    (restart-bind ((r1 (lambda (arg1 arg2) (+ arg1 arg2))\\n                       :interactive-function (lambda () (list 1 2))))\\n      (invoke-restart-interactively (optional (find-restart? 'r1))))\\n    3)\\n\\n  (deftest invoke-restart-interactively.1b\\n    (restart-bind ((r1 (lambda () (+ 1 2))))\\n      (invoke-restart-interactively 'r1))\\n    3)\\n\\n  (deftest invoke-restart-interactively.1c\\n    (restart-bind ((r1 (lambda () (+ 1 2))))\\n      (invoke-restart-interactively (optional (find-restart?'r1))))\\n    3)\\n\\n  (deftest invoke-restart-interactively.2\\n    (signals-error\\n     (restart-bind ((r1 (lambda (arg1 arg2) (+ arg1 arg2))\\n                        :interactive-function (lambda () '())))\\n       (invoke-restart-interactively 'r1))\\n     match-error))\\n\\n  (deftest invoke-restart-interactively.3\\n    (signals-error\\n     (restart-bind ((r1 (lambda (arg1) arg1)\\n                        :interactive-function (lambda () '(1 2 3))))\\n       (invoke-restart-interactively 'r1))\\n     match-error)))\\n\\n(defsuite box\\n\\n  (deftest box.1\\n    (let ((#'c (box)))\\n      (c))\\n    #void)\\n\\n  (deftest box.2\\n    (let ((#'c (box (+ 1 1))))\\n      (c))\\n    2)\\n\\n  (deftest box.3\\n    (let ((#'c (box (+ 1 1))))\\n      (c (+ 2 2))\\n      (assert (= (c) 4))\\n      (c (+ 4 4))\\n      (c))\\n    8))\\n\";\n\n//# sourceURL=webpack://lispx-test-node/./test/boot-test.lispx?");

/***/ }),

/***/ "./test/control-test.lispx":
/*!*********************************!*\
  !*** ./test/control-test.lispx ***!
  \*********************************/
/***/ ((module) => {

"use strict";
eval("module.exports = \";;; Control-related Tests\\n\\n;;; Coroutines\\n\\n;; The following implementation of coroutines follows the one at URL\\n;; `http://okmij.org/ftp/continuations/implementations.html#dynamic-wind'\\n;;\\n;; We use it for testing that built-in operators properly suspend and\\n;; resume.\\n\\n(defconstant +coroutine-prompt+ 'coroutine-prompt\\n  \\\"The prompt used for delimiting coroutines.\\\")\\n\\n(defclass yield-record ()\\n  (value continuation)\\n  (:documentation \\\"Instances of this class are yielded.\\\"))\\n\\n(defun make-yield-record (v k)\\n  \\\"Create a new yield record with the given yielded value and resume continuation.\\\"\\n  (make-instance #^yield-record :value v :continuation k))\\n\\n(defun yield v?\\n  \\\"Yield a value (which defaults to void).\\\"\\n  (take-subcont +coroutine-prompt+ k\\n    (make-yield-record (optional v?) k)))\\n\\n(defun resume (yield-record . v?)\\n  \\\"Resume a suspended coroutine with a value (which defaults to void).\\\"\\n  (push-delim-subcont +coroutine-prompt+ (slot-value yield-record 'continuation)\\n    (optional v?)))\\n\\n(defmacro coroutine body\\n  \\\"Evaluate the body expressions as a coroutine.\\\"\\n  (list* #'push-prompt '+coroutine-prompt+ body))\\n\\n(defun run-coroutine (#'thunk)\\n  \\\"Get all values yielded by a coroutine, and its final result, and\\ncollect them in a list.\\\"\\n  (loop-let -run- ((result (coroutine (thunk))))\\n    (if (typep result #^yield-record)\\n        (cons (slot-value result 'value) (-run- (resume result)))\\n        (list result))))\\n\\n(defun run-coroutine-with-values (#'thunk values)\\n  \\\"Like `run-coroutine' but uses a list of values that are sent to the\\ncoroutine with `resume'.\\\"\\n  (loop-let -run- ((result (coroutine (thunk))) (values values))\\n    (if (typep result #^yield-record)\\n        (cons (slot-value result 'value)\\n              (-run- (resume result (car values)) (cdr values)))\\n        (list result))))\\n\\n(defsuite coroutines\\n\\n  (deftest coroutine.1\\n    (coroutine 1 2 (= 1 1))\\n    #t)\\n\\n  (deftest coroutine.2\\n    (progn\\n      (def yield-record (coroutine 1 2 (+ (yield (= 1 1)) 3)))\\n      (assert (typep yield-record #^yield-record))\\n      (assert (= #t (slot-value yield-record 'value)))\\n      (assert (= 33 (resume yield-record 30))))\\n    #void)\\n\\n  (deftest coroutine.progn.1\\n    (run-coroutine\\n     (lambda ()\\n       (progn\\n         (yield 1)\\n         (yield 2)\\n         3)))\\n    '(1 2 3))\\n\\n  (deftest coroutine.progn.2\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (progn\\n         (yield 1)\\n         (yield 2)))\\n     '(#void 3))\\n    '(1 2 3))\\n\\n  (deftest coroutine.vau.1\\n    (run-coroutine\\n     (lambda ()\\n       ((vau () #ignore\\n          (yield 1)\\n          (yield 2)\\n          3))))\\n    '(1 2 3))\\n\\n  (deftest coroutine.vau.2\\n    (run-coroutine-with-values\\n     (lambda ()\\n       ((vau () #ignore\\n          (yield 1)\\n          (yield 2))))\\n     '(#void 3))\\n    '(1 2 3))\\n\\n  (deftest coroutine.lambda.1\\n    (run-coroutine\\n     (lambda ()\\n       ((lambda ()\\n          (yield 1)\\n          (yield 2)\\n          3))))\\n    '(1 2 3))\\n\\n  (deftest coroutine.lambda.2\\n    (run-coroutine-with-values\\n     (lambda ()\\n       ((lambda ()\\n          (yield 1)\\n          (yield 2))))\\n     '(#void 3))\\n    '(1 2 3))\\n\\n  (deftest coroutine.def.1\\n    (run-coroutine\\n     (lambda ()\\n       (def (x y) (list (yield 1) (yield 2)))\\n       (list x y)))\\n    '(1 2 (#void #void)))\\n\\n  (deftest coroutine.def.2\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (def (x y) (list (yield 1) (yield 2)))\\n       (list x y))\\n     '(3 4))\\n    '(1 2 (3 4)))\\n\\n  (deftest coroutine.if.1\\n    (run-coroutine\\n     (lambda ()\\n       (if #t (yield 1) (yield 2))))\\n    '(1 #void))\\n\\n  (deftest coroutine.if.2\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (if #t (yield 1) (yield 2)))\\n     '(3))\\n    '(1 3))\\n\\n  (deftest coroutine.if.3\\n    (run-coroutine\\n     (lambda ()\\n       (if #f (yield 1) (yield 2))))\\n    '(2 #void))\\n\\n  (deftest coroutine.if.4\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (if #f (yield 1) (yield 2)))\\n     '(3))\\n    '(2 3))\\n\\n  (deftest coroutine.if.5\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (if (yield 1) (yield 2) (yield 3)))\\n     '(#t 4))\\n    '(1 2 4))\\n\\n  (deftest coroutine.if.6\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (if (yield 1) (yield 2) (yield 3)))\\n     '(#f 4))\\n    '(1 3 4))\\n\\n  (deftest coroutine.if.7\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (if (progn (yield 1) (yield 2))\\n           (progn (yield 3) (yield 3.5))\\n           (progn (yield 4) (yield 4.5))))\\n     '(#void #t #void 5))\\n    '(1 2 3 3.5 5))\\n\\n  (deftest coroutine.if.8\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (if (progn (yield 1) (yield 2))\\n           (progn (yield 3) (yield 3.5))\\n           (progn (yield 4) (yield 4.5))))\\n     '(#void #f #void 5))\\n    '(1 2 4 4.5 5))\\n\\n  (deftest coroutine.loop.1\\n    (run-coroutine\\n     (lambda ()\\n       (def ct 1)\\n       (def env (the-environment))\\n       (block exit\\n         (loop\\n          (if (= ct 5)\\n              (return-from exit ct)\\n              (progn\\n                (yield ct)\\n                (yield (- ct))\\n                (set env ct (+ ct 1))))))))\\n    '(1 -1 2 -2 3 -3 4 -4 5))\\n\\n  (deftest coroutine.block.1\\n    (run-coroutine\\n     (lambda ()\\n       (block b\\n         (yield 1)\\n         (yield 2)\\n         3)))\\n    '(1 2 3))\\n\\n  (deftest coroutine.block.2\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (block b\\n         (yield 1)\\n         (yield 2)))\\n     '(#void 3))\\n    '(1 2 3))\\n\\n  (deftest coroutine.block.3\\n    (run-coroutine\\n     (lambda ()\\n       (block b\\n         (yield 1)\\n         (yield 2)\\n         (return-from b 3))))\\n    '(1 2 3))\\n\\n  (deftest coroutine.block.4\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (block b\\n         (yield 1)\\n         (return-from b (yield 2))))\\n     '(#void 3))\\n    '(1 2 3))\\n\\n  (deftest coroutine.catch.1\\n    (run-coroutine\\n     (lambda ()\\n       (catch 'b\\n         (yield 1)\\n         (yield 2)\\n         3)))\\n    '(1 2 3))\\n\\n  (deftest coroutine.catch.2\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (catch 'b\\n         (yield 1)\\n         (yield 2)))\\n     '(#void 3))\\n    '(1 2 3))\\n\\n  (deftest coroutine.catch.3\\n    (run-coroutine\\n     (lambda ()\\n       (catch 'b\\n         (yield 1)\\n         (yield 2)\\n         (throw 'b 3))))\\n    '(1 2 3))\\n\\n  (deftest coroutine.catch.4\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (catch 'b\\n         (yield 1)\\n         (throw 'b (yield 2))))\\n     '(#void 3))\\n    '(1 2 3))\\n\\n  (deftest coroutine.unwind-protect.1\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (unwind-protect (yield 1)\\n         (yield 2)\\n         (yield 2.5)\\n         3))\\n     '(4 #void #void))\\n    '(1 2 2.5 4))\\n\\n  (deftest coroutine.unwind-protect.2\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (block exit\\n         (unwind-protect (return-from exit (yield 1))\\n           (yield 2)\\n           (yield 2.5)\\n           3)))\\n     '(4 #void #void))\\n    '(1 2 2.5 4))\\n\\n  (deftest coroutine.unwind-protect.3\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (block exit\\n         (unwind-protect (return-from exit 4)\\n           (yield 2)\\n           (yield 2.5)\\n           3)))\\n     '(#void #void))\\n    '(2 2.5 4))\\n\\n  (deftest coroutine.unwind-protect.4\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (block exit\\n         (unwind-protect (progn (yield 1) (yield 1.5))\\n           (yield 2)\\n           (yield 2.5)\\n           3)))\\n     '(#void 4 #void #void))\\n    '(1 1.5 2 2.5 4))\\n\\n  (deftest coroutine.progv.1\\n    (run-coroutine\\n     (lambda ()\\n       (defdynamic *x*)\\n       (progv (list *x*) (list 1)\\n         (yield (dynamic *x*))\\n         (yield (dynamic *x*))\\n         (progv (list *x*) (list 2)\\n           (yield (dynamic *x*))\\n           (yield (dynamic *x*))\\n           3))))\\n    '(1 1 2 2 3))\\n\\n  (deftest coroutine.progv.2\\n    (run-coroutine-with-values\\n     (lambda ()\\n       (defdynamic *x*)\\n       (progv (list *x*) (list 1)\\n         (yield (dynamic *x*))\\n         (yield (dynamic *x*))\\n         (progv (list *x*) (list 2)\\n           (yield (dynamic *x*))\\n           (yield (dynamic *x*)))))\\n     '(#void #void #void 3))\\n    '(1 1 2 2 3))\\n\\n  (deftest coroutine.fun.1\\n    (run-coroutine\\n     (lambda ()\\n       (list (yield 1) (yield 2))))\\n    '(1 2 (#void #void)))\\n\\n  (deftest coroutine.fun.2\\n    (run-coroutine-with-values\\n     (lambda () (list (yield 1) (yield 2)))\\n     '(3 4))\\n    '(1 2 (3 4))))\\n\\n;;; Basic Operator Tests\\n\\n(defsuite push-prompt\\n\\n  (deftest push-prompt.1\\n    (signals-error\\n     (push-prompt)\\n     match-error))\\n\\n  (deftest push-prompt.2\\n    (push-prompt 'p)\\n    #void)\\n\\n  (deftest push-prompt.3\\n    (push-prompt 'p 1 2 (= 1 1))\\n    #t)\\n\\n  (deftest push-prompt.4\\n    (progn\\n      (block ret\\n        (push-prompt 'p\\n          (assert (prompt-set-p 'p))\\n          (return-from ret)))\\n      (assert (not (prompt-set-p 'p))))\\n    #void)\\n\\n  (deftest push-prompt.5\\n    (progn\\n      (assert (not (prompt-set-p 'p)))\\n      (assert (not (prompt-set-p 'q)))\\n      (push-prompt 'p\\n        (assert (prompt-set-p 'p))\\n        (assert (not (prompt-set-p 'q)))\\n        (push-prompt 'q\\n          (assert (prompt-set-p 'p))\\n          (assert (prompt-set-p 'q)))\\n        (assert (prompt-set-p 'p))\\n        (assert (not (prompt-set-p 'q))))\\n      (assert (not (prompt-set-p 'p)))\\n      (assert (not (prompt-set-p 'q))))\\n    #void))\\n\\n(defsuite take-subcont\\n\\n  (deftest take-subcont.1\\n    (signals-error\\n     (take-subcont)\\n     match-error))\\n\\n  (deftest take-subcont.2\\n    (signals-error\\n     (take-subcont 'p)\\n     match-error))\\n\\n  (deftest take-subcont.3\\n    (signals-error\\n     (take-subcont 'p #ignore)\\n     prompt-not-found-error :prompt 'p))\\n\\n  (deftest take-subcont.4\\n    (signals-error\\n     (push-prompt 'p (take-subcont 'q #ignore))\\n     prompt-not-found-error :prompt 'q))\\n\\n  (deftest take-subcont.5\\n    (push-prompt 'p (take-subcont 'p #ignore))\\n    #void)\\n\\n  (deftest take-subcont.6\\n    (push-prompt 'p (take-subcont 'p #ignore (= 1 1)))\\n    #t)\\n\\n  (deftest take-subcont.7\\n    (push-prompt 'p (take-subcont 'p k (push-delim-subcont 'p k (= 1 1))))\\n    #t))\\n\\n(defsuite push-delim-subcont\\n\\n  (deftest push-delim-subcont.1\\n    (signals-error\\n     (push-delim-subcont)\\n     match-error))\\n\\n  (deftest push-delim-subcont.2\\n    (signals-error\\n     (push-delim-subcont 'p)\\n     match-error))\\n\\n  (deftest push-delim-subcont.3\\n    (signals-error\\n     (push-delim-subcont 'p 12)\\n     type-error :datum 12))\\n\\n  (deftest push-delim-subcont.4\\n    (let ((k (push-prompt 'p (+ 100 (take-subcont 'p k k)))))\\n      (assert (= 102 (push-delim-subcont 'p k (+ 1 1))))\\n      (assert (= 120 (push-delim-subcont 'p k (+ 10 10)))))\\n    #void)\\n\\n  (deftest push-delim-subcont.5\\n    (let ((k (push-prompt 'p (push-prompt 'q (take-subcont 'p k k)))))\\n      (push-delim-subcont 'p k\\n        (assert (prompt-set-p 'q))\\n        (assert (prompt-set-p 'p))))\\n    #void))\\n\\n(defsuite prompt-set-p\\n\\n  (deftest prompt-set-p.1\\n    (signals-error\\n     (prompt-set-p)\\n     match-error))\\n\\n  (deftest prompt-set-p.2\\n    (prompt-set-p 'p)\\n    #f)\\n\\n  (deftest prompt-set-p.3\\n    (push-prompt 'p (progn (prompt-set-p 'p)))\\n    #t))\\n\\n(defsuite push-subcont-barrier\\n\\n  (deftest push-subcont-barrier.1\\n    (signals-error\\n     (push-subcont-barrier\\n      (take-subcont 'p1 sk))\\n     prompt-not-found-error :prompt 'p1))\\n\\n  (deftest push-subcont-barrier.2\\n    (signals-error\\n     (push-prompt 'p1\\n       (push-subcont-barrier\\n         (take-subcont 'p1 sk)))\\n     prompt-not-found-error :prompt 'p1)))\\n\\n(defsuite dynamics\\n\\n  (deftest defdynamic.1\\n    (progn\\n      (defdynamic *x* 1)\\n      (defdynamic *y* (+ 1 1))\\n      (assert (= (dynamic *x*) 1))\\n      (assert (= (dynamic *y*) 2))\\n      (dynamic-let ((*x* 3))\\n        (assert (= (dynamic *x*) 3))\\n        (assert (= (dynamic *y*) 2))\\n        (dynamic-let ((*y* 4))\\n          (assert (= (dynamic *x*) 3))\\n          (assert (= (dynamic *y*) 4)))\\n        (assert (= (dynamic *x*) 3))\\n        (assert (= (dynamic *y*) 2)))\\n      (assert (= (dynamic *x*) 1))\\n      (assert (= (dynamic *y*) 2)))\\n    #void)\\n\\n  (deftest* defdynamic.redefine\\n    (defdynamic *a* (+ 1 1))\\n    (def old-a *a*)\\n    (assert (= (dynamic *a*) 2))\\n    (assert (= (dynamic old-a) 2))\\n    (defdynamic *a* (+ 2 2))\\n    (assert (= (dynamic *a*) 4))\\n    (assert (= (dynamic old-a) 4))\\n    (assert (eq old-a *a*))\\n    (defdynamic *a*)\\n    (assert (= (dynamic *a*) #void))\\n    (assert (= (dynamic old-a) #void))\\n    (assert (eq old-a *a*)))\\n\\n  (deftest progv.1\\n    (progn\\n      (defdynamic *x* 1)\\n      (defdynamic *y* 2)\\n      (assert (= (dynamic *x*) 1))\\n      (assert (= (dynamic *y*) 2))\\n      (progv (list *x*) (list 3)\\n        (assert (= (dynamic *x*) 3))\\n        (assert (= (dynamic *y*) 2))\\n        (progv (list *y*) (list 4)\\n          (assert (= (dynamic *x*) 3))\\n          (assert (= (dynamic *y*) 4)))\\n        (assert (= (dynamic *x*) 3))\\n        (assert (= (dynamic *y*) 2)))\\n      (assert (= (dynamic *x*) 1))\\n      (assert (= (dynamic *y*) 2)))\\n    #void)\\n\\n  (deftest dynamic.1\\n    (progn\\n      (defdynamic *foo*)\\n      (assert (= (dynamic *foo*) #void))\\n      (assert (= (slot-value *foo* 'value) #void))\\n      (assert (typep *foo* #^dynamic))\\n      (assert (typep *foo* #^standard-object))\\n      (assert (typep *foo* #^object))\\n      (assert (subclassp #^dynamic #^standard-object))\\n      (assert (subclassp #^dynamic #^object)))\\n    #void)\\n\\n  (deftest set-dynamic.1\\n    (progn\\n      (defdynamic *bar*)\\n      (dynamic-let ((*bar* 1))\\n        (set-dynamic *bar* 2)\\n        (assert (= 2 (dynamic *bar*)))\\n        (dynamic-let ((*bar* 3))\\n          (assert (= 3 (dynamic *bar*))))\\n        (assert (= 2 (dynamic *bar*)))\\n        (set-dynamic *bar* 4)\\n        (assert (= 4 (dynamic *bar*))))\\n      (assert (= #void (dynamic *bar*)))\\n      #t))\\n\\n  (deftest dynamic-let*.1\\n    (dynamic-let* () (+ 1 1))\\n    2)\\n\\n  (deftest dynamic-let*.2\\n    (progn\\n      (defdynamic *x* 1)\\n      (dynamic-let* ((*x* 2)) (+ 1 (dynamic *x*))))\\n    3)\\n\\n  (deftest dynamic-let*.2\\n    (progn\\n      (defdynamic *x* 1)\\n      (defdynamic *y* 0)\\n      (dynamic-let* ((*x* 2) (*y* (+ (dynamic *x*) 1)))\\n        (list (dynamic *x*) (dynamic *y*))))\\n    '(2 3))\\n\\n  (deftest dynamic-let-sanity-check\\n    (progn\\n      (defdynamic *x* 1)\\n      (defdynamic *y* 0)\\n      (dynamic-let ((*x* 2) (*y* (+ (dynamic *x*) 1)))\\n        (list (dynamic *x*) (dynamic *y*))))\\n    '(2 2)))\\n\\n(defsuite default-prompt\\n\\n  (deftest |Default prompt exists|\\n    (boundp '+default-prompt+ (the-environment))))\\n\\n;;; Simple Control Operators\\n\\n(defsuite loop\\n\\n  (deftest loop.1\\n    (let ((ct 0))\\n      (def env (the-environment))\\n      (block exit\\n        (loop 'just-a-symbol-to-test-implicit-progn\\n              (if (= ct 10)\\n                  (return-from exit ct)\\n                  (set env ct (+ ct 1))))))\\n    10))\\n\\n(defsuite catch/throw\\n\\n  (deftest catch.1\\n    (signals-error\\n     (catch)\\n     match-error))\\n\\n  (deftest catch.2\\n    (catch 'x)\\n    #void)\\n\\n  (deftest catch.3\\n    (catch 'x 1 2 3 (= 1 1))\\n    #t)\\n\\n  (deftest catch.4\\n    (catch 'x 1 (throw 'x (= 1 2)) 3 (= 1 1))\\n    #f)\\n\\n  (deftest catch.5\\n    (catch 'x 1 (throw 'x) 3 (= 1 1))\\n    #void)\\n\\n  (deftest catch.6\\n    (catch 'x 1 (catch 'y (throw 'x 44)) 3 (= 1 1))\\n    44)\\n\\n  (deftest catch.7\\n    (catch 'x 1 (catch 'y (throw 'y 2)) 3 55)\\n    55)\\n\\n  (deftest catch.8\\n    (catch 'x 1 (catch 'y (throw 'y 2)))\\n    2)\\n\\n  (deftest catch.9\\n    (catch 'x 1 (catch 'y (throw 'y)))\\n    #void))\\n\\n(defsuite block/return-from\\n\\n  (deftest block.1\\n    (signals-error\\n     (block)\\n     match-error))\\n\\n  (deftest block.2\\n    (block x)\\n    #void)\\n\\n  (deftest block.3\\n    (block x 1 2 3 (= 1 1))\\n    #t)\\n\\n  (deftest block.4\\n    (block x 1 (return-from x (= 1 2)) 3 (= 1 1))\\n    #f)\\n\\n  (deftest block.5\\n    (block x 1 (return-from x) 3 (= 1 1))\\n    #void)\\n\\n  (deftest block.6\\n    (block x 1 (block y (return-from x (= 1 2))) 3 (= 1 1))\\n    #f)\\n\\n  (deftest block.7\\n    (block x 1 (block y (return-from y 2)) 3 (= 1 1))\\n    #t)\\n\\n  (deftest block.8\\n    (block x 1 (block y (return-from y 2)))\\n    2)\\n\\n  (deftest block.9\\n    (block x 1 (block y (return-from y)))\\n    #void))\\n\\n(defsuite unwind-protect\\n\\n  (deftest unwind-protect.1\\n    (signals-error\\n     (unwind-protect)\\n     match-error))\\n\\n  (deftest unwind-protect.2\\n    (unwind-protect (= 1 1))\\n    #t)\\n\\n  (deftest unwind-protect.3\\n    (progn\\n      (def env (the-environment))\\n      (+ (unwind-protect 1 2 3 (set env x 10))\\n         x))\\n    11)\\n\\n  (deftest unwind-protect.4\\n    (progn\\n      (def env (the-environment))\\n      (+ (block exit\\n           (unwind-protect (return-from exit 1) 2 3 (set env x 10)))\\n         x))\\n    11)\\n\\n  (deftest unwind-protect.5\\n    (block exit\\n      (unwind-protect 1 2 3 (return-from exit 4)))\\n    4)\\n\\n  (deftest unwind-protect.6\\n    (block exit\\n      (unwind-protect (return-from exit 1) 2 3 (return-from exit 4)))\\n    4))\\n\";\n\n//# sourceURL=webpack://lispx-test-node/./test/control-test.lispx?");

/***/ }),

/***/ "./test/delimcc-test.lispx":
/*!*********************************!*\
  !*** ./test/delimcc-test.lispx ***!
  \*********************************/
/***/ ((module) => {

"use strict";
eval("module.exports = \";;; Delimited Control Tests\\n\\n;; The following tests are adapted from the file `testd0.ml' of the\\n;; caml-shift distribution, available at the URL\\n;; `http://okmij.org/ftp/continuations/implementations.html'\\n\\n;; First, some preliminary definitions.\\n\\n(defun abort-subcont (prompt value)\\n  \\\"Utility that captures the continuation but just ignores it.\\\"\\n  (take-subcont prompt #ignore value))\\n\\n(defmacro push-subcont (k . forms)\\n  \\\"We don't have `push-subcont' but for these tests we can emulate it\\nwith a `push-delim-subcont' that pushes an unused prompt.\\\"\\n  (list* #'push-delim-subcont ''unused-prompt k forms))\\n\\n(defun shift (p #'f)\\n  \\\"The `shift' operator, adapted from the file `delimcc.ml'.\\\"\\n  (take-subcont p sk\\n    (push-prompt p\\n      (f (lambda (#'c)\\n           (push-delim-subcont p sk (c)))))))\\n\\n(defun prompt-set-p (prompt)\\n  \\\"Return true if a prompt is set, false otherwise.\\n(Note that this leads to stack growth, i.e. leaks.)\\\"\\n  (handler-case ((prompt-not-found-error (lambda #ignore #f)))\\n    (take-subcont prompt k (push-delim-subcont prompt k #t))))\\n\\n;; The actual tests.\\n\\n(defsuite caml-shift\\n\\n  (deftest test1\\n    (progn\\n      (assert (not (prompt-set-p 'p)))\\n      (push-prompt 'p\\n        (assert (prompt-set-p 'p))\\n        1))\\n    1)\\n\\n  (deftest test2\\n    (+ (push-prompt 'p (push-prompt 'p 5))\\n       4)\\n    9)\\n\\n  (deftest test3\\n    (+ (push-prompt 'p (+ 6 (abort-subcont 'p 5)))\\n       4)\\n    9)\\n\\n  (deftest |test3'|\\n    (+ (push-prompt 'p\\n         (push-prompt 'p (+ 6 (abort-subcont 'p 5))))\\n       4)\\n    9)\\n\\n  (deftest |test3''|\\n    (+ (push-prompt 'p\\n         (push-prompt 'p (+ 6 (abort-subcont 'p 5)))\\n         (+ (abort-subcont 'p 7)\\n            10))\\n       20)\\n    27)\\n\\n  (deftest |test3'''|\\n    (signals-error\\n     (progn\\n       (push-prompt 'p\\n         (push-prompt 'p (+ 6 (abort-subcont 'p 5)))\\n         (+ (abort-subcont 'p 7)\\n            10))\\n       (abort-subcont 'p 9))\\n     prompt-not-found-error :prompt 'p))\\n\\n  (deftest test4\\n    (+ (push-prompt 'p\\n         (+ (take-subcont 'p sk\\n              (push-prompt 'p\\n                (push-subcont sk 5)))\\n            10))\\n       20)\\n    35)\\n\\n  (deftest test5\\n    (+ (push-prompt 'p0\\n         (+ (shift 'p0 (lambda (#'sk)\\n                         (+ 100 (sk (lambda () (sk (lambda () 3)))))))\\n            2))\\n       10)\\n    117)\\n\\n  (deftest |test5'|\\n    (+ 10 (push-prompt 'p0\\n            (+ 2 (shift 'p0 (lambda (#'sk)\\n                              (sk (lambda () (+ 3 100))))))))\\n    115)\\n\\n  (deftest |test5''|\\n    (+ (push-prompt 'p0\\n         (+ (shift 'p0 (lambda (#'sk)\\n                         (+ (sk (lambda ()\\n                                  (push-prompt 'p1\\n                                    (+ 9 (sk (lambda () (abort-subcont 'p1 3)))))))\\n                            100)))\\n            2))\\n       10)\\n    115)\\n\\n  (deftest |test5'''|\\n    (+ (push-prompt 'p0\\n         (let ((v (shift 'p0 (lambda (#'sk)\\n                               (+ (sk (lambda ()\\n                                        (push-prompt 'p1\\n                                          (+ 9 (sk (lambda ()\\n                                                     (abort-subcont 'p1 3)))))))\\n                                  100)))))\\n           (+ v 2)))\\n       10)\\n    115)\\n\\n  (deftest test54\\n    (+ (push-prompt 'p0\\n         (let ((v (shift 'p0 (lambda (#'sk)\\n                               (+ (sk (lambda ()\\n                                        (push-prompt 'p1\\n                                          (+ 9 (sk (lambda ()\\n                                                     (abort-subcont 'p0 3)))))))\\n                                  100)))))\\n           (+ v 2)))\\n       10)\\n    124)\\n\\n  (deftest test6\\n    (+ (flet ((push-twice (sk)\\n                (push-subcont sk (push-subcont sk 3))))\\n         (push-prompt 'p1\\n           (push-prompt 'p2\\n             (+ (take-subcont 'p1 sk\\n                  (push-twice sk))\\n                1))))\\n       10)\\n    15)\\n\\n  (deftest test7\\n    (+ (flet ((push-twice (sk)\\n                (push-subcont sk\\n                  (push-subcont sk\\n                    (take-subcont 'p2 sk2\\n                      (push-subcont sk2\\n                        (push-subcont sk2 3)))))))\\n         (push-prompt 'p1\\n           (+ (push-prompt 'p2\\n                (+ 10 (push-prompt 'p3\\n                        (take-subcont 'p1 sk (push-twice sk)))))\\n              1)))\\n       100)\\n    135)\\n\\n  (deftest |test7'|\\n    (+ (flet ((push-twice (#'sk)\\n                (sk (lambda ()\\n                      (sk (lambda ()\\n                            (shift 'p2 (lambda (#'sk2)\\n                                         (sk2 (lambda ()\\n                                                (sk2 (lambda () 3))))))))))))\\n         (push-prompt 'p1\\n           (+ (push-prompt 'p2\\n                (+ 10 (push-prompt 'p3\\n                        (shift 'p1 (lambda (#'sk) (push-twice #'sk))))))\\n              1)))\\n       100)\\n    135))\\n\\n;;; Delimited Dynamic Binding\\n\\n;; The following tests are adapted from the files `caml-dynvar.ml' and\\n;; `dynvar-scheme48-problem.scm' of the DBplusDC distribution, available\\n;; at the URL `http://okmij.org/ftp/Computation/dynamic-binding.html'\\n\\n(defun dset (dynvar value)\\n  \\\"Set the value of a dynamic variable and return the old value.\\\"\\n  (prog1 (dynamic dynvar)\\n    (set-dynamic dynvar value)))\\n\\n(defsuite dbplusdc\\n\\n  (deftest testc\\n    (progn\\n      (defdynamic *p*)\\n      (dynamic-let ((*p* 0))\\n        (flet ((f () (dynamic *p*)))\\n          (let ((x (f))\\n                (y (dynamic-let ((*p* 1)) (f)))\\n                (z (f)))\\n            (list x y z)))))\\n    '(0 1 0))\\n\\n  (deftest test1\\n    (progn\\n      (defdynamic *p*)\\n      (dynamic-let ((*p* 1))\\n        (list (dynamic *p*) (dynamic *p*))))\\n    '(1 1))\\n\\n  (deftest test11\\n    (progn\\n      (defdynamic *p*)\\n      (dynamic-let ((*p* 1))\\n        (list (dynamic *p*)\\n              (dynamic *p*)\\n              (dynamic-let ((*p* 2))\\n                (dynamic *p*)))))\\n    '(1 1 2))\\n\\n  ;; It's probably a happy accident that this test works, since\\n  ;; our `dset' is pretty different from DBplusDC's.\\n  (deftest test12\\n    (progn\\n      (defdynamic *p*)\\n      (dynamic-let ((*p* 1))\\n        (let* ((v1 (dynamic *p*))\\n               (v2 (dynamic-let ((*p* 2))\\n                     (let ((v3 (dset *p* 12))\\n                           (v4 (dynamic *p*)))\\n                       (list v3 v4)))))\\n          (let ((v5 (dynamic *p*)))\\n            (list v1 v2 v5)))))\\n    '(1 (2 12) 1))\\n\\n  (deftest test_eq4\\n    (progn\\n      (defdynamic *p*)\\n      (dynamic-let ((*p* 1))\\n        (push-prompt 'p0\\n          (dynamic *p*))))\\n    1)\\n\\n  (deftest test_eq5\\n    (progn\\n      (defdynamic *p*)\\n      (dynamic-let ((*p* 1))\\n        (push-prompt 'p0\\n          (dynamic-let ((*p* 2))\\n            (take-subcont 'p0 #ignore\\n              (dynamic *p*))))))\\n    1)\\n\\n  (deftest test_eq6\\n    (progn\\n      (defdynamic *p*)\\n      (defdynamic *q*)\\n      ((lambda (#'f)\\n         (dynamic-let ((*p* 2) (*q* 20))\\n           (#'f (lambda () 0))))\\n       (dynamic-let ((*p* 1))\\n         (push-prompt 'p\\n           (dynamic-let ((*q* 10))\\n             ((lambda (x) (+ (dynamic *p*) (dynamic *q*)))\\n              (shift 'p (lambda (f) f))))))))\\n    12))\\n\";\n\n//# sourceURL=webpack://lispx-test-node/./test/delimcc-test.lispx?");

/***/ }),

/***/ "./test/hierarchy-test.lispx":
/*!***********************************!*\
  !*** ./test/hierarchy-test.lispx ***!
  \***********************************/
/***/ ((module) => {

"use strict";
eval("module.exports = \";;; Class Hierarchy Integrity Test\\n\\n(defsuite class-hierarchy\\n\\n  (defun test-hierarchy (classes)\\n    (let ((env (the-environment)))\\n      (loop-let -test-classes- ((classes classes) (superclass (class object)))\\n        (dolist (c classes #t)\\n          (typecase c\\n            (symbol\\n             (let ((class (find-class c env)))\\n               (assert (subclassp class superclass))))\\n            (cons\\n             (let ((class (find-class (car c) env)))\\n               (assert (subclassp class superclass))\\n               (-test-classes- (cdr c) class)))\\n            (object\\n             (assert #f)))))))\\n\\n  (deftest test-hierarchy\\n    (test-hierarchy\\n     '(string\\n       symbol\\n       number\\n       boolean\\n       (list cons\\n             nil)\\n       void\\n       ignore\\n       environment\\n       (class built-in-class\\n              standard-class)\\n       (operator built-in-operator\\n                 fexpr\\n                 function)\\n       (input-stream string-input-stream)\\n       (output-stream string-output-stream\\n                      js-console-output-stream)\\n       (standard-object dynamic\\n                        (condition (error type-error\\n                                          unbound-symbol-error\\n                                          unbound-slot-error\\n                                          unbound-method-error\\n                                          assertion-error\\n                                          match-error\\n                                          (stream-error end-of-file)\\n                                          reader-error\\n                                          prompt-not-found-error\\n                                          restart-error\\n                                          simple-error))\\n                        handler-frame\\n                        restart-handler\\n                        condition-handler)\\n       continuation)))\\n\\n  (deftest test-hierarchy.sanity-check\\n    (signals-error\\n     (test-hierarchy\\n      '((number\\n         boolean)))\\n     assertion-error)))\\n\";\n\n//# sourceURL=webpack://lispx-test-node/./test/hierarchy-test.lispx?");

/***/ }),

/***/ "./test/js-test.lispx":
/*!****************************!*\
  !*** ./test/js-test.lispx ***!
  \****************************/
/***/ ((module) => {

"use strict";
eval("module.exports = \";;; JavaScript Interface Tests\\n\\n(defsuite js-eq\\n\\n  (deftest js-eq.1\\n    (and (js-eq +js-true+ +js-true+)\\n         (js-eq +js-false+ +js-false+)\\n         (not (js-eq +js-true+ +js-false+))\\n         (js-eq (to-js-number 1) (to-js-number 1))\\n         (not (js-eq (to-js-number 1) (to-js-number 2)))\\n         (js-eq (to-js-string \\\"1\\\") (to-js-string \\\"1\\\"))\\n         (not (js-eq (to-js-string \\\"1\\\") (to-js-string \\\"2\\\")))))\\n\\n  (deftest js-eq.2\\n    (signals-error\\n     (js-eq 1)\\n     match-error))\\n\\n  (deftest js-eq.3\\n    (signals-error\\n     (js-eq)\\n     match-error)))\\n\\n(defsuite null-and-undefined\\n\\n  (deftest null-and-undefined-exist\\n    (and (eq (class-of +js-null+) (class object))\\n         (eq (class-of +js-undefined+) (class object))))\\n\\n  (deftest null-and-undefined-distinguishable\\n    (not (js-eq +js-null+ +js-undefined+))))\\n\\n(defsuite js-booleans\\n\\n  (deftest js-booleans-exist\\n    (and\\n     (eq (class-of +js-true+) (class object))\\n     (eq (class-of +js-false+) (class object))))\\n\\n  (deftest js-booleans-identity\\n    (and (not (eq +js-true+ +js-false+))\\n         (eq +js-true+ +js-true+)\\n         (eq +js-false+ +js-false+)\\n         (not (eq +js-true+ #t))\\n         (not (eq +js-false+ #f))))\\n\\n  (deftest to-lisp-boolean.1\\n    (and (to-lisp-boolean +js-true+)\\n         (not (to-lisp-boolean +js-false+))))\\n\\n  (deftest to-lisp-boolean.2\\n    (signals-error\\n     (to-lisp-boolean 12)\\n     type-error :datum 12 :expected-type \\\"boolean\\\"))\\n\\n  (deftest to-lisp-boolean.3\\n    (signals-error\\n     (to-lisp-boolean)\\n     match-error))\\n\\n  (deftest to-js-boolean.1\\n    (and (to-lisp-boolean (to-js-boolean #t))\\n         (not (to-lisp-boolean (to-js-boolean #f)))))\\n\\n  (deftest to-js-boolean.2\\n    (signals-error\\n     (to-js-boolean 12)\\n     type-error :datum 12 :expected-type 'boolean))\\n\\n  (deftest to-js-boolean.3\\n    (signals-error\\n     (to-js-boolean)\\n     match-error)))\\n\\n(defsuite js-numbers\\n\\n  (deftest to-lisp-number.1\\n    (and\\n     (= -1 (to-lisp-number (to-js-number -1)))\\n     (= 0 (to-lisp-number (to-js-number 0)))\\n     (= 1 (to-lisp-number (to-js-number 1)))))\\n\\n  (deftest to-lisp-number.2\\n    (signals-error\\n     (to-lisp-number #t)\\n     type-error :datum #t :expected-type \\\"number\\\"))\\n\\n  (deftest to-lisp-number.3\\n    (signals-error\\n     (to-lisp-number)\\n     match-error))\\n\\n  (deftest to-js-number.1\\n    (eq (class-of (to-js-number 1))\\n        (class object)))\\n\\n  (deftest to-js-number.1a\\n    (and (js-eq (to-js-number 1)\\n                (to-js-number 1))\\n         (not (js-eq (to-js-number 1)\\n                     (to-js-number 2)))))\\n\\n  (deftest to-js-number.2\\n    (signals-error\\n     (to-js-number #t)\\n     type-error :datum #t :expected-type 'number))\\n\\n  (deftest to-js-number.3\\n    (signals-error\\n     (to-js-number)\\n     match-error)))\\n\\n(defsuite js-strings\\n\\n  (deftest to-lisp-string.1\\n    (= \\\"a\\\" (to-lisp-string (to-js-string \\\"a\\\"))))\\n\\n  (deftest to-lisp-string.2\\n    (signals-error\\n     (to-lisp-string #t)\\n     type-error :datum #t :expected-type \\\"string\\\"))\\n\\n  (deftest to-lisp-string.3\\n    (signals-error\\n     (to-lisp-string)\\n     match-error))\\n\\n  (deftest to-js-string.1\\n    (eq (class-of (to-js-string \\\"1\\\"))\\n        (class object)))\\n\\n  (deftest to-js-string.1a\\n    (and (js-eq (to-js-string \\\"1\\\")\\n                (to-js-string \\\"1\\\"))\\n         (not (js-eq (to-js-string \\\"1\\\")\\n                     (to-js-string \\\"2\\\")))))\\n\\n  (deftest to-js-string.2\\n    (signals-error\\n     (to-js-string #t)\\n     type-error :datum #t :expected-type 'string))\\n\\n  (deftest to-js-string.3\\n    (signals-error\\n     (to-js-string)\\n     match-error)))\\n\\n(defsuite to-lisp-function\\n\\n  (deftest to-lisp-function.1\\n    (let ((#'parseInt (to-lisp-function (js-global \\\"parseInt\\\"))))\\n      (js-eq (parseInt (to-js-string \\\"123\\\")) (to-js-number 123))))\\n\\n  (deftest to-lisp-function.1a\\n    (let* ((#'parseInt (to-lisp-function (js-global \\\"parseInt\\\")))\\n           (result (parseInt)))\\n      (to-lisp-boolean (call-js-function (js-global \\\"isNaN\\\") result))))\\n\\n  (deftest to-lisp-function.2\\n    (signals-error\\n     (to-lisp-function 12)\\n     type-error :datum 12 :expected-type \\\"function\\\")))\\n\\n(defsuite to-js-function\\n\\n  (deftest to-js-function.1\\n    (let ((x 0))\\n      (def env (the-environment))\\n      (flet ((fun (v1 v2) (set env x (+ v1 v2))))\\n        (call-js-function (to-js-function #'fun) 1 2))\\n      (= x 3)))\\n\\n  (deftest to-js-function.2\\n    (signals-error\\n     (to-js-function 12)\\n     type-error :datum 12 :expected-type 'operator)))\\n\\n(defsuite apply-js-function\\n\\n  (deftest apply-js-function.1\\n    (js-eq (apply-js-function (js-global \\\"parseInt\\\") (list (to-js-string \\\"12\\\")))\\n           (to-js-number 12)))\\n\\n  (deftest apply-js-function.2\\n    (signals-error\\n     (apply-js-function 12 (list))\\n     type-error :datum 12 :expected-type \\\"function\\\"))\\n\\n  (deftest apply-js-function.3\\n    (signals-error\\n     (apply-js-function (js-global \\\"parseInt\\\"))\\n     match-error))\\n\\n  (deftest apply-js-function.4\\n    (signals-error\\n     (apply-js-function)\\n     match-error)))\\n\\n(defsuite call-js-function\\n\\n  (deftest call-js-function.1\\n    (js-eq (call-js-function (js-global \\\"parseInt\\\") (to-js-string \\\"12\\\"))\\n           (to-js-number 12)))\\n\\n  (deftest call-js-function.2\\n    (signals-error\\n     (call-js-function 12)\\n     type-error :datum 12 :expected-type \\\"function\\\"))\\n\\n  (deftest call-js-function.3\\n    (signals-error\\n     (call-js-function)\\n     match-error)))\\n\\n(defsuite js-lambda\\n\\n  (deftest js-lambda.1\\n    (let ((x 0))\\n      (def env (the-environment))\\n      (let ((fun (js-lambda (v1 v2) (set env x (+ v1 v2)))))\\n        (call-js-function fun 1 2))\\n      (= x 3)))\\n\\n  (deftest js-lambda.2\\n    (signals-error\\n     (js-lambda)\\n     match-error))\\n\\n  (deftest js-lambda.3\\n    (signals-error\\n     (js-lambda 12)\\n     type-error :datum 12 :expected-type '(or symbol ignore list)))\\n\\n  (deftest js-lambda.4\\n    (eq #void (call-js-function (js-lambda ()))))\\n\\n  (deftest js-lambda.barrier\\n    (signals-error\\n     (push-prompt 'foo\\n       (call-js-function (js-lambda () (take-subcont 'foo k))))\\n     prompt-not-found-error :prompt 'foo)))\\n\\n(defsuite js-global\\n\\n  (deftest js-global.1\\n    (let ((Math (js-global \\\"Math\\\")))\\n      (= 4 (to-lisp-number (call-js-method Math \\\"round\\\" (to-js-number 4.1))))))\\n\\n  (deftest js-global.2\\n    (signals-error\\n     (js-global)\\n     match-error))\\n\\n  (deftest js-global.3\\n    (js-eq +js-undefined+ (js-global \\\"ThisGlobalDoesNotExist\\\")))\\n\\n  (deftest js-global.4\\n    (signals-error\\n     (js-global 12)\\n     type-error :datum 12 :expected-type 'string)))\\n\\n(defsuite js-new\\n\\n  (deftest js-new.1\\n    (let ((re (js-new (js-global \\\"RegExp\\\") (to-js-string \\\"abc\\\"))))\\n      (and (to-lisp-boolean (call-js-method re \\\"test\\\" (to-js-string \\\"abcdef\\\")))\\n           (not (to-lisp-boolean (call-js-method re \\\"test\\\" (to-js-string \\\"uvwxyz\\\")))))))\\n\\n  (deftest js-new.2\\n    (signals-error\\n     (js-new)\\n     match-error))\\n\\n  (deftest js-new.3\\n    (signals-error\\n     (js-new \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type \\\"function\\\")))\\n\\n(defsuite js-get\\n\\n  (deftest js-get.1\\n    (let ((Math (js-global \\\"Math\\\")))\\n      (to-lisp-string (call-js-method (js-get Math \\\"PI\\\") \\\"toString\\\")))\\n    \\\"3.141592653589793\\\")\\n\\n  (deftest js-get.2\\n    (signals-error\\n     (js-get)\\n     match-error))\\n\\n  (deftest js-get.3\\n    (signals-error\\n     (js-get 12)\\n     match-error))\\n\\n  (deftest js-get.4\\n    (signals-error\\n     (js-get 12 'bar)\\n     type-error :datum 'bar :expected-type 'string)))\\n\\n(defsuite js-arrays\\n\\n  (define-js-method array-push \\\"push\\\")\\n  (define-js-method array-pop \\\"pop\\\")\\n\\n  (deftest js-array.1\\n    (let ((arr (js-array)))\\n      (assert (= (to-js-number 0) (js-get arr \\\"length\\\")))\\n      (array-push arr \\\"a\\\")\\n      (assert (= (to-js-number 1) (js-get arr \\\"length\\\")))\\n      (array-push arr \\\"b\\\")\\n      (assert (= (to-js-number 2) (js-get arr \\\"length\\\")))\\n      (assert (= \\\"b\\\" (array-pop arr)))\\n      (assert (= (to-js-number 1) (js-get arr \\\"length\\\")))\\n      (assert (= \\\"a\\\" (array-pop arr)))\\n      (assert (= (to-js-number 0) (js-get arr \\\"length\\\")))\\n      #t))\\n\\n    (deftest js-array.2\\n      (let ((arr (js-array (+ 1 1) (+ 2 2))))\\n        (assert (= (to-js-number 2) (js-get arr \\\"length\\\")))\\n        (assert (= 4 (array-pop arr)))\\n        (assert (= 2 (array-pop arr)))\\n        #t))\\n\\n    (deftest list-to-js-array.1\\n      (js-array-to-list (list-to-js-array '(1 2 3)))\\n      '(1 2 3))\\n\\n    (deftest list-to-js-array.2\\n      (signals-error\\n       (list-to-js-array)\\n       match-error))\\n\\n    (deftest list-to-js-array.3\\n      (signals-error\\n       (list-to-js-array 12)\\n       type-error :datum 12 :expected-type 'list))\\n\\n    (deftest js-array-to-list.1\\n      (signals-error\\n       (js-array-to-list)\\n       match-error))\\n\\n    (deftest js-array-to-list.2\\n      (signals-error\\n       (js-array-to-list 12)\\n       assertion-error))\\n\\n    (deftest js-array-to-list.3\\n      (js-array-to-list (js-array 1 2 3))\\n      '(1 2 3))\\n\\n    (deftest js-array-elt.1\\n      (elt (js-array 1 2 3) 0)\\n      1)\\n\\n    (deftest js-array-elt.2\\n      (elt (js-array 1 2 3) 1)\\n      2)\\n\\n    (deftest js-array-elt.3\\n      (signals-error\\n       (elt (js-array 1 2 3) 10)\\n       out-of-bounds-error))\\n\\n    (deftest js-array-elt.4\\n      (signals-error\\n       (elt (js-array 1 2 3) -1)\\n       out-of-bounds-error))\\n\\n    (deftest js-array-length.1\\n      (length (js-array))\\n      0)\\n\\n    (deftest js-array-length.2\\n      (length (js-array 1 2 3))\\n      3))\\n\\n(defsuite apply-js-method\\n\\n  (deftest apply-js-method.1\\n    (js-eq\\n     (apply-js-method (to-js-number 9.656) \\\"toFixed\\\" (list (to-js-number 2)))\\n     (to-js-string \\\"9.66\\\")))\\n\\n  (deftest apply-js-method.2\\n    (js-eq\\n     (apply-js-method (to-js-number 9) \\\"toString\\\" (list))\\n     (to-js-string \\\"9\\\")))\\n\\n  (deftest apply-js-method.3\\n    (signals-error\\n     (apply-js-method)\\n     match-error))\\n\\n  (deftest apply-js-method.4\\n    (signals-error\\n     (apply-js-method \\\"foo\\\")\\n     match-error))\\n\\n  (deftest apply-js-method.4a\\n    (signals-error\\n     (apply-js-method \\\"foo\\\" \\\"bar\\\")\\n     match-error))\\n\\n  (deftest apply-js-method.5\\n    (signals-error\\n     (apply-js-method \\\"foo\\\" \\\"methodDoesNotExist\\\" (list))\\n     type-error :expected-type \\\"function\\\"))\\n\\n  (deftest apply-js-method.6\\n    (signals-error\\n     (apply-js-method \\\"foo\\\" 12 (list))\\n     type-error :datum 12 :expected-type 'string)))\\n\\n(defsuite call-js-method\\n\\n  (deftest call-js-method.1\\n    (js-eq\\n     (call-js-method (to-js-number 9.656) \\\"toFixed\\\" (to-js-number 2))\\n     (to-js-string \\\"9.66\\\")))\\n\\n  (deftest call-js-method.2\\n    (js-eq\\n     (call-js-method (to-js-number 9) \\\"toString\\\")\\n     (to-js-string \\\"9\\\")))\\n\\n  (deftest call-js-method.3\\n    (signals-error\\n     (call-js-method)\\n     match-error))\\n\\n  (deftest call-js-method.4\\n    (signals-error\\n     (call-js-method \\\"foo\\\")\\n     match-error))\\n\\n  (deftest call-js-method.5\\n    (signals-error\\n     (call-js-method \\\"foo\\\" \\\"methodDoesNotExist\\\")\\n     type-error :expected-type \\\"function\\\"))\\n\\n  (deftest call-js-method.6\\n    (signals-error\\n     (call-js-method \\\"foo\\\" 12)\\n     type-error :datum 12 :expected-type 'string)))\\n\\n(defsuite js-method\\n\\n  (deftest js-method.1\\n    (progn\\n      (def #'to-string (js-method \\\"toString\\\"))\\n      (js-eq (to-string (to-js-number 12)) (to-js-string \\\"12\\\"))))\\n\\n  (deftest js-method.2\\n    (signals-error\\n     (js-method)\\n     match-error)))\\n\\n(defsuite define-js-method\\n\\n  (deftest define-js-method.1\\n    (progn\\n      (define-js-method to-string \\\"toString\\\")\\n      (js-eq (to-string (to-js-number 12)) (to-js-string \\\"12\\\"))))\\n\\n  (deftest define-js-method.2\\n    (progn\\n      (define-js-method regexp-test \\\"test\\\")\\n      (let ((re (js-new (js-global \\\"RegExp\\\") (to-js-string \\\"abc\\\"))))\\n        (and (to-lisp-boolean (regexp-test re (to-js-string \\\"abcdef\\\")))\\n             (not (to-lisp-boolean (regexp-test re (to-js-string \\\"uvwxyz\\\"))))))))\\n\\n  (deftest define-js-method.3\\n    (signals-error\\n     (define-js-method)\\n     match-error))\\n\\n  (deftest define-js-method.4\\n    (signals-error\\n     (define-js-method symbol)\\n     match-error)))\\n\\n(defsuite js-undefined-option\\n\\n  (deftest js-undefined-option.1\\n    (js-undefined-option (+ 12 12))\\n    (some 24))\\n\\n  (deftest js-undefined-option.2\\n    (js-undefined-option +js-undefined+)\\n    #nil)\\n\\n  (deftest js-undefined-option.3\\n    (signals-error\\n     (js-undefined-option)\\n     match-error)))\\n\\n(defsuite js-null-option\\n\\n  (deftest js-null-option.1\\n    (js-null-option (+ 12 12))\\n    (some 24))\\n\\n  (deftest js-null-option.2\\n    (js-null-option +js-null+)\\n    #nil)\\n\\n  (deftest js-null-option.3\\n    (signals-error\\n     (js-null-option)\\n     match-error)))\\n\\n(defsuite await\\n\\n  (def Promise (js-global \\\"Promise\\\"))\\n  (define-js-method resolve \\\"resolve\\\")\\n  (define-js-method reject \\\"reject\\\")\\n\\n  (deftest await.1\\n    (progn\\n      (assert (= 1 (await (resolve Promise 1))))\\n      (assert (= 2 (await (resolve Promise 2))))\\n      3)\\n    3)\\n\\n  (deftest await.2\\n    (signals-error\\n     (progn\\n       (assert (= 1 (await (resolve Promise 1))))\\n       (await (reject Promise (make-simple-error \\\"foo\\\"))))\\n     simple-error :message \\\"foo\\\"))\\n\\n  (deftest await.3\\n    (signals-error\\n     (await)\\n     match-error)))\\n\\n(defsuite sleep\\n\\n  (deftest sleep.1\\n    (progn (sleep 1) #t))\\n\\n  (deftest sleep.2\\n    (signals-error\\n     (sleep)\\n     match-error))\\n\\n  (deftest sleep.3\\n    (signals-error\\n     (sleep \\\"foo\\\")\\n     type-error :datum \\\"foo\\\" :expected-type 'number)))\\n\\n(defsuite sync\\n\\n  (deftest sync.1\\n    (signals-error\\n     (sync)\\n     match-error))\\n\\n  (deftest sync.2\\n    (prog1 #t\\n      ((sync #'%sleep) 1)))\\n\\n  (def Promise (js-global \\\"Promise\\\"))\\n  (define-js-method resolve \\\"resolve\\\")\\n  (define-js-method reject \\\"reject\\\")\\n\\n  (deftest sync.3\\n    (progn\\n      (assert (= 1 ((sync (lambda () (resolve Promise 1))))))\\n      (assert (= 2 ((sync (lambda () (resolve Promise 2))))))\\n      3)\\n    3)\\n\\n  (deftest sync.4\\n    (signals-error\\n     (progn\\n       (assert (= 1 ((sync (lambda () (resolve Promise 1))))))\\n       ((sync (lambda () (reject Promise (make-simple-error \\\"foo\\\"))))))\\n     simple-error :message \\\"foo\\\")))\\n\\n(defsuite define-js-method/sync\\n\\n  (def Promise (js-global \\\"Promise\\\"))\\n\\n  (deftest define-js-method/sync.1\\n    (progn\\n      (define-js-method/sync resolve-sync \\\"resolve\\\")\\n      (= (resolve-sync Promise 12) 12)))\\n\\n  (deftest define-js-method/sync.2\\n    (progn\\n      (define-js-method/sync reject-sync \\\"reject\\\")\\n      (signals-error\\n       (reject-sync Promise (make-simple-error \\\"foo\\\"))\\n       simple-error :message \\\"foo\\\"))))\\n\\n(defsuite js-misc\\n\\n  (deftest can-define-lisp-methods-on-js-objects\\n    (progn\\n      (defgeneric foo (obj))\\n      (defmethod foo ((obj object)) 12)\\n      (assert (= 12 (foo +js-null+)))\\n      (assert (= 12 (foo (to-js-string \\\"foo\\\")))))\\n    #void)\\n\\n  (deftest js-exception-becomes-condition\\n    (block exit\\n      (handler-bind ((object (lambda (c)\\n                               (when (js-eq (call-js-method c \\\"toString\\\")\\n                                            (to-js-string \\\"ReferenceError: x is not defined\\\"))\\n                                 (return-from exit #t)))))\\n        (call-js-function (js-global \\\"eval\\\") (to-js-string \\\"x\\\"))))))\\n\";\n\n//# sourceURL=webpack://lispx-test-node/./test/js-test.lispx?");

/***/ }),

/***/ "./test/print-test.lispx":
/*!*******************************!*\
  !*** ./test/print-test.lispx ***!
  \*******************************/
/***/ ((module) => {

"use strict";
eval("module.exports = \";;; LispX Printer Tests\\n\\n(defsuite *print-escape*\\n\\n  (deftest *print-escape*.defined\\n    (and (typep *print-escape* (class dynamic))\\n         (typep (dynamic *print-escape*) (class boolean)))))\\n\\n(defsuite *print-level?*\\n\\n  (deftest *print-level?*.defined\\n    (and (typep *print-level?* (class dynamic))\\n         (typep (dynamic *print-level?*) (class list))))\\n\\n  (defun write-with-level (object level)\\n    (dynamic-let ((*print-level?* (some level)))\\n      (with-standard-output-to-string (write object))))\\n\\n  (deftest *print-level?*.cons\\n    (progn\\n      (def a '(1 (2 (3 (4 (5 (6)))))))\\n      (assert (= \\\"#\\\" (write-with-level a 0)))\\n      (assert (= \\\"(1 #)\\\" (write-with-level a 1)))\\n      (assert (= \\\"(1 (2 #))\\\" (write-with-level a 2)))\\n      (assert (= \\\"(1 (2 (3 #)))\\\" (write-with-level a 3)))\\n      (assert (= \\\"(1 (2 (3 (4 #))))\\\" (write-with-level a 4)))\\n      (assert (= \\\"(1 (2 (3 (4 (5 #)))))\\\" (write-with-level a 5)))\\n      (assert (= \\\"(1 (2 (3 (4 (5 (6))))))\\\" (write-with-level a 6)))\\n      (assert (= \\\"(1 (2 (3 (4 (5 (6))))))\\\" (write-with-level a 7)))\\n      #t))\\n\\n  (deftest *print-level?*.standard-object\\n    (progn\\n      (defclass a () (slot))\\n      (defun make-a (slot) (make-instance (class a) :slot slot))\\n      (def a (make-a (make-a (make-a 12))))\\n      (assert (= \\\"#\\\" (write-with-level a 0)))\\n      (assert (= \\\"#<a :slot #>\\\" (write-with-level a 1)))\\n      (assert (= \\\"#<a :slot #<a :slot #>>\\\" (write-with-level a 2)))\\n      (assert (= \\\"#<a :slot #<a :slot #<a :slot 12>>>\\\" (write-with-level a 3)))\\n      (assert (= \\\"#<a :slot #<a :slot #<a :slot 12>>>\\\" (write-with-level a 4)))\\n      #t)))\\n\\n(defsuite write\\n\\n  (deftest write.1\\n    (with-standard-output-to-string (write 1) (write 2))\\n    \\\"12\\\")\\n\\n  (deftest write.1a\\n    (with-standard-output-to-string\\n      (assert (= 1 (write 1))))\\n    \\\"1\\\")\\n\\n  (deftest write.2\\n    (signals-error\\n     (write)\\n     match-error))\\n\\n  (deftest write.3\\n    (let ((s1 (make-string-output-stream))\\n          (s2 (make-string-output-stream)))\\n      (write 1 :stream s1)\\n      (write 2 :stream s2)\\n      (and (= \\\"1\\\" (get-output-stream-string s1))\\n           (= \\\"2\\\" (get-output-stream-string s2)))))\\n\\n  (deftest write.4a\\n    (signals-error\\n     (write \\\"foo\\\" :stream)\\n     match-error))\\n\\n  (deftest write.4b\\n    (signals-error\\n     (write \\\"foo\\\" :stream 12)\\n     type-error :datum 12 :expected-type 'output-stream)))\\n\\n(defsuite write-to-string\\n\\n  (deftest write-to-string.1\\n    (write-to-string 12)\\n    \\\"12\\\")\\n\\n  (deftest write-to-string.2\\n    (write-to-string \\\"foo\\\")\\n    \\\"\\\\\\\"foo\\\\\\\"\\\")\\n\\n  (deftest write-to-string.3\\n    (signals-error\\n     (write-to-string)\\n     match-error)))\\n\\n(defsuite print\\n\\n  (deftest print.1\\n    (with-standard-output-to-string (print \\\"foo\\\"))\\n    \\\"\\\\n\\\\\\\"foo\\\\\\\"\\\")\\n\\n  (deftest print.1a\\n    (with-standard-output-to-string\\n      (assert (= \\\"foo\\\" (print \\\"foo\\\"))))\\n    \\\"\\\\n\\\\\\\"foo\\\\\\\"\\\")\\n\\n  (deftest print.2\\n    (with-standard-output-to-string (print \\\"foo\\\") (print \\\"bar\\\"))\\n    \\\"\\\\n\\\\\\\"foo\\\\\\\"\\\\n\\\\\\\"bar\\\\\\\"\\\")\\n\\n  (deftest print.3\\n    (signals-error\\n     (print)\\n     match-error))\\n\\n  (deftest print1.1\\n    (with-standard-output-to-string (print \\\"foo\\\") (print1 \\\"bar\\\"))\\n    \\\"\\\\n\\\\\\\"foo\\\\\\\"\\\\\\\"bar\\\\\\\"\\\")\\n\\n  (deftest print1.1a\\n    (with-standard-output-to-string\\n      (assert (= \\\"foo\\\" (print1 \\\"foo\\\"))))\\n    \\\"\\\\\\\"foo\\\\\\\"\\\")\\n\\n  (deftest print1.2\\n    (with-standard-output-to-string (print1 \\\"foo\\\") (print1 \\\"bar\\\"))\\n    \\\"\\\\\\\"foo\\\\\\\"\\\\\\\"bar\\\\\\\"\\\")\\n\\n  (deftest print1.3\\n    (signals-error\\n     (print1)\\n     match-error))\\n\\n  (deftest uprint.1\\n    (with-standard-output-to-string (uprint \\\"foo\\\"))\\n    \\\"\\\\nfoo\\\")\\n\\n  (deftest uprint.1a\\n    (with-standard-output-to-string\\n      (assert (= \\\"foo\\\" (uprint \\\"foo\\\"))))\\n    \\\"\\\\nfoo\\\")\\n\\n  (deftest uprint.2\\n    (with-standard-output-to-string (uprint \\\"foo\\\") (uprint \\\"bar\\\"))\\n    \\\"\\\\nfoo\\\\nbar\\\")\\n\\n  (deftest uprint.3\\n    (signals-error\\n     (uprint)\\n     match-error))\\n\\n  (deftest uprint1.1\\n    (with-standard-output-to-string (uprint \\\"foo\\\") (uprint1 \\\"bar\\\"))\\n    \\\"\\\\nfoobar\\\")\\n\\n  (deftest uprint1.1a\\n    (with-standard-output-to-string\\n      (assert (= \\\"foo\\\" (uprint1 \\\"foo\\\"))))\\n    \\\"foo\\\")\\n\\n  (deftest uprint1.2\\n    (signals-error\\n     (uprint1)\\n     match-error))\\n\\n  (deftest uprint1.3\\n    (with-standard-output-to-string (uprint1 \\\"foo\\\") (uprint1 \\\"bar\\\"))\\n    \\\"foobar\\\"))\\n\";\n\n//# sourceURL=webpack://lispx-test-node/./test/print-test.lispx?");

/***/ }),

/***/ "./test/read-test.lispx":
/*!******************************!*\
  !*** ./test/read-test.lispx ***!
  \******************************/
/***/ ((module) => {

"use strict";
eval("module.exports = \";;; LispX Reader Tests\\n\\n(defsuite read\\n\\n  (deftest read.1\\n    (with-standard-input-from-string \\\"foo\\\" (read))\\n    'foo)\\n\\n  (deftest read.1a\\n    (with-standard-input-from-string \\\"foo\\\" (read (dynamic *standard-input*)))\\n    'foo)\\n\\n  (deftest read.2\\n    (signals-error\\n     (with-standard-input-from-string \\\"(foo\\\" (read))\\n    end-of-file))\\n\\n  (deftest read.2a\\n    (signals-error\\n     (with-standard-input-from-string \\\"(foo\\\" (read (dynamic *standard-input*)))\\n    end-of-file))\\n\\n  ;; Incomplete objects always cause `end-of-file', even if EOF-ERROR-P is false.\\n  (deftest read.2b\\n    (signals-error\\n     (with-standard-input-from-string \\\"(foo\\\" (read (dynamic *standard-input*) #f))\\n     end-of-file))\\n\\n  (deftest read.2c1\\n    (signals-error\\n     (with-standard-input-from-string \\\"\\\" (read))\\n     end-of-file))\\n\\n  (deftest read.2c2\\n    (signals-error\\n     (with-standard-input-from-string \\\"\\\" (read (dynamic *standard-input*)))\\n     end-of-file))\\n\\n  (deftest read.2d\\n    (with-standard-input-from-string \\\"\\\" (read (dynamic *standard-input*) #f))\\n    #void)\\n\\n  (deftest read.2e\\n    (with-standard-input-from-string \\\"\\\" (read (dynamic *standard-input*) #f 44))\\n    44)\\n\\n  (deftest read.3a\\n    (signals-error\\n     (with-standard-input-from-string \\\"\\\" (read 'not-a-stream))\\n     unbound-method-error :class (class symbol) :method-name 'stream-read))\\n\\n  (deftest read.3b\\n    (signals-error\\n     (with-standard-input-from-string \\\"\\\" (read (dynamic *standard-input*) 12))\\n     type-error :datum 12 :expected-type 'boolean))\\n\\n  (deftest read.4\\n    (with-standard-input-from-string \\\"1 2 3\\\"\\n      (assert (= 1 (read)))\\n      (assert (= 2 (read)))\\n      (assert (= 3 (read)))\\n      (assert (= 4 (read (dynamic *standard-input*) #f 4)))\\n      #t)))\\n\";\n\n//# sourceURL=webpack://lispx-test-node/./test/read-test.lispx?");

/***/ }),

/***/ "./test/stream-test.lispx":
/*!********************************!*\
  !*** ./test/stream-test.lispx ***!
  \********************************/
/***/ ((module) => {

"use strict";
eval("module.exports = \";;; Stream Tests\\n\\n(defsuite stream-tests\\n\\n  (deftest standard-streams-defined\\n    (and (typep *standard-input* (class dynamic))\\n         (typep *standard-output* (class dynamic)))))\\n\\n(defsuite string-input-streams\\n\\n  (deftest make-string-input-stream.1\\n    (typep (make-string-input-stream \\\"foo\\\") (class string-input-stream)))\\n\\n  (deftest make-string-input-stream.2\\n    (signals-error\\n     (make-string-input-stream)\\n     match-error))\\n\\n  (deftest make-string-input-stream.3\\n    (signals-error\\n     (make-string-input-stream 12)\\n     type-error :datum 12 :expected-type 'string))\\n\\n  (deftest with-standard-input-from-string.1\\n    (with-standard-input-from-string \\\"foo\\\")\\n    #void)\\n\\n  (deftest with-standard-input-from-string.2\\n    (with-standard-input-from-string \\\"12\\\" (read))\\n    12)\\n\\n  (deftest with-standard-input-from-string.2\\n    (with-standard-input-from-string \\\"12 #t\\\" (list (read) (read)))\\n    (list 12 #t)))\\n\\n(defsuite string-output-streams\\n\\n  (deftest make-string-output-stream.1\\n    (typep (make-string-output-stream) (class string-output-stream)))\\n\\n  (deftest with-standard-output-to-string.1\\n    (with-standard-output-to-string (print \\\"foo\\\") (print \\\"bar\\\"))\\n    \\\"\\\\n\\\\\\\"foo\\\\\\\"\\\\n\\\\\\\"bar\\\\\\\"\\\")\\n\\n  (deftest with-standard-output-to-string.2\\n    (with-standard-output-to-string)\\n    \\\"\\\")\\n\\n  (deftest get-output-stream-string.1\\n    (let ((s (make-string-output-stream)))\\n      (get-output-stream-string s))\\n    \\\"\\\")\\n\\n  (deftest get-output-stream-string.2\\n    (signals-error\\n     (get-output-stream-string)\\n     match-error))\\n\\n  (deftest get-output-stream-string.3\\n    (signals-error\\n     (get-output-stream-string 1)\\n     type-error :datum 1 :expected-type 'string-output-stream)))\\n\\n(defsuite fresh-line\\n\\n  (deftest fresh-line.1\\n    (with-standard-output-to-string (fresh-line) (uprint1 \\\"a\\\") (fresh-line))\\n    \\\"\\\\na\\\\n\\\")\\n\\n  (deftest fresh-line.2\\n    (let ((sos (make-string-output-stream)))\\n      (with-standard-output-to-string\\n       (fresh-line) (uprint1 \\\"a\\\") (fresh-line)\\n       (fresh-line sos) (dynamic-let ((*standard-output* sos)) (uprint1 \\\"b\\\")) (fresh-line sos)\\n       (assert (= \\\"\\\\nb\\\\n\\\" (get-output-stream-string sos)))))\\n    \\\"\\\\na\\\\n\\\"))\\n\";\n\n//# sourceURL=webpack://lispx-test-node/./test/stream-test.lispx?");

/***/ }),

/***/ "./test/test-util-test.lispx":
/*!***********************************!*\
  !*** ./test/test-util-test.lispx ***!
  \***********************************/
/***/ ((module) => {

"use strict";
eval("module.exports = \";; Test before and after (mix in some sleeping to test async)\\n\\n(def #'test-util-test:x (box 1))\\n\\n(defsuite test-util-test:1\\n  (before\\n    (sleep 1)\\n    (sleep 1)\\n    (test-util-test:x 2)\\n    (sleep 1))\\n  (after\\n    (sleep 1)\\n    (sleep 1)\\n    (assert (= (test-util-test:x) 3))\\n    (test-util-test:x 4)\\n    (sleep 1))\\n  (deftest* check-it\\n    (sleep 1)\\n    (sleep 1)\\n    (assert (= (test-util-test:x) 2))\\n    (test-util-test:x 3)\\n    (sleep 1))\\n  (deftest* check-it-again\\n    (sleep 1)\\n    (sleep 1)\\n    (assert (= (test-util-test:x) 3))\\n    (sleep 1)))\\n\\n(defsuite test-util-test:2\\n  (deftest* check-it-again-afterwards\\n    (assert (= (test-util-test:x) 4))))\\n\\n;; Test before-each and after-each\\n\\n(def #'test-util-test:y (box 1))\\n(def #'test-util-test:z (box 10))\\n\\n(defsuite test-util-test:3\\n  (before-each\\n    (sleep 1)\\n    (sleep 1)\\n    (test-util-test:y (+ (test-util-test:y) 1))\\n    (sleep 1))\\n  (after-each\\n    (sleep 1)\\n    (sleep 1)\\n    (test-util-test:z (- (test-util-test:z) 1))\\n    (sleep 1))\\n  (deftest* t1\\n    (sleep 1)\\n    (sleep 1)\\n    (assert (= (test-util-test:y) 2))\\n    (assert (= (test-util-test:z) 10))\\n      (sleep 1))\\n  (deftest* t2\\n    (sleep 1)\\n    (sleep 1)\\n    (assert (= (test-util-test:y) 3))\\n    (assert (= (test-util-test:z) 9))\\n    (sleep 1)))\\n\\n(defsuite test-util-test:4\\n  (deftest* t3\\n    (assert (= (test-util-test:y) 3))\\n    (assert (= (test-util-test:z) 8))))\\n\";\n\n//# sourceURL=webpack://lispx-test-node/./test/test-util-test.lispx?");

/***/ }),

/***/ "./test/test-util.lispx":
/*!******************************!*\
  !*** ./test/test-util.lispx ***!
  \******************************/
/***/ ((module) => {

"use strict";
eval("module.exports = \";;; Test Utilities\\n\\n;; Import Mocha test framework functions from the global scope.\\n(def #'mocha:describe (to-lisp-function (js-global \\\"describe\\\")))\\n(def #'mocha:it (to-lisp-function (js-global \\\"it\\\")))\\n(def #'mocha:before (to-lisp-function (js-global \\\"before\\\")))\\n(def #'mocha:before-each (to-lisp-function (js-global \\\"beforeEach\\\")))\\n(def #'mocha:after (to-lisp-function (js-global \\\"after\\\")))\\n(def #'mocha:after-each (to-lisp-function (js-global \\\"afterEach\\\")))\\n\\n(defexpr deftest (name expression . expected?) env\\n  \\\"Run the EXPRESSION as a test identified by NAME and compare its\\nresult to an EXPECTED value (which defaults to true).  Tests may\\ncapture a continuation to the default prompt.\\\"\\n  (mocha:it\\n   (to-js-string (symbol-name name))\\n   (js-lambda ()\\n     ;; Push the default prompt for asynchronous tests.\\n     ;;\\n     ;; Note that this is quite subtle: when an asynchronous test\\n     ;; captures to this prompt, it returns a promise here.  This\\n     ;; promise is returned to Mocha, which does the right thing (wait\\n     ;; for it to resolve).\\n     (push-prompt +default-prompt+\\n       ;; Test and expected expressions are evaluated in fresh child\\n       ;; environments of the current environment, so that bindings\\n       ;; they create don't affect other tests.\\n       (assert (= (eval expression (make-environment env))\\n                  (eval (optional expected? #t) (make-environment env))))))))\\n\\n(defmacro deftest* (name . forms)\\n  \\\"Run the FORMS as a test identified by NAME, but unlike `deftest',\\nsimply ignore their result.  The test only fails when the forms signal\\nan error.  Use this instead of `deftest' if you just want to run some\\nforms, and don't need to compare their result to an expected value.\\\"\\n  (list #'deftest name (list* #'prog1 #t forms)))\\n\\n(defexpr defsuite (name . forms) env\\n  \\\"Evaluate FORMS as a group of tests identified by NAME.  The forms\\nmust not capure any continuations.\\\"\\n  (mocha:describe\\n   (to-js-string (symbol-name name))\\n   (js-lambda ()\\n     ;; We don't push a prompt here.  The reason is that Mocha\\n     ;; describe() doesn't support promises anyway.  So any Lisp code\\n     ;; that tries to capture a continuation directly inside a suite\\n     ;; will get a \\\"prompt not found\\\" error (due to the barrier inside\\n     ;; `js-lambda').\\n     (eval (list* #'progn forms) env))))\\n\\n(defun %make-mocha-hook (#'mocha-hook-fun)\\n  \\\"Metaprogramming utility to create Mocha hooks.  Hooks may capture.\\\"\\n  (vau forms env\\n    (mocha-hook-fun\\n     (js-lambda ()\\n       (push-prompt 'default-prompt\\n         (eval (list* #'progn forms) env))))))\\n\\n(def #'before (%make-mocha-hook #'mocha:before))\\n(def #'before-each (%make-mocha-hook #'mocha:before-each))\\n(def #'after (%make-mocha-hook #'mocha:after))\\n(def #'after-each (%make-mocha-hook #'mocha:after-each))\\n\\n(defexpr signals-error (form condition-class . slot-specs) env\\n  \\\"Evaluate the FORM and assert that it signals a condition of the\\ngiven CONDITION-CLASS.  Expected slots of the condition can be\\nspecified by SLOT-SPECS (a plist) and will be compared against the\\nslots of the signalled condition.\\\"\\n  (let ((class (find-class condition-class env)))\\n    (block exit\\n      (handler-bind ((object (lambda (c)\\n                               (if (typep c class)\\n                                   (progn\\n                                     (loop-let -check-slots- ((slot-specs slot-specs))\\n                                       (when (not (null slot-specs))\\n                                         (let (((name value . rest-slot-specs) slot-specs))\\n                                           (unless (= (slot-value c name) (eval value env))\\n                                             (return-from exit #f))\\n                                           (-check-slots- rest-slot-specs))))\\n                                     (return-from exit #t))\\n                                   (return-from exit #f)))))\\n        (eval form env))\\n      #f)))\\n\";\n\n//# sourceURL=webpack://lispx-test-node/./test/test-util.lispx?");

/***/ }),

/***/ "lispx-vm":
/*!*************************************!*\
  !*** external "../lispx-vm.umd.js" ***!
  \*************************************/
/***/ ((module) => {

"use strict";
module.exports = __WEBPACK_EXTERNAL_MODULE_lispx_vm__;

/***/ }),

/***/ "?0ac7":
/*!**********************!*\
  !*** util (ignored) ***!
  \**********************/
/***/ (() => {

eval("/* (ignored) */\n\n//# sourceURL=webpack://lispx-test-node/util_(ignored)?");

/***/ }),

/***/ "./node_modules/chai/index.mjs":
/*!*************************************!*\
  !*** ./node_modules/chai/index.mjs ***!
  \*************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"Assertion\": () => (/* binding */ Assertion),\n/* harmony export */   \"AssertionError\": () => (/* binding */ AssertionError),\n/* harmony export */   \"assert\": () => (/* binding */ assert),\n/* harmony export */   \"config\": () => (/* binding */ config),\n/* harmony export */   \"core\": () => (/* binding */ core),\n/* harmony export */   \"default\": () => (__WEBPACK_DEFAULT_EXPORT__),\n/* harmony export */   \"expect\": () => (/* binding */ expect),\n/* harmony export */   \"should\": () => (/* binding */ should),\n/* harmony export */   \"use\": () => (/* binding */ use),\n/* harmony export */   \"util\": () => (/* binding */ util),\n/* harmony export */   \"version\": () => (/* binding */ version)\n/* harmony export */ });\n/* harmony import */ var _index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./index.js */ \"./node_modules/chai/index.js\");\n\n\nconst expect = _index_js__WEBPACK_IMPORTED_MODULE_0__.expect;\nconst version = _index_js__WEBPACK_IMPORTED_MODULE_0__.version;\nconst Assertion = _index_js__WEBPACK_IMPORTED_MODULE_0__.Assertion;\nconst AssertionError = _index_js__WEBPACK_IMPORTED_MODULE_0__.AssertionError;\nconst util = _index_js__WEBPACK_IMPORTED_MODULE_0__.util;\nconst config = _index_js__WEBPACK_IMPORTED_MODULE_0__.config;\nconst use = _index_js__WEBPACK_IMPORTED_MODULE_0__.use;\nconst should = _index_js__WEBPACK_IMPORTED_MODULE_0__.should;\nconst assert = _index_js__WEBPACK_IMPORTED_MODULE_0__.assert;\nconst core = _index_js__WEBPACK_IMPORTED_MODULE_0__.core;\n\n/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (_index_js__WEBPACK_IMPORTED_MODULE_0__);\n\n\n//# sourceURL=webpack://lispx-test-node/./node_modules/chai/index.mjs?");

/***/ }),

/***/ "./test/all-tests.mjs":
/*!****************************!*\
  !*** ./test/all-tests.mjs ***!
  \****************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var _lisp_tests_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./lisp-tests.mjs */ \"./test/lisp-tests.mjs\");\n/* harmony import */ var _vm_test_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./vm-test.mjs */ \"./test/vm-test.mjs\");\n/* harmony import */ var _eval_test_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./eval-test.mjs */ \"./test/eval-test.mjs\");\n/* harmony import */ var _control_test_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./control-test.mjs */ \"./test/control-test.mjs\");\n/* harmony import */ var _seq_test_mjs__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./seq-test.mjs */ \"./test/seq-test.mjs\");\n/* harmony import */ var _stream_test_mjs__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./stream-test.mjs */ \"./test/stream-test.mjs\");\n/* harmony import */ var _read_test_mjs__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./read-test.mjs */ \"./test/read-test.mjs\");\n/* harmony import */ var _print_test_mjs__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./print-test.mjs */ \"./test/print-test.mjs\");\n/* harmony import */ var _js_test_mjs__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./js-test.mjs */ \"./test/js-test.mjs\");\n\n\n\n\n\n\n\n\n\n\n\n//# sourceURL=webpack://lispx-test-node/./test/all-tests.mjs?");

/***/ }),

/***/ "./test/control-test.mjs":
/*!*******************************!*\
  !*** ./test/control-test.mjs ***!
  \*******************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var chai__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! chai */ \"./node_modules/chai/index.mjs\");\n/* harmony import */ var lispx_vm__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! lispx-vm */ \"lispx-vm\");\n\n\n\n\nconst vm = new lispx_vm__WEBPACK_IMPORTED_MODULE_1__.VM();\n\nfunction lookup_operator(name)\n{\n    return vm.get_environment().lookup(vm.fsym(name));\n}\n\ndescribe(\"Continuations\", () => {\n\n    it(\"Test %TAKE-SUBCONT suspension and resumption.\", () => {\n\n        const take_subcont = lookup_operator(\"%take-subcont\");\n        const prompt = vm.str(\"p\");\n        const handler = vm.alien_function(() => {}); // Unused here.\n\n        const susp = vm.operate(take_subcont, vm.list(prompt, handler));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(susp, vm.Suspension);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(susp.prompt, prompt);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(susp.handler, handler);\n\n        const k = susp.continuation;\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(k, vm.Continuation);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isNull(k.inner);\n\n        const resum = new vm.Resumption(k, vm.alien_function(() => vm.num(100)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(resum.resume(), vm.num(100)));\n\n    });\n\n});\n\ndescribe(\"Dynamic Variables\", () => {\n\n    it(\"Can create a dynamic variable with initial value.\", () => {\n\n        const dyn = vm.make_dynamic(12);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(12, dyn.get_value());\n        dyn.set_value(11);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(11, dyn.get_value());\n\n    });\n\n    it(\"Can dynamically bind variables.\", () => {\n\n        const dyn1 = vm.make_dynamic(1);\n        const dyn2 = vm.make_dynamic(2);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(1, dyn1.get_value());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(2, dyn2.get_value());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(30, vm.progv([dyn1, dyn2], [10, 20], () => {\n            return dyn1.get_value() + dyn2.get_value();\n        }));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(1, dyn1.get_value());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(2, dyn2.get_value());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() =>\n            vm.progv([dyn1], [10],\n                     () => { throw new Error(\"value: \" + dyn1.get_value()) }),\n            \"value: 10\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(1, dyn1.get_value());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(2, dyn2.get_value());\n\n    });\n\n});\n\n\n//# sourceURL=webpack://lispx-test-node/./test/control-test.mjs?");

/***/ }),

/***/ "./test/eval-test.mjs":
/*!****************************!*\
  !*** ./test/eval-test.mjs ***!
  \****************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var chai__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! chai */ \"./node_modules/chai/index.mjs\");\n/* harmony import */ var lispx_vm__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! lispx-vm */ \"lispx-vm\");\n/* harmony import */ var _test_util_lispx__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./test-util.lispx */ \"./test/test-util.lispx\");\n/* harmony import */ var _boot_test_lispx__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./boot-test.lispx */ \"./test/boot-test.lispx\");\n/* harmony import */ var _control_test_lispx__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./control-test.lispx */ \"./test/control-test.lispx\");\n/* harmony import */ var _stream_test_lispx__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./stream-test.lispx */ \"./test/stream-test.lispx\");\n/* harmony import */ var _read_test_lispx__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./read-test.lispx */ \"./test/read-test.lispx\");\n/* harmony import */ var _print_test_lispx__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./print-test.lispx */ \"./test/print-test.lispx\");\n/* harmony import */ var _js_test_lispx__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./js-test.lispx */ \"./test/js-test.lispx\");\n/* harmony import */ var _hierarchy_test_lispx__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./hierarchy-test.lispx */ \"./test/hierarchy-test.lispx\");\n\n\n\n\n\n\n\n\n\n\n\n\n\nconst vm = time(\"Boot LispX\", () => new lispx_vm__WEBPACK_IMPORTED_MODULE_1__.VM());\n\n/*\n * Utilities.\n */\n\nfunction time(name, fun)\n{\n    const start = new Date().getTime();\n    const result = fun();\n    const end = new Date().getTime();\n    const time = end - start;\n    console.log(name + \": \"  + time + \"ms\");\n    return result;\n}\n\nfunction make_child_environment()\n{\n    return vm.make_environment(vm.get_environment());\n}\n\nfunction quote(expr)\n{\n    return vm.list(vm.sym(\"quote\"), expr);\n}\n\ndescribe(\"Evaluation & Operation\", () => {\n\n    it(\"Built-in operators are defined.\", () => {\n\n        const operators = [\n            \"%vau\",\n            \"%def\",\n            \"%progn\",\n            \"%if\",\n            \"%loop\",\n            \"%unwind-protect\",\n        ];\n\n        for (const name of operators) {\n            const op = vm.get_environment().lookup(vm.fsym(name));\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(op.get_name(), vm.sym(name));\n            vm.assert_type(op, vm.Built_in_operator);\n        }\n\n    });\n\n    it(\"Built-in functions are defined.\", () => {\n\n        const functions = [\n            \"%*\",\n            \"%+\",\n            \"%-\",\n            \"%/\",\n            \"%<\",\n            \"%<=\",\n            \"%=\",\n            \"%>\",\n            \"%>=\",\n            \"%add-method\",\n            \"%boundp\",\n            \"%car\",\n            \"%catch\",\n            \"%cdr\",\n            \"%class-name\",\n            \"%class-of\",\n            \"%class-symbol\",\n            \"%cons\",\n            \"%eq\",\n            \"%eval\",\n            \"%find-method\",\n            \"%function-symbol\",\n            \"%intern\",\n            \"%keyword-symbol\",\n            \"%list*\",\n            \"%list-length\",\n            \"%list-subseq\",\n            \"%make-environment\",\n            \"%make-instance\",\n            \"%make-standard-class\",\n            \"%nth\",\n            \"%nthcdr\",\n            \"%panic\",\n            \"%progv\",\n            \"%push-delim-subcont\",\n            \"%push-prompt\",\n            \"%push-subcont-barrier\",\n            \"%reinitialize-standard-class\",\n            \"%reverse\",\n            \"%set-slot-value\",\n            \"%slot-bound-p\",\n            \"%slot-value\",\n            \"%some\",\n            \"%string-subseq\",\n            \"%subclassp\",\n            \"%symbol-name\",\n            \"%take-subcont\",\n            \"%throw\",\n            \"%typep\",\n            \"%unwrap\",\n            \"%variable-symbol\",\n            \"%wrap\",\n        ];\n\n        for (const name of functions) {\n            const op = vm.get_environment().lookup(vm.fsym(name));\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(op.get_name(), vm.sym(name));\n            vm.assert_type(op, vm.Function);\n        }\n\n    });\n\n    it(\"Many objects evaluate to themselves.\", () => {\n\n        const examples = [\n            vm.num(1),\n            vm.kwd(\"foo\"),\n            vm.t(),\n            vm.f(),\n            vm.nil(),\n            vm.void(),\n            vm.ignore(),\n            // Plain JS objects, too.\n            12,\n            true,\n            false,\n            undefined,\n            { hello: \"world\" },\n            [1,2],\n        ];\n\n        for (const ex of examples)\n            for (const eval_fun of [vm.eval, vm.eval_form])\n                chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(eval_fun(ex), ex);\n\n    });\n\n    it(\"Symbols evaluate to the value they are bound to.\", () => {\n\n        const examples = [\n            [vm.csym(\"object\"), vm.lisp_class(vm.Object)],\n            [vm.csym(\"class\"), vm.lisp_class(vm.Class)],\n        ];\n\n        for (const [symbol, value] of examples)\n            for (const eval_fun of [vm.eval, vm.eval_form])\n                chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(eval_fun(symbol), value);\n\n    });\n\n    it(\"Evaluating an unbound symbol causes an error.\", () => {\n\n        for (const eval_fun of [vm.eval, vm.eval_form]) {\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => eval_fun(vm.sym(\"this-is-not-bound\")),\n                          \"Unbound variable: this-is-not-bound\");\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => eval_fun(vm.fsym(\"this-is-not-bound\")),\n                          \"Unbound function: this-is-not-bound\");\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => eval_fun(vm.csym(\"this-is-not-bound\")),\n                          \"Unbound class: this-is-not-bound\");\n        }\n\n    });\n\n    it(\"Evaluating a cons whose car is not an operator causes an error.\", () => {\n\n        const examples = [ vm.nil(), undefined, \"foo\", vm.str(\"foo\"), vm.num(1) ];\n\n        for (const ex of examples)\n            for (const eval_fun of [vm.eval, vm.eval_form])\n                chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => eval_fun(vm.list(ex)),\n                              \"Type assertion failed\");\n\n    });\n\n    it(\"The environment to evaluate in can be specified.\", () => {\n\n        const env = make_child_environment();\n        env.put(vm.sym(\"foo\"), vm.num(12));\n        for (const eval_fun of [vm.eval, vm.eval_form])\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(eval_fun(vm.sym(\"foo\"), env),\n                            vm.num(12)));\n\n    });\n\n    it(\"vm.eval_form() doesn't swallow suspensions.\", () => {\n\n        const form = vm.list(vm.sym(\"take-subcont\"), vm.str(\"prompt\"), vm.sym(\"k\"));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_form(form), \"Prompt not found\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.eval(form), vm.Suspension);\n\n    });\n\n    it(\"%EVAL uses the root environment if no environment is specified.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.eval(vm.list(vm.sym(\"%eval\"), quote(vm.csym(\"object\")))),\n                          vm.Class);\n\n    });\n\n    it(\"Symbols in the operator position are looked up in the function namespace.\", () => {\n\n        const env = make_child_environment();\n        env.put(vm.fsym(\"foo\"), vm.eval_js_string(\"(%vau #ignore #ignore 100)\"));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval_js_string(\"(foo)\", env), vm.num(100)));\n\n    });\n\n    it(\"Non-symbol expressions in the operator position are evaluated normally.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval_js_string(\"((%vau #ignore #ignore 200))\"), vm.num(200)));\n\n    });\n\n    it(\"Test vm.operate().\", () => {\n\n        const list_op = vm.get_environment().lookup(vm.fsym(\"list\"));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.operate(list_op, vm.nil()), vm.nil()));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.operate(list_op, vm.list(vm.num(1))), vm.list(vm.num(1))));\n\n    });\n\n    it(\"The environment to operate in can be specified.\", () => {\n\n        const def_op = vm.get_environment().lookup(vm.fsym(\"%def\"));\n        const env = make_child_environment();\n        vm.operate(def_op, vm.list(vm.sym(\"x\"), vm.num(1)), env);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(env.lookup(vm.sym(\"x\")), vm.num(1)));\n\n    });\n\n    it(\"match() binds symbols to their operands.\", () => {\n\n        const env = vm.make_environment();\n        const rhs = vm.num(12);\n        const result = vm.match(vm.sym(\"x\"), rhs, env);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(result, rhs));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(env.lookup(vm.sym(\"x\")), vm.num(12)));\n\n    });\n\n    it(\"match() doesn't allow keyword definiends.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.match(vm.kwd(\"x\"), vm.nil()),\n                      \"Match error\");\n\n    });\n\n    it(\"match() recursively matches conses.\", () => {\n\n        const env = vm.make_environment();\n        const rhs = vm.cons(vm.num(12), vm.num(33));\n        const result = vm.match(vm.cons(vm.sym(\"x\"), vm.sym(\"y\")), rhs, env);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(rhs, result));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(env.lookup(vm.sym(\"x\")), vm.num(12)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(env.lookup(vm.sym(\"y\")), vm.num(33)));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.match(vm.cons(vm.sym(\"x\"), vm.sym(\"y\")), vm.t(), env),\n                      \"Match error\");\n\n    });\n\n    it(\"match() requires the operand of #NIL to be #NIL.\", () => {\n\n        const env = vm.make_environment();\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.match(vm.nil(), vm.nil(), env), vm.nil()));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.match(vm.nil(), vm.t(), env),\n                      \"Match error\");\n\n    });\n\n    it(\"#IGNORE ignores its operand in match().\", () => {\n\n        const env = vm.make_environment();\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.match(vm.ignore(), vm.num(1), env), vm.num(1)));\n\n    });\n\n    it(\"Other definiends in match() are an error.\", () => {\n\n        const env = vm.make_environment();\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.match(vm.num(1), vm.void(), env),\n                      \"Type assertion failed\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.match(vm.t(), vm.void(), env),\n                      \"Type assertion failed\");\n\n    });\n\n});\n\ndescribe(\"Fexprs\", () => {\n\n    it(\"Fexprs are anonymous.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.eval_js_string(\"(%vau #ignore #ignore #ignore)\").get_name(),\n                     vm.sym(\"anonymous operator\"));\n\n    });\n\n});\n\ndescribe(\"%VAU\", () => {\n\n    it(\"%VAU constructs simple fexprs.\", () => {\n\n        const fexpr = vm.eval_js_string(\"(%vau #ignore #ignore 12)\");\n        vm.assert_type(fexpr, vm.Fexpr);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval(vm.list(fexpr)), vm.num(12)));\n\n    });\n\n    it(\"%VAU sets fexpr properties.\", () => {\n\n        const def_env = make_child_environment();\n        const fexpr = vm.eval_js_string(\"(%vau x y z)\", def_env);\n        vm.assert_type(fexpr, vm.Fexpr);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(fexpr.param_tree, vm.sym(\"x\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(fexpr.env_param, vm.sym(\"y\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(fexpr.body_form, vm.sym(\"z\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(fexpr.def_env, def_env));\n\n    });\n\n    it(\"%VAU evaluates the body form and passes on errors.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"((%vau #ignore #ignore z))\"),\n                      \"Unbound variable: z\");\n\n    });\n\n    it(\"%VAU receives the operands.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval_js_string(\"((%vau (x y z) #ignore z) 1 2 3)\"),\n                        vm.num(3)));\n\n    });\n\n    it(\"%VAU throws for illegal definiends.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(%vau 1 #ignore #ignore)\"),\n                      \"Type assertion failed\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(%vau #ignore 1 #ignore)\"),\n                      \"Type assertion failed\");\n\n    });\n\n    it(\"%VAU binds the environment parameter.\", () => {\n\n        const dyn_env = make_child_environment();\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval_js_string(\"((%vau #ignore env env))\", dyn_env),\n                        dyn_env));\n\n    });\n\n});\n\ndescribe(\"%DEF\", () => {\n\n    it(\"%DEF evaluates the expression and matches the definiend.\", () => {\n\n        const env = make_child_environment();\n        const fexpr = vm.eval_js_string(\"(%def #'some-fexpr (%vau x #ignore 12))\", env);\n        vm.assert_type(fexpr, vm.Fexpr);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval_js_string(\"(some-fexpr)\", env), vm.num(12)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(env.lookup(vm.fsym(\"some-fexpr\")), fexpr));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.get_environment().lookup(vm.fsym(\"some-fexpr\")),\n                      \"Unbound function: some-fexpr\");\n\n    });\n\n    it(\"%DEF throws for illegal definiends.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(%def 1 #ignore)\"),\n                      \"Type assertion failed\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(%def #t #ignore)\"),\n                      \"Type assertion failed\");\n\n    });\n\n    it(\"%DEF passes on errors from the expression.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(%def #ignore x)\"),\n                      \"Unbound variable: x\");\n\n    });\n\n});\n\ndescribe(\"%PROGN\", () => {\n\n    it(\"%PROGN evaluates its operands and returns the result of the last.\", () => {\n\n        const env = make_child_environment();\n        env.put(vm.sym(\"x\"), vm.num(3));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval_js_string(\"(%progn 1 2 x)\", env),\n                        vm.num(3)));\n\n    });\n\n    it(\"%PROGN evaluates to #VOID if there are no operands.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval_js_string(\"(%progn)\"),\n                        vm.void()));\n\n    });\n\n    it(\"%PROGN passes on errors from the operands.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(%progn 1 x 2)\"),\n                      \"Unbound variable: x\");\n\n    });\n\n});\n\ndescribe(\"%IF\", () => {\n\n    it(\"%IF evaluates the test and sub-expressions.\", () => {\n\n        const env = make_child_environment();\n\n        env.put(vm.sym(\"x\"), vm.t());\n        env.put(vm.sym(\"y\"), vm.f());\n\n        env.put(vm.sym(\"a\"), vm.num(1));\n        env.put(vm.sym(\"b\"), vm.num(2));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval_js_string(\"(%if x a b)\", env), vm.num(1)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval_js_string(\"(%if y a b)\", env), vm.num(2)));\n\n    });\n\n    it(\"%IF requires a boolean test.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(%if 1 2 3)\"),\n                      \"Type assertion failed\");\n\n    });\n\n    it(\"%IF passes on errors from evaluating the subexpressions.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(%if x 2 3)\"),\n                      \"Unbound variable: x\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(%if #t y 3)\"),\n                      \"Unbound variable: y\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(%if #f 2 z)\"),\n                      \"Unbound variable: z\");\n\n    });\n\n    it(\"%IF only evaluates one of consequent and alternative, not both.\", () => {\n\n        // X is unbound.\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval_js_string(\"(%if #t 1 x)\"), vm.num(1)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval_js_string(\"(%if #f x 1)\"), vm.num(1)));\n\n    });\n\n});\n\ndescribe(\"Wrapping and unwrapping.\", () => {\n\n    it(\"A function has the same name as its wrapped operator.\", () => {\n\n        const wrapped = vm.alien_function(() => vm.t(), \"foo\");\n        const fun = vm.wrap(wrapped);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(fun.get_name(), vm.sym(\"foo\"));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.eval(vm.list(wrapped)), vm.t());\n\n    });\n\n    it(\"Functions can be unwrapped.\", () => {\n\n        const wrapped = vm.alien_function(() => vm.t());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.wrap(wrapped).unwrap(), wrapped);\n\n    });\n\n    it(\"Wrapping induces argument evaluation.\", () => {\n\n        const env = make_child_environment();\n        env.put(vm.sym(\"x\"), vm.num(1));\n        env.put(vm.sym(\"y\"), vm.num(2));\n        env.put(vm.fsym(\"f\"), vm.wrap(vm.eval_js_string(\"(%vau args #ignore args)\")));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.eval_js_string(\"(f x y)\", env),\n                        vm.list(vm.num(1), vm.num(2))));\n\n    });\n\n    it(\"Wrap requires its argument to be an operator.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.wrap(1), \"Type assertion failed\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.wrap(vm.t()), \"Type assertion failed\");\n\n    });\n\n    it(\"Functions can be (multiply) wrapped.\", () => {\n\n        const doubly_wrapped = vm.wrap(vm.wrap(vm.alien_function(() => vm.t())));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.eval(vm.list(doubly_wrapped)), vm.t());\n\n    });\n\n});\n\ndescribe(\"Alien Operators\", () => {\n\n    it(\"Alien operators can be defined from JS functions.\", () => {\n\n        const op = vm.alien_operator((a, b, c) => {\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(a, vm.sym(\"x\"));\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(b, vm.sym(\"y\"));\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(c, vm.sym(\"z\"));\n            return vm.t();\n        }, \"foo\");\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(op.get_name(), vm.sym(\"foo\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(op instanceof vm.Built_in_operator);\n\n        const result = vm.operate(op, vm.list(vm.sym(\"x\"), vm.sym(\"y\"), vm.sym(\"z\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(result, vm.t()));\n\n    });\n\n    it(\"Alien functions can be defined from JS functions.\", () => {\n\n        const op = vm.alien_function((a, b, c) => a + b + c, \"foo\");\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(op.get_name(), vm.sym(\"foo\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(op instanceof vm.Function);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(op.unwrap() instanceof vm.Built_in_operator);\n\n        const env = make_child_environment();\n        env.put(vm.sym(\"x\"), 1);\n        env.put(vm.sym(\"y\"), 2);\n        env.put(vm.sym(\"z\"), 3);\n        const result = vm.operate(op, vm.list(vm.sym(\"x\"), vm.sym(\"y\"), vm.sym(\"z\")), env);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(result, 6));\n\n    });\n\n    it(\"Alien operators and functions can be anonymous.\", () => {\n\n        const op = vm.alien_operator(() => {});\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(op.get_name(), vm.sym(\"anonymous built-in operator\")));\n\n        const fun = vm.alien_function(() => {});\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(op.get_name(), vm.sym(\"anonymous built-in operator\")));\n\n    });\n\n});\n\ndescribe(\"Generic Functions\", () => {\n\n    it(\"Test add_method() and find_method().\", () => {\n\n        // Define a method M1 on OBJECT.\n        const method_name = vm.sym(\"m1\");\n        const method = vm.alien_function(() => vm.void());\n        vm.lisp_class(vm.Object).add_method(method_name, method);\n\n        // Test that strings and numbers inherit the method.\n        for (const cls of [vm.lisp_class(vm.Object),\n                           vm.lisp_class(vm.String),\n                           vm.lisp_class(vm.Number)]) {\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(method === cls.find_method(method_name));\n\n            // Test an unbound method.\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => cls.find_method(vm.sym(\"m2\")),\n                          \"Unbound method: m2\");\n        }\n\n        // Override the method for strings.\n        const str_method = vm.alien_function(() => vm.void());\n        vm.lisp_class(vm.String).add_method(method_name, str_method);\n\n        // Test that it returns the new method for strings...\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(str_method === vm.lisp_class(vm.String).find_method(method_name));\n        // ...and still the old one for numbers and objects.\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(method === vm.lisp_class(vm.Number).find_method(method_name));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(method === vm.lisp_class(vm.Object).find_method(method_name));\n    });\n\n});\n\ndescribe(\"Panicking\", () => {\n\n    it(\"Panics can not be caught by condition handlers.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(panic #void)\"),\n                      \"LISP panic!\");\n\n        const env = make_child_environment();\n        env.put(vm.sym(\"cause\"), new Error(\"it happened\"));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(panic cause)\", env),\n                      \"LISP panic: it happened\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() =>\n            vm.eval_js_string(`(block b\n                                 (handler-bind ((object (lambda (e) (return-from b))))\n                                   (panic cause)))`, env),\n            \"LISP panic: it happened\");\n\n    });\n\n    it(\"Panics do not trigger UNWIND-PROTECT.\", () => {\n\n        const env = make_child_environment();\n        env.put(vm.sym(\"cause\"), new Error(\"it happened\"));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(`(unwind-protect (panic cause)\n                                                 this-var-is-unbound)`, env),\n                      \"LISP panic: it happened\");\n\n    });\n\n    it(\"ERROR panics if there is no handler.\", () => {\n\n        const env = make_child_environment();\n        env.put(vm.sym(\"cause\"), new Error(\"foo\"));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.eval_js_string(\"(error cause)\", env),\n                      \"LISP panic: foo\");\n\n    });\n\n});\n\n\n//# sourceURL=webpack://lispx-test-node/./test/eval-test.mjs?");

/***/ }),

/***/ "./test/js-test.mjs":
/*!**************************!*\
  !*** ./test/js-test.mjs ***!
  \**************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var chai__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! chai */ \"./node_modules/chai/index.mjs\");\n/* harmony import */ var lispx_vm__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! lispx-vm */ \"lispx-vm\");\n\n\n\n\nconst vm = new lispx_vm__WEBPACK_IMPORTED_MODULE_1__.VM();\n\ndescribe(\"JavaScript Interface\", () => {\n\n    it(\"Test to_lisp_boolean().\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.to_lisp_boolean(true), vm.t());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.to_lisp_boolean(false), vm.f());\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.to_lisp_boolean(12), \"Type assertion failed\");\n\n    });\n\n    it(\"Test apply_js_method().\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.apply_js_method(9.656, vm.str(\"toFixed\"), vm.list(2)),\n                     \"9.66\");\n\n    });\n\n    it(\"Test js_global().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(Math === vm.js_global(vm.str(\"Math\")));\n\n    });\n\n    it(\"Test js_new() and js_get().\", () => {\n\n        class Point\n        {\n            constructor(x, y)\n            {\n                this.x = x;\n                this.y = y;\n            }\n        }\n\n        const pt = vm.js_new(Point, 1, 2);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(pt.x, 1);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(pt.y, 2);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.js_get(pt, vm.str(\"x\")), 1);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.js_get(pt, vm.str(\"y\")), 2);\n\n    });\n\n    it(\"Test to_js_function().\", () => {\n\n        const js_fun = vm.to_js_function(vm.get_environment().lookup(vm.fsym(\"list\")));\n        vm.assert_type(js_fun, \"function\");\n        const list = js_fun(vm.num(1), vm.num(2));\n        vm.assert_type(list, vm.List);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.elt(list, 0), vm.num(1)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.elt(list, 1), vm.num(2)));\n\n    });\n\n    it(\"Test to_lisp_function().\", () => {\n\n        const js_fun = () => 123;\n        const lisp_fun = vm.to_lisp_function(js_fun);\n        vm.assert_type(lisp_fun, vm.Operator);\n        vm.get_environment().put(vm.fsym(\"some-fun\"), lisp_fun);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(123, vm.eval(vm.list(vm.sym(\"some-fun\"))));\n\n    });\n\n    it(\"Test js_elt().\", () => {\n        const array = [1, 2, 3];\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(1, vm.js_elt(array, vm.num(0)));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(2, vm.js_elt(array, vm.num(1)));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.js_elt(\"foo\", vm.num(1)), \"Assertion failed\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.js_elt(array, vm.f()), \"expected number\");\n    });\n\n    it(\"Test JS console output.\", () => {\n\n        function test_output(expected_result, expected_output_lines, thunk)\n        {\n            const output_lines = [];\n            function output_function(line) { output_lines.push(line); };\n            const stream = new vm.JS_console_output_stream(output_function);\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(thunk(stream), expected_result);\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(output_lines, expected_output_lines);\n        }\n\n        test_output(vm.f(), [], (stream) => {\n            return stream.fresh_line()\n        });\n        test_output(vm.f(), [], (stream) => {\n            stream.fresh_line();\n            return stream.fresh_line();\n        });\n        test_output(vm.void(), [], (stream) => {\n            return stream.force_output()\n        });\n        test_output(vm.void(), [], (stream) => {\n            stream.fresh_line();\n            return stream.force_output()\n        });\n        test_output(vm.str(\"foo\"), [], (stream) => {\n            return stream.write_string(vm.str(\"foo\"));\n        });\n        test_output(vm.void(), [\"foo\"], (stream) => {\n            stream.write_string(vm.str(\"foo\"));\n            return stream.force_output();\n        });\n        test_output(vm.void(), [\"foobar\"], (stream) => {\n            stream.write_string(vm.str(\"foo\"));\n            stream.write_string(vm.str(\"bar\"));\n            return stream.force_output();\n        });\n        test_output(vm.void(), [\"foobar\", \"foobar\"], (stream) => {\n            stream.write_string(vm.str(\"foo\"));\n            stream.write_string(vm.str(\"bar\"));\n            stream.force_output();\n            stream.write_string(vm.str(\"foo\"));\n            stream.write_string(vm.str(\"bar\"));\n            return stream.force_output();\n        });\n        test_output(vm.void(), [\"foo\\nbar\", \"foobar\"], (stream) => {\n            stream.write_string(vm.str(\"foo\"));\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(stream.fresh_line(), vm.t());\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(stream.fresh_line(), vm.f());\n            stream.write_string(vm.str(\"bar\"));\n            stream.force_output();\n            stream.write_string(vm.str(\"foo\"));\n            stream.write_string(vm.str(\"bar\"));\n            return stream.force_output();\n        });\n        test_output(vm.void(), [\"\\n\"], (stream) => {\n            stream.write_string(vm.str(\"\\n\"));\n            return stream.force_output();\n        });\n        test_output(vm.void(), [\"\\n\"], (stream) => {\n            stream.write_string(vm.str(\"\\n\"));\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(stream.fresh_line(), vm.f());\n            return stream.force_output();\n        });\n        test_output(vm.void(), [\"\\nfoo\\nbar\", \"quux\"], (stream) => {\n            stream.write_string(vm.str(\"\\n\"));\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(stream.fresh_line(), vm.f());\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(vm.str(\"foo\"), stream.write_string(vm.str(\"foo\")));\n            stream.write_string(vm.str(\"\\n\"));\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(stream.fresh_line(), vm.f());\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(vm.str(\"bar\"), stream.write_string(vm.str(\"bar\")));\n            stream.force_output();\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(vm.str(\"quux\"), stream.write_string(vm.str(\"quux\")));\n            return stream.force_output();\n        });\n\n    });\n\n});\n\n\n//# sourceURL=webpack://lispx-test-node/./test/js-test.mjs?");

/***/ }),

/***/ "./test/lisp-tests.mjs":
/*!*****************************!*\
  !*** ./test/lisp-tests.mjs ***!
  \*****************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var lispx_vm__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! lispx-vm */ \"lispx-vm\");\n/* harmony import */ var _test_util_lispx__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./test-util.lispx */ \"./test/test-util.lispx\");\n/* harmony import */ var _test_util_test_lispx__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./test-util-test.lispx */ \"./test/test-util-test.lispx\");\n/* harmony import */ var _boot_test_lispx__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./boot-test.lispx */ \"./test/boot-test.lispx\");\n/* harmony import */ var _control_test_lispx__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./control-test.lispx */ \"./test/control-test.lispx\");\n/* harmony import */ var _delimcc_test_lispx__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./delimcc-test.lispx */ \"./test/delimcc-test.lispx\");\n/* harmony import */ var _stream_test_lispx__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./stream-test.lispx */ \"./test/stream-test.lispx\");\n/* harmony import */ var _read_test_lispx__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./read-test.lispx */ \"./test/read-test.lispx\");\n/* harmony import */ var _print_test_lispx__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./print-test.lispx */ \"./test/print-test.lispx\");\n/* harmony import */ var _js_test_lispx__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./js-test.lispx */ \"./test/js-test.lispx\");\n/* harmony import */ var _hierarchy_test_lispx__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./hierarchy-test.lispx */ \"./test/hierarchy-test.lispx\");\n\n\n\n\n\n\n\n\n\n\n\n\n\ndescribe(\"Lisp Tests\", () => {\n\n    const vm = new lispx_vm__WEBPACK_IMPORTED_MODULE_0__.VM();\n    vm.eval_js_string(_test_util_lispx__WEBPACK_IMPORTED_MODULE_1__);\n    vm.eval_js_string(_test_util_test_lispx__WEBPACK_IMPORTED_MODULE_2__);\n    vm.eval_js_string(_boot_test_lispx__WEBPACK_IMPORTED_MODULE_3__);\n    vm.eval_js_string(_control_test_lispx__WEBPACK_IMPORTED_MODULE_4__);\n    vm.eval_js_string(_delimcc_test_lispx__WEBPACK_IMPORTED_MODULE_5__);\n    vm.eval_js_string(_stream_test_lispx__WEBPACK_IMPORTED_MODULE_6__);\n    vm.eval_js_string(_read_test_lispx__WEBPACK_IMPORTED_MODULE_7__);\n    vm.eval_js_string(_print_test_lispx__WEBPACK_IMPORTED_MODULE_8__);\n    vm.eval_js_string(_js_test_lispx__WEBPACK_IMPORTED_MODULE_9__);\n    vm.eval_js_string(_hierarchy_test_lispx__WEBPACK_IMPORTED_MODULE_10__);\n\n}).timeout(100000);\n\n\n//# sourceURL=webpack://lispx-test-node/./test/lisp-tests.mjs?");

/***/ }),

/***/ "./test/print-test.mjs":
/*!*****************************!*\
  !*** ./test/print-test.mjs ***!
  \*****************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var chai__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! chai */ \"./node_modules/chai/index.mjs\");\n/* harmony import */ var lispx_vm__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! lispx-vm */ \"lispx-vm\");\n\n\n\n\nconst vm = new lispx_vm__WEBPACK_IMPORTED_MODULE_1__.VM();\n\nfunction string_output_stream()\n{\n    return new vm.String_output_stream();\n}\n\ndescribe(\"Printer\", () => {\n\n    it(\"Test writing objects.\", () => {\n\n        const examples = {\n            // JS objects write stupidly for now\n            \"#<object>\": undefined,\n            \"#<object>\": null,\n            \"#<object>\": true,\n            \"#<object>\": 12,\n            \"#<object>\": \"foo\",\n            \"#<object>\": {},\n            \"#<object>\": [],\n            // Unreadable objects\n            \"#<string-output-stream>\": string_output_stream(),\n            // Standard objects\n            \"#<error>\":\n            new vm.Error(),\n            \"#<type-error :datum 12 :expected-type boolean>\":\n            new vm.Type_error(vm.num(12), vm.sym(\"boolean\")),\n            // Constants\n            \"#t\": vm.t(),\n            \"#f\": vm.f(),\n            \"#nil\": vm.nil(),\n            \"#void\": vm.void(),\n            \"#ignore\": vm.ignore(),\n            // Numbers\n            \"-111111111111.111111111\": vm.num(\"-111111111111.111111111\"),\n            \"0\": vm.num(\"0\"),\n            \"111111111111.111111111\": vm.num(\"111111111111.111111111\"),\n            // Strings\n            \"\\\"foo-bar\\\"\": vm.str(\"foo-bar\"),\n            // Symbols\n            \"foo-bar\": vm.sym(\"foo-bar\"),\n            \"|foo bar|\": vm.sym(\"foo bar\"),\n            \"Iñtërnâtiônàlizætiøn\":\n            vm.sym(\"Iñtërnâtiônàlizætiøn\"),\n            \"|Iñtërnâtiônàlizætiøn Iñtërnâtiônàlizætiøn|\":\n            vm.sym(\"Iñtërnâtiônàlizætiøn Iñtërnâtiônàlizætiøn\"),\n            // Keywords\n            \":foo-bar\": vm.kwd(\"foo-bar\"),\n            \":|foo bar|\": vm.kwd(\"foo bar\"),\n            // Function symbols\n            \"#'foo-bar\": vm.fsym(\"foo-bar\"),\n            \"#'|foo bar|\": vm.fsym(\"foo bar\"),\n            // Class symbols\n            \"#^foo-bar\": vm.csym(\"foo-bar\"),\n            \"#^|foo bar|\": vm.csym(\"foo bar\"),\n            // Conses\n            \"(1)\": vm.list(vm.num(1)),\n            \"((1))\": vm.list(vm.list(vm.num(1))),\n            \"(1 2 3)\": vm.list(vm.num(1), vm.num(2), vm.num(3)),\n            \"(1 2 . 3)\": vm.list_star(vm.num(1), vm.num(2), vm.num(3)),\n        };\n\n        for (const [string, object] of Object.entries(examples)) {\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(string, vm.write_to_js_string(object)));\n        }\n\n    });\n\n    it(\"Test writing objects whose output depends on *PRINT-ESCAPE*.\", () => {\n\n        // Lisp object, *PRINT-ESCAPE* = #T, *PRINT-ESCAPE* = #F\n        const examples = [\n            // Strings\n            [vm.str(\"foo\"), \"\\\"foo\\\"\", \"foo\"],\n            [vm.str(\"fo\\\"o\"), \"\\\"fo\\\\\\\"o\\\"\", \"fo\\\"o\"],\n            [vm.str(\"fo\\no\"), \"\\\"fo\\no\\\"\", \"fo\\no\"],\n            [vm.str(\"fo\\\\o\"), \"\\\"fo\\\\\\\\o\\\"\", \"fo\\\\o\"],\n            [vm.str(\"foo|bar\"), \"\\\"foo|bar\\\"\", \"foo|bar\"],\n            // Normal symbols\n            [vm.sym(\"foo\"), \"foo\", \"foo\"],\n            [vm.sym(\"foo-bar\"), \"foo-bar\", \"foo-bar\"],\n            [vm.sym(\"let*\"), \"let*\", \"let*\"],\n            // Symbols that parse as numbers\n            [vm.sym(\"0\"), \"|0|\", \"0\"],\n            [vm.sym(\"12\"), \"|12|\", \"12\"],\n            [vm.sym(\"-12.12\"), \"|-12.12|\", \"-12.12\"],\n            // Symbols that contain whitespace\n            [vm.sym(\"foo bar\"), \"|foo bar|\", \"foo bar\"],\n            [vm.sym(\"fo\\no\"), \"|fo\\no|\", \"fo\\no\"],\n            // Symbols that contain terminating characters\n            [vm.sym(\"fo|o\"), \"|fo\\\\|o|\", \"fo|o\"],\n            [vm.sym(\"fo(o\"), \"|fo(o|\", \"fo(o\"],\n            [vm.sym(\"fo\\\"o\"), \"|fo\\\"o|\", \"fo\\\"o\"],\n            // Symbols that contain the escape character\n            [vm.sym(\"fo\\\\o\"), \"|fo\\\\\\\\o|\", \"fo\\\\o\"],\n        ];\n\n        for (const [object, escaped, not_escaped] of examples) {\n            const escaped_result = vm.progv([vm.PRINT_ESCAPE], [vm.t()],\n                                            () => vm.write_to_js_string(object));\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.strictEqual(escaped, escaped_result);\n            const not_escaped_result = vm.progv([vm.PRINT_ESCAPE], [vm.f()],\n                                                () => vm.write_to_js_string(object));\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.strictEqual(not_escaped, not_escaped_result);\n        }\n\n    });\n\n});\n\n\n//# sourceURL=webpack://lispx-test-node/./test/print-test.mjs?");

/***/ }),

/***/ "./test/read-test.mjs":
/*!****************************!*\
  !*** ./test/read-test.mjs ***!
  \****************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var chai__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! chai */ \"./node_modules/chai/index.mjs\");\n/* harmony import */ var lispx_vm__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! lispx-vm */ \"lispx-vm\");\n\n\n\n\nconst vm = new lispx_vm__WEBPACK_IMPORTED_MODULE_1__.VM();\n\nfunction string_input_stream(js_string)\n{\n    return new vm.String_input_stream(vm.str(js_string));\n}\n\ndescribe(\"Reader\", () => {\n\n    it(\"Whitespace is correctly parsed.\", () => {\n\n        /*\n         * This tests the situation where we there is only\n         * whitespace, and no more object to read, before the EOF.\n         */\n\n        const examples = [\n            \"\", \"    \", \"  \\n\", \"\\n   \\t \\n   \",\n            \";\", \"   ;\", \"; hello\", \";;;;; headline\", \"\\t\\n;foo\",\n            \"; foo \\n ; bar \\n\", \";;;; foo\\n\\t \",\n            \"#||#\", \"#||##||#\", \"#|#|hello|#|#\\n;foo\\n\\n\"\n        ];\n\n        for (const ex of examples) {\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(ex)), \"EOF\");\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.read(string_input_stream(ex), false), vm.void());\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.read(string_input_stream(ex), false, -1), -1);\n            /*\n             * Check whether dot is allowed as eof_value. See read().\n             */\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.read(string_input_stream(ex), false, vm.sym(\".\")),\n                         vm.sym(\".\"));\n        }\n\n        /*\n         * Append an object after a newline to the example strings and test\n         * that it is read.\n         */\n\n        for (const ex of examples) {\n            const st = string_input_stream(ex + \"\\n1\");\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read(st), vm.num(1)));\n        }\n\n    });\n\n    it(\"Objects are correctly parsed.\", () => {\n\n        const examples = {\n\n            // Numbers.\n            \"1\":\n            vm.num(\"1\"),\n            \"-1\":\n            vm.num(\"-1\"),\n            \"100000000000000000.1378912739821739821\":\n            vm.num(\"100000000000000000.1378912739821739821\"),\n            \"-100000000000000000.1378912739821739821\":\n            vm.num(\"-100000000000000000.1378912739821739821\"),\n\n            // Symbols.\n            \"a\":\n            vm.sym(\"a\"),\n            \"hello-world\":\n            vm.sym(\"hello-world\"),\n            \"Iñtërnâtiônàlizætiøn\":\n            vm.sym(\"Iñtërnâtiônàlizætiøn\"),\n\n            // Escape characters in symbols.\n            \"a\\\\nb\":\n            vm.sym(\"a\\nb\"),\n            \"a\\\\\\\\b\":\n            vm.sym(\"a\\\\b\"),\n            \"\\\\t\\\\n\\\\\\\\\\\\|\\\\\\\"\":\n            vm.sym(\"\\t\\n\\\\|\\\"\"),\n\n            // Stuff that's almost a number but parses as a symbol.\n            \"1.\":\n            vm.sym(\"1.\"),\n            \".1\":\n            vm.sym(\".1\"),\n            \".1.\":\n            vm.sym(\".1.\"),\n            \"1.a\":\n            vm.sym(\"1.a\"),\n\n            // Keyword symbols.\n            \":foo\":\n            vm.sym(\"foo\").to_keyword_symbol(),\n            \":|foo|\":\n            vm.sym(\"foo\").to_keyword_symbol(),\n\n            // Function symbols.\n            \"#'foo\":\n            vm.sym(\"foo\").to_function_symbol(),\n            \"#'|foo|\":\n            vm.sym(\"foo\").to_function_symbol(),\n\n            // Class symbols.\n            \"#^foo\":\n            vm.sym(\"foo\").to_class_symbol(),\n            \"#^|foo|\":\n            vm.sym(\"foo\").to_class_symbol(),\n\n            // Strings.\n            '\"\"':\n            vm.str(\"\"),\n            '\"foo\"':\n            vm.str(\"foo\"),\n            '\"foo bar\"':\n            vm.str(\"foo bar\"),\n            '\"foo\\\\nbar\"':\n            vm.str(\"foo\\nbar\"),\n            '\"foo\\nbar\"':\n            vm.str(\"foo\\nbar\"),\n            '\"foo\\\\nbar\"':\n            vm.str(\"foo\\\\nbar\"),\n            '\"foo\\\\nbar\"':\n            vm.str(\"foo\\nbar\"),\n            '\"\\\\t\\\\n\\\\\\\\\\\\|\\\\\\\"\"':\n            vm.str(\"\\t\\n\\\\|\\\"\"),\n            '\"Iñtërnâtiônàlizætiøn\"':\n            vm.str(\"Iñtërnâtiônàlizætiøn\"),\n\n            // Escaped symbols.\n            '||':\n            vm.sym(\"\"),\n            '|foo|':\n            vm.sym(\"foo\"),\n            '|foo bar|':\n            vm.sym(\"foo bar\"),\n            '|foo\\nbar|':\n            vm.sym(\"foo\\nbar\"),\n            '|foo\\\\nbar|':\n            vm.sym(\"foo\\nbar\"),\n            '|foo\\nbar|':\n            vm.sym(\"foo\\nbar\"),\n            '|\\\\t\\\\n\\\\\\\\\\\\|\\\\\\\"|':\n            vm.sym(\"\\t\\n\\\\|\\\"\"),\n            '|Iñtërnâtiônàlizætiøn|':\n            vm.sym(\"Iñtërnâtiônàlizætiøn\"),\n\n            // Constants.\n            \"#t\":\n            vm.t(),\n            \"#f\":\n            vm.f(),\n            \"#nil\":\n            vm.nil(),\n            \"#void\":\n            vm.void(),\n            \"#ignore\":\n            vm.ignore(),\n\n            // Lists.\n            \"()\":\n            vm.nil(),\n            \"( \\n )\":\n            vm.nil(),\n            \"(1)\":\n            vm.list(vm.num(1)),\n            \"(1 . 2)\":\n            vm.cons(vm.num(1), vm.num(2)),\n            \"(1 2)\":\n            vm.list(vm.num(1), vm.num(2)),\n            \"(1 2 . 3)\":\n            vm.list_star(vm.num(1), vm.num(2), vm.num(3)),\n            \"(())\":\n            vm.list(vm.nil()),\n            \"( ( ) )\":\n            vm.list(vm.nil()),\n            \"( ( ) . ( ) )\":\n            vm.cons(vm.nil(), vm.nil()),\n\n            // Line comments inside lists.\n            \"(;\\n)\":\n            vm.nil(),\n            \"(1 . ;;;\\n 1)\":\n            vm.cons(vm.num(1), vm.num(1)),\n            \"(;\\n 1 ;\\n . ;\\n 1 ;\\n)\":\n            vm.cons(vm.num(1), vm.num(1)),\n            \"(1 ;\\n 2 ;\\n 3 ;\\n)\":\n            vm.list(vm.num(1), vm.num(2), vm.num(3)),\n\n            // Block comments inside lists.\n            \"(#||#)\":\n            vm.nil(),\n            \"(1 . #||# 1)\":\n            vm.cons(vm.num(1), vm.num(1)),\n            \"(#||# 1 . #||# 1)\":\n            vm.cons(vm.num(1), vm.num(1)),\n            \"(#||# 1 . #||# 1 #||#)\":\n            vm.cons(vm.num(1), vm.num(1)),\n            \"(1 2 #||# 3)\":\n            vm.list(vm.num(1), vm.num(2), vm.num(3)),\n\n            // Quote.\n            \"'foo\":\n            vm.list(vm.sym(\"quote\"), vm.sym(\"foo\")),\n            \"'()\":\n            vm.list(vm.sym(\"quote\"), vm.nil()),\n            \"'(1 . 2)\":\n            vm.list(vm.sym(\"quote\"), vm.cons(vm.num(1), vm.num(2))),\n            \"' foo-can-have-whitespace\":\n            vm.list(vm.sym(\"quote\"), vm.sym(\"foo-can-have-whitespace\")),\n        };\n\n        /*\n         * Try parsing each object.\n         *\n         * Here each object gets ended by the EOF.\n         */\n\n        for (const [string, object] of Object.entries(examples)) {\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read(string_input_stream(string)), object));\n        }\n\n        /*\n         * Try the same but append some whitespace before the object.\n         */\n\n        for (const [string, object] of Object.entries(examples)) {\n            const s = \"\\n;foo\\n  \" + string;\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read(string_input_stream(s)), object));\n        }\n\n        /*\n         * Try the same but append some whitespace before and after\n         * the object.\n         */\n\n        for (const [string, object] of Object.entries(examples)) {\n            const s = \"\\n;foo\\n  \" + string + \" \\n\";\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read(string_input_stream(s)), object));\n        }\n\n        /*\n         * Try the same but append whitespace before and a terminating\n         * character after the object.\n         */\n\n        for (const term_char of [\"(\", \")\", \";\", \"\\\"\", \"\\'\", \"|\"]) {\n            for (const [string, object] of Object.entries(examples)) {\n                const s = \"\\n;foo\\n  \" + string + term_char;\n                const stream = string_input_stream(s);\n                (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read(stream), object));\n\n                // Test that the terminating character has been unread.\n                // (To be pedantic, in the case of lists and delimited\n                // objects like strings, it has never been read in the\n                // first place.)\n                chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(stream.read_byte(), term_char);\n            }\n        }\n\n    });\n\n    it(\"List parsing errors are reported informatively.\", () => {\n\n        const examples = {\n            \".\": \"Consing dot not allowed here\",\n            \"#||#.\": \"Consing dot not allowed here\",\n            \"#||# .\": \"Consing dot not allowed here\",\n            \"(.\": \"Consing dot at start of list\",\n            \"(#||#.\": \"Consing dot at start of list\",\n            \"( .\": \"Consing dot at start of list\",\n            \"(#||# .\": \"Consing dot at start of list\",\n            \"(1 . 1 1)\": \"Multiple objects after consing dot\",\n            // Would be better to report \"consing dot not allowed here\"\n            // but I won't complain.\n            \"(1 . 1 . 1)\": \"Multiple objects after consing dot\",\n            \")\": \"Unbalanced parenthesis\"\n        };\n\n        for (const [string, message] of Object.entries(examples)) {\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(string)),\n                          message);\n        }\n\n    });\n\n    it(\"Incomplete objects at EOF always cause an error.\", () => {\n\n        /*\n         * A stream that ends with an incomplete object.\n         *\n         * Test that an EOF error is thrown regardless of eof_error_p.\n         */\n\n        const examples = [\n            '\"foo', \"bla\\\\\", \"|foo\", \"|foo\\\\\", \"foo\\\\\",\n            \"'\", \"'(\",\n            \"(\", \"((\", \"(()\", \"(1 .\", \"(1 . (\",\n            \"#|\", \"#||\", \"(#|\", \"(#||\", \"(#||#\",\n            \"#|#|\", \"#|#||\", \"#|#||#\",\n        ];\n\n        for (const e of examples) {\n\n            const stream1 = string_input_stream(e);\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(stream1), \"EOF\");\n\n            const stream2 = string_input_stream(e);\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(stream2, false), \"EOF\");\n\n            const stream3 = string_input_stream(e);\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(stream3, false, -1), \"EOF\");\n\n        }\n\n    });\n\n    it(\"Invalid escape characters lose.\", () => {\n\n        const msg = \"Invalid escape character x\";\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"\\\\x\")), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream('\"\\\\x\"')), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"|\\\\x|\")), msg);\n\n    });\n\n    it(\"Sharpsign at EOF causes an error.\", () => {\n\n        const msg = \"EOF\";\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"#\")), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"#'\")), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"#'\\n\")), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"#';\\n;\")), msg);\n\n    });\n\n    it(\"Unknown alphabetic characters after sharpsign cause an error.\", () => {\n\n        const msg = \"Illegal constant\";\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"#foo\")), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"#b\")), msg);\n\n    });\n\n    it(\"Unknown other characters after sharpsign cause an error.\", () => {\n\n        const msg = \"Illegal dispatching character\";\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"#_\")), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"# \")), msg);\n\n    });\n\n});\n\ndescribe(\"Reader Macros\", () => {\n\n    it(\"Test set_macro_character() with terminating char.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_macro_character(\"~\"));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_terminating_macro_character(\"~\"));\n\n        vm.set_macro_character(\"~\", function(stream, b) {\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(b, \"~\");\n            // Note that reader macro has to return option.\n            return vm.some(vm.list(vm.sym(\"blargh\"), vm.read(stream)));\n        }, false);\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.is_macro_character(\"~\"));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.is_terminating_macro_character(\"~\"));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read(string_input_stream(\"(1 ~foo ~bar 2)\")),\n                        vm.read(string_input_stream(\"(1 (blargh foo) (blargh bar) 2)\"))));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read(string_input_stream(\"(foo~bar)\")),\n                        vm.read(string_input_stream(\"(foo (blargh bar))\"))));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read(string_input_stream(\"(|foo|~bar)\")),\n                        vm.read(string_input_stream(\"(foo (blargh bar))\"))));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"~\")), \"EOF\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"(foo ~\")), \"EOF\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"(foo ~)\")), \"Unbalanced parenthesis\");\n\n        vm.unset_macro_character(\"~\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_macro_character(\"~\"));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_terminating_macro_character(\"~\"));\n\n    });\n\n    it(\"Test set_macro_character() with non-terminating char.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_macro_character(\"~\"));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_terminating_macro_character(\"~\"));\n\n        vm.set_macro_character(\"~\", function(stream, b) {\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(b, \"~\");\n            // Note that reader macro has to return option.\n            return vm.some(vm.list(vm.sym(\"blargh\"), vm.read(stream)));\n        }, true);\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.is_macro_character(\"~\"));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_terminating_macro_character(\"~\"));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read(string_input_stream(\"(1 ~foo ~bar 2)\")),\n                        vm.read(string_input_stream(\"(1 (blargh foo) (blargh bar) 2)\"))));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read(string_input_stream(\"(foo~bar)\")),\n                        vm.read(string_input_stream(\"(foo~bar)\"))));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read(string_input_stream(\"(|foo|~bar)\")),\n                        vm.read(string_input_stream(\"(foo (blargh bar))\"))));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"~\")), \"EOF\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"(foo ~\")), \"EOF\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.read(string_input_stream(\"(foo ~)\")), \"Unbalanced parenthesis\");\n\n        vm.unset_macro_character(\"~\");\n\n    });\n\n    it(\"Test read_delimited_list().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read_delimited_list(string_input_stream(\"1 2 3 ]\"), \"]\"),\n                        vm.read(string_input_stream(\"(1 2 3)\"))));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.read_delimited_list(string_input_stream(\"1 2 . 3 ]\"), \"]\"),\n                        vm.read(string_input_stream(\"(1 2 . 3)\"))));\n\n    });\n\n});\n\ndescribe(\"eval_{stream,string,js_string}\", () => {\n\n    it(\"Test eval_{stream,string,js_string}().\", () => {\n\n        const eval_x_funs = [\n            (js_string, env) => {\n                const stream = new vm.String_input_stream(vm.str(js_string));\n                return vm.eval_stream(stream, env);\n            },\n            (js_string, env) => vm.eval_string(vm.str(js_string), env),\n            (js_string, env) => vm.eval_js_string(js_string, env)\n        ];\n\n        for (const eval_x_fun of eval_x_funs) {\n\n            /*\n             * Test that it evaluates forms and returns the final result.\n             */\n\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(eval_x_fun(\"\"), vm.void()));\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(eval_x_fun(\"; Hello\"), vm.void()));\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(eval_x_fun(\"1 2 3\"), vm.num(3)));\n\n            /*\n             * Test that it uses the VM's root environment by default.\n             */\n\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(eval_x_fun(\"#'%vau\") instanceof vm.Built_in_operator);\n\n            /*\n             * Test that another environment can be supplied.\n             */\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => eval_x_fun(\"#'%vau\", vm.make_environment()),\n                          \"Unbound function: %vau\");\n\n            /*\n             * Test that suspensions are not swallowed.\n             */\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => eval_x_fun(\"1 (take-subcont 'p k k) 2\"),\n                          \"Prompt not found\");\n\n        }\n\n    });\n\n});\n\n\n//# sourceURL=webpack://lispx-test-node/./test/read-test.mjs?");

/***/ }),

/***/ "./test/seq-test.mjs":
/*!***************************!*\
  !*** ./test/seq-test.mjs ***!
  \***************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var chai__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! chai */ \"./node_modules/chai/index.mjs\");\n/* harmony import */ var lispx_vm__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! lispx-vm */ \"lispx-vm\");\n\n\n\n\nconst vm = new lispx_vm__WEBPACK_IMPORTED_MODULE_1__.VM();\n\n/*\n * These tests can be somewhat cursory because we have additional\n * testing of the exported Lisp functions.\n */\n\ndescribe(\"Sequence & List Utilities\", () => {\n\n    it(\"Test list_star().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list_star(),\n                        vm.nil()));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list_star(vm.num(1)),\n                        vm.num(1)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list_star(vm.num(1), vm.num(2)),\n                        vm.cons(vm.num(1), vm.num(2))));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list_star(vm.num(1), vm.num(2), vm.num(3)),\n                        vm.cons(vm.num(1), vm.cons(vm.num(2), vm.num(3)))));\n\n    });\n\n    it(\"Test nthcdr().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.nthcdr(0, vm.list(1, 2, 3)),\n                        vm.list(1, 2, 3)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.nthcdr(1, vm.list(1, 2, 3)),\n                        vm.list(2, 3)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.nthcdr(2, vm.list(1, 2, 3)),\n                        vm.list(3)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.nthcdr(3, vm.list(1, 2, 3)),\n                        vm.nil()));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.nthcdr(4, vm.list(1, 2, 3)), \"Out of bounds\");\n\n    });\n\n    it(\"Test list_length().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list_length(vm.nil()), 0));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list_length(vm.list(1, 2, 3)), 3));\n\n    });\n\n    it(\"Test mapcar().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.nil(), vm.mapcar((elt) => false, vm.nil())));\n\n        const list = vm.list(vm.num(1), vm.num(2), vm.num(3));\n        const expected = vm.list(vm.num(2), vm.num(3), vm.num(4));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(expected, vm.mapcar((elt) => vm.add(elt, vm.one()), list));\n\n    });\n\n    it(\"Test mapc().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.nil(), vm.mapc((elt) => false, vm.nil())));\n\n        let array = [];\n        const list = vm.list(vm.num(1), vm.num(2), vm.num(3));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(list === vm.mapc((elt) => array.push(elt), list));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(array, [vm.num(1), vm.num(2), vm.num(3)]);\n\n    });\n\n    it(\"Test list_subseq().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list_subseq(vm.list(1, 2, 3), 0),\n                        vm.list(1, 2, 3)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list_subseq(vm.list(1, 2, 3), 0, 1),\n                        vm.list(1)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list_subseq(vm.list(1, 2, 3), 2),\n                        vm.list(3)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list_subseq(vm.list(1, 2, 3), 1, 2),\n                        vm.list(2)));\n\n    });\n\n    it(\"Test string_subseq().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.string_subseq(vm.str(\"123\"), 0),\n                        vm.str(\"123\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.string_subseq(vm.str(\"123\"), 0, 1),\n                        vm.str(\"1\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.string_subseq(vm.str(\"123\"), 2),\n                        vm.str(\"3\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.string_subseq(vm.str(\"123\"), 1, 2),\n                        vm.str(\"2\")));\n\n    });\n\n    it(\"Test append().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.append(vm.nil(), vm.nil()), vm.nil()));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.append(vm.list(1), vm.nil()), vm.list(1)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.append(vm.nil(), vm.list(1)), vm.list(1)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.append(vm.nil(), vm.list(1, 2, 3)), vm.list(1, 2, 3)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.append(vm.list(1, 2, 3), vm.nil()), vm.list(1, 2, 3)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.append(vm.list(1, 2), vm.list(3, 4, 5)), vm.list(1, 2, 3, 4, 5)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.append(vm.list(1, 2, 3), 4), vm.list_star(1, 2, 3, 4)));\n\n    });\n\n    it(\"Test some().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.some(vm.num(12)), vm.list(vm.num(12))));\n\n    });\n\n    it(\"Test optional().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.optional(vm.nil()), vm.void()));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.optional(vm.nil(), () => 12), 12));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.optional(vm.some(10)), 10));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.optional(vm.some(10), () => 12), 10));\n\n    });\n\n});\n\n\n//# sourceURL=webpack://lispx-test-node/./test/seq-test.mjs?");

/***/ }),

/***/ "./test/stream-test.mjs":
/*!******************************!*\
  !*** ./test/stream-test.mjs ***!
  \******************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var chai__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! chai */ \"./node_modules/chai/index.mjs\");\n/* harmony import */ var lispx_vm__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! lispx-vm */ \"lispx-vm\");\n\n\n\n\nconst vm = new lispx_vm__WEBPACK_IMPORTED_MODULE_1__.VM();\n\nfunction string_input_stream(js_string)\n{\n    return new vm.String_input_stream(vm.str(js_string));\n}\n\nfunction string_output_stream()\n{\n    return new vm.String_output_stream();\n}\n\ndescribe(\"String Input Streams\", () => {\n\n    it(\"UTF-8 bytes can be read from a string.\", () => {\n\n        const st = string_input_stream(\"bar\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => st.unread_byte(\"x\"), \"Cannot unread\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false, false, -1), \"b\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false, false, -1), \"b\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(false, -1), \"b\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => st.unread_byte(\"x\"), \"Cannot unread\");\n\n        st.unread_byte(\"b\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false, false, -1), \"b\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(false, -1), \"b\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false, false, -1), \"a\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(false, -1), \"a\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false, false, -1), \"r\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(false, -1), \"r\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false, false, -1), -1);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false, false, -1), -1);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(false, -1), -1);\n\n        st.unread_byte(\"r\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false, false, -1), \"r\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(false, -1), \"r\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(false, -1), -1);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => st.peek_byte(false, true), \"EOF\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => st.read_byte(true), \"EOF\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => st.read_byte(true), \"EOF\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false, false, -1), -1);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(false, -1), -1);\n\n        st.unread_byte(\"r\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false, false, -1), \"r\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(false, -1), \"r\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false, false, -1), -1);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(false, -1), -1);\n\n    });\n\n    it(\"read_byte() has convenient default arguments.\", () => {\n\n        const st = string_input_stream(\"bar\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(), \"b\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(), \"a\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(), \"r\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(false, -1), -1);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(false), vm.void());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => st.read_byte(), \"EOF\");\n\n    });\n\n    it(\"Test skipping whitespace in peek_byte().\", () => {\n\n        const st = string_input_stream(\"  b a r\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false), \" \");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(), \" \");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false), \" \");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(true), \"b\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(), \"b\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false), \" \");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(true), \"a\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(), \"a\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(true), \"r\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.read_byte(), \"r\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(true, false, -1), -1);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.peek_byte(false, false, -1), -1);\n\n    });\n\n});\n\ndescribe(\"String Output Streams\", () => {\n\n    it(\"UTF-8 bytes can be written to a string.\", () => {\n\n        const st = string_output_stream();\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(st.get_string(), vm.str(\"\")));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.write_byte(\"a\"), \"a\");\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(st.get_string(), vm.str(\"a\")));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.write_byte(\"b\"), \"b\");\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(st.get_string(), vm.str(\"ab\")));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(st.write_string(vm.str(\"Iñtërnâtiônàlizætiøn\")),\n                        vm.str(\"Iñtërnâtiônàlizætiøn\")));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(st.get_string(), vm.str(\"abIñtërnâtiônàlizætiøn\")));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.write_byte(\"c\"), \"c\");\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(st.get_string(), vm.str(\"abIñtërnâtiônàlizætiønc\")));\n\n    });\n\n    it(\"Test force_output()\", () => {\n\n        /*\n         * force_output() is a no-op for string output streams,\n         * just test that it's implemented.\n         */\n\n        const st = string_output_stream();\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(st.force_output(), vm.void()));\n\n    });\n\n    it(\"Test fresh_line()\", () => {\n\n        const st = string_output_stream();\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(st.fresh_line(), vm.t()));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(st.write_byte(\"a\"), \"a\");\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(st.fresh_line(), vm.t()));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(st.get_string(), vm.str(\"\\na\\n\")));\n\n    });\n\n    it(\"Test FRESH-LINE and FORCE-OUTPUT\", () => {\n\n        class Test_stream extends vm.Output_stream\n        {\n            constructor() {\n                super();\n                this.fresh_line_call_ct = 0;\n                this.force_output_call_ct = 0;\n            }\n            fresh_line() { this.fresh_line_call_ct++; return vm.f(); }\n            force_output() { this.force_output_call_ct++; return vm.void(); }\n        }\n\n        // Test that they can be called with a stream as argument\n        const test_stream_1 = new Test_stream();\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(0, test_stream_1.fresh_line_call_ct);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(0, test_stream_1.force_output_call_ct);\n        vm.eval_form(vm.list(vm.sym(\"fresh-line\"), test_stream_1));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(1, test_stream_1.fresh_line_call_ct);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(0, test_stream_1.force_output_call_ct);\n        vm.eval_form(vm.list(vm.sym(\"force-output\"), test_stream_1));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(1, test_stream_1.fresh_line_call_ct);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(1, test_stream_1.force_output_call_ct);\n\n        // Test that they work on *STANDARD-OUTPUT* otherwise\n        const test_stream_2 = new Test_stream();\n        vm.progv([vm.STANDARD_OUTPUT], [test_stream_2], () => {\n            vm.eval_form(vm.list(vm.sym(\"fresh-line\")));\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(1, test_stream_2.fresh_line_call_ct);\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(0, test_stream_2.force_output_call_ct);\n            vm.eval_form(vm.list(vm.sym(\"force-output\")));\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(1, test_stream_2.fresh_line_call_ct);\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(1, test_stream_2.force_output_call_ct);\n        });\n    });\n\n});\n\n\n//# sourceURL=webpack://lispx-test-node/./test/stream-test.mjs?");

/***/ }),

/***/ "./test/test-util.mjs":
/*!****************************!*\
  !*** ./test/test-util.mjs ***!
  \****************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"check_class\": () => (/* binding */ check_class),\n/* harmony export */   \"check_class_linkage\": () => (/* binding */ check_class_linkage),\n/* harmony export */   \"check_class_name\": () => (/* binding */ check_class_name),\n/* harmony export */   \"check_metaclass\": () => (/* binding */ check_metaclass),\n/* harmony export */   \"check_superclass\": () => (/* binding */ check_superclass)\n/* harmony export */ });\n/* harmony import */ var chai__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! chai */ \"./node_modules/chai/index.mjs\");\n/*\n * Test Utilities.\n */\n\n\n\n/*\n * Verify that a class is properly set up.\n */\nfunction check_class(vm, name, js_class, js_superclass, js_metaclass)\n{\n    check_class_name(vm, js_class, name);\n    check_superclass(vm, js_class, js_superclass);\n    check_metaclass(vm, js_class, js_metaclass);\n    check_class_linkage(vm, js_class);\n};\n\n/*\n * Verify that a class is properly named and registered in the root environment.\n */\nfunction check_class_name(vm, js_class, name)\n{\n    const lisp_class = vm.lisp_class(js_class);\n    const name_sym = vm.sym(name);\n\n    // The name is an ordinary symbol in the variable namespace.\n    chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(lisp_class.get_name(), name_sym);\n\n    // The symbol it's registered under in the environment is a\n    // symbol in the class namespace.\n    chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.get_environment().lookup(name_sym.to_class_symbol()),\n                 lisp_class);\n};\n\n/*\n * Verify that a class is properly connected with its superclass.\n */\nfunction check_superclass(vm, js_class, js_superclass)\n{\n    chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.lisp_class(js_class).get_superclass(),\n                 vm.lisp_class(js_superclass));\n\n    (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(js_class.prototype instanceof js_superclass);\n};\n\n/*\n * Verify that a class is properly connected with its metaclass.\n */\nfunction check_metaclass(vm, js_class, js_metaclass)\n{\n    // Thanks to our nice object system, the following things\n    // are both true:\n\n    // 1) The Lisp class metaobject points to the Lisp\n    // metaclass metaobject.\n    chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(vm.lisp_class(js_class)),\n                 vm.lisp_class(js_metaclass));\n\n    // 2) The Lisp class metaobject is also an instance of the\n    // JS metaclass.\n    chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.lisp_class(js_class),\n                      js_metaclass);\n\n    // (and of course of CLASS and OBJECT, too)\n    chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.lisp_class(js_class),\n                      vm.Class);\n    chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.lisp_class(js_class),\n                      vm.Object);\n};\n\n/*\n * Verify that a JS class is properly linked to its class metaobject.\n */\nfunction check_class_linkage(vm, js_class)\n{\n    chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.lisp_class(js_class).get_js_class(),\n                 js_class);\n};\n\n\n//# sourceURL=webpack://lispx-test-node/./test/test-util.mjs?");

/***/ }),

/***/ "./test/vm-test.mjs":
/*!**************************!*\
  !*** ./test/vm-test.mjs ***!
  \**************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var chai__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! chai */ \"./node_modules/chai/index.mjs\");\n/* harmony import */ var _test_util_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./test-util.mjs */ \"./test/test-util.mjs\");\n/* harmony import */ var lispx_vm__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! lispx-vm */ \"lispx-vm\");\n\n\n\n\n\n\nconst vm = new lispx_vm__WEBPACK_IMPORTED_MODULE_2__.VM();\n\ndescribe(\"Objects\", () => {\n\n    it(\"Lisp and JS objects can be distinguished.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.is_lisp_object(vm.str(\"foo\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.is_lisp_object(vm.sym(\"foo\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.is_lisp_object(vm.lisp_class(vm.Object)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.is_lisp_object(vm.lisp_class(vm.Class)));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_lisp_object(null));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_lisp_object(undefined));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_lisp_object(true));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_lisp_object(false));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_lisp_object(12));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_lisp_object(\"foo\"));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_lisp_object({}));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_lisp_object([]));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.is_lisp_object(String));\n\n    });\n\n    it(\"JS objects have OBJECT as class metaobject.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(null), vm.lisp_class(vm.Object));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(undefined), vm.lisp_class(vm.Object));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(true), vm.lisp_class(vm.Object));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(false), vm.lisp_class(vm.Object));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(12), vm.lisp_class(vm.Object));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(\"foo\"), vm.lisp_class(vm.Object));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of({}), vm.lisp_class(vm.Object));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of([]), vm.lisp_class(vm.Object));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(String), vm.lisp_class(vm.Object));\n\n    });\n\n    it(\"Value equality has strict equality semantics for JS objects.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(null, null));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(undefined, undefined));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(\"foo\", \"foo\"));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(12, 12));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.equal(null, undefined));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.equal(undefined, null));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.equal({}, {}));\n\n    });\n\n    it(\"Objects of different classes or JS objects can not be compared.\", () => {\n\n        const msg = \"Type assertion failed\";\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.compare(vm.num(1), vm.str(\"foo\")), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.compare(vm.str(\"foo\"), vm.num(1)), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.compare([], vm.num(1)), msg);\n\n    });\n\n});\n\ndescribe(\"Classes\", () => {\n\n    /*\n     * These tests could be improved by using the check_class() utility\n     * from test-util.mjs but so far I have been too lazy to do this.\n     */\n\n    it(\"Classes are named and registered in the root environment.\", () => {\n\n        function check_vm_class_name(vm, js_class, name)\n        {\n            /*\n             * Use check from utility.\n             */\n            (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_name)(vm, js_class, name);\n\n            /*\n             * Add additional check that the JS constructor functions\n             * of VM classes have a proper name starting with \"Lisp_\".\n             */\n            const js_name = \"Lisp_\" + name.replace(/-/g, \"_\");\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(js_class.name, js_name);\n        }\n\n        check_vm_class_name(vm, vm.Object, \"object\");\n        check_vm_class_name(vm, vm.String, \"string\");\n        check_vm_class_name(vm, vm.Symbol, \"symbol\");\n        check_vm_class_name(vm, vm.Number, \"number\");\n        check_vm_class_name(vm, vm.Boolean, \"boolean\");\n        check_vm_class_name(vm, vm.List, \"list\");\n        check_vm_class_name(vm, vm.Cons, \"cons\");\n        check_vm_class_name(vm, vm.Nil, \"nil\");\n        check_vm_class_name(vm, vm.Void, \"void\");\n        check_vm_class_name(vm, vm.Ignore, \"ignore\");\n        check_vm_class_name(vm, vm.Environment, \"environment\");\n        check_vm_class_name(vm, vm.Class, \"class\");\n        check_vm_class_name(vm, vm.Built_in_class, \"built-in-class\");\n        check_vm_class_name(vm, vm.Standard_class, \"standard-class\");\n        check_vm_class_name(vm, vm.Operator, \"operator\");\n        check_vm_class_name(vm, vm.Built_in_operator, \"built-in-operator\");\n        check_vm_class_name(vm, vm.Fexpr, \"fexpr\");\n        check_vm_class_name(vm, vm.Function, \"function\");\n        check_vm_class_name(vm, vm.Continuation, \"continuation\");\n        check_vm_class_name(vm, vm.Dynamic, \"dynamic\");\n        check_vm_class_name(vm, vm.Input_stream, \"input-stream\");\n        check_vm_class_name(vm, vm.String_input_stream, \"string-input-stream\");\n        check_vm_class_name(vm, vm.Output_stream, \"output-stream\");\n        check_vm_class_name(vm, vm.String_output_stream, \"string-output-stream\");\n        check_vm_class_name(vm, vm.JS_console_output_stream, \"js-console-output-stream\");\n\n        check_vm_class_name(vm, vm.Standard_object, \"standard-object\");\n        check_vm_class_name(vm, vm.Condition, \"condition\");\n        check_vm_class_name(vm, vm.Error, \"error\");\n        check_vm_class_name(vm, vm.Type_error, \"type-error\");\n        check_vm_class_name(vm, vm.Unbound_symbol_error, \"unbound-symbol-error\");\n        check_vm_class_name(vm, vm.Unbound_slot_error, \"unbound-slot-error\");\n        check_vm_class_name(vm, vm.Unbound_method_error, \"unbound-method-error\");\n        check_vm_class_name(vm, vm.Assertion_error, \"assertion-error\");\n        check_vm_class_name(vm, vm.Out_of_bounds_error, \"out-of-bounds-error\");\n        check_vm_class_name(vm, vm.Match_error, \"match-error\");\n        check_vm_class_name(vm, vm.Stream_error, \"stream-error\");\n        check_vm_class_name(vm, vm.End_of_file, \"end-of-file\");\n        check_vm_class_name(vm, vm.Reader_error, \"reader-error\");\n        check_vm_class_name(vm, vm.Prompt_not_found_error, \"prompt-not-found-error\");\n\n    });\n\n    it(\"Classes have correct superclasses set.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isNull(vm.lisp_class(vm.Object).get_superclass());\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.String, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Symbol, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Number, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Boolean, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.List, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Cons, vm.List);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Nil, vm.List);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Void, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Ignore, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Environment, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Class, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Built_in_class, vm.Class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Standard_class, vm.Class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Operator, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Built_in_operator, vm.Operator);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Fexpr, vm.Operator);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Function, vm.Operator);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Continuation, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Input_stream, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.String_input_stream, vm.Input_stream);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Output_stream, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.String_output_stream, vm.Output_stream);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.JS_console_output_stream, vm.Output_stream);\n\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Standard_object, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Dynamic, vm.Standard_object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Condition, vm.Standard_object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Error, vm.Condition);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Type_error, vm.Error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Unbound_symbol_error, vm.Error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Unbound_slot_error, vm.Error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Unbound_method_error, vm.Error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Assertion_error, vm.Error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Out_of_bounds_error, vm.Error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Match_error, vm.Error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Stream_error, vm.Error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.End_of_file, vm.Stream_error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Reader_error, vm.Error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_superclass)(vm, vm.Prompt_not_found_error, vm.Error);\n\n    });\n\n    it(\"Classes have correct metaclasses set.\", () => {\n\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Object, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.String, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Symbol, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Number, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Boolean, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.List, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Cons, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Nil, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Void, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Ignore, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Environment, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Class, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Built_in_class, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Standard_class, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Operator, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Built_in_operator, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Fexpr, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Function, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Continuation, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Input_stream, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.String_input_stream, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Output_stream, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.String_output_stream, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.JS_console_output_stream, vm.Built_in_class);\n\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Standard_object, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Dynamic, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Condition, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Error, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Type_error, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Unbound_symbol_error, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Unbound_slot_error, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Unbound_method_error, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Assertion_error, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Out_of_bounds_error, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Match_error, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Stream_error, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.End_of_file, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Reader_error, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_metaclass)(vm, vm.Prompt_not_found_error, vm.Standard_class);\n\n    });\n\n    it(\"JS classes are linked to their class metaobjects and vice versa.\", () => {\n\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.String);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Symbol);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Number);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Boolean);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.List);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Cons);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Nil);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Void);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Ignore);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Environment);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Built_in_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Standard_class);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Operator);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Built_in_operator);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Fexpr);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Function);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Continuation);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Dynamic);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Input_stream);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.String_input_stream);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Output_stream);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.String_output_stream);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.JS_console_output_stream);\n\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Standard_object);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Condition);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Type_error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Unbound_symbol_error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Unbound_slot_error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Assertion_error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Match_error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Stream_error);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.End_of_file);\n        (0,_test_util_mjs__WEBPACK_IMPORTED_MODULE_1__.check_class_linkage)(vm, vm.Reader_error);\n\n    });\n\n    it(\"Test is_subclass().\", () => {\n\n        function is_subclass(sub, sup)\n        {\n            return vm.is_subclass(vm.lisp_class(sub), vm.lisp_class(sup));\n        }\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(is_subclass(vm.Object, vm.Object));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(is_subclass(vm.Class, vm.Object));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(is_subclass(vm.Object, vm.Class));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(is_subclass(vm.Standard_class, vm.Object));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(is_subclass(vm.Standard_class, vm.Class));\n\n    });\n\n    it(\"Classes can be created dynamically.\", () => {\n\n        const point2d_class = vm.make_standard_class(vm.sym(\"point-2d\"),\n                                                     vm.lisp_class(vm.Standard_object));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point2d_class, vm.Standard_class);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point2d_class, vm.Class);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point2d_class, vm.Object);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.class_of(point2d_class), vm.lisp_class(vm.Standard_class)));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.typeOf(point2d_class.get_js_class(), \"function\");\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.is_subclass(point2d_class, vm.lisp_class(vm.Standard_object)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.is_subclass(point2d_class, vm.lisp_class(vm.Object)));\n\n        const point2d = vm.make_instance(point2d_class, vm.sym(\"x\"), 1, vm.sym(\"y\"), 2);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point2d, point2d_class.get_js_class());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point2d, vm.Standard_object);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point2d, vm.Object);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.class_of(point2d), point2d_class));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(point2d.slot_value(vm.sym(\"x\")), 1));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(point2d.slot_value(vm.sym(\"y\")), 2));\n\n        const point3d_class = vm.make_standard_class(vm.sym(\"point-3d\"),\n                                                     point2d_class);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point3d_class, vm.Standard_class);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point3d_class, vm.Class);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point3d_class, vm.Object);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.class_of(point3d_class), vm.lisp_class(vm.Standard_class)));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.typeOf(point3d_class.get_js_class(), \"function\");\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.is_subclass(point3d_class, point2d_class));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.is_subclass(point3d_class, vm.lisp_class(vm.Standard_object)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.is_subclass(point3d_class, vm.lisp_class(vm.Object)));\n\n        const point3d = vm.make_instance(point3d_class);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point3d, point3d_class.get_js_class());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point3d, point2d_class.get_js_class());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point3d, vm.Standard_object);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(point3d, vm.Object);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.class_of(point3d), point3d_class));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(point2d_class.get_js_class().name, \"point-2d\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(point3d_class.get_js_class().name, \"point-3d\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(point2d.constructor.name, \"point-2d\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(point3d.constructor.name, \"point-3d\");\n\n    });\n\n    it(\"has_lisp_class() can be used to distinguish Lisp classes from other classes.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_lisp_class(vm.Object));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_lisp_class(vm.Class));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(!vm.has_lisp_class(Object));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(!vm.has_lisp_class(String));\n\n    });\n\n});\n\ndescribe(\"Strings\", () => {\n\n    it(\"Lisp strings can be created from JS strings.\", () => {\n\n        const js_euro = \"\\u{20AC}\";\n        const lisp_euro = vm.str(js_euro);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(lisp_euro, vm.String);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(lisp_euro, vm.Object);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(lisp_euro), vm.lisp_class(vm.String));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(lisp_euro.get_utf8_bytes(), \"\\u{E2}\\u{82}\\u{AC}\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(lisp_euro.to_js_string(), js_euro);\n\n    });\n\n    it(\"Lisp strings have value equality.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.str(\"foo\"), vm.str(\"foo\")));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.equal(vm.str(\"foo\"), vm.str(\"bar\")));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.equal(vm.str(\"foo\"), vm.sym(\"foo\")));\n\n    });\n\n});\n\ndescribe(\"Symbols\", () => {\n\n    it(\"Symbols can be created from strings.\", () => {\n\n        const sym = vm.sym(\"henlo world\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(sym, vm.Symbol);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(sym, vm.Object);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(sym), vm.lisp_class(vm.Symbol));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(sym.get_string(), vm.str(\"henlo world\")));\n\n    });\n\n    it(\"Symbols are interned.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.sym(\"foo\") === vm.sym(\"foo\"));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.sym(\"foo\") !== vm.sym(\"bar\"));\n\n        // Can also use intern() on strings.\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.sym(\"foo\") === vm.intern(vm.str(\"foo\")));\n\n    });\n\n    it(\"Equality works for symbols.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.sym(\"foo\"), vm.sym(\"foo\")));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.equal(vm.sym(\"foo\"), vm.sym(\"bar\")));\n\n    });\n\n    it(\"Symbols have string names.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.sym(\"foo\").get_string(), vm.str(\"foo\")));\n\n    });\n\n    it(\"Symbols have namespaces.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.sym(\"foo\").get_namespace(),\n                        vm.VARIABLE_NAMESPACE));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.sym(\"foo\").to_variable_symbol().get_namespace(),\n                        vm.VARIABLE_NAMESPACE));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.sym(\"foo\").to_function_symbol().get_namespace(),\n                        vm.FUNCTION_NAMESPACE));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.sym(\"foo\").to_class_symbol().get_namespace(),\n                        vm.CLASS_NAMESPACE));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.sym(\"foo\").to_keyword_symbol().get_namespace(),\n                        vm.KEYWORD_NAMESPACE));\n\n    });\n\n    it(\"Symbols in different namespaces are distinct.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.sym(\"foo\") !== vm.sym(\"foo\").to_function_symbol());\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.sym(\"foo\") !== vm.sym(\"foo\").to_class_symbol());\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.sym(\"foo\") !== vm.sym(\"foo\").to_keyword_symbol());\n\n    });\n\n    it(\"Symbols in the same namespace are the same.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.sym(\"foo\").to_variable_symbol() === vm.sym(\"foo\").to_variable_symbol());\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.sym(\"foo\").to_function_symbol() === vm.sym(\"foo\").to_function_symbol());\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.sym(\"foo\").to_class_symbol() === vm.sym(\"foo\").to_class_symbol());\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.sym(\"foo\").to_keyword_symbol() === vm.sym(\"foo\").to_keyword_symbol());\n\n    });\n\n    it(\"The namespace of a symbol is a string.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.sym(\"foo\").get_namespace(), \"variable\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.sym(\"foo\").to_variable_symbol().get_namespace(), \"variable\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.sym(\"foo\").to_function_symbol().get_namespace(), \"function\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.sym(\"foo\").to_class_symbol().get_namespace(), \"class\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.sym(\"foo\").to_keyword_symbol().get_namespace(), \"keyword\");\n\n    });\n\n    it(\"Namespaces can be passed to intern().\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.sym(\"foo\"), vm.intern(vm.str(\"foo\")));\n\n        for (const ns of [vm.VARIABLE_NAMESPACE,\n                          vm.FUNCTION_NAMESPACE,\n                          vm.CLASS_NAMESPACE,\n                          vm.KEYWORD_NAMESPACE]) {\n\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.sym(\"foo\", ns), vm.intern(vm.str(\"foo\"), ns));\n        }\n\n    });\n\n    it(\"fsym() creates function symbols.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.fsym(\"foo\"), vm.sym(\"foo\").to_function_symbol());\n\n    });\n\n    it(\"csym() creates class symbols.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.csym(\"foo\"), vm.sym(\"foo\").to_class_symbol());\n\n    });\n\n    it(\"kwd() creates keyword symbols.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.kwd(\"foo\"), vm.sym(\"foo\").to_keyword_symbol());\n\n    });\n\n});\n\ndescribe(\"Numbers\", () => {\n\n    it(\"Zero and one are defined.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.zero(), vm.num(0)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.one(), vm.num(1)));\n\n    });\n\n    it(\"Lisp numbers can be created from JS strings and numbers.\", () => {\n\n        const n1 = vm.num(1);\n        const n2 = vm.num(\"1\");\n        const n3 = vm.num(\"21092183098213098210938092183092131221420943032.3292103283\");\n        const n4 = vm.num(\"21092183098213098210938092183092131221420943032.3292103283\");\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(n1, n2));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(n3, n4));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.equal(n1, n3));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.equal(n1, n4));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(n1, vm.Number);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(n1, vm.Object);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(n1), vm.lisp_class(vm.Number));\n\n    });\n\n    it(\"Lisp numbers can be turned into strings.\", () => {\n\n        const s1 = \"-21092183098213098210938092183092131221420943032.329210328332921032833292\";\n        const s2 = \"21092183098213098210938092183092131221420943032.329210328332921032833292\";\n        const n1 = vm.num(s1);\n        const n2 = vm.num(s2);\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.str(s1), n1.to_string()));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.str(s2), n2.to_string()));\n\n    });\n\n    it(\"Lisp numbers can be transformed to JS numbers.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.num(-1).to_js_number(), -1));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.num(0).to_js_number(), 0));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.num(1).to_js_number(), 1));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.num(-1.123).to_js_number(), -1.123));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.num(1.123).to_js_number(), 1.123));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.num(Number.MIN_SAFE_INTEGER).to_js_number(),\n                        Number.MIN_SAFE_INTEGER));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.num(Number.MAX_SAFE_INTEGER).to_js_number(),\n                        Number.MAX_SAFE_INTEGER));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.num(\"-10000000000000000000000000000000000000\").to_js_number(),\n                        -1e+37));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.num(\"10000000000000000000000000000000000000\").to_js_number(),\n                        1e+37));\n\n    });\n\n    it(\"Numbers can be compared.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.compare(vm.num(1), vm.num(1)) === 0);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.compare(vm.num(1), vm.num(2)) === -1);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.compare(vm.num(2), vm.num(1)) === 1);\n\n    });\n\n    it(\"Numbers can be added.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.add(vm.num(10), vm.num(20)), vm.num(30)));\n\n    });\n\n    it(\"Numbers can be subtracted.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.subtract(vm.num(10), vm.num(20)), vm.num(-10)));\n\n    });\n\n    it(\"Numbers can be multiplied.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.multiply(vm.num(10), vm.num(20)), vm.num(200)));\n\n    });\n\n    it(\"Numbers can be divided.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.divide(vm.num(10), vm.num(20)), vm.num(0.5)));\n\n    });\n\n});\n\ndescribe(\"Booleans\", () => {\n\n    it(\"The booleans, er, exist.\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.t(), vm.t()));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.f(), vm.f()));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.equal(vm.t(), vm.f()));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.t(), vm.Boolean);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.t(), vm.Object);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.f(), vm.Boolean);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.f(), vm.Object);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(vm.t()), vm.lisp_class(vm.Boolean));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(vm.f()), vm.lisp_class(vm.Boolean));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.t().to_js_boolean());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.f().to_js_boolean());\n\n    });\n\n});\n\ndescribe(\"Lists\", () => {\n\n    it(\"cons() creates conses.\", () => {\n\n        const c1 = vm.cons(vm.num(1), vm.num(2));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(c1, vm.Cons);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(c1, vm.List);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(c1, vm.Object);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(c1), vm.lisp_class(vm.Cons));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(c1.car(), vm.num(1)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(c1.cdr(), vm.num(2)));\n\n    });\n\n    it(\"Conses have value equality.\", () => {\n\n        const c1 = vm.cons(vm.num(1), vm.num(2));\n        const c2 = vm.cons(vm.num(1), vm.num(2));\n        const c3 = vm.cons(vm.num(100), vm.num(200));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(c1, c2));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.equal(c1, c3));\n\n    });\n\n    it(\"car and cdr are mutable.\", () => {\n\n        const c1 = vm.cons(vm.num(1), vm.num(2));\n        c1.set_car(vm.str(\"foo\"));\n        c1.set_cdr(vm.str(\"bar\"));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(c1, vm.cons(vm.str(\"foo\"), vm.str(\"bar\"))));\n\n    });\n\n    it(\"Test elt().\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.elt(vm.nil(), 0), \"Type assertion failed\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.elt(vm.nil(), 1), \"Type assertion failed\");\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.elt(vm.list(1, 2, 3), 0), 1);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.elt(vm.list(1, 2, 3), 1), 2);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.elt(vm.list(1, 2, 3), 2), 3);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.elt(vm.list(1, 2, 3), 3), \"Type assertion failed\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.elt(vm.list(1, 2, 3), 4), \"Type assertion failed\");\n\n    });\n\n    it(\"Test array_to_list() and list_to_array().\", () => {\n\n        let examples = [\n            [[],\n             vm.nil()],\n            [[vm.num(1)],\n             vm.cons(vm.num(1), vm.nil())],\n            [[vm.num(1), vm.num(2)],\n             vm.cons(vm.num(1), vm.cons(vm.num(2), vm.nil()))]\n        ];\n\n        for (const [array, list] of examples) {\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.array_to_list(array), list));\n            // Need deepEqual for arrays.\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(vm.list_to_array(list), array);\n        }\n\n    });\n\n    it(\"Test list().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list(),\n                        vm.nil()));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list(vm.num(1)),\n                        vm.array_to_list([vm.num(1)])));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.list(vm.num(1), vm.num(2)),\n                        vm.array_to_list([vm.num(1), vm.num(2)])));\n\n    });\n\n    it(\"Test reverse().\", () => {\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.reverse(vm.nil()), vm.nil()));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.reverse(vm.list(1, 2, 3)), vm.list(3, 2, 1)));\n\n    });\n\n});\n\ndescribe(\"#NIL\", () => {\n\n    it(\"Exists.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.nil(), vm.Nil);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.nil(), vm.List);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.nil(), vm.Object);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(vm.nil()), vm.lisp_class(vm.Nil));\n\n    });\n\n});\n\ndescribe(\"#VOID\", () => {\n\n    it(\"Exists.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.void(), vm.Void);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.void(), vm.Object);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(vm.void()), vm.lisp_class(vm.Void));\n\n    });\n\n});\n\ndescribe(\"#IGNORE\", () => {\n\n    it(\"Exists.\", () => {\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.ignore(), vm.Ignore);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(vm.ignore(), vm.Object);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.class_of(vm.ignore()), vm.lisp_class(vm.Ignore));\n\n    });\n\n});\n\ndescribe(\"Environments\", () => {\n\n    it(\"Unbound variables lose.\", () => {\n\n        const e1 = vm.make_environment();\n        const e2 = vm.make_environment(e1);\n        const msg = \"Unbound variable\";\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => e1.lookup(vm.sym(\"x\")), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => e2.lookup(vm.sym(\"x\")), msg);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(e1.is_bound(vm.sym(\"x\")));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(e2.is_bound(vm.sym(\"x\")));\n\n    });\n\n    it(\"JS undefined can be used as a value.\", () => {\n\n        const e1 = vm.make_environment();\n        const e2 = vm.make_environment(e1);\n\n        e1.put(vm.sym(\"x\"), undefined);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e1.lookup(vm.sym(\"x\")), undefined);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e2.lookup(vm.sym(\"x\")), undefined);\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e1.is_bound(vm.sym(\"x\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e2.is_bound(vm.sym(\"x\")));\n\n    });\n\n    it(\"Values are inherited from the parent environment.\", () => {\n\n        const e1 = vm.make_environment();\n        const e2 = vm.make_environment(e1);\n\n        e1.put(vm.sym(\"x\"), 12);\n        e1.put(vm.sym(\"y\"), 14);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e1.lookup(vm.sym(\"x\")), 12);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e1.lookup(vm.sym(\"y\")), 14);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e2.lookup(vm.sym(\"x\")), 12);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e2.lookup(vm.sym(\"y\")), 14);\n\n        e2.put(vm.sym(\"x\"), 42);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e1.lookup(vm.sym(\"x\")), 12);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e1.lookup(vm.sym(\"y\")), 14);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e2.lookup(vm.sym(\"x\")), 42);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e2.lookup(vm.sym(\"y\")), 14);\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e1.is_bound(vm.sym(\"x\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e1.is_bound(vm.sym(\"y\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e2.is_bound(vm.sym(\"x\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e2.is_bound(vm.sym(\"y\")));\n\n    });\n\n    it(\"Values can be mutated.\", () => {\n\n        const e1 = vm.make_environment();\n        const e2 = vm.make_environment(e1);\n\n        e1.put(vm.sym(\"x\"), 12);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e1.lookup(vm.sym(\"x\")), 12);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e2.lookup(vm.sym(\"x\")), 12);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e1.is_bound(vm.sym(\"x\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e2.is_bound(vm.sym(\"x\")));\n\n        e1.put(vm.sym(\"x\"), 42);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e1.lookup(vm.sym(\"x\")), 42);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e2.lookup(vm.sym(\"x\")), 42);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e1.is_bound(vm.sym(\"x\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e2.is_bound(vm.sym(\"x\")));\n\n        e2.put(vm.sym(\"x\"), 64);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e1.lookup(vm.sym(\"x\")), 42);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e2.lookup(vm.sym(\"x\")), 64);\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e1.is_bound(vm.sym(\"x\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e2.is_bound(vm.sym(\"x\")));\n\n    });\n\n    it(\"An environment can contain same-named symbols with different namespaces.\", () => {\n\n        const e1 = vm.make_environment();\n\n        const v = vm.sym(\"x\");\n        const f = vm.fsym(\"x\");\n        const c = vm.csym(\"x\");\n\n        e1.put(v, 1);\n        e1.put(f, 2);\n        e1.put(c, 3);\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e1.lookup(v), 1);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e1.lookup(f), 2);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e1.lookup(c), 3);\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e1.is_bound(vm.sym(\"x\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e1.is_bound(vm.fsym(\"x\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(e1.is_bound(vm.csym(\"x\")));\n\n    });\n\n});\n\ndescribe(\"Standard objects\", () => {\n\n    it(\"make_instance() creates new standard objects.\", () => {\n\n        const obj = vm.make_instance(vm.lisp_class(vm.Standard_object));\n        vm.assert(obj instanceof vm.Standard_object);\n        vm.assert(obj instanceof vm.Object);\n        vm.assert(vm.equal(vm.class_of(obj), vm.lisp_class(vm.Standard_object)));\n\n    });\n\n    it(\"Slots can be created by make_instance() and accessed with slot_value().\", () => {\n\n        const obj = vm.make_instance(vm.lisp_class(vm.Standard_object),\n                                     vm.sym(\"x\"), vm.num(12),\n                                     vm.kwd(\"y\"), vm.num(24));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(obj.slot_value(vm.sym(\"x\")),\n                        vm.num(12)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(obj.slot_value(vm.sym(\"y\")),\n                        vm.num(24)));\n\n        // Can also use keyword symbols.\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(obj.slot_value(vm.kwd(\"x\")),\n                        vm.num(12)));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(obj.slot_value(vm.kwd(\"y\")),\n                        vm.num(24)));\n\n    });\n\n    it(\"Slots can be updated with set_slot_value().\", () => {\n\n        const obj = vm.make_instance(vm.lisp_class(vm.Standard_object));\n        obj.set_slot_value(vm.sym(\"foo\"), vm.num(1));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(obj.slot_value(vm.sym(\"foo\")),\n                        vm.num(1)));\n        obj.set_slot_value(vm.sym(\"foo\"), vm.num(2));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(obj.slot_value(vm.sym(\"foo\")),\n                        vm.num(2)));\n\n    });\n\n    it(\"is_slot_bound() checks if slots are bound.\", () => {\n\n        const obj = vm.make_instance(vm.lisp_class(vm.Standard_object));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(obj.is_slot_bound(vm.sym(\"foo\")));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(obj.is_slot_bound(vm.sym(\"bar\")));\n\n        obj.set_slot_value(vm.sym(\"foo\"), vm.num(1));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(obj.is_slot_bound(vm.sym(\"foo\")));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(obj.is_slot_bound(vm.sym(\"bar\")));\n\n    });\n\n});\n\ndescribe(\"assert()\", () => {\n\n    it(\"It does nothing if the boolean is true.\", () => {\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.assert(true), undefined);\n    });\n\n    it(\"It throws an exception if the boolean is false.\", () => {\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.assert(false), \"Assertion failed\");\n    });\n\n    it(\"Exceptions are instances of vm.Assertion_error.\", () => {\n        try {\n            vm.assert(false);\n        } catch(e) {\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(e, vm.Assertion_error);\n            return;\n        }\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(false);\n    });\n\n    it(\"It supports a message.\", () => {\n        try {\n            vm.assert(false, \"Message\");\n        } catch(e) {\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e.message, \"Message\");\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(e, vm.Assertion_error);\n            return;\n        }\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(false);\n    });\n\n    it(\"The message defaults to 'Assertion failed'.\", () => {\n        try {\n            vm.assert(false);\n        } catch(e) {\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e.message, \"Assertion failed\");\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.instanceOf(e, vm.Assertion_error);\n            return;\n        }\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(false);\n    });\n\n});\n\ndescribe(\"abstract_method()\", () => {\n\n    it(\"It throws an error.\", () => {\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.abstract_method(), \"Congratulations\");\n    });\n\n});\n\n/***** Types *****/\n\ndescribe(\"has_type()\", () => {\n\n    it(\"It supports string type specs.\", () => {\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(\"foo\", \"string\"));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(12, \"number\"));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type({}, \"object\"));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type(12, \"string\"));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type(\"foo\", \"object\"));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type({}, \"number\"));\n    });\n\n    it(\"It supports function type specs.\", () => {\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(vm.str(\"12\"), vm.Object));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(vm.str(\"12\"), vm.String));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(vm.num(12), vm.Number));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(vm.num(12), vm.Object));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type(vm.str(\"foo\"), vm.Number));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type(vm.num(12), vm.String));\n    });\n\n    it(\"It supports 'any' type specs.\", () => {\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(null, vm.TYPE_ANY));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(12, vm.TYPE_ANY));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(\"foo\", vm.TYPE_ANY));\n    });\n\n    it(\"It supports 'null' type specs.\", () => {\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(null, vm.TYPE_NULL));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type(12, vm.TYPE_NULL));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type(\"foo\", vm.TYPE_NULL));\n    });\n\n    it(\"It supports n-ary 'or' type specs.\", () => {\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type(null, vm.type_or()));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(null, vm.type_or(vm.TYPE_NULL)));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(null, vm.type_or(vm.TYPE_NULL, \"string\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(null, vm.type_or(\"string\", vm.TYPE_NULL)));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(null, vm.type_or(vm.TYPE_NULL, \"string\", \"number\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(null, vm.type_or(\"string\", vm.TYPE_NULL, \"number\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(null, vm.type_or(\"string\", \"number\", vm.TYPE_NULL)));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(12, vm.type_or(vm.TYPE_NULL, \"string\", \"number\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(12, vm.type_or(\"string\", vm.TYPE_NULL, \"number\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(12, vm.type_or(\"string\", \"number\", vm.TYPE_NULL)));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type(true, vm.type_or(vm.TYPE_NULL, \"string\", \"number\")));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type(true, vm.type_or(\"string\", vm.TYPE_NULL, \"number\")));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type(true, vm.type_or(\"string\", \"number\", vm.TYPE_NULL)));\n\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type(12, vm.type_or(vm.TYPE_NULL, \"string\")));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.isFalse(vm.has_type(12, vm.type_or(\"string\", vm.TYPE_NULL)));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(12, vm.type_or(\"number\", \"string\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(12, vm.type_or(\"string\", \"number\")));\n\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(\"foo\", vm.type_or(\"number\", \"string\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.has_type(\"foo\", vm.type_or(\"string\", \"number\")));\n    });\n\n    it(\"It throws if the type spec is illegal.\", () => {\n        var msg = \"Unknown type spec\";\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.has_type(1), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.has_type(1, 1), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.has_type(1, null), msg);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.has_type(1, { foo: 12 }), msg);\n    });\n\n    it(\"Type specs can be converted to symbolic Lisp data.\", () => {\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.to_lisp_type_spec(vm.TYPE_ANY),\n                        vm.sym(\"object\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.to_lisp_type_spec(vm.TYPE_NULL),\n                        vm.str(\"null\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.to_lisp_type_spec(\"function\"),\n                        vm.str(\"function\")));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.to_lisp_type_spec(vm.type_or()),\n                        vm.list(vm.sym(\"or\"))));\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(vm.to_lisp_type_spec(vm.type_or(vm.TYPE_NULL, vm.String)),\n                        vm.list(vm.sym(\"or\"), vm.str(\"null\"), vm.sym(\"string\"))));\n    });\n\n});\n\ndescribe(\"assert_type()\", () => {\n\n    it(\"It supports type specs and returns the datum.\", () => {\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.assert_type(vm.t(), vm.Boolean), vm.t());\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.assert_type(vm.sym(\"foo\"), vm.Symbol), vm.sym(\"foo\"));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.assert_type(\"foo\", \"string\"), \"foo\");\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(vm.assert_type(12, \"number\"), 12);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.deepEqual(vm.assert_type({}, \"object\"), {});\n    });\n\n    it(\"It throws if the datum doesn't match the type spec.\", () => {\n        try {\n            vm.assert_type(12, \"string\");\n        } catch(e) {\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e.message, \"Type assertion failed: expected \\\"string\\\"\");\n            chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(e.lisp_slot_datum, 12);\n            (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.equal(e[\"lisp_slot_expected-type\"], vm.str(\"string\")));\n            return;\n        }\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(false);\n    });\n\n    it(\"Error messages contain the type spec.\", () => {\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.assert_type(vm.nil(), vm.Cons),\n                      \"Type assertion failed: expected cons\");\n    });\n\n    it(\"It supports ordinary, non-Lisp JS classes.\", () => {\n        (0,chai__WEBPACK_IMPORTED_MODULE_0__.assert)(vm.assert_type(new String(\"foo\"), String));\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.throws(() => vm.assert_type(new String(\"foo\"), Number));\n    });\n\n});\n\n/***** UTF-8 *****/\n\ndescribe(\"utf8_encode()\", () => {\n\n    it(\"It transforms UTF-16 to UTF-8.\", () => {\n        // The Unicode code point for the Euro sign, 0x20AC, becomes\n        // three bytes in UTF-8:\n        // https://en.wikipedia.org/wiki/UTF-8#Examples\n        const euro_utf16 = \"\\u{20AC}\";\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(euro_utf16.length, 1);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(euro_utf16.charCodeAt(0), 0x20AC);\n        const euro_utf8 = vm.utf8_encode(euro_utf16);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(euro_utf8.length, 3);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(euro_utf8.charCodeAt(0), 0xE2);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(euro_utf8.charCodeAt(1), 0x82);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(euro_utf8.charCodeAt(2), 0xAC);\n    });\n\n});\n\ndescribe(\"utf8_decode()\", () => {\n\n    it(\"It transforms UTF-8 to UTF-16.\", () => {\n        const euro_utf8 = \"\\u{E2}\\u{82}\\u{AC}\";\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(euro_utf8.length, 3);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(euro_utf8.charCodeAt(0), 0xE2);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(euro_utf8.charCodeAt(1), 0x82);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(euro_utf8.charCodeAt(2), 0xAC);\n        const euro_utf16 = vm.utf8_decode(euro_utf8);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(euro_utf16.length, 1);\n        chai__WEBPACK_IMPORTED_MODULE_0__.assert.equal(euro_utf16.charCodeAt(0), 0x20AC);\n    });\n\n});\n\n\n//# sourceURL=webpack://lispx-test-node/./test/vm-test.mjs?");

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/global */
/******/ 	(() => {
/******/ 		__webpack_require__.g = (function() {
/******/ 			if (typeof globalThis === 'object') return globalThis;
/******/ 			try {
/******/ 				return this || new Function('return this')();
/******/ 			} catch (e) {
/******/ 				if (typeof window === 'object') return window;
/******/ 			}
/******/ 		})();
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module can't be inlined because the eval devtool is used.
/******/ 	var __webpack_exports__ = __webpack_require__("./test/all-tests.mjs");
/******/ 	
/******/ 	return __webpack_exports__;
/******/ })()
;
});