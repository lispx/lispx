Currently, calling DEFCLASS again with the same name creates a new
class.  Instead, it should redefine the existing class and update
its superclass if it was changed.

Note that the same issue affects other DEF- forms like DEFDYNAMIC.